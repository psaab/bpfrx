INCUS_REMOTE="loss"
VM0="bpfrx-userspace-fw0"
VM1="bpfrx-userspace-fw1"
LAN_HOST="cluster-userspace-host"
PROFILE="bpfrx-cluster-userspace"

# Isolated userspace cluster on loss:
# - WAN uses a distinct spare mlx0 VF pair
# - LAN uses a distinct VLAN from the primary loss cluster
# Resolve live PCI BDFs from stable VF indices at deploy time.
VF_WAN_INDEX=(2 3)
VF_LAN_INDEX=(10 11)
SRIOV_PARENT="mlx0"
SRIOV_LAN_PARENT="mlx1"
VF_LAN_VLAN=3667
LAN_HOST_ATTACH_MODE="physical"
LAN_HOST_VF_INDEX=12

NET_MGMT="incusbr0"
NET_HEARTBEAT="bpu-hb0"
NET_FABRIC="bpu-fab0"
NET_CLAN=""

CLUSTER_CONF="docs/ha-cluster-userspace.conf"

LAN_ADDR="10.0.61.102/24"
LAN_GW="10.0.61.1"
