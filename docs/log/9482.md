# #9482 — the HA cold-prime bulk sync never runs

## 2026-09-10T00:05Z — Action: re-verify BOTH halves at base
- **File(s)**: none (measurement)
- Compiler, not grep: `go doc .../userspace.LegacyDataPlaneAdapter | grep ExportOwnerRG`
  lists ONLY the unpaged `ExportOwnerRGSessions(rgIDs, max)`. Positive control —
  `go doc .../userspace.Manager` lists BOTH, including the paged form. So the
  absence is a property of the adapter, not of my search.
- `pkg/daemon/daemon_ha_userspace_export.go` asserts `userspaceSessionExporter`,
  whose sole method is `ExportOwnerRGSessionsPaged`.
- Publication chain closed: `pkg/dataplane/userspace/manager.go` `Boot()` returns
  `NewLegacyDataPlaneAdapter(New())`, and `legacy_dataplane.go:15` declares the
  adapter as `dataplane.RuntimeDataPlane`. So the adapter IS what `d.dataplane()`
  hands the assertion.
- Still live on the cluster at this base: both `loss:xpf-userspace-fw0` and `fw1`
  logged `owed cold-prime re-drive failed ... err="bulk sync table-truth snapshot:
  dataplane does not export owner-RG sessions"` within the last 3 minutes.

## 2026-09-10T00:15Z — Action: the fix, in two halves
- **File(s)**: `pkg/dataplane/userspace/legacy_dataplane.go`,
  `pkg/daemon/bulk_snapshot_published_type_9482.go`
- The forwarder, matching its neighbours' shape.
- A compile-time belt binding the real (unexported) interface to BOTH published
  concrete types. Proved it bites BEFORE relying on it: renaming the forwarder
  away breaks the build with
  `bulk_snapshot_published_type_9482.go:54:31: ... does not implement
  userspaceSessionExporter (missing method ExportOwnerRGSessionsPaged)` — the
  diagnostic names the file and the method.

## 2026-09-10T00:25Z — Action: bind the WIRING, not a fake
- **File(s)**: `pkg/daemon/bulk_snapshot_published_type_9482_test.go`,
  `pkg/dataplane/userspace/legacy_paged_forward_target_9482_test.go`
- The pre-existing coverage is blind by construction: `wiringExporterDP` (#7259)
  and `recordingExporter` (#6031) each declare the method on themselves, so they
  prove the resolver correct for a type that satisfies the interface and say
  nothing about the published one. The new cells go through `dpuserspace.Boot()`.
- Control measurement, with the belt temporarily neutralised so the package
  builds and the behavioural cells are scoreable: 2 of 3 RED at base, and the
  refusal-branch control stayed GREEN — which is what makes it a control.
- One cell is SOURCE-level on purpose: a forwarder delegating to the UNPAGED
  manager method compiles, satisfies the interface, and is INDISTINGUISHABLE at
  runtime without a live helper (both manager methods open with the same
  `m.proc == nil` guard returning the identical error — verified in
  manager_status.go and owner_rg_export_paging_9344.go). It would reintroduce
  #9344's unbounded >64 MiB request, which #5085 turns into deleted live sessions.

## 2026-09-10T00:40Z — Action: mutation matrix
- **File(s)**: `scripts/mutate-9482.py`
- 4/4 KILLED, 0 SURVIVED, 0 VOID. Baseline 4884 tests ran, 0 failed.
- The matrix carries a carve-out the standard rule needs: for a COMPILE-TIME
  guard the kill signal IS a build failure, so a build break scores
  `KILLED(compile)` only when the diagnostic names the guard's file AND the
  missing method, and `VOID(build)` otherwise. Without that distinction "the
  build broke" is ambiguous between "the guard fired" and "my mutant was
  malformed".
- The ENOSPC check excludes the `injected:` fixture (#9500): a PASSING
  `pkg/configstore` test emits `injected: no space left on device`, so an
  unfiltered grep condemns healthy runs.
- NOT mutated, deliberately: deleting the belt itself. Removing a control is a
  no-op by design, so survival there would be correct rather than a gap — the
  thing to mutate is what the control guards, which is Q1.

## 2026-09-10T00:18Z — Action: validate on the cluster
- **File(s)**: ledger row from the run
- `make test` on a quiesced tree: 82 Go packages, 0 reds, 11 Rust suites green, no
  real ENOSPC line, no timeout panic, no DATA RACE.
- `make cluster-deploy` -> `apply-cos-config.sh` -> `make test-failover` in one
  `with-cluster.sh` lock cell (mandatory: this is session-sync code):
  **22 passed, 0 failed, verdict=PASS.**

### The MECHANISM, not just the outcome
The defect's own error string, counted PER PID so the claim is attributable
rather than a window artifact:

| node | old pid (pre-deploy) | new pid (fixed binary) |
|---|---|---|
| fw0 | 36 occurrences | **0** |
| fw1 | 22 occurrences | **0** |

The old-pid counts are the positive control: the grep demonstrably MATCHES, so
the zero is an absence and not a broken search. The last occurrence on each node
predates its new `Started xpfd.service` (fw0 00:14:46 vs 00:16:10; fw1 00:15:48 vs
00:16:04).

And the consequence I predicted in the issue body, now observed:

```
cluster sync: bulk sync complete  source=table-truth sessions=16 epoch=3
cluster sync: bulk transfer complete  epoch=3 sessions=16
cluster: no-RETH startup promotion hold released  reason=bulk-sync-complete
```

on BOTH nodes. Before the fix that release was ALWAYS `timeout-degraded` after the
full 30s, because the `bulk-sync-complete` edge it prefers could never fire. The
table-truth bulk now carries a real window (16 sessions) instead of framing none.

### Unexpected, and relevant to #9484
Throughput on this run was **22.7 Gbit/s** — inside `harness-compare`'s
`[21.375, 23.625]` green band, against the post-#9452 samples of 18.9 / 20.3 /
19.0 that are all below it. A plausible mechanism: before this fix BOTH nodes
re-drove an owed cold-prime bulk once a minute, forever, each attempt taking the
helper control path. That is a HYPOTHESIS from one sample, not a result — #9484 is
the last item in my queue and is where it gets sampled properly. Recorded here so
the next reader knows the band moved on the same day as this fix.
