# 9153 — a packed `then` clause dropped actions past the second

- **Timestamp**: 2026-09-06
- **Action**: `flattenThenChain8939` now composes with `expandFlatRun` on the
  `then` schema, so the last chain link's packed Keys run is SPLIT rather than
  only hoisted. Applies on both arms (chain present and absent).
- **File(s)**: pkg/config/compiler_firewall.go,
  pkg/config/filter_then_packed_run_9153_test.go
