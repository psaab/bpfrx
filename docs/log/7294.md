# 7294 — structural session-scan admission

- **Timestamp**: 2026-09-02
  - **Action**: Re-anchored the call-site census by symbol at master
    `192f40502` (the issue cites `52f51200e`) and corrected three
    premises. The acquire census is 21 sites across 11 files, not the 7
    files listed: all 7 still exist, and `pkg/api/metrics_sessions.go`,
    `pkg/grpcapi/peer_only_5968.go`, `server_nat.go` and
    `server_show_nat.go` have joined them. `pkg/api` and `pkg/grpcapi`
    are currently FULLY covered — no #6216-shaped gap is open there.
    The uncovered walks are `pkg/cli` (18 sites, zero acquires,
    in-process) and the daemon-internal background walkers; both are
    defensible and are recorded as out-of-scope with reasons rather
    than left silent. The body's caution that "`SessionCount` can read a
    cached counter" is NOT true in this tree — both non-test
    implementations walk, and only test fakes return a stored field, so
    treating it as sometimes-cheap would have made the guard too loose.
    Of the three named false-hit sites two are explained and one
    (`buildTracerouteArgv`) touches no session primitive at all and
    could not be reproduced; it is used as a control, not as evidence.
  - **File(s)**: (issue comment)

- **Timestamp**: 2026-09-02
  - **Action**: Item 1 — registration guard. Parses `pkg/api` and
    `pkg/grpcapi`, finds every CALL of a session-scan primitive plus the
    `pkg/natshow` carriers, and requires admission directly or through
    every caller that reaches it. Three build decisions, each forced by
    a measurement rather than chosen: call-position matching (kills the
    `api.go:33` interface-field hit by construction); receiver-qualified
    acquire matching (so the #8151 wrong-budget shape still reds, which
    a "does it acquire?" guard would rate compliant); and admission
    through a caller or a dedicated acquire HELPER identified by its
    return signature, NOT arbitrary transitive reachability. The first
    version used transitive propagation and two mutations escaped it —
    deleting the real acquire from `showSessionsTop` and from
    `GetSessions` both left the guard green, because a `ShowText`
    dispatcher that calls one acquiring handler was marked admitted and
    carried every handler it dispatches to. Both red under the
    signature-anchored rule.
  - **File(s)**: `pkg/diagcmd/session_walk_registration_7294_test.go`,
    `pkg/api/README.md`, `docs/log/7294.md`

- **Timestamp**: 2026-09-02
  - **Action**: Item 3 prerequisite — bound the #5880 lease-REUSE half to
    the real peer-only methods before changing them. #5880 has two halves
    and only one was bound to production: "these paths acquire at all" is
    checked against the real methods by
    `TestPeerOnlyStillAcquiresAdmission`, but "a nested delegate holding
    an ancestor's lease does not self-reject at capacity" was only ever
    driven through the `leaseProbeCluster` STUB in `pkg/api`. Item 3
    makes the peer paths stop taking a local slot, at which point the
    stub would go on re-acquiring the session-walk limiter and the
    pkg/api test would go on passing about a shape that no longer
    exists — green about a model rather than vacuous out loud.
    Measured first: REST delegates in-process to exactly four gRPC
    methods, and only the three peer-only ones carry a lease
    (`ClearSessions` is delegated at `api/sessions.go:803` BEFORE any
    acquire; the `Acquire` at `:824` gates only the nil-service
    fallback). So those three are the only production carriers, which is
    why the binding has to be on them.
    The new cell pairs each reuse assertion with a control on the same
    call: at capacity WITHOUT the lease it must be refused. Every
    assertion in the cell is "this call succeeded", which is also what a
    limiter with spare capacity produces, so without the control the
    test would pass on a fixture that was never full. Verified by
    mutation — `AcquireCtx` -> `Acquire` reds the reuse assertion,
    removing admission entirely reds the control, two different cells
    for two different defects.
  - **File(s)**: `pkg/grpcapi/peer_only_lease_reuse_7294_test.go`,
    `pkg/api/sessions_lease_5880_test.go`, `docs/log/7294.md`

- **Timestamp**: 2026-09-02
  - **Action**: Item 3 prerequisite #2 — made a failed peer fetch visible
    on the session LIST surface. `writeSessionList` discarded the error
    with `if pr, err := ...; err == nil`, returning 200 with the peer
    table absent and no reason. Summary and zone-pairs already
    classified it; the list surface did not, which is the shape that
    survives review because two of three places look right. The path is
    currently unreachable (the REST handler stamps the lease, so
    `PeerSessions` takes the reuse arm and never returns `ErrBusy`), and
    item 3's remote budget makes it reachable — but it is fixed because
    it is wrong: unreachable today is a property of the call graph, not
    a guarantee. Also stopped reporting an admission refusal as
    `unreachable` on all three surfaces; a refusal means the peer IS
    reachable and we declined to ask, and calling it a partition sends
    an operator after a network fault that does not exist. Three
    mutations, three distinct reds: reverting to the swallow, collapsing
    `busy` back into `unreachable` (with the genuine-failure subtest
    staying green, so the two sides are distinguished rather than
    renamed), and emitting the fields for callers that did not opt in.
  - **File(s)**: `pkg/api/sessions.go`, `pkg/api/types.go`,
    `pkg/api/sessions_peer_refusal_visible_7294_test.go`,
    `pkg/api/README.md`, `docs/log/7294.md`
