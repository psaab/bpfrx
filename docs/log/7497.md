# PR #7497 (part 1) — cap binding plans at the slot-keyed map capacity

- **Timestamp**: 2026-09-02 20:30 UTC
  - **Action**: Establish the #7497 blocker census at `1581ea5a7` and land the
    first of four PRs. All seven remaining blockers still hold; only the line
    numbers in the issue body had drifted. Two premises in the issue were
    corrected by measurement rather than argument. First, acceptance
    criterion 6 states the loss cluster cannot validate the change because it
    is symmetric; it is symmetric only as configured, and `ethtool -L` on a
    stopped dataplane manufactures the asymmetry (the reduction is refused
    while xpfd runs, and the driver names its own reason: the zerocopy AF_XDP
    sockets we hold). Second, a claim of mine that the Go side had no 4096
    bound was wrong on both limbs and is retracted on the issue: the constant
    is 1,048,576, and the composed index is guarded in six places.
  - **File(s)**: (investigation only — recorded in issue comments)

- **Timestamp**: 2026-09-02 20:45 UTC
  - **Action**: Name the capacity of the two shim maps keyed by the binding
    VALUE's slot field, and refuse at plan time any plan that would mint a
    slot they cannot address. `userspace_bindings` is keyed by the composed
    index `ifindex * 16 + queue` and bounded by `BindingArrayMaxEntries`
    (1,048,576), but `userspace_heartbeat` and `userspace_xsk_map` are keyed
    by `binding.slot` and hold 4,096 entries. `slot` is assigned densely by
    the planner, a plain counter over the bindings it planned, so it is a
    ceiling on the TOTAL BINDING COUNT and the existing guards — checked
    against a bound 256x larger — do not stand in for it. Enforced at plan
    time rather than at `register_xsk_slot`, which runs during bringup after
    teardown; and refusing the whole plan rather than truncating, because an
    unbound queue does not reduce throughput but takes
    `drop_degraded_transit` on every transit packet while the interface still
    reads up. Prerequisite work for the planner change, not a defect
    reachable today: the count is bounded by `16 * interfaces` under both the
    current and proposed rules, so the ceiling is unchanged by #7497.
  - **File(s)**: userspace-xdp/src/binding_index.rs, userspace-xdp/src/lib.rs,
    userspace-dp/src/server/helpers/planning.rs,
    userspace-dp/src/main_tests.rs, pkg/dataplane/constants.go,
    pkg/dataplane/loader_userspace_shim.go,
    pkg/dataplane/userspace_shim_loader_test.go,
    pkg/dataplane/userspace_xdp_manifest.json (the rebuilt .o is
    byte-identical — the named constant equals the literal it replaced),
    docs/afxdp-packet-processing.md

- **Timestamp**: 2026-09-02 20:55 UTC
  - **Action**: Record two instrument failures from this change, both of
    which returned a wrong answer in the direction that would have been
    accepted. The mutation battery classified all four Rust mutations as
    VOID (did not compile) when every one was in fact CAUGHT: cargo prints
    `error: test failed, to rerun pass ...` for a TEST failure, and the
    detector's `^error:` pattern could not tell that from a compile error.
    Accepting it would have meant concluding the guards were unverifiable.
    Separately, a check for pre-existing rustfmt drift ran `rustfmt --check`
    on file copies extracted outside the source tree, where they cannot
    resolve their `mod` paths; rustfmt exited 1 with empty stdout and the
    check read the empty output as "clean", which led to reformatting two
    files that were already dirty at master and to churn in a third
    (`ipv6_ext_walk.rs`, reached because bare `rustfmt` formats the whole
    module tree of the file it is given). Both were caught only by looking
    at a result that did not fit — four simultaneous VOIDs, and fmt hunks at
    line numbers an appended block cannot reach.
  - **File(s)**: (tooling — no repository files changed)
