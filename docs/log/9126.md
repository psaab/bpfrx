# 9126 — display set dropped empty containers and emitted a bare deactivate

- **Timestamp**: 2026-09-06
- **Action**: `formatSetNodes` now emits `set <path>` for a non-leaf node with no
  children, so an empty container round-trips and a `deactivate` on one is
  replayable. Corrected two #8997 expectations that were asserting a property of
  the set view rather than of the tree.
- **File(s)**: pkg/config/ast_format.go,
  pkg/config/empty_container_setview_9126_test.go,
  pkg/config/elided_delete_bare_flag_8997_test.go
