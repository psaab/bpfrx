# 8326 — cluster status rendered a failed apply as healthy

- **Timestamp**: 2026-09-02
  - **Action**: Verified all four premises before implementing, plus one
    the brief did not raise: **is `applied` ever written?** It is
    (`rg_state.go:190`, `:240`, via `markAppliedLocked`), which is what
    made the fix viable — reading a field nothing maintains would have
    rendered every group as not-forwarding.
    Confirmed: `IsActive()` and `DesiredActive()` both returned
    `s.active`; `rg_forwarding_status_7367.go` read `IsActive()` under a
    comment asserting it was the APPLIED value; and a SECOND false
    comment, not in the brief, sat on the `RGForwarding.Active` field
    itself in `pkg/cluster/rg_forwarding.go` — the type's own contract,
    which is the doc a consumer actually reads.
    `DesiredActive()` had **zero** non-test callers. Removed: two names
    for one field is what made "IsActive is the APPLIED value, not
    DesiredActive" read as a considered distinction instead of a
    mistake. The sentence is only plausible while two accessors exist
    that could differ.
    Second-order effect worth recording: the consumers of
    `RGForwarding.Active` are a verdict classifier and a formatter, not
    control flow — so no dataplane behaviour changes and `make
    test-failover` is not owed. But the classifier at
    `rg_forwarding.go:78` computes `owned && !fwd.Active ->
    OwnedNotForwarding`, so with the desired value it classified a
    failed apply on an owned group as **Consistent**. The fix does not
    only correct a rendered word; it makes the divergence classifier
    work for the case it was written for.
  - **File(s)**: `pkg/daemon/rg_state.go`,
    `pkg/daemon/rg_forwarding_status_7367.go`,
    `pkg/cluster/rg_forwarding.go`, `pkg/daemon/rg_state_test.go`,
    `pkg/daemon/rg_forwarding_applied_8326_test.go`, `docs/log/8326.md`
