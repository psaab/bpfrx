# #6941 — WireGuard endpoint device identity: re-derivation at master `630957613`

- **Timestamp**: 2026-08-28
- **Action**: Re-derived BOTH halves from source (the issue explicitly marks the
  routing half as attributed-not-verified and asks for it to be settled first).
  Everything below was read or executed at master, not carried from the issue.

## Both halves, read

| step | site | result |
|---|---|---|
| routing | `pkg/daemon/daemon_run_routehelpers.go:59` `collectAppliedTunnels` | emits a `TunnelConfig` for `ifc.Tunnel` **and** for each `unit.Tunnel` |
| routing | `pkg/routing/tunnel.go:208-215` | splits purely on `tc.Mode == "wireguard"` → `wgDesired`, else `desired` |
| routing | `pkg/routing/tunnel.go:377` | `mode == "wireguard"` → `applyWireguardTunLocked`, **`continue`ing before the `AnchorOnly` branch at :410** |
| dataplane | `pkg/config/tunnelemit.go:100-104` | interface-level WG with units emits **one** endpoint keyed by the **lowest** unit ref, carrying `iface.Tunnel` |
| dataplane | `pkg/config/types.go:412` `TunnelNameMap` | a unit with its OWN tunnel stanza maps to that unit's device, deliberately (#1910) |
| dataplane | `pkg/dataplane/userspace/interfaces.go:424` `snapshotLinuxName` | resolves the unit ref through that map |
| rust | `coordinator/tunnel_supervision.rs:822-828` | `tunnel_name` = `ifindex_to_name[endpoint.logical_ifindex]` — **not** `endpoint.linux_name` |
| rust | `forwarding_build/tunnels.rs:125` | `endpoint.linux_name` → `interface_label`, a display name (`status.rs:1131`) |
| rust | `tunnel_supervision.rs:397-402, 502-507, 969-975` | `endpoint.linux_name` is ALSO functional: three coherence gates compare it against `ifindex_to_name[logical_ifindex]` |

**The routing half is confirmed**: routing does create `wg0` from the
interface-level record. The issue's characterisation of the unit device as "the
mode-independent tuntap anchor" is **wrong in the general case** — a unit tunnel
under an interface-level WG *inherits* `mode wireguard` unless overridden, and
the mode-WG branch returns before the anchor branch, so `wg0u3` is normally a
second persistent WG TUN, not a tuntap anchor. It is an anchor only when the
unit explicitly overrides the mode (e.g. `mode gre`).

## Reproduced end to end

`buildTunnelEndpointSnapshots` on the divergent config yields:

```
interface rows:  Name="wg0"   LinuxName="wg0"   Ifindex=100  addrs=[]
                 Name="wg0.3" LinuxName="wg0u3" Ifindex=101  addrs=[10.66.3.1/24]
tunnel endpoint: Interface="wg0.3" LinuxName="wg0u3" Ifindex=101 Mode="wireguard"
```

(The builder skips rows with `Ifindex<=0`, so a host without these devices
produces an EMPTY endpoint list — an artifact that reads as "inert" if not
checked. Ifindexes were supplied to run the production builder on a
production-shaped input.)

## The precondition the issue does not state

The divergence requires the **lowest configured unit** to carry its own tunnel
stanza. Measured:

| shape | nameMap | emitted ref | endpoint device | diverges? |
|---|---|---|---|---|
| unit 3 has own tunnel (only unit) | `wg0.3:wg0u3` | `wg0.3` | `wg0u3` | **yes** |
| unit 1 plain + unit 3 has own tunnel | `wg0.1:wg0`, `wg0.3:wg0u3` | `wg0.1` | `wg0` | no |
| units, none with own tunnel | `wg0.0:wg0` | `wg0.0` | `wg0` | no |

## Which side is wrong — the question the issue asks

`ep.Tunnel.Name` and `TunnelNameMap[ep.Name]` were compared across six tunnel
shapes (interface-level GRE, per-unit GRE, interface-level GRE + unit tunnel,
interface-level WG plain, interface-level WG + unit tunnel, IPIP). They
**agree in every shape except the divergent one**.

But "make the endpoint use `tunnel.Name`" is NOT a safe fix, and the reason is
the fact that settles the question: **the unit's ADDRESSES are on the unit
device too**. `wg0.3`'s `10.66.3.1/24` lands on `wg0u3`; `wg0` carries none.
So the dataplane is INTERNALLY CONSISTENT — endpoint device == address device
== `wg0u3` — and forcing the endpoint onto `wg0` would attach the WireGuard
engine to an addressless device. That is a fix matched to the symptom's shape
that breaks the datapath.

**Conclusion: the dataplane side is not wrong.** What is anomalous is that
routing materialises `wg0` from the interface-level record while no emitted
endpoint binds to it — an orphan device — and #6861 already documents the
dataplane's resolution as the production authority for endpoint→device binding
(`compiler_validate_strict_tunnel_ipip.go`, `emittedTunnelDeviceNames`).

## The larger defect found while settling it

The root is that `EmitTunnelEndpointNames` emits an endpoint whose **key** comes
from one object (the lowest unit) and whose **payload** comes from another (the
interface-level tunnel). The device divergence is one consequence. The other is
worse and is not in the issue:

For the supported per-unit-peer shape (`tunnel_perunit_deepcopy_test.go:123`,
interface-level WG peers plus per-unit peers), measured:

```
iface.Tunnel  peers=1 [cccc]
unit 1 tunnel peers=2 [cccc aaaa]
unit 2 tunnel peers=2 [cccc bbbb]
ENDPOINT ref="gr-0/0/0.1" tunnel.Name="gr-0-0-0" peers=1 [cccc]
```

`pkg/dataplane/userspace/tunnels.go:142` is the ONLY path from config to the
dataplane for WG peers, and it reads the endpoint's `TunnelConfig`. So the
per-unit peers **never reach the dataplane**. #3898 deep-copies per-unit
`WgPeers` precisely so each unit owns its own set; nothing then consumes them.
The pattern is tested at the config layer and non-functional at the runtime
layer, and it fails silently — the tunnel comes up and only the
interface-level peers can handshake.

## Both locally-available "make them agree" edits are HARMFUL

This is the part that decides the issue, and it was established by tracing every
read of `logical_ifindex` and of the endpoint's `linux_name` rather than by
reasoning about the two names.

**Edit 1 — set the endpoint's `LinuxName` from `tunnel.Name`** (leaving the
ifindex alone). `tunnels.go:123` sources `LinuxName` and `Ifindex` from the SAME
interface row, so this breaks their co-sourcing. Three Rust gates
(`wg_tombstone_respawn_coherent` at `tunnel_supervision.rs:969-975`, its GRE twin
at `:502-507`, and the deferred prune at `:397-402`) require
`endpoint.linux_name == ifindex_to_name[logical_ifindex]`. Today both are
`wg0u3` and the gate passes. After this edit they are `wg0` and `wg0u3`, and a
**tombstoned WireGuard control thread never respawns** — a silent, permanent
tunnel outage after any thread death.

**Edit 2 — repoint the endpoint's `Ifindex` at the parent row.**
`logical_ifindex` is not just a name handle. It sets `egress_ifindex`
(`forwarding/tunnel.rs:74`), and therefore the **to-zone every policy matches
on** via `egress_zone_id`; a parent row shared by differently-zoned units
resolves to the 0 sentinel, which no operator permit matches. It is also the key
of `tunnel_endpoint_by_ifindex`, so a route written `next-hop @wg0.3` would stamp
`tunnel_endpoint_id = 0` and **never encapsulate**. Every live session stores the
old `egress_ifindex`, so three re-ownership gates (`session_glue/mod.rs:107`,
`gre.rs:997`, `frame/wg.rs:488`) turn those sessions **drop-only**, and HA
owner-RG attribution falls to 0 (`forwarding/ha.rs:35`).

So the divergence cannot be repaired at the binding layer at all. Both edits
convert a latent inconsistency into an active outage, and neither is detectable
by any existing test.

## Not done

No fix in this pass. A device-only change is refuted above; the coherent repair
is a peer-set decision (merge every unit's peers into the single
interface-level endpoint, or reject the shape), which is a design change to
WireGuard per-unit semantics rather than an agreement patch, and is larger than
this issue as filed.

**File(s)**: docs/log/6941.md
