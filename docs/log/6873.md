# #6873 — host-inbound unknown-zone admit: cold-boot reachability

- **Timestamp**: 2026-08-27
- **Action**: Establish whether the cold-boot window is reachable, and pin the
  invariant that answers it
  - **The premise is half right, and the fix direction inverts.** The gap is
    real: `host_inbound_admits` has no `snapshot_installed` consultation, and
    an empty `zone_host_inbound` sends EVERY zone id to `None => true`. But the
    empty table is not observable, so a gate at the admit site would be dead
    code — a check on a state no reader can reach.
  - **Measured, by tracing the worker lifecycle rather than the flag**:
    - `bring_up_workers` is the only worker spawn path (README:133 forbids a
      bare `std::thread::spawn` for a worker); reconcile runs `tear_down` ->
      `apply_snapshot` -> `bring_up_workers`, so a worker is spawned only after
      a snapshot-derived state is published.
    - `reconcile(None, ..)` takes its early exit AFTER `tear_down`, so "no
      snapshot" means "no workers", never "workers on an empty table".
    - `stop_inner` JOINS every worker via `workers.stop_and_clear(..)` BEFORE
      `self.forwarding = ForwardingState::default()` (#6592). The reset is never
      visible to a live reader; the code says so itself.
  - **The invariant was load-bearing and unpinned.** The closest existing cell,
    `reconcile_with_none_snapshot_reaches_no_snapshot_early_exit` (#1328),
    asserts `workers.live.is_empty()` from a `Coordinator::new()` that never had
    a worker — remove the teardown and it stays green. A fixture that cannot
    produce the state it asserts absent is mutation-insensitive by construction,
    and that one has read like coverage since #1328.
  - **Two cells, in this order, because the second asserts an ABSENCE**:
    1. `cold_forwarding_state_admits_every_host_inbound_service_6873` — the
       PAYLOAD PROOF. A default `ForwardingState` admits ssh, https, v6 DNS and
       raw GRE through `host_inbound_admits_iface`, the entry point the
       local-delivery poll path calls. Carries a control: the same packets on
       the same zone id are DENIED once the zone is in the table, so the cell
       shows EMPTINESS is the cause rather than the admit path being open.
       None of the four probes is ICMP, so none can reach admit via the
       #3171/#3201 global accept.
    2. `no_snapshot_reconcile_leaves_no_reader_for_the_empty_table_6873` — the
       ORDERING GUARD, built from a coordinator that HAS a live worker record
       AND a populated host-inbound table. Asserts the dangerous state is
       reached (table emptied) AND that nothing survives to observe it.
  - **The comment rewrite is the anti-re-close deliverable.** The `None => true`
    arm cited #3405 and #5659 — both accurate, neither answering the question —
    and so read as settled. It now states which cases are settled and by which
    issue, what stays admitted ON PURPOSE, and the ordering argument with its
    citation.
  - **File(s)**: `userspace-dp/src/afxdp/forwarding/host_inbound.rs`,
    `userspace-dp/src/afxdp/forwarding/host_inbound_tests.rs`,
    `userspace-dp/src/afxdp/coordinator/tests.rs`,
    `docs/junos-cli-reference.md`, `docs/log/6873.md`

- **Timestamp**: 2026-08-27
- **Action**: Correct my own "the invariant was unpinned" claim
  - I checked `reconcile_with_none_snapshot_reaches_no_snapshot_early_exit`
    (#1328), found it mutation-insensitive, and concluded the invariant was
    unpinned. That is one candidate examined and a claim made about the
    population — the same error as reading a single mutation's null result as a
    census.
  - Measured: `teardown_quiesce_skipped_on_no_snapshot_even_with_live_workers`
    seeds a LIVE worker and already asserts `workers.records.is_empty()` after
    a no-snapshot reconcile. The worker-teardown half WAS pinned — by a cell
    whose stated subject is #2522 quiesce timing.
  - The discriminating mutation settles what the new cell adds. Deleting
    `self.forwarding = ForwardingState::default()` from `stop_inner`:
    - new cell **RED** (the emptied-table assertion),
    - quiesce cell **GREEN** (it never reads the forwarding state).
    So the cell earns its place on the "the dangerous state is actually
    REACHED" half, not on the worker half — and without that half, "no reader
    survives" would guard a state nobody had shown was dangerous.
  - **File(s)**: `userspace-dp/src/afxdp/coordinator/tests.rs`,
    `docs/log/6873.md`

- **Timestamp**: 2026-08-27
- **Action**: Sweep for the CLAIM, not for the issue number — and find the half
  of the answer I had missed
  - Prompted by a #6846 follow-up: a doc claim about `transmit-rate percent`
    being inert had been wrong since #4228, four months before #6846, so no
    sweep scoped to "docs my change touched" could have found it. Applied the
    same lens here rather than acknowledging it.
  - **The sweep found something material to the open PR.**
    `docs/host-inbound-service-matrix.md:939` says the lo0 fence fixed "the SAME
    cold-boot fail-open the host-inbound table closed in **#5644**". I had
    concluded "no cold-boot gate is needed" without ever finding #5644 — because
    I searched `snapshot_installed` and the AF_XDP admit path, which is one
    enforcement surface of two.
  - **Both answers are correct, on different surfaces**, and that is the point:
    - KERNEL nft chain: the window IS reachable (a boot apply can fail with no
      prior table to retain), so it has a real fence —
      `installHostInboundColdBootFence` (#5644, lo0 analogue #6476), guarded by
      `TestColdBootHostInboundInstallFailureInstallsFence`.
    - USERSPACE AF_XDP classifier: unreachable by worker-lifecycle ordering.
      There is no failed-apply equivalent, because a worker exists only once a
      state has been published.
  - **This is very likely why the arm keeps being re-audited.** An auditor who
    reads "#5644 closed the host-inbound cold-boot fail-open" and then finds no
    fence at this admit site sees a contradiction and files it. The comment now
    states both surfaces and why they answer differently, which is the actual
    anti-re-open content — the previous version would have left the same trap.
  - I had also swept only `docs/junos-cli-reference.md` when writing the
    operator paragraph, and never opened `docs/host-inbound-service-matrix.md`
    — the file whose entire subject is this feature. Same horizon, one directory
    over.
  - **File(s)**: `userspace-dp/src/afxdp/forwarding/host_inbound.rs`,
    `docs/log/6873.md`
