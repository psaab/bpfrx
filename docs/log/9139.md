# #9139 — IPsec SA sync and re-initiation hardcoded to RG0

- **Timestamp**: 2026-09-06
- **Action**: Confirmed both halves against master: `advertiseIPsecSAOnce` gated
  on `IsLocalPrimary(0)` and `reinitiateIPsecSAs` was wired only to
  `applyRG0OwnershipTransition`, so on an asymmetric active/active cluster the
  node holding the SAs never advertised and the node taking a data RG never
  re-initiated.
- **File(s)**: —

- **Timestamp**: 2026-09-06
- **Action**: Widened the advertise gate to `IsLocalPrimaryAny()` (the #3764
  precedent) and added the missing re-initiate leg to `applyRethServicesForRG`.
  Scoped BOTH legs with `ownsIPsecConn`, because widening the gate alone would
  make a node taking one RG initiate the live peer's other-RG tunnels — trading
  a missed re-initiation for a duplicate one.
- **File(s)**: `pkg/daemon/ipsec_sa_rg_9139.go` (new), `pkg/daemon/daemon_ha.go`

- **Timestamp**: 2026-09-06
- **Action**: A test cell caught a hazard the design missed: `m.groups[0]` exists
  only if RG0 is DECLARED, so `IsLocalPrimary(0)` is permanently false on a
  config carrying only `redundancy-group 1` — gating an unanchored connection on
  it would have SILENTLY STOPPED re-initiating it. Routed RG0 through
  `LocalGroupPrimary` (#8640) with an `IsLocalPrimaryAny()` fallback only when
  RG0 is absent.
- **File(s)**: `pkg/daemon/ipsec_sa_rg_9139.go`

- **Timestamp**: 2026-09-06
- **Action**: FIRST mutation pass: 5 killed, **3 SURVIVED — and all three were
  the WIRING**: revert the advertise gate, delete the per-RG re-initiate call
  site, drop the scoping inside `reinitiateIPsecSAs`. Every one of them is the
  fix; the filter function they call was never the broken part. Added seams
  (`ipsecInitiateFn`, `ipsecActiveNamesFn`, `SetPeerIPsecSAsForTesting`) so the
  CALL SITES are observable without a live swanctl, then cells that drive
  `applyRethServicesForRG` and `advertiseIPsecSAOnce` themselves.
- **File(s)**: `pkg/daemon/daemon.go`, `pkg/daemon/daemon_ha.go`,
  `pkg/cluster/sync.go`, `pkg/daemon/ipsec_sa_rg_9139_test.go`

- **Timestamp**: 2026-09-06
- **Action**: SECOND pass: 9/10 — severing the GATE'S CALL SITE still survived
  (the gate function was bound, its caller was not). Added the active-names seam
  and a cell asserting a non-primary node never performs the read, with a
  positive control. THIRD pass: 10/10 killed.
- **File(s)**: `pkg/daemon/ipsec_sa_rg_9139_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Documented the per-RG contract and the undeclared-RG0 rule.
- **File(s)**: `pkg/cluster/README.md`
