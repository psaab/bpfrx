# #9326 — a commit synchronously dialed every syslog stream

- **Timestamp**: 2026-09-07
- **Action**: Re-derived the measurement rather than inheriting it, driving the
  real `applySyslogConfig`: **15.01 s** for one apply with 3 unreachable TCP
  streams, and the repeats confirmed no idempotence gate. Read the code to
  confirm the mechanism (`NewSyslogClientTransport` dials at construction).
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: Checked the load-bearing precondition BEFORE relying on it: TCP/TLS
  reconnect lazily on `Send`, but **UDP never does** (`writeMsg` returns the
  error and the UDP path has no reconnect branch). So the deferred constructor
  is TCP/TLS-only and REFUSES udp; UDP keeps its construction dial and instead
  gained the 5 s dialer its siblings already had.
- **File(s)**: `pkg/logging/syslog.go`

- **Timestamp**: 2026-09-07
- **Action**: Idempotence gate scoped to the client rebuild only — the
  zone-name/policy-name/filter-term/interface-name wiring above it is derived
  from the apply result and netlink, so gating the whole function would silently
  stop refreshing all of it. Fingerprint covers the resolved source addresses
  and is forgotten on both teardown paths.
- **File(s)**: `pkg/daemon/daemon_system.go`, `pkg/daemon/daemon.go`

- **Timestamp**: 2026-09-07
- **Action**: Typed the `security log stream <s> host` leaf
  (`ValidateSyslogHost`, the `ValidateNTPServer` shape). Ran the new rejection
  against every shipped config first: none sets a syslog host, so it cannot
  break one.
- **File(s)**: `pkg/config/schema_validators_system.go`,
  `pkg/config/schema_security.go`

- **Timestamp**: 2026-09-07
- **Action**: Mutation table, 7 cells. **M1 — reverting the daemon to the
  DIALING constructor, i.e. the defect itself — SURVIVED the first pass.** The
  daemon cells counted clients reaching the warm (which happens either way) and
  the library cells proved the deferred constructor does not dial without
  proving the daemon USES it. Binding a constructor is not binding the call
  site. Closed with a cell that applies against a REAL loopback listener: an
  unreachable host cannot distinguish "did not dial" from "dialed and failed",
  a reachable one can. Re-scored 7/7 KILLED.
- **File(s)**: `pkg/daemon/syslog_commit_dial_9326_test.go`,
  `pkg/logging/syslog_deferred_dial_9326_test.go`,
  `pkg/config/syslog_host_typed_9326_test.go`

- **Timestamp**: 2026-09-07
- **Action**: Documented the three changes and why the UDP asymmetry is
  deliberate.
- **File(s)**: `pkg/logging/README.md`
