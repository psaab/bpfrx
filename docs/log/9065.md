# #9065 — cmd/cli positional selector/modifier ladder

- **Timestamp**: 2026-09-06
- **Action**: Built a CENSUS before fixing anything: 15 operational-tree nodes
  declare a value slot beside keyword children. Drove each (selector, modifier)
  pair through `handleShow` with a generic `grpc.ClientConnInterface` recorder.
  Six pairs silently dropped the selector — ONE named on the issue
  (`show interfaces <if> extensive`) and five it does not contain
  (`<if> queue`, `<if> statistics`, `<if> tunnel`, and both
  `show security zones <zone> {detail,terse}` forms).
- **File(s)**: — (probe, since removed)

- **Timestamp**: 2026-09-06
- **Action**: Fixed the ORDERING rather than the members: added
  `cmdtree.SplitModifiers` / `SplitModifiersAt`, a position-independent split
  driven by the tree, and routed `showInterfaces` and the `show security zones`
  arm through it.
- **File(s)**: `pkg/cmdtree/modifier_split.go`, `cmd/cli/show_interfaces.go`,
  `cmd/cli/show_security.go`

- **Timestamp**: 2026-09-06
- **Action**: A client-only fix would have been INERT for two modifiers: the
  daemon ignored `req.Filter` for `interfaces-extensive`, `interfaces-statistics`
  and `zones-detail`, so a bound selector would have shown the operator EVERY
  interface/zone with nothing saying it was discarded. Added server-side
  filtering to all three, using `showInterfacesDetail`'s exact-match rule.
- **File(s)**: `pkg/grpcapi/server_show_interfaces_text.go`,
  `pkg/grpcapi/server_show_zones_text.go`, `pkg/grpcapi/server_show.go`

- **Timestamp**: 2026-09-06
- **Action**: Fixed the three members whose SHAPE the census cannot reach, each
  recorded as a distinct shape: the zone-name TYPE break
  (`strconv.ParseUint` on `trust`), the NAT pool DISCARDED operand, and
  `handlePing`'s DISCARDED errors.
- **File(s)**: `cmd/cli/show_flow.go`, `cmd/cli/show_nat.go`, `cmd/cli/main.go`

- **Timestamp**: 2026-09-06
- **Action**: Wrote the completeness gate, then measured its blind spot and
  narrowed the claim rather than shipping it. A per-node "at least one modifier
  must carry the selector" floor was written, run, and WITHDRAWN: it reported
  `show security policies from-zone X to-zone Y` and the cos rewrite-rule pair
  as silent drops, and both are correct code that scopes CLIENT-SIDE. A gate
  that manufactures evidence for a defect that is not there is worse than one
  with a stated hole. Kept a GLOBAL carry floor, which kills "refuse
  everything" without asserting anything about client-side scoping.
- **File(s)**: `cmd/cli/selector_survives_modifier_9065_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Two mutations of the SHARED helper survived the integration gate
  (its predicate is "carried OR refused", and a broken split refuses
  everything). Guarded the helper where the helper lives instead of weakening
  the gate. Mutation table, 9 cells, all applied+built+bounded: 9 KILLED.
- **File(s)**: `pkg/cmdtree/modifier_split_9065_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Documented the rule.
- **File(s)**: `cmd/cli/README.md`
