# #7740 — the GC-reap NAT64 release call site was unbound

- **Timestamp**: 2026-08-27
- **Action**: Bind it, and enumerate every call in the reap loop
  - **The reap loop's full census**, which is the question that is cheap now and
    expensive to rediscover:

    | call in `reap_expired_sessions` | bound? | by |
    |---|---|---|
    | `release_source_nat_allocation_for_worker` | **BOUND** | #6901's two cells |
    | `release_nat64_allocation_for_worker` | was UNBOUND | **this PR's two cells** |
    | `delete_session_map_entry_for_removed_session_with_origin` | **UNBOUND** | nothing — measured, see below |
    | the `invalidate_slot` loop | **BOUND** | #3776's `reaped_session_flow_cache_slot_is_invalidated` |

  - **Third finding: the session-map delete is also unbound.** Neutering
    `delete_session_map_entry_for_removed_session_with_origin` leaves the crate
    green at 4676 collected. **But "unbound" and "unbindable with what exists"
    are different claims**: that call writes through a BPF map fd, and every
    existing cell in this module passes `-1` for the fds, so no current fixture
    can observe it. Binding it needs a map-fd fixture that does not exist yet.
    Recorded as a finding, not folded in.
  - **Two cells, mirroring #6901**, because "the reap releases" and "the reap
    releases the allocation THIS session holds" are different claims:
    - the first uses the observable `nat64_4381_release_untracks_flow` already
      relies on — a live flow's re-allocation is IDEMPOTENT, so a different port
      after the reap is the release and nothing else. The idempotence is a
      PRECONDITION assert, so the post-reap difference cannot be read off a flow
      that was never live.
    - the second runs two flows through a ONE-ADDRESS pool, so only the
      translated port disambiguates, reaps one, and requires the other is still
      live and idempotent.
  - **Paired proof, as specified**: neutering the NAT64 release reds the two new
    cells at **4678 collected** — the control count — and **#6901's two cells
    stay green**. The two call sites are two lines apart, so "it reds" is cheap
    and "it reds the right thing" is what needed showing.
  - **A flake, characterised not attributed**:
    `slowpath::tests::enqueue_refuses_frame_above_live_mtu` failed once in the
    control run and passed in the mutation run at the same tree, then 5/5 in
    isolation. Six green to one red, on a test my diff cannot reach (test-only
    additions in another module). Third distinct load-sensitive flake this
    campaign, after the WG one and #7728's process-global GRE counter.
  - **My own error, worth recording**: I left a background mutation job running
    against `loop_body/mod.rs` and then edited that same file. Its closing
    `git checkout --` reverted my uncommitted test module — silently, with a
    clean tree afterwards. **One harness at a time per worktree**; a restore is
    indiscriminate about whose work it undoes. I had this exact lesson from the
    #6846 matrix and repeated it.
  - **File(s)**: `userspace-dp/src/afxdp/worker/loop_body/mod.rs`,
    `docs/log/7740.md`
