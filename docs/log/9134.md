# 9134 — HEAD bypassed read authorization on all 40 guarded routes

- **Timestamp**: 2026-09-06
- **Action**: `readAuthz` now resolves the permission through
  `readPermissionFor`, which adjudicates a safe non-GET method against the GET
  entry (the mux routes HEAD to the GET pattern). Fixed at the resolver rather
  than by adding 40 `"HEAD "` table keys.
- **File(s)**: pkg/api/authz.go, pkg/api/head_authz_bypass_9134_test.go
