# 9017 — firewall family any / closed-world

- **Timestamp**: 2026-09-05
- **Action**: declared `any` as a `firewall family` child sharing inet's subtree;
  set `closedWorld: true` on the `family` compound key so an undeclared
  address-family token is refused instead of silently voiding the filter.
- **File(s)**: pkg/config/schema_cos.go,
  pkg/config/schema_firewall_family_any_9017.go,
  pkg/config/firewall_family_any_9017_test.go, docs/config-schema.md
- **Update**: `closedWorld: true` reverted (it inherits and closed the whole
  filter grammar); replaced with `validateFirewallFilterFamilyTokensAST`, a
  prewalk gate. `any` switched from a shared children map to a deep copy after
  #8768 showed node-identity dedup hiding one of the two paths. Added
  `any filter` to the compact-normalize scope, without which the compact
  spelling still minted zero filters.
- **File(s)**: pkg/config/compiler_firewall_family_9017.go,
  pkg/config/compiler_prewalk.go, pkg/config/compact_normalize_scope.go,
  pkg/config/schema_firewall_family_any_9017.go, plus census registrations in
  8436/8690/8763/8768/8880/8929/7484 and the 2419 differential gate.
