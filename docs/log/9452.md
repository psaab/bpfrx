# #9452 — `make test-failover` red on master: manual failback leaves no RG owner

## 2026-09-09T21:52Z — Action: read the live cluster BEFORE theorising
- **File(s)**: none (measurement)
- Captured `show chassis cluster status` on both nodes of
  `loss:xpf-userspace-fw{0,1}` under `with-cluster.sh`. All three RGs reported
  `Transfer ready: yes` and `Takeover ready: yes` on BOTH nodes — so the
  transfer-readiness reason the acceptance asked for was not the false term.
- Also surfaced: `Warning: peer wall clock is 90s ahead of this node (fabric
  auth window 30s)` — `System clock synchronized: no` on both nodes, node0 83s
  behind node1.

## 2026-09-09T21:53Z — Action: falsify the obvious hypotheses
- **File(s)**: none (measurement)
- A single `request chassis cluster failover redundancy-group 0` from the
  primary moved RG0 to node0 in under 5s. All THREE issued back-to-back also
  moved within 5s. So the manual-failover path is not broken in steady state,
  and the back-to-back loop is not the trigger. The reboot is.

## 2026-09-09T21:55Z — Action: reproduce `make test-failover` at origin/master
- **File(s)**: `/tmp/.../run1.log`
- 20 passed / 4 failed, identical cells to the issue. Ledger row recorded VOID
  (`exe_check=UNAVAILABLE`); the gate's own exit status was 1.

## 2026-09-09T21:56Z — Action: name the mechanism from the journal
- **File(s)**: none (measurement)
- fw0 (rejoining, pid 734) logged at 5 Hz for the whole window:
  `cluster: election blocked by readiness gate rg={0,1,2} ready=false
  reasons="[session sync startup hold: bulk sync not yet complete]"`
- Timeline on fw0's clock: hold armed 21:54:57.567 (`no-RETH startup promotion
  hold active timeout=30s`); manual failover landed ~21:55:11 (14s in); test
  read at +5s; hold released 21:55:27.568 with
  `reason=timeout-degraded`; all three RGs promoted at that same instant.
- So every RG DID move — 19s late. fw1 had demoted immediately, so for those
  19s the RGs were owned by NEITHER node. That also explains the 4th red cell
  (`ip neigh show proxy` empty on BOTH nodes) and the throughput fall from 22.5
  to 18.1 Gbit/s (~20-25s of a 120s stream carrying nothing).
- Contrast, same node, 2026-08-31 boot: hold released at +6s with
  `reason=bulk-sync-complete`. The hold running to its full timeout is what
  widened the window; the fabric-auth clock skew is the plausible reason bulk
  sync no longer completes, and is filed separately.

## 2026-09-09T22:05Z — Action: fix the readiness gate's missing third case
- **File(s)**: `pkg/cluster/election.go`
- Added `peerYieldedOwnership` + `readinessReasonText`; rewired `runElection`'s
  readiness gate so a promotion driven by a LIVE peer reporting non-ownership
  (`StateSecondaryHold`, or weight 0) promotes DEGRADED instead of holding. A
  peer that is merely SECONDARY with weight > 0, or absent, still holds.

## 2026-09-09T22:10Z — Action: add the binding cells
- **File(s)**: `pkg/cluster/election_peer_yielded_9452_test.go`
- 7 cells, all driving `runElection` (not the predicate) plus one source-level
  wiring cell. Two controls: cold boot must still HOLD, and a live primary must
  not be auto-preempted. Behavioural cells run in BOTH preempt modes because the
  live cluster is non-preempt and the two reach the gate by different branches.

## 2026-09-09T22:25Z — Action: mutation matrix
- **File(s)**: `scripts/mutate-9452.py`
- 6/6 KILLED, 0 SURVIVED, 0 VOID. Baseline 1259 tests ran, 0 failed.

## 2026-09-09T22:30Z — Action: make the harness able to tell the two apart
- **File(s)**: `test/incus/test-failover.sh`
- Fixed `sleep 5` -> bounded poll reporting elapsed seconds per RG; failure text
  carries `rg_ownership_diagnosis` (both nodes' readiness lines, the peer's view,
  the election journal tail); the `request` command's stderr is captured instead
  of discarded. Cell count unchanged (one aggregate pass, per-RG fails).

## 2026-09-09T22:35Z — Action: module docs
- **File(s)**: `pkg/cluster/README.md`, `docs/ha-failover-status.md`

## 2026-09-09T22:33Z — Action: validate on the cluster (deploy + CoS + gate)
- **File(s)**: `test/results/ledger.d/4d2a5361dcae8af1.json`
- `make cluster-deploy` -> `apply-cos-config.sh` -> `make test-failover` in one
  `with-cluster.sh` lock cell. **22 passed, 0 failed, verdict=PASS.**
- The four previously-red cells are GREEN, and the transfer is now immediate:
  `RG0 moved to fw0 in 0s`, `RG1 in 0s`, `RG2 in 1s` (was 19-30s).
  `only the RG owner answers proxy-ARP for 172.16.80.7` PASSES.
- All six control cells the issue names as "green today and must stay green" are
  still green, including `fw0 rejoined as secondary for every redundancy group
  (no auto-preempt)` and `fw1 remains primary after fw0 rejoin`.

## 2026-09-09T22:40Z — Action: attribute the throughput signal, and RETRACT my own attribution
- **File(s)**: `pkg/cluster/election.go`, `pkg/cluster/README.md`,
  `docs/ha-failover-status.md`,
  `pkg/cluster/election_peer_yielded_9452_test.go`
- I had written into three docs and one code comment that the 22.5 -> 18.1
  Gbit/s fall was "the same defect measured a second way". The arithmetic was
  convincing — ~25s of a 120s stream carrying nothing is about that shortfall —
  and it is WRONG. The fix refuted it: with the ownership gap closed to 0-1s the
  average came back at **18.9**, and
  `make harness-compare GATE=test-failover` puts the green band at
  `[21.375, 23.625]`, median 22.5. Closing the gap bought ~0.8 Gbit/s, not ~4.4.
- So: **SEPARATE signal.** It fails nothing (the gate's own bar is 1.0 Gbit/s)
  and every invariant held, which is why harness-compare classifies it
  `flake-candidate — re-run THIS gate before bisecting`. Every claim above was
  corrected in place rather than left standing with a hedge.

## 2026-09-09T22:45Z — Action: file the residue separately
- #9482 — the HA cold-prime bulk sync never runs: #9344 moved the daemon's
  `userspaceSessionExporter` to `ExportOwnerRGSessionsPaged`, which
  `*LegacyDataPlaneAdapter` (the only `RuntimeDataPlane` the daemon publishes
  under the userspace backend) does not have, so the type assertion in
  `userspaceBulkSnapshot` fails on every attempt. Read out of the compiler with
  `go doc`, with the `Manager` as the positive control. This is why the #7162
  hold now always runs its full 30s and always releases `timeout-degraded` —
  but it is NOT why a manual failover blackholed the RG, since that hold is
  documented as releasing on its own timer regardless of sync state.

## 2026-09-09T23:05Z — Action: second gate run (one green run is one sample)
- **File(s)**: `test/results/ledger.d/22aef9f5d64378a9.json`
- `make test-failover` again, same build, no re-deploy: **22 passed, 0 failed,
  verdict=PASS**. RG0 1s, RG1 0s, RG2 0s. Throughput 20.3 Gbit/s.
- So the throughput post-fix set is {18.9, 20.3} against a green band of
  [21.375, 23.625]. Both below it, both far above the gate's own 1.0 bar, and
  the 1.4 spread between two identical runs is a third of the remaining gap —
  which is exactly why harness-compare calls it `flake-candidate`. Reported as a
  SET, not as the first sample. Filed as #9484, with the two standing cluster
  conditions (NTP skew past the fabric auth window, and #9482's permanently
  failing cold-prime bulk) named as things to rule out before any bisect.

## 2026-09-09T23:46Z — Action: exercise the diagnosis helper, which until now had never RUN
- **File(s)**: none (measurement)
- `rg_ownership_diagnosis` only executes on a FAILING cell, and both post-fix
  runs passed — so the new diagnostic was inert code carrying an unfalsifiable
  claim. Extracted the function text verbatim out of `test-failover.sh` with
  `sed` (not retyped) and ran it against the live cluster under the lock. All
  three channels render: both nodes' RG block, both views, and a journal tail.
- That probe also produced the strongest evidence in this issue — the MECHANISM,
  not just the outcome. fw0's journal for the second gate run:
  ```
  22:35:18.578  cluster: no-RETH startup promotion hold active timeout=30s
  22:35:39.816  cluster: promoting NOT-READY RG rg=0 reason="Promoted DEGRADED:
                peer transferred out (secondary-hold) while this node is not ready
                (session sync startup hold: bulk sync not yet complete); holding
                secondary would leave redundancy group 0 owned by neither node"
  22:35:39.816  cluster: primary transition rg=0
  22:35:40.2    ... rg=1 ... 22:35:40.6 ... rg=2
  22:35:48.578  cluster: no-RETH sync hold timeout ... releasing promotion hold
                in degraded mode
  ```
  The promotion happened 8.8s BEFORE the hold expired, through the new escape —
  not because the timeout eventually fired. A green cell alone could not have
  distinguished those two.

## 2026-09-09T23:50Z — Action: fix a diagnostic the fix made wrong
- **File(s)**: `pkg/cluster/election.go`
- `runElection`'s warn read `cluster: promoting NOT-READY RG after degraded
  timeout`. That site now has TWO causes (the #7161/#7939 timeout and a yielded
  peer), so naming one in the message made it wrong half the time — visible in
  the journal above, where a yielded-peer promotion was announced as a timeout.
  The message is now `cluster: promoting NOT-READY RG` and the cause stays in
  `reason`, the field that varies. `electSingleNode`'s twin still has exactly one
  cause and still names it.

## 2026-09-09T23:55Z — Action: VOID a `make test` run, and say why
- **File(s)**: none
- The second `make test` reported three named reds —
  `TestSpellingGateMembersAreRecorded7484` (pkg/config),
  `TestDeadResponderIsEvictedAndRestarted9237` (pkg/daemon),
  `TestSessionSocketNamesTheResponseCap9322` (pkg/dataplane/userspace) — in three
  packages, none of which this diff touches. It reads like a shared-cause
  regression.
- It is VOID, and the cause was mine: I ran `git rebase origin/master` in that
  worktree **while the run was still live in it**, so files were rewritten under
  a running `go test ./...`.
- The DECISIVE evidence is the pkg/config cell's own output, not a control run.
  It compares a recorded schema enumeration (`testdata/spelling_gate_members_7484.txt`)
  against the live schema, and the leaf it reported as DEPARTED —
  `snmp community <*> routing-instance <*> clients` — is exactly the leaf #9483
  ADDED in the commits that rebase pulled in. A leaf can only depart an
  enumeration that already records it, so the test had to be reading the
  post-rebase artifact against a schema that was not yet, or no longer, the
  matching one. That is a tree-mid-rewrite signature, not a diff.
- Stated precisely, because a control only excludes the variables held fixed in
  BOTH arms: the pristine-`origin/master` worktree does pass the cell
  (`ok pkg/config 84.395s`), but that arm differs from the voided run in TWO ways
  — tree content AND tree stability — so by itself it cannot say which mattered.
  The arm that isolates stability is the re-run below: same content, quiesced
  tree, nothing touching the worktree.
- Recorded because this is a void shape the scoring rules did not list: the tree
  mutating under the run leaves no ENOSPC line, no build break and no hang — just
  named reds in packages you did not touch. Never rebase a worktree with a run
  live in it; re-run on a quiesced tree.
- Note on the ENOSPC void check itself: `pkg/configstore/persist_failure_test.go`
  defines `errDiskFull = errors.New("injected: no space left on device")`, so a
  HEALTHY full-tree run can print that string. The check has to be
  `grep "no space left on device" | grep -v "injected:"` or it manufactures false
  VOIDs. Every log in this issue is 0 under both forms, so no run here was
  voided for disk and none should have been.
