# 7422 — show-surface rows where a configured value is printed as live state

- **Timestamp**: 2026-09-02
  - **Action**: Re-verified all 8 rows at master `04f94e9c3`. **Rows 3, 6,
    7 and 8 are already CLOSED** — row 6's ifindex sub-claim by #8319
    (my own #7357 work, an hour earlier), rows 3/8/11 by earlier work
    under this issue. Every closure confirmed by what the renderer
    EMITS, not by the presence of a `*_7422_test.go` beside it: all four
    have one, and a test file proves intent, not outcome.
  - **File(s)**: (issue comment)

- **Timestamp**: 2026-09-02
  - **Action**: Row 4 fixed at TWO sites; the issue names one. The
    renderer at `cos_show.go` read `TransmitRateBytes` alone, and the
    three transmit-rate forms are mutually exclusive, so a
    `percent`/`remainder` scheduler always rendered `-`. The second site
    is worse and unnamed: `cos_sections.go` keyed `guaranteeEnabled` off
    the configured bytes, and the runtime merge below only adopts the
    dataplane's RESOLVED rate when that flag is set — so a percent
    scheduler had its true, already-known absolute rate discarded on
    `show class-of-service interface`.
    Verified the Rust side first rather than trusting the row: percent
    resolves in `cos_effective_transmit_rate_bytes` (#4228 Gap 2) and
    remainder through #6846's sibling pre-pass. So the
    `ACCEPTED-BUT-INERT` doc at `types_cos.go` was stale for BOTH fields
    — a rationale that outlived the fix invalidating it, and it had gone
    on to justify the renderer's dash. Corrected, and the two mechanisms
    recorded as different because they are easy to assume shared.
  - **File(s)**: `pkg/dataplane/userspace/format/cos.go`,
    `pkg/dataplane/userspace/format/cos_show.go`,
    `pkg/dataplane/userspace/format/cos_sections.go`,
    `pkg/config/types_cos.go`,
    `pkg/dataplane/userspace/format/transmit_rate_forms_7422_test.go`,
    `docs/log/7422.md`

- **Timestamp**: 2026-09-02
  - **Action**: Row 13 nearly struck in error, recorded because the
    near-miss is the point. The log flags ARE threaded end to end
    (`builder.go:108` -> `default_log_session_init` on the wire ->
    `forwarding_build/mod.rs:548` -> `policy.rs:3031`), which reads as
    "not inert, strike the row". But the commit advisory
    (`validateDefaultPolicyLogWarnings`, #3534) says they are inert when
    the default verdict is DENY/REJECT — no session is installed for the
    records to fire on, and the deny is logged unconditionally by the
    policy-deny record instead. Live only under default-PERMIT. So the
    row REPRODUCES conditionally and the renderers surface the flags
    unconditionally. The evidence for the strike and the evidence
    against it were one level apart, same as the `cli_show_routing.go`
    grep on #7357.
  - **File(s)**: (issue comment)
