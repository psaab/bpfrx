# 8971 — a trailing token on a `then` action was silently discarded

- **Timestamp**: 2026-09-06
- **Action**: the children walk now records tokens beyond a `then` action's
  declared arity in `UnknownActions`, so the existing #2399 strict gate refuses
  them and the tolerant path warns. Arity read from the schema, not listed.
  This also settles item 1: the A–F divergence was over malformed input, which
  no longer commits.
- **File(s)**: pkg/config/compiler_firewall.go,
  pkg/config/then_trailing_token_8971_test.go
