# #8879 batch 2 — admitting four more elision pairs

- **Timestamp**: 2026-09-05
- **Action**: adjudicate + admit 4 of the 31 remaining SILENT rows
- **File(s)**: `pkg/config/compact_normalize_8662.go`,
  `pkg/config/elision_admissions_8879_test.go`,
  `pkg/config/compact_scope_blind_pairs_8852_test.go`

## Verdict table (gate column FIRST)

The gate column comes first deliberately: a row whose elided spelling is
REJECTED or WARNED must not be admitted, because admitting it converts a loud
operator-visible refusal into a silent acceptance (#8868, `system login`). Only
after the gate says accept/accept does the compiled-value column mean anything.

| pair | gate braced/elided | warns b/e | braced | elided | braced LIVE | verdict |
|---|---|---|---|---|---|---|
| `security address-book` | accept / accept | 0 / 0 | `addrs=1`   | `<nil>`     | yes | SILENT-DROP |
| `protocols ospf`        | accept / accept | 0 / 0 | `areas=1`   | `<nil>`     | yes | SILENT-DROP |
| `chassis device-map`    | accept / accept | 0 / 0 | `entries=1` | `<nil>`     | yes | SILENT-DROP |
| `system ntp`            | accept / accept | 0 / 0 | `servers=1` | `servers=0` | yes | SILENT-DROP |

**Ratio: 4 admitted, 0 declined.** After admission all four read `differ=false`
with the braced arm still live — the *fold-does-the-work* label, not LATENT and
not INERT: they DIFFERED before admission, so the stanza is neither inert nor
merely latent.

## Corpus sweep

Shipped-config sweep after the batch: **9 pass / 5 fail — identical to the
pre-batch baseline.** The five failures are pre-existing and unrelated; no
shipped config changed verdict.

## Mutation matrix

Mutants remove one admission each from the scope predicate — production code,
not the test, because deleting a control is a no-op by design.

| mutant | executed | killed assertion | liveness |
|---|---|---|---|
| drop `chassis device-map`    | 8 subtests | `:143` property (`Errorf`) — `entries=1` vs `<nil>`   | `:136` still passing |
| drop `protocols ospf`        | 8 subtests | `:143` property (`Errorf`) — `areas=1` vs `<nil>`     | `:136` still passing |
| drop `security address-book` | 8 subtests | `:143` property (`Errorf`) — `addrs=1` vs `<nil>`     | `:136` still passing |
| drop `system ntp`            | 8 subtests | `:143` property (`Errorf`) — `servers=1` vs `servers=0` | `:136` still passing |

Each mutant killed ONLY its own row: the matrix is as wide as the arms mutated,
and all four arms were mutated separately.

## The liveness assertion got stronger this batch

Batch 1 asserted liveness as "braced is not `<nil>` and not empty". That admits
a fixture whose compiled value equals the COMPILED DEFAULT, and such a row reads
CLEAN WHILE BROKEN — losing the value and keeping it produce the same answer.
Liveness is now asserted against an EMPTY config.

Control for the new check (mutating the FIXTURE, which is what the check
guards): swapping the `system ntp` fixture to `boot-server`, so the braced arm
compiles to `servers=0` — exactly what an empty config produces — fires `:136`
with the default-value diagnosis. Under the batch-1 check that same fixture
passed as "live", since `servers=0` is neither `<nil>` nor empty.

## Blind-pair registration

All four are blind to the #8852 census for the same structural reason as batch
1, so `TestScopeWideningYieldsAdjudicatedSites8852` red'd on exactly these four
new blind pairs (29 total). Shapes were re-derived, not assumed:
all four are `plain-container`, and they are registered with that shape.

## No smoke owed

Nothing here touches cluster, VRRP, session sync, or failover — the change is
confined to the config-mode brace-elision scope predicate and its guards, so
`make test-failover` is not owed. Stated explicitly rather than omitted.

## No doc change owed

`docs/config-schema.md` describes the admission MECHANISM (the three-decision
table at "the pair is admitted to `compactNormalizeInScope`") but does not
enumerate the admitted pairs and carries no pair count, so adding four pairs
leaves nothing in it stale.
