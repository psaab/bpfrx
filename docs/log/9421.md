# 9421 — screen family node packed tail

- **Timestamp**: 2026-09-07
- **Action**: Measured the defect through all reachable config channels in an
  owned worktree off `origin/master` (`19b082fe8`). Hierarchical
  `ids-option P { icmp [ ping-death fragment ]; }` was ACCEPTed by
  `SchemaValidate`, `CompileConfig` (strict), `CompileConfigLenient` and
  `configstore.CheckText` with `ICMP.PingDeath=false`, `UnknownLeaves=[]`,
  `Warnings=[]`; the identical flat-set statement was REJECTed by strict
  `CompileConfig` with the #1960/#3318 message and compiled
  `PingDeath=true`, `UnknownLeaves=[icmp ping-death fragment]` on the lenient
  path. Three divergent observables for one statement.
- **File(s)**: (probe only, removed before commit)

- **Timestamp**: 2026-09-07
- **Action**: Widened the probe past the reported case. The family depth loses
  EVERY packed spelling, not only the bracketed one: `icmp ping-death;`,
  `ip tear-drop;`, `udp flood threshold 900;`, `icmp flood threshold 500;` and
  — the gate-reachability half — `tcp bogus-check;`, which the flat shape
  REJECTS and the hierarchical shape accepted in silence.
  `limit-session source-ip-based 100;` was the one that already worked: its
  pair is admitted to `compactNormalizeInScope`, which is the mechanism #9056
  owns and the reason the other four families were not covered.
- **File(s)**: (probe only, removed before commit)

- **Timestamp**: 2026-09-07
- **Action**: Implemented the normalisation. `screenNormalizeFamilies` /
  `screenFamilyBody` expand a family node's packed tail into the chain shape
  `SetPath` produces for the identical statement; `compileScreen` calls it
  immediately after the existing `packedBody(inst.node, idsSchema)`.
- **File(s)**: `pkg/config/compiler_security_screen_family_9421.go` (new),
  `pkg/config/compiler_security_screen.go`

- **Timestamp**: 2026-09-07
- **Action**: Added the acceptance table — a cell per screen family for the
  nested, brace-elided and bracketed spellings, asserting in BOTH AST shapes
  that the strict verdict is identical, that `SchemaValidate` agrees, and that
  the compiled profile (every typed field, not merely that the profile exists)
  is identical; plus the refusal-message cell and an AST-not-mutated cell.
- **File(s)**: `pkg/config/screen_family_packed_tail_9421_test.go`,
  `pkg/configstore/screen_family_packed_tail_9421_test.go`

- **Timestamp**: 2026-09-07
- **Action**: Documented the depth rule.
- **File(s)**: `docs/config-schema.md`

- **Timestamp**: 2026-09-07
- **Action**: Validation. `go test ./...` (whole repo, not a package list) green
  before and after. Mutation proof recorded in the PR body.
