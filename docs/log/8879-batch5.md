# #8879 batch 5 — two of the four were filed as BENIGN

- **Timestamp**: 2026-09-05
- **Action**: adjudicate 4 pairs, two of them recovered from the sweep's SAME column
- **File(s)**: `pkg/config/compact_normalize_8662.go`,
  `pkg/config/elision_admissions_8879_test.go`,
  `pkg/config/compact_scope_blind_pairs_8852_test.go`

## The headline

Batch 4 established that the sweep's verdicts can be contingent on the single
leaf `synthPair` synthesises. That finding implies something uncomfortable that
batch 4 did not act on: **the SAME column is where a wrong verdict is
invisible.** A row the instrument clears is never re-opened.

After batch 4 removed the 15 contaminated rows, only 6 genuine SAME rows were
left — small enough to check by hand. Five were checkable (the sixth,
`bridge-domains <name>`, is instance-named and structurally undeclarable):

| SAME row | re-checked by hand | verdict |
|---|---|---|
| `chassis cluster` | identical | genuinely SAME |
| `class-of-service classifiers` | identical | genuinely SAME |
| `class-of-service rewrite-rules` | identical | genuinely SAME |
| `class-of-service fairness` | **DIFFERS** | **SILENT DROP** |
| `security policy-stats` | **DIFFERS** | **SILENT DROP** |

**Two of five rows published as benign are silent drops.**

- `class-of-service fairness` — the entire `FairnessExpectations` list is lost
  (`[…]` becomes `null`).
- `security policy-stats` — `PolicyStatsEnabled` flips from **true to false**.
  A security feature is silently turned off by the choice of spelling.

Neither would ever have been re-examined: they were counted in the benign
column of a published table.

## Verdict table (gate column FIRST)

| pair | provenance | gate | warns b/e | strict b/e | braced | elided | LIVE | INERT |
|---|---|---|---|---|---|---|---|---|
| `class-of-service fairness` | **SAME column** | SILENT | 0/0 | ok/ok | `fair=1` | `fair=0` | yes | no |
| `security policy-stats` | **SAME column** | SILENT | 0/0 | ok/ok | `stats=true` | `stats=false` | yes | no |
| `security ipsec` | SILENT column | SILENT | 0/0 | ok/ok | `ipsecprop=1` | `ipsecprop=0` | yes | no |
| `services rpm` | SILENT column | SILENT | 0/0 | ok/ok | `rpm=1` | `<nil>` | yes | no |

**Ratio: 4 admitted, 0 declined** — and unlike batch 2, that is not an artifact
of drawing only from the SILENT column, since half this batch came from the
column that is supposed to contain no defects at all.

## A fixture that failed strict commit — caught, not shipped

The first `services rpm` fixture omitted `probe-type` and `target`, so its
BRACED arm failed strict compile. That is precisely the unanswerable third
state the batch-4 cell exists to resolve, and shipping it would have made this
row look measured while measuring nothing. Corrected before use; the cell
carries a comment saying why both leaves are required.

## Mutation matrix

| mutant | executed | killed |
|---|---|---|
| drop `class-of-service fairness` | 20 subtests | `:271` — `fair=1` vs `fair=0` |
| drop `security policy-stats` | 20 subtests | `:271` — `stats=true` vs `stats=false` |
| drop `security ipsec` | 20 subtests | `:271` — `ipsecprop=1` vs `ipsecprop=0` |
| drop `services rpm` | 20 subtests | `:271` — `rpm=1` vs `<nil>` |

**A mutation-harness defect was caught by the integrity check, not by the
matrix.** The first run reported "MUTANT DID NOT APPLY" four times: the backup
had been taken BEFORE the admissions were applied, so the restore trap wiped
them. The post-matrix count check (`must be 4`) is what surfaced it. Counting
the effect rather than trusting the harness is the only reason this batch did
not silently ship with its admissions reverted.

## Corpus sweep

9 pass / 5 fail — identical to baseline.

## Smoke — reasoning, not a bare assertion

`security ipsec` touches an area the cluster consumes (IPsec SA sync), so the
usual one-line "no smoke owed" is not enough here. The change cannot regress an
existing deployment: the braced spelling already compiled and is untouched, and
the only behaviour change is that a previously-DROPPED elided spelling now
compiles its children. No cluster, VRRP, session-sync or failover CODE is
modified. The shipped-config corpus confirms no config changed verdict.
`make test-failover` is therefore not owed — stated with the reasoning so a
reviewer can disagree with it.

## No doc change owed

`docs/config-schema.md` documents the admission mechanism but does not
enumerate the pairs and carries no pair count.
