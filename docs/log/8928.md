# #8928 — validation-suppressing elision: the ip-monitoring instance was NOT fixed

- **Timestamp**: 2026-09-06
  - **Premise correction**: the issue records both named instances as fixed.
    Measured at master, only one is. `routing-options forwarding-table` rejects
    a dangling export policy in both spellings; `services ip-monitoring` did
    not:

    ```
    braced  bad rpm-probe -> REJECTED "match rpm-probe \"nosuchprobe\" does not reference a configured probe"
    elided  bad rpm-probe -> committed clean
    ```

    And the elided form suppressed a SECOND validation: the braced GOOD control
    is rejected for "at least one then preferred-route route is required" while
    the elided GOOD control commits clean — so the whole policy is dropped, not
    just its reference check.
  - **Cause — the #8858 shape, third instance**: the pair was already admitted
    and inert because a link above it was missing.

    ```
    services      -> ip-monitoring   ADMITTED
    ip-monitoring -> policy          MISSING    <- the break
    match         -> rpm-probe       ADMITTED   (unreachable)
    ```
  - **Scoped to stay out of #8921's hazard**: `policy match` is deliberately
    NOT admitted. Measured, it is effective at THREE schema sites — `security
    policies from-zone <n> policy`, `security policies global policy`, `services
    ip-monitoring policy` — so admitting it would change security-policy
    compilation to fix an ip-monitoring defect. It is not needed: the body from
    `match` inward is already braced. A cell pins that it stays unadmitted.
  - **Three census ratchets moved and were adjudicated, not regenerated**:
    - #8880 unsplittable population 453 → **454** (one more pair in scope, so
      one more unsplittable).
    - #8929 depth-2 un-admitted population 25 → **24** (the intended direction;
      the ratchet reds so an admission cannot pass unremarked).
    - #8690 registered `services ip-monitoring policy` as unexaminable, for the
      exception its own note carves out: the leaf under test IS the stanza's
      validity condition, so no control fixture can be built.
  - **File(s)**: pkg/config/compact_normalize_scope.go,
    pkg/config/ip_monitoring_elision_8928_test.go,
    pkg/config/compact_normalize_scope_8690_test.go,
    pkg/config/unsplittable_pairs_ratchet_8880_test.go,
    pkg/config/elision_admissions_8879_test.go
