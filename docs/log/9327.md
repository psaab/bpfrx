# #9327 — two worker-loop cost cliffs a prior validation scored NEG

- **Timestamp**: 2026-09-07
- **Action**: RE-MEASURED both rather than inheriting either verdict, per the
  brief. Item 1, my own fixture on this tree: n=16384 finds-nothing **1.745 ms**,
  n=60000 finds-nothing **6.466 ms**, n=60000 all-stale **39.148 ms**. Lower
  than the issue's numbers (12.28 / 59.83 ms — different machine) but crossing
  the same threshold decisively: the project's own `WORKER_COMMAND_DRAIN_BUDGET`
  is justified against a ~1.97 ms RX-ring fill, and the *finds-nothing* case is
  already there by 16k sessions. The NEG verdict is wrong: the epoch gate bounds
  FREQUENCY, not COST.
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: Item 2 re-measured independently: pops per claim K=15/F=1 ->
  `[16, 16]` (no amortization, ever), K=12/F=4 -> `[13,1,1,1,13,1,1,1]`. The
  in-code claim that the spike "amortizes itself" is FALSE; the real cost is
  (K+F)/F per claim, degrading to K per claim as F -> 1, i.e. worst exactly as
  an address approaches exhaustion.
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: Item 1 fixed by mirroring the in-tree `iter_with_idle_budgeted`
  (#6297) precedent: added `iter_with_origin_budgeted` and a resumable
  `DeleteDropSweep` stepped across worker-loop passes, buffer retained.
  `reconcile_peer_synced_against_shared` is kept as a run-to-completion wrapper
  so the five existing #8586 semantics cells pass UNCHANGED — pacing and
  semantics stay separately testable.
- **File(s)**: `userspace-dp/src/session/lookup.rs`,
  `userspace-dp/src/afxdp/session_glue/mod.rs`,
  `userspace-dp/src/afxdp/worker/loop_body/mod.rs`

- **Timestamp**: 2026-09-07
- **Action**: Item 2: corrected the comment with the measured cost rather than
  bounding the walk. The comment's OTHER argument — that a bare scan budget
  cannot tell "scanned enough" from "genuinely full", so `None` on budget
  exhaustion becomes a spuriously dropped NAT translation — is sound and
  unaffected, and it is the right reason to leave the walk unbounded. The
  amortization claim was the WRONG reason, and it was the stated one. Pinned by
  a cell so it cannot return.
- **File(s)**: `userspace-dp/src/nat/allocator.rs`,
  `userspace-dp/src/nat/tests_pool.rs`

- **Timestamp**: 2026-09-07
- **Action**: Mutation table, 5 cells. **M4 (reallocate the buffer every pass)
  SURVIVED** the first pass: the cell compared capacity ACROSS passes, and every
  pass refilled a fresh `Vec` to the same length and hence the same capacity.
  Re-shaped to observe capacity surviving a pass that collects NOTHING, which is
  the only shape that distinguishes a cleared buffer from a reallocated one.
  M1 was VOID(build) at `usize::MAX` and re-scored at a buildable large value.
  Final: 5/5 KILLED.
- **File(s)**: `userspace-dp/src/afxdp/session_glue/tests.rs`

- **Timestamp**: 2026-09-07
- **Action**: Documented the budget and the approximate-resumption contract.
- **File(s)**: `userspace-dp/src/afxdp/session_glue/README.md`
