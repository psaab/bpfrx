# 7357 — #6534 remainder: flow-export + the ifindex-dependent port-mirroring drops

- **Timestamp**: 2026-09-02
  - **Action**: Re-verified the remainder by symbol at master `236568b42`
    before scoping. **§1 (flow-export, three renderers) is already DONE**
    under #7422 — `config.FlowServerExcludedReason` exists and the
    builder plus all three named renderers consult it. §3 was already
    done under #8166. Only §2 remained.
    Near-miss worth recording: grepping for `FlowServerExcludedReason`
    returned no hit in `cli_show_routing.go` and I was one step from
    reporting "two of three renderers annotate, the third still lies" —
    which is the exact shape this issue is about and would have been
    confidently wrong. The call is one indirection away behind
    `flowServerNotInstalledSuffix`. An absence claim from a symbol grep
    needs a positive control; the cheap one was "does that renderer emit
    an annotation at all?"
  - **File(s)**: (issue comment)

- **Timestamp**: 2026-09-02
  - **Action**: §2 — the three runtime drops (the body counts two and
    misses `:81`) are recorded as verdicts at apply time and rendered.
    Design correction against the body's prescription: it says to thread
    "the resolved ifindex map" to the renderers, which invites
    re-derivation against a live interface table and answers a different
    question from the one the operator asked. The renderer gets the
    APPLIED verdict instead. Granularity is per-drop — instance-level
    for the unresolved output, input-level for the other two — because
    an instance with one claimed input is partially installed and a
    whole-instance NOT INSTALLED would be the same lie with the sign
    flipped.
    An existing #2114 canary (`TestOptionalCapabilityProbesUseDPProbe`)
    caught the capability assertion being made against the stored `dp`
    field instead of `dpProbe()`; under the live indirection that
    answers "absent" for a healthy backend, which would have rendered an
    excluded instance un-annotated. Fixed to `dpProbe()`.
    Five mutations across five layers, each caught: CLI surface passing
    nil, gRPC surface passing nil, input-level drops annotated at
    instance granularity, the readback re-deriving, and the readback
    handing out the snapshot's own backing array.
  - **File(s)**: `pkg/dataplane/userspace/mirrors.go`,
    `pkg/dataplane/userspace/manager_mirrors.go`,
    `pkg/dataplane/userspace/protocol.go`,
    `pkg/dataplane/userspace/builder.go`,
    `pkg/dataplane/userspace/format/mirror_show.go`,
    `pkg/daemon/daemon_dp_live.go`,
    `pkg/cli/show_services_mirror.go`,
    `pkg/grpcapi/server_show_forwarding.go`,
    `userspace-dp/src/main_tests.rs`,
    `docs/junos-cli-reference.md`, `docs/log/7357.md`
