# #7163 — Noise session-sync transport (IN PROGRESS, NOT READY)

## Premise check (worktree at origin/master `a56ced45b`)

- The issue thread says "the lane's PR A ... proceeds". **PR A never landed.**
  No Noise dependency, no identity plumbing, no version machinery, no
  characterisation tests, `SessionSyncWireVersion` still `1`. The only PR
  matching "7163" is #7935 (heartbeat interval, matched on text).
- The plan doc exists only on the unmerged branch
  `research/5078-syncauth-reflection` at `2d2390dfd`.
- The defect is intact: `syncAuthProof` at `sync_auth.go:325`,
  `syncDeriveFrameKey`'s canonical sort at `:379-382`, and
  `ac.readKey = ac.writeKey = frameKey` at `:649-650`.

## Scope finding

`sync_auth_upgrade.go` (#6628, in-place PSK upgrade) **shares both defective
primitives** — `syncAuthProof` at `:290`/`:413` and `syncDeriveFrameKey` at
`:323`/`:373`/`:438`, the last two with the SAME two nonces, so it is undirected
there too. Fixing only the connect handshake leaves a second admission path with
the identical oracle — and it becomes the one an attacker picks.

## What is DONE

- `github.com/flynn/noise` v1.1.0 pinned. Chosen over nyquist on evidence:
  nyquist has **no tagged release** (defeats "pin an exact version" and CVE
  tracking) and carries 4 production deps to flynn's 1.
- `sync_auth_noise_7163.go`: Noise_NNpsk0 handshake, prologue binding
  version + cluster-id + ROLE-ORDERED node ids + fabric index, directional keys
  from `Split()`.
- Role from the TRANSPORT (dialer = initiator), threaded through
  `handleNewConnection`'s two call sites.
- `wrapSyncConn` now writes two INDEPENDENT keys.
- `SessionSyncWireVersion` 1 -> 2, with the reasoning for not touching
  `CurrentHAProtocolVersion`.
- Superseded tests REPLACED, not deleted, with a supersession note naming where
  each guard went.

## What is NOT done — this branch must not be merged

1. **The #6628 upgrade path is still on the old primitives.** The oracle is
   still live there. This is the largest remaining piece.
2. Two accept-side tests fail and one hangs: the responder no longer speaks
   first, so fixtures that wait for a server HELLO need inverting.
3. No `make test-failover`, no full `go test ./...`, no release note.

## Defects found by running, not reading

- **Noise mandates a 256-bit PSK**; `ControlLinkAuthKey` is an arbitrary-length
  config string, so every real cluster would have failed the handshake.
  `syncNoisePSK` now derives 32 bytes via labelled SHA-256 (not truncation,
  which would collapse keys sharing a prefix; not zero-padding, which leaves
  most of the PSK constant).
- The happy-path fixture gave both ends node id 0, so their role-ordered
  prologues differed and the handshake failed. That was the identity binding
  **working**; the fixture was wrong.
