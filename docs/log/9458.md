# #9458 — Complete fix/6204-pci-func-decimal

Tracker: "#6204 still live on master (func parsed base-16); re-apply fix to
pkg/netname."

## Verdict

The tracker's premise is **false in the direction it states and true in a
different one**. The base-16 function parse is UNOBSERVABLE — not a bug — so
re-applying the branch's fix would land a no-op for the second time. What IS
live on master is the **false rationale defending that parse**, in three places,
one of them a TEST ROW that pins it.

## Timeline

- **Timestamp**: 2026-09-09 ~15:00
  **Action**: Read #6204 (CLOSED not-a-bug), PR #6320 (CLOSED, "does not fix the
  stated bug"), PR #6671 (CLOSED, "the premise is wrong and the patch is a
  no-op"), and #6677 (the REAL ARI gap, closed by merged PR #7420 `3c49cabd7` —
  derive the pre-rename kernel name from the kernel, not arithmetic).
  **File(s)**: none (read-only)

- **Timestamp**: 2026-09-09 ~15:05
  **Action**: Measured the structural bound on the live host. 175 PCI devices,
  46 ARI-enabled, every final field in `{0..7}` (max 7 at `0000:b7:0e.7`).
  `PCI_FUNC(devfn) = devfn & 0x07` — bits 7:3 are the slot — so base-16 and
  base-10 agree on every address the kernel can produce.
  **File(s)**: none (measurement)

- **Timestamp**: 2026-09-09 ~15:15
  **Action**: Corrected the false rationale; added the guard that keeps the
  correction falsifiable.
  **File(s)**: `pkg/netname/netname.go`, `pkg/netname/README.md`,
  `pkg/netname/netname_7426_test.go`,
  `pkg/netname/pci_function_domain_9458_test.go` (new)

## Acceptance table

| # | Cell | Before | After |
|---|------|--------|-------|
| A1 | `netname.go` rationale for the function parse base | "an ARI device can carry functions above 9 (up to 255), which a base-10 parse rejects outright" — FALSE | states the structural bound, names the real ARI mechanism (#6677 / PR #7420), says the base is unobservable in BOTH directions |
| A2 | `README.md` "Gotchas" bullet | same false claim + "Latent on the development host ... but not hypothetical" — reads the max-7 observation as sampling luck | states the bound as structural, publishes the denominator (175 devices), cites the guard |
| A3 | `netname_7426_test.go` row `"0000:b7:00.a"` | comment presents it as ARI coverage, so a red there reads as a broken guarantee | relabelled a CHANGE DETECTOR; comment states sysfs cannot emit this spelling and that the row is not evidence the base protects anything |
| A4 | anything re-measuring the bound | nothing — the claim was unfalsifiable | `TestPCIFunctionFieldIsThreeBits` reds if a kernel ever presents a field above 7 |
| A5 | the predicate behind A4 | n/a | `TestMaxFunctionFieldPredicate` drives it with inputs the host does not have, incl. `.a` (error, not 10) and empty (error, not a clean bound) |

No behaviour change: `FromPCIAddr` is byte-identical. Deliberately NOT changing
the parse base — the owner has closed two PRs that did, and a third no-op would
be re-litigating a settled question.

## Mutation score

Bound at 300 s per cell; a run consuming its budget scores VOID. Application
confirmed by occurrence count, never by exit status.

| Cell | Mutation | Applied (count) | Verdict |
|------|----------|-----------------|---------|
| BASE | none | live sysfs path 4 occurrences | PASS, `max 7 at 0000:b7:0e.7 over 175 devices, 46 ARI-enabled` |
| M1 | **sever the CALL SITE**: `os.ReadDir("/sys/bus/pci/devices")` → a fixture topology containing `0000:65:00.8` | live-path 4→2 (2 remaining are prose), fixture-path 0→2 | **KILLED** rc=1, `FAIL=1`, and the message is the RIGHT one: `PCI function field 8 at 0000:65:00.8 exceeds 7, over 3 devices (1 ARI-enabled)` |
| M2 | threshold sweep: M1's fixture **plus** `if max > 7` → `if max > 255` | `max > 7` 1→0, `max > 255` 0→1 | **SURVIVED — by design.** Reports `bound holds: max 8`. Proves the `> 7` predicate is what fires in M1, not something incidental to the fixture |
| M3 | call site → an EMPTY topology | `pci-empty` 0→1 | **SKIP**, `"/sys/bus/pci/devices is empty — nothing measured"` — a collected-nothing run does not read as a clean bound |

Restored from a `cp` backup after every cell and diffed byte-identical.
(`git checkout -- <file>` was deliberately NOT used: it restores to HEAD, which
would have deleted the then-uncommitted new test file.)

## Validation

- `go build ./...` rc=0; `go vet ./pkg/netname/` rc=0; `gofmt -l pkg/netname/` empty.
- `go test ./pkg/netname/` — 33 pass, 0 fail.
- **Base-controlled full-package run, `TMPDIR` UNSET.** Branch vs a clean
  `origin/master` worktree (`c525bb966`), same command, same three packages
  (`pkg/dataplane/userspace`, `pkg/daemon`, `pkg/netname`):

  | | RUN | PASS | FAIL |
  |---|---|---|---|
  | clean base `c525bb966` | 4898 | 4823 | **0** |
  | this branch | 4906 | 4831 | **0** |

  Delta exactly `+8 RUN / +8 PASS / +0 FAIL` — this change's 2 tests and 6
  subtests.
- **A VOID RUN I NEARLY SCORED, recorded because the near-miss is the lesson.**
  An earlier pass of the same three packages reported **74 FAIL on BOTH the
  branch AND the clean base**, 28 top-level names, concentrated in
  `pkg/daemon` + `pkg/dataplane/userspace`. I read the agreement as proof the
  reds were pre-existing and not mine. It was not proof: both runs had
  `TMPDIR=/var/tmp/gobuildtmp` exported, so **the control shared the premise
  with the thing it was controlling for**, which makes two runs one. The cause
  is a 108-byte `sun_path` limit — these tests build unix-socket paths from
  `t.TempDir()`, and `/var/tmp/gobuildtmp` (19 chars) overruns a 14-char budget,
  failing with `bind: invalid argument`, a message that names neither length nor
  the temp dir. With the variable unset both runs are green, as the table above
  shows. `TMPDIR`/`GOTMPDIR` must stay unset for this repo's Go suite.
- **ENOSPC discipline.** Every run was scored only after grepping for
  `no space left on device`. An early full-tree run reported 6 hits; all six
  were `injected:` fixtures in `pkg/configstore`'s own error-injection tests —
  a FALSE POSITIVE for the void check. The real count is 0 on every run above,
  so the void check must exclude the `injected:` prefix.
- Mutation cell M1 re-run with `TMPDIR` unset: **KILLED**, same message
  (`field 8 at 0000:65:00.8 exceeds 7, over 3 devices`), RUN=1 FAIL=1.
- No Rust file touched, so the `make test` Rust leg cannot be affected.
