# #9328 — oversized DF-clear IPv4 forwarded and booked as a success

- **Timestamp**: 2026-09-07
- **Action**: Re-derived the facts rather than inheriting the issue's
  `Executed` labels. Positive-controlled grep for MF/offset WRITERS: the only
  hits are `nat64.rs` (2477/3276/3853), which copy MF and offset verbatim from
  an existing IPv6 Fragment Header — the control proves the grep CAN find a
  writer. No generic MTU comparison anywhere in `tx/` (only the TCP
  segmentation path and a slow-path label). Confirmed: no IPv4 transit
  fragmenter.
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: Found the structural cause: the DF-clear oversize case returned
  the SAME `EgressMtuDecision::Forward` value as a frame that fits, so nothing
  downstream could tell them apart and the oversize submission was booked as
  `enqueue_ok` + `tx_bytes_total`. Added `ForwardOversizeNoDf` (behaviourally
  identical) and a `record_exception` at the dispatcher.
- **File(s)**: `userspace-dp/src/afxdp/icmp_ptb.rs`,
  `userspace-dp/src/afxdp/tx/dispatch/mod.rs`

- **Timestamp**: 2026-09-07
- **Action**: The cheap half only. The REAL half — fragment per RFC 791,
  drop-and-count, or keep forwarding — is a policy decision I did not take: the
  wire-level fate of the oversize submission is unmeasured, and changing
  forwarding on an unmeasured population is how a fix becomes a regression. A
  PTB is specifically wrong here (Fragmentation-Needed is meaningful only to a
  sender that set DF). Filed separately with the decisive next measurement.
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: `oversized_v4_without_df_forwards` red, exactly as the issue
  predicted. Read its stated reason first: it pins "forward, do not PTB-storm",
  which is TRUE and unchanged. What it also pinned silently was that the
  outcome is indistinguishable from a fitting frame — not a decision anyone
  took, the absence of a distinction. Claim kept verbatim, expected value
  updated, plus an in-MTU control so the new variant cannot leak the other way.
- **File(s)**: `userspace-dp/src/afxdp/icmp_ptb_tests.rs`

- **Timestamp**: 2026-09-07
- **Action**: Mutation table. **M3 (sever the record_exception) SURVIVED** the
  first pass — the decision-level cell proves the value is distinguishable and
  says nothing about whether anything acts on it. FIFTH instance of that shape
  this campaign. Bound by driving the real dispatcher harness and asserting the
  recorded reason, plus an in-MTU control. Re-scored 3/3 KILLED.
- **File(s)**: `userspace-dp/src/afxdp/tx/dispatch/tests/ptb.rs`

- **Timestamp**: 2026-09-07
- **Action**: Documented the population, the verification method, and the open
  policy.
- **File(s)**: `userspace-dp/src/afxdp/tx/README.md`
