# #8943 batch 1 — the `nat` family

- **Timestamp**: 2026-09-05
- **Action**: admit the five remaining `security nat` children; close the third
  #8928 fail-open
- **File(s)**: `pkg/config/compact_normalize_8662.go`,
  `pkg/config/elision_admissions_8879_test.go`,
  `pkg/config/compact_scope_blind_pairs_8852_test.go`

## Verdict table (gate column FIRST)

| pair | gate | warns b/e | strict b/e | braced | elided | LIVE |
|---|---|---|---|---|---|---|
| `nat destination` | SILENT | 0/0 | ok/ok | `dpools=1` | `<nil>` | yes |
| `nat static` | SILENT | 1/0 | ok/ok | `static=1` | `static=0` | yes |
| `nat nat64` | SILENT | 0/0 | ok/ok | `nat64=1` | `nat64=0` | yes |
| `nat natv6v4` | SILENT | 0/0 | ok/ok | `natv6v4=set` | `<nil>` | yes |
| `nat proxy-arp` | SILENT | 0/0 | ok/ok | `parp=1` | `parp=0` | yes |

**5 admitted, 0 declined.** `nat source` was admitted alone in #8929; its five
siblings behave identically.

## #8921 collision check — done before admitting, not assumed

The scope predicate is keyed on `(container, head)` with no parent context, so
an admission is effective at **every** schema site declaring that pair. Walked
the schema:

    nat destination  1 site      nat nat64    1 site
    nat natv6v4      1 site      nat proxy-arp 1 site
    nat static       1 site      nat source   1 site

One site each, so each admission is effective only where it was adjudicated.

## The third #8928 fail-open, closed — and controlled

`nat destination` is the consequential one. A destination-NAT rule naming a
pool that does not exist:

    braced, undefined pool   REFUSED   ("references undefined pool nope7717")
    elided, undefined pool   ACCEPTED  <- fail-open
    CONTROL: braced, DEFINED pool      ACCEPTED

The control matters: without it, "elided accepts" is equally consistent with the
validator not firing on that path at all — which is exactly how
`class-of-service forwarding-classes` nearly became a wrongly-reported third
instance. After the admission the elided spelling is refused too.

Added as the third row of `TestElisionSuppressedAValidation8879`. Three
instances now: `routing-options forwarding-table` (ECMP silently disabled),
`services ip-monitoring` (WAN failover never arms), `security nat destination`
(destination NAT silently does not translate).

## My own bound applied to my own batch

The depth-2 sweep's SAME verdicts are **not clearances** — it synthesises one
body statement per pair (#8946). Every row here was re-measured by hand with a
purpose-built fixture rather than taken from the sweep's DROPS column, and the
fail-open was found by asking a question the sweep does not ask at all.

## Guards that moved, and moved together

`TestDepth2UnadmittedPopulation8929` red on the admission, by design: the
population fell 50 → 45 and five `knownDropping` entries had to go with it
(26 → 21). Both numbers move together or the cell fires — which is the
two-sided property added after mutation showed a silent removal went unnoticed.

Blind-pair shapes derived by the sentinel method; all five `plain-container`.

## Mutations

Dropping each admission kills two assertions: the property at `:522` plus, for
`nat destination`, the fail-open guard at `:933`, and for the other four the
population count at `:1211`. 49 subtests executing; admission count re-verified
at 5 afterwards.

## Corpus

9 pass / 5 fail — unchanged.

## Smoke — reasoning, not assertion

`security nat` compiles into dataplane NAT, so a bare "no smoke owed" would be
too thin. No cluster, VRRP, session-sync or failover CODE is modified. The
braced spelling already compiled and is untouched; the only behaviour change is
that a previously **dropped** elided spelling now compiles its children, which
cannot regress a config that works today. The corpus confirms no shipped config
changes verdict.
