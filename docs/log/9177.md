# #9177 — bulk stat reset above the #8966 accept, and BulkAck accepting a FUTURE epoch

Two small, INDEPENDENT defects in one reader (`pkg/cluster/sync_conn_read.go`).
Unrelated edits, kept as separate cells.

## Inherited work

`/var/tmp/wt-9177` was left by a lane that stopped mid-task on a spend limit:
branch `fix/9177-bulk-stat-order-and-ack`, **0 commits ahead**, 2 dirty files —
a modified `pkg/cluster/sync_conn_read.go` and an untracked
`pkg/cluster/sync_bulk_stat_and_ack_9177_test.go`.

It was SALVAGED, not redone, after reading it against the issue's last comment.
Both edits are exactly the two the issue's "Fix direction" names, at the two line
numbers its liveness check re-derived, and the test file already carried the two
load-bearing controls the acceptance asks for. What this lane added: the rebase
onto current `origin/master`, the base-vs-fix control measurement, the mutation
matrix, this log, and the module-doc update.

## 2026-09-09T22:50Z — Action: rebase the inherited worktree
- **File(s)**: none (git)
- `git fetch origin` -> `git merge --ff-only origin/master` (a44439654 ->
  5e11bec91). `sync_conn_read.go` was UNCHANGED upstream across that span, so the
  inherited edit needed no adaptation. `git diff --stat origin/master` afterwards
  shows exactly one path, the one the inherited work touched.

## 2026-09-09T22:55Z — Action: re-verify BOTH defects at the new base
- **File(s)**: none (measurement)
- With the inherited production edit reverted to `HEAD:` and the test file kept,
  `go test ./pkg/cluster/ -run 9177 -count=1` reds exactly three cells:
  `TestRefusedBulkStartDoesNotReopenTheStatus9177`,
  `TestRefusedBulkStartDoesNotMiscountIncrementalInstalls9177` (V052) and
  `TestFutureEpochBulkAckIsRefused9177` (V053). The six control cells are GREEN
  at the base, which is what makes them controls rather than duplicates.
- With the edit restored: 9/9 pass, and the full `pkg/cluster` package is green
  (`ok ... 76.331s`).

## 2026-09-09T23:05Z — Action: mutation matrix
- **File(s)**: `scripts/mutate-9177.py`
- Four mutants, each scored on `go test -json` Action=="fail" with the NAME
  compared against the cell, occurrence-counted in both directions, bounded so a
  hang scores VOID, and checked for `no space left on device`.

## 2026-09-09T23:15Z — Action: module docs
- **File(s)**: `docs/sync-protocol.md`

## 2026-09-09T23:25Z — Action: full suite on a quiesced, rebased tree
- **File(s)**: none (measurement)
- Rebased onto `0e13dbe03` (master with #9452 merged) BEFORE starting the run, not
  during — a rebase under a live `go test ./...` is a void shape that produces
  named reds in packages you never touched.
- `make test`: **0 reds** across the Go leg, 21 Rust `test result: ok`. No real
  ENOSPC line, no timeout panic, no DATA RACE.
- An EARLIER run of the same tree red `TestRunClosesSurvivingListenerOnBindFailure`
  (`pkg/api`) with `bind: address already in use` — a port collision between two
  concurrent full-tree runs, in a package this diff does not touch. VOID, not
  counted. Two controls: it passes alone (`-count=3`, and the whole package), and
  it passed in the concurrent #9452 run on the same base.
- Note the ENOSPC check has to exclude the fixture:
  `pkg/configstore/persist_failure_test.go` defines
  `errDiskFull = errors.New("injected: no space left on device")`, so a healthy
  run prints that string. Every log here is 0 under both the naive and the
  filtered form.

## 2026-09-09T23:40Z — Action: the mandatory cluster gate
- **File(s)**: `test/results/ledger.d/4cb6e7b0ac98f135.json`
- This diff touches session sync, so `make test-failover` is mandatory
  (CLAUDE.md). Ran `make cluster-deploy` -> `apply-cos-config.sh` ->
  `make test-failover` in one `with-cluster.sh` lock cell on the loss userspace
  cluster: **22 passed, 0 failed, verdict=PASS**.
- Incidentally the third independent confirmation of #9452 on MERGED master:
  RG0/RG1/RG2 each moved to fw0 in 0s. Throughput 19.0 Gbit/s — a third sample
  for #9484's set {18.9, 20.3, 19.0} against its [21.375, 23.625] green band.
