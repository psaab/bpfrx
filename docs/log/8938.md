# #8938 — the depth-2 census was 1-of-6 because the fixtures elided the wrong brace

- **Timestamp**: 2026-09-05
- **Action**: correct #8929's false sample; record the measured census
- **File(s)**: `pkg/config/elision_admissions_8879_test.go`

## The fixture error

The elision under test is HEAD's brace under MID. For `(flow, tcp-session)`:

    braced   security { flow { tcp-session { no-syn-check; } } }
    elided   security { flow tcp-session { no-syn-check; } }

#8929's fixtures compared instead:

    security { flow { tcp-session { … } } }   vs   security flow { tcp-session { … } }

which elides `(security, flow)` — an **admitted** pair that folds correctly —
and leaves `tcp-session`'s brace intact. **Five of the six rows tested a pair
that was never in the population.** Only `nat source` was constructed correctly,
which is exactly why it was the only DROP found.

Reported as "1 of 6". The real figure is **26 of 50**.

The fixture is a silent claim about which question was answered, and this one
answered a question whose answer was already known to be "no drop".

## Corrected census

    26 DROPS    20 SAME    4 CANNOT-ANSWER

Five of the six `nat` children drop (`destination`, `nat64`, `natv6v4`,
`proxy-arp`, `static`; `source` was fixed in #8931). `flow` loses `aging`,
`tcp-session`, `udp-session`, `icmp-session`, `traceoptions`. `syslog` loses
`file`, `host`, `user`.

**`nat destination` is a THIRD instance of the #8928 validation-suppression
class**: braced with a rule naming an undefined pool is REFUSED —
*"destination-nat rule-set rs1 rule r1 references undefined pool nope7717"* —
and the elided spelling commits clean.

## The SAME column bounds nothing, with evidence rather than caution

`policy-statement then` reads SAME under this instrument and is **known
broken** (#8933: the compiler consumes the packed token into the terminal-action
slot, so the term stops terminating). One synthesised body statement per pair
inherits the leaf-contingency defect now found in three separate instruments,
**two of them mine**. So 26 is a LOWER BOUND.

## Population is 50, not 51

#8929 counted a slice that double-counted `family inet6`, which appears under
two top-level stanzas. The predicate is keyed on `(mid, head)` and knows nothing
about the stanza, so the same pair reached by two routes is ONE pair —
the sites-vs-pairs unit mismatch this campaign already hit once, on the
320-sites / 95-pairs reconciliation.

## The guard's membership check was one-sided

Mutation found it: **deleting a `knownDropping` entry left the cell green.** The
check caught an entry that should have been removed and was blind to one removed
silently. A count assertion now closes it, so both directions move together —
an entry may only leave when the pair leaves the population with it.

## Mutations

| mutant | result |
|---|---|
| delete a `knownDropping` entry | KILLED (after the count assertion; **survived before it**) |
| admit `nat static` | KILLED — membership `:1159` and population `:1170` |

## What this does not change

The #8929 fix itself (`nat source` admitted) stands: it was measured by hand
against the correct spellings. Only the sample's verdicts were wrong, and the
population count was off by the duplicate.
