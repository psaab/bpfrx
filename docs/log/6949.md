# #6949 — the JSON session-delta leg dropped the whole policy attribution

**Branch** `fix/6949-session-delta-policy-attribution` ·
**Base** `origin/master` · Refs #4626, #5865, #7194

## 1. The defect

A session delta leaves the helper on **two** wires:

| Leg | Producer | Driven by |
|-----|----------|-----------|
| binary `MSG_SESSION_OPEN` | `event_stream::codec::session_sync::encode_session_open` | the event stream (primary HA path) |
| JSON `SessionDeltaInfo` | `afxdp::session_delta::session_delta_info` | `drain_session_deltas` + `ExportOwnerRGSessions` |

They had diverged on five fields. The binary frame has carried, as trailing
length-gated fields:

| Field | Since | Binary offset | JSON leg before this change |
|-------|-------|---------------|------------------------------|
| `policy_id` | #3056 / #3301 | `[+0:+4]` | **absent** |
| `policy_counter_idx` | #3073 | `[+4:+8]` | **absent** |
| app inactivity timeout (seconds) | #3227 | `[+8:+12]` | **absent** |
| `nat64` | #4565 | `FLAG_NAT64`, flags bit `1<<5` | **absent** |
| `nat64_snat_v4` | #4565 | `[+12:+16]` | **absent** |

The Go consumer (`pkg/dataplane/userspace/protocol_ha.go`, `SessionDeltaInfo`)
declared and read all five **unconditionally**, on whichever leg delivered the
delta.

### Reachability — this is not an exotic fallback

`eventStreamFallbackLoop` (`pkg/daemon/daemon_ha_userspace_stream.go`) drains the
JSON leg every **100 ms** while the binary stream is down and every **5 s** even
while it is up. `ExportOwnerRGSessions(rgIDs, 0)` re-serializes the entire owned
session table through the same struct on **every** helper-requested FullResync. A
helper restart or an event-stream reconnect on any clustered node reaches it.

### Impact

Every session learned through the JSON leg imported `PolicyID = 0`,
`PolicyCounterIdx = 0`, no application timeout and no NAT64 pool source:

* **Attribution** — renders `unattributed` on `show security flow session`, REST
  and gRPC.
* **Invalidation** — exempt from the commit-time deletion-clear and the #4234
  policy-rematch, both of which skip id 0
  (`pkg/daemon/daemon_policy_invalidate.go`). A session the operator meant to
  drop keeps forwarding.
* **Counters** — no per-rule hit counter after a failover promotion (#3073).
* **Timeouts** — ages on the global per-protocol timeout, not the
  per-application one (#3227/#3301).
* **NAT64** — this one is not a mis-attribution. `snat_v4` is chosen by
  `allocate_source` and is **not** derivable from the synced forward v6 key
  (#4565), so a NAT64 session promoted from this leg cannot rebuild its reverse
  v4→v6 BIB at all: a translation failure after failover.
* **HA retroactive loss** — each reconciliation copy carries a fresh #2170
  install generation, so a later JSON copy can **overwrite an earlier, correct,
  event-derived copy on the peer** under latest-generation-wins. A correctly
  attributed session can lose its attribution retroactively.

### Why it survived four releases

A rendered `policy_id` of 0 displays as `unattributed` (#6851) — identical to a
session no policy admitted. Three states (*attributed to N*, *attributed to 0*,
*attribution dropped*) were crushed into two, so the failure mode was
indistinguishable from healthy on every operator surface.

And the one place an auditor would naturally look said it was fine:
`protocol_ha.go` claimed since #3301 that the policy fields were *"decoded from
the trailing fields of the binary open frame **AND mirrored on the JSON
RPC-fallback delta**"*. The second half had never been true.

## 2. The agreement mechanism: single-source, not a second copy

A divergence between the two producers is **always** a bug — they describe one
session for one peer. So the derivation is single-sourced rather than written
twice and held in agreement by a test:

```rust
// userspace-dp/src/session/sync_attribution.rs
pub(crate) struct SessionSyncAttribution {
    pub(crate) policy_id: u32,
    pub(crate) policy_counter_idx: u32,
    pub(crate) inactivity_timeout_secs: u32,
    pub(crate) nat64: bool,
    pub(crate) nat64_snat_v4: Option<Ipv4Addr>,
}
impl SessionSyncAttribution {
    pub(crate) fn from_session(decision: &SessionDecision, metadata: &SessionMetadata) -> Self
}
```

`from_session` also owns the two **non-trivial conversions** that would otherwise
have been a second divergence waiting to happen even after the fields were
added: the saturating ns→s timeout, and the `(nat64, rewrite_src)` pool-source
selection.

Both producers destructure it **exhaustively — no `..`**:

```rust
let SessionSyncAttribution {
    policy_id, policy_counter_idx, inactivity_timeout_secs, nat64, nat64_snat_v4,
} = SessionSyncAttribution::from_session(decision, metadata);
```

so a field added to the struct **fails to compile** until both legs carry it.
That is the structural guard, at compile time rather than in a text scan.

### Why not a cross-producer agreement test alone

Because a test that pinned the JSON producer to `policy_id: 7` would encode
*which side is believed correct*, and would pass even if the binary side were the
broken one. The one agreement assertion that survives is "the two legs describe
the same session", which is what
`session_delta_json_and_binary_agree_on_policy_attribution_6949` asserts — one
session, both wires, neither side pinned to a literal. It is kept **alongside**
the single-source seam: it is what catches a future producer that stops using the
helper, and the pre-existing binary-side codec tests (#3301/#4565) are the other
half — they pin the binary leg's own values, so a mutation inside the shared
helper (which moves both legs together and leaves the agreement test green) still
reds. See cell **M6**.

### The binary wire is byte-identical

The refactor moved *where* the five values are computed, not *what* they are.
`policy_id` / `policy_counter_idx` are the same metadata reads; the ns→s
conversion is the same saturating expression; `FLAG_NAT64` is the same
`decision.nat.nat64`; and `nat64_snat_v4_octets(None)` is the old
`_ => [0u8; 4]`. The pre-existing binary-side codec tests
(`test_encode_session_open_carries_policy_fields_3301`,
`test_encode_session_open_carries_nat64_flag_and_snat_v4`,
`test_encode_session_open_carries_session_id_5212`) pin exact offsets and values
and are unchanged and green — and cell **M6** shows
`test_encode_session_open_carries_policy_fields_3301` still reds when the shared
helper is mutated, so it is the half of the guard the agreement test cannot be.

A correction, since it is the kind of claim that is easy to assert and wrong: an
earlier revision of the agreement test's doc comment said a seam mutation would
leave that test GREEN, because both legs move together. Cell **M6** measured it,
and that is only half true. The five equality assertions do all still hold — two
legs can agree on a wrong value, which is exactly the limit of an agreement
assertion — but the test still reds, on its POSITIVE CONTROL
(`want.policy_id == 4242`, `tests_session_delta_json.rs:329`). The comment in the
source now states what the matrix measured rather than what seemed obvious.

### The three-state fixture

The agreement fixture's `policy_id` is **4242, not 0**. A fixture whose correct
value is 0 cannot distinguish "attributed to policy 0" from "attribution
dropped" — the exact confusion that hid the defect.
`session_delta_info_zero_attribution_is_present_not_absent_6949` covers the third
state from the other side: a genuinely unattributed session must emit the keys
carrying an **explicit 0**, never an absent key.

### Parity audit of the whole struct

The issue asked to check the same struct against the binary codec for any other
field that had drifted. Parsing the JSON key set out of both ends:

* **41** Go `json:` tags on `SessionDeltaInfo`, **all** present on the Rust
  producer;
* **no** Rust key the Go side does not declare.

No other drift. The one binary-frame field with no JSON counterpart is
`FLAG_IS_REVERSE`, and that is a *symmetric* absence: the Go
`SessionDeltaInfo` has no `IsReverse` field and the Go binary decoder never
reads the flag either (`IsReverse` exists only on `SessionSyncRequest`, the
Go→helper install direction). Neither leg carries it to a consumer, so there is
nothing to diverge.

`TestSessionDeltaInfoKeySetsAreInParityWithRust6949` makes that audit a standing
guard: it compares the two key SETS rather than the spelling of keys someone
thought to test, which is the only shape that can see "five fields the consumer
reads and the producer never emitted". It is scoped to the struct, which is what
distinguishes it from the per-field `rustSerdeRenameIn` guards — those search the
whole file, so a field MOVED to a different struct would still satisfy them (see
cell **G3**).

### A note on `nat64`

`nat64` is inert on the Go JSON path today: `userspaceConvertDeltaV6` keys off a
non-empty `Nat64SnatV4`, not the bool. That matches the binary leg's *post-decode*
behaviour exactly — there, `Nat64` gates the trailing snat_v4 decode
(`eventstream.go`) and the converter then uses only `Nat64SnatV4`. Carrying the
bool keeps the two legs bit-for-bit comparable (a NAT64 session with no v4
`rewrite_src` emits `nat64: true, nat64_snat_v4: ""` on BOTH legs, and both are
treated as not-NAT64 downstream), and the Go consumer declares the field, so the
key-set guard requires it.

## 3. Red at master

Pristine `origin/master` worktree, test files copied in, nothing else changed.
Named failing tests, tests-collected > 0 on both planes:

```
Rust (cargo test --bin xpf-userspace-dp -- --test-threads=1)
  test result: FAILED. 0 passed; 3 failed; 0 ignored; 4710 filtered out
    session_delta_json_and_binary_agree_on_policy_attribution_6949
      -> "the JSON session-delta leg carries no `policy_id` key at all"
    session_delta_info_zero_attribution_is_present_not_absent_6949
      -> left: None, right: Some(0)
    sync_attribution_exhaustive_destructure_6949
      -> src/session/sync_attribution.rs: No such file (the seam does not exist)

Go (go test -count=1 -v -run 6949 ./pkg/dataplane/userspace/)
  --- FAIL: TestSessionDeltaPolicyAttributionWireKeysLockstepWithRust6949
  --- FAIL: TestSessionDeltaPolicyAttributionCommentIsNotFalse6949
      -> no `#[serde(rename = ...)]` found on `pub policy_id` in binding.rs
```

The master serialization dump captured in that run is the primary evidence — it
contains `rt_flow_session_id`, `log_session_init`, `nat_src_ip` … and **none** of
`policy_id`, `policy_counter_idx`, `app_timeout`, `nat64`, `nat64_snat_v4`.

## 4. Mutation matrix

One mutation, ONE line per cell. Gated on a **named failing test** and
**tests-collected > 0** — `rc != 0` alone is not "it red" (cargo prints
`error: test failed` for a build break too, and a build break is a VOID cell,
not a red). Every cell was run **full-suite**, never under a `-run <issue>`
filter, so a pre-existing test that owns a property is not hidden.

Rust cells: `cargo test --bin xpf-userspace-dp -- --test-threads=1` (parallel
deadlocks this crate), dedicated `CARGO_TARGET_DIR` under `/var/tmp`, in a
worktree of its own so no other lane's harness can touch the tree mid-cell.

| Cell | Mutation | rc | tests collected | named failing | which test(s) |
|------|----------|----|-----------------|---------------|---------------|
| **C0** | control — unmutated tree | 0 | 4711 | 0 | — |
| **M1** | `session_delta.rs`: `policy_id,` -> `policy_id: 0,` on the JSON literal | 101 | 4710 | 1 | `session_delta_json_and_binary_agree_on_policy_attribution_6949` |
| **M2** | `session_delta.rs`: `policy_counter_idx,` -> `policy_counter_idx: 0,` | 101 | 4710 | 1 | `session_delta_json_and_binary_agree_on_policy_attribution_6949` |
| **M3** | `session_delta.rs`: `app_timeout: inactivity_timeout_secs,` -> `app_timeout: 0,` | 101 | 4710 | 1 | `session_delta_json_and_binary_agree_on_policy_attribution_6949` |
| **M4** | `session_delta.rs`: `nat64,` -> `nat64: false,` | 101 | 4710 | 1 | `session_delta_json_and_binary_agree_on_policy_attribution_6949` |
| **M5** | `session_delta.rs`: `nat64_snat_v4: nat64_snat_v4_string(..)` -> `String::new()` | 101 | 4710 | 1 | `session_delta_json_and_binary_agree_on_policy_attribution_6949` |
| **M6** | `sync_attribution.rs`: `policy_id: metadata.policy_id,` -> `policy_id: 0,` (the shared seam) | 101 | 4709 | 2 | `session_delta_json_and_binary_agree_on_policy_attribution_6949 event_stream::codec::tests::test_encode_session_open_carries_policy_fields_3301` |
| **M7** | `session_delta.rs`: add `..` to the `SessionSyncAttribution` destructure | 101 | 4710 | 1 | `sync_attribution_exhaustive_destructure_6949` |
| **M8** | `binding.rs`: serde rename `policy_id` -> `policy_idXX` (Rust spelling drifts) | 1 | 2 | 1 | `TestSessionDeltaPolicyAttributionWireKeysLockstepWithRust6949` |
| **M9** | `protocol_ha.go`: tag `json:"policy_id"` -> `json:"policy_idXX"` (Go spelling drifts) | 1 | 2 | 1 | `TestSessionDeltaPolicyAttributionWireKeysLockstepWithRust6949` |
| **G0** | control — unmutated tree (Go leg) | 0 | 3 | 0 | — |
| **G1** | `protocol_ha.go`: tag `json:"app_timeout"` -> `json:"app_timeoutXX"` | 1 | 3 | 2 | `TestSessionDeltaInfoKeySetsAreInParityWithRust6949 TestSessionDeltaPolicyAttributionWireKeysLockstepWithRust6949` |
| **G2** | `binding.rs`: serde rename `app_timeout` -> `app_timeoutXX` | 1 | 3 | 2 | `TestSessionDeltaInfoKeySetsAreInParityWithRust6949 TestSessionDeltaPolicyAttributionWireKeysLockstepWithRust6949` |
| **G3** | `binding.rs`: rename the STRUCT so the key-set parse cannot find it | 1 | 3 | 1 | `TestSessionDeltaInfoKeySetsAreInParityWithRust6949` |

**Reading it.**

* **C0 / G0** — the controls are clean, so every red below is the mutation and
  not a dirty tree.
* **M1–M5** — each of the five JSON-leg assignments is load-bearing, and each
  localises to exactly ONE named test. That is the point of one mutation per
  line: a compound mutation over the five would have reddened the same cell five
  times and proved only that *something* in the block matters.
* **M6** is the seam cell. Mutating inside `SessionSyncAttribution::from_session`
  moves BOTH legs together — and the pre-existing binary-side
  `test_encode_session_open_carries_policy_fields_3301` still reds, which is why
  it is the half of the guard the agreement test cannot be. The agreement test
  reds too, but on its POSITIVE CONTROL, not on an equality: see the correction
  in §2.
* **M7** — the compile-time invariant is only as good as the absence of `..`.
  Adding one COMPILES (so this is a genuine red, not a build break) and the
  textual guard catches it, alone.
* **M8 / M9** — the paired cells. Drifting the RUST spelling and drifting the GO
  spelling each red the lockstep, so it binds the agreement and not one side.
  (`collected=2` because the mutation worktree sits at `877ff9ab`, one commit
  before the key-set guard landed; that guard's own cells are G1–G3, run at
  `734675e5`.)
* **G1 / G2** — the same pairing for the key-set guard, which reds in both
  directions.
* **G3** — the vacuous-green check. Renaming the Rust struct so the parse cannot
  find it makes the guard FAIL rather than pass on an empty key set. A parse-based
  guard that goes green when its parse breaks is the same failure shape it exists
  to catch.

## 4b. Cluster smoke (owed: this is session-sync code)

`make test-failover` on the loss userspace cluster, in one `with-cluster.sh`
lock cell — `cluster-deploy` (both nodes, sha256-verified, embedded-shim
pre-flight PASS on each) -> `apply-cos-config.sh` -> `test-failover`:

**15 passed, 1 failed.** Every HA assertion is green:

```
PASS  fw0 is primary for every redundancy group
PASS  iperf3 target reachable (172.16.80.200)
PASS  IPv6 transit OK at baseline (5/5 to 2001:559:8585:80::200)
PASS  fw0 has 11 established sessions
PASS  fw1 has 11 synced sessions          <- the leg this change touches
PASS  iperf3 survived fw0 reboot (failover to fw1)
PASS  fw0 xpfd restarted after reboot
PASS  fw0 rejoined as secondary (no auto-preempt)
PASS  fw1 remains primary after fw0 rejoin
PASS  iperf3 survived fw0 rejoin
PASS  fw0 became primary for all RGs after manual failover
PASS  iperf3 survived manual failover
PASS  IPv6 transit OK after manual failover (5/5)
PASS  iperf3 completed successfully
FAIL  iperf3 throughput too low: 0.0937 Gbps (expected >= 1.0 Gbps)
```

The single FAIL is **not this change** and not a throughput regression. It is
the CoS policer on the port the smoke measures, which is the pre-existing
**#7673**. Discriminating control, run in ONE lock cell, same build, same path,
same target, seconds apart, only the destination port differing:

| destination port | forwarding class | measured |
|---|---|---|
| 5211 | `iperf-uncapped` (no transmit-rate) | **21.4 Gbits/sec** |
| 5201 | `iperf-100m` (transmit-rate **100.00 Mb/s**) | **96.1 Mbits/sec** |

`test-failover.sh:197` passes no `-p`, so it measures iperf3's default port
5201; `cos-iperf-config.set:79-81` maps destination-port 5201 to `iperf-100m`;
`:57` binds that class to `scheduler-100m`; and `test-failover.sh:72` then
asserts `MIN_THROUGHPUT=1.0` Gbps. Running the two documented steps in the
documented order caps the measured flow at 100 Mb/s by construction — 93.7
Mbits/s is that policer at ~94% efficiency. `docs/engineering-style.md:99,103`
measures the throughput row on **5211** for exactly this reason. The full
analysis is posted on #7673, which had listed this as one of three undecided
candidate explanations.

A 220x gap that appears and disappears with the destination port cannot be a
change to session-delta metadata carriage.

**Not claimed:** a clean absolute throughput number. The host was oversubscribed
by parallel campaign lanes (load avg 22 on 16 cores) and two runs minutes apart
gave 21.4 and 6.28 Gbits/sec on the *same* uncapped port. Whether the uncapped
path still meets the documented >= 23 Gbit/s bar needs a quiet host and is a
separate question this control does not answer.

## 5. Golden fixture

`userspace-dp/tests/fixtures/protocol_wire_v1.json` (the default-specimen wire
snapshot) reds on the addition. The diff was **classified before regenerating**:
5 keys added, **0 removed, 0 values changed**, every one at its type-zero default
— the benign additive shape.

## 6. Rolling-upgrade safety and what is NOT fixed here

Both sides are `serde(default)` / `omitempty`, so the change is additive in both
directions: an old helper omits the keys and they decode to `0`/`""`/`false` —
precisely the pre-#6949 behaviour of this leg — and an old daemon ignores keys it
does not know.

That means this issue's parity is true **only when both sides are new**. Making
it durable across a mixed-version cluster — fail-closed capability negotiation,
and the canonical-schema equivalence test that would have caught this whole class
— is out of scope here and tracked on **#7194**.

Also unchanged and still open: `afxdp/bpf_map/publish_conntrack.rs` hardcodes
`log_flags: 0` on the BPF mirror while the same rows carry a real `policy_id`
(noted on the issue as a likely latent defect of the same family). It is a
different producer with a different consumer and is left for its own change
rather than folded in here.

`docs/sync-protocol.md` needs no update: it documents the Go↔Go cluster wire,
which was never missing these fields.

## 7. Docs updated in this change

* `docs/session-sync-architecture.md` — new "Policy attribution on BOTH
  session-delta legs (#6949)" subsection under *Delta Drain (Fallback Path)*,
  plus the NAT64 reverse-BIB wire list.
* `userspace-dp/src/session/README.md` — "Policy attribution is single-sourced
  across both session-delta legs (#6949)", and a correction to the #3395 scope
  paragraph that said #3301 carried `policy_id` on "the `SessionDeltaInfo` /
  `SessionSyncRequest` wire" without qualifying which leg.
* `pkg/dataplane/userspace/protocol_ha.go` — the false sentence, corrected and
  annotated with what now holds it true.
