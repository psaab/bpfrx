# 7159 — re-measuring the 100E100M surplus-sharing p99.9 mouse gate

Work log for #7159 (the #1359 remainder). Newest entry last.

- **Timestamp**: 2026-09-02
  - **Action**: Root-caused the 2026-08-31 surplus leg's "0 of 15 valid
    reps" to the harness, not the dataplane. `test-mouse-latency.sh`
    stopped its elephant generator from the EXIT trap with `kill
    "$IPERF_PID"`, where `IPERF_PID` is the LOCAL `incus exec` client.
    Measured directly against `loss:cluster-userspace-host`: a
    backgrounded `incus exec ... -- sh -c "sleep 137"` was still
    running 8 s after its local client was killed. So every
    early-INVALID rep orphaned a 90 s, N-stream iperf3 while the next
    rep started ~8 s later, and the reps then shared the 1 Gb/s exact
    class. The recorded cell matches that arithmetic: reps 00-03 at
    ~1.0-1.23 Gb/s, rep 04 at ~0.40 Gb/s, reps 05-14 on a ~0.25 Gb/s
    plateau — 1 Gb/s over the ~3.5 concurrent 90 s runs a 28 s rep
    cadence sustains.
  - **File(s)**: `test/incus/mouse-elephant-lib.sh`,
    `test/incus/mouse-elephant-selftest.sh`,
    `test/incus/test-mouse-latency.sh`,
    `test/incus/test_mouse_latency_shell_test.py`,
    `scripts/run-selftests.sh`, `Makefile`

- **Timestamp**: 2026-09-02 (written BEFORE the run, deliberately)
  - **Action**: Pre-registered the analysis plan for the re-measurement
    so the reading cannot be chosen after the numbers are in.
    - **Order**: surplus leg FIRST, exact leg second — inverting the
      2026-08-31 order (exact 07:06-07:34, surplus 07:35-07:58). If the
      dichotomy survives both orders, elapsed session time is excluded
      as its cause.
    - **Primary verdict**: the aggregator's own gate in `summary.json`
      — loaded/idle `p999_us` ratio against the 2.0 threshold, per leg.
    - **If the surplus loaded cell yields fewer than 3 valid reps**:
      report the aggregator verdict as INSUFFICIENT-DATA (a third
      state, not a pass and not a fail) and ADDITIONALLY report the
      phase decomposition the issue body prescribes for exactly this
      case — per-rep `phase_us.read_us` (echo path) against
      `sleep_overshoot_us` and `drain_us` (client-side). Attribution
      rule, fixed in advance: the tail counts as path-side only if the
      surplus/exact ratio is carried by `read_us` while the two
      client-side phases stay comparable between legs. If
      `sleep_overshoot_us` moves with it, the probe driver is the
      suspect and the run says nothing about arbitration.
    - **Counters**: `xpf_userspace_cos_{root,queue}_token_starvation_
      parks_total` and `xpf_userspace_cos_v_min_*` snapshotted on both
      nodes at three points per leg (before the idle cell, before the
      loaded cell, after the loaded cell), so the loaded-cell delta is
      not contaminated by the idle cell.
    - **VOID conditions**: a failed deploy, a preflight abort, or a cell
      whose reps produce no `probe.json` is VOID and gets re-run — it is
      not rounded toward either verdict. Artifacts are counted, not exit
      codes.
  - **File(s)**: `docs/log/7159.md`

- **Timestamp**: 2026-09-02
  - **Action**: Tested the 2026-06-10 evidence base for the #8268
    contamination signature, rather than assuming it undeterminable.
    `git log -S IPERF_PID` dates the orphan defect to the harness's
    first commit (`840d5545d`, 2026-04-25), so it predates
    `docs/pr/1829-p2/evidence.md` and the question is real.
    **Verdict: signature ABSENT, and the mechanism could not have
    applied.** Two independent reasons, both from the per-rep table in
    that document:
    1. The signature is an aggregate collapse to roughly
       shaper / concurrent-overlapping-runs, persisting after the first
       rejected rep. The per-rep elephant column is FLAT: surplus-loaded
       1.27 / 1.26 / 1.27 / 1.27 Gb/s across reps 0-3, exact-loaded
       0.95 / 0.95, sc100m-loaded 0.10 / 0.10. Contamination drives that
       column DOWN across successive reps; here it does not move, and it
       sits ABOVE the 1.0 Gb/s configuration because surplus is
       borrowing.
    2. The cascade needs a rep that exits BEFORE its probe (the
       `cwnd-not-settled` / cursor-capture / settle-pull class), because
       only those leave iperf3 with ~70 s of budget unspent. The single
       invalid rep in that sweep is surplus-loaded rep 3, invalidated
       `degenerate-coroutine` — a probe-side validity verdict inside
       `probe.json`, recorded WITH p99, p99.9 and an elephant figure, so
       that rep ran to completion and its iperf3 was waited on. It is
       also the last rep in its cell.
    So the 4.4-6.5x dichotomy stands as a real prior, #1359 item 3's
    premise does NOT dissolve, and the run in flight is a clean re-test
    rather than a replacement for corrupted numbers.
    **Limit of the determination**: the raw trees
    (`/tmp/xpf-1829-gate-20260610-195328`, `/tmp/xpf-1829-gate-supp-*`)
    are no longer on the runner, so this rests on the published per-rep
    table. That table carries exactly the column the signature is
    defined on, so the test is answerable from it; a claim needing the
    raw JSON would not be.
  - **File(s)**: `docs/log/7159.md`

- **Timestamp**: 2026-09-02 (recorded BEFORE the fresh run's numbers)
  - **Action**: Pre-registered the per-coroutine tier structure seen in
    the three 2026-08-31 surplus reps that DID settle, so it cannot be
    retrofitted to whatever the fresh run shows. Three aggregator-
    rejected reps are not a result; this is a shape to look for, stated
    in advance.
    Mouse coroutines split into sharp tiers under surplus and not under
    exact (attempts per coroutine over a 60 s rep, 100 coroutines,
    rounded to the nearest 50):

    | leg / rep | starved | mid | normal |
    |---|---|---|---|
    | surplus rep_00 | 20 @ ~200 | 18 @ ~2450 | 62 @ ~2900 |
    | surplus rep_01 | 15 @ ~100 | 19 @ ~2550 | 66 @ ~2900 |
    | surplus rep_02 | 10 @ ~100 | 15 @ ~2450 | 75 @ ~2900 |
    | exact rep_00 | — | 7 @ ~2850 | 93 @ ~2900 |
    | exact rep_01/02 | — | — | 100 @ ~2900 |

    The starved coroutines are not idle and not stalled once: they
    record `max_sleep_overshoot_us: null` (they never had spare time to
    pace with), `max_drain_us` 33-59 us (client send side healthy), and
    `max_read_us` ~1.56 s. They ran ~308 ms per transaction for the
    whole rep while their 60-80 peers on the SAME best-effort class ran
    at ~0.8 ms. Whole-leg phase percentiles say the same thing:
    `sleep_overshoot_us` p99.9 is 3.0-3.5 ms under surplus and 3.1-3.8
    ms under exact, `drain_us` p99.9 is 56-57 us against 50-53 us, and
    the entire difference sits in `read_us` p99.9 — 526-823 ms against
    8.3-9.2 ms.
    Two readings this does NOT distinguish, named now: a per-flow or
    per-worker arbitration effect (the tier sizes are near multiples of
    100/6, and the mlx5 VF has 6 RX queues -> 6 workers), versus a
    whole-class effect that happens to spare most flows. The artifacts
    carry no flow->worker mapping, so this run cannot choose between
    them.
  - **File(s)**: `docs/log/7159.md`
