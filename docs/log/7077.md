# docs/log/7077.md — #7077 (NPTv6 mask grammar)

- **Timestamp**: 2026-08-29
- **Action**: Make `parse_prefix` digits-only, converge the Go mirror, and
  re-point the three tests whose subject the convergence removed.
- **File(s)**: `userspace-dp/src/nptv6.rs`,
  `pkg/dataplane/compiler_nptv6_helper_grammar.go`, `pkg/dataplane/compiler_nat.go`,
  `pkg/dataplane/compiler_nptv6_helper_grammar_7077_test.go`

## Why this was held once before

The fix is three lines. The consequence is not: `+` was measured to be the ONLY
divergence between the two grammars, so closing it removes the sole fixture for
`TestHelperAcceptedNPTv6PrefixIsSkippedNotRejected_7077` — the fail-on-revert
guard for the `|| helperInstalls` arm, a real #6894 r9 regression fix.

**Closing the divergence removes the only test fixture for the defence against
divergence.** Keeping a defect alive so a test has something to assert would be
backwards, so the divergence is now INJECTED via `nptv6HelperWouldInstallFn`.

## The seam is production code added for a test, and that is argued not assumed

`nptv6HelperWouldInstallFn` exists because the property — "a rule the helper
would install must be warn-and-skipped, never rejected" — is otherwise
unbindable once the grammars agree. Both stubbing cells assert two premises
first: that Go genuinely refuses the string (or the reject closure is never
reached), and that the REAL predicate refuses it too (or the stub is redundant
and the cell would pass with the seam removed).

## Test 3 split one fact into two

Its final assertion was "the retained config must APPLY", which held only because
the helper accepted `/+48`. Now:

- with the real grammar, the apply must **reject** — the grammars agree, so this
  is a helper-refused rule and hard-erroring is the correct disposition;
- with an injected divergence, the apply must **succeed** — the no-brick arm.

Strictly more informative than the single assertion it replaces.

## The drift guard did its job

`TestNPTv6HelperGrammarMatchesTheRustParser_7077` reddened at the first `go test`
rather than after a merge. That is the argument for keeping the Go mirror once
the grammars converge: with nothing watching, a future re-divergence is silent.
