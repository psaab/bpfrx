# #7714 — `daemon_nft.go` past the REFACTOR tier

- **Timestamp**: 2026-08-27
- **Action**: Seam B — state the term-lowering disposition contract
  - **Two candidate seams were measured before cutting.** Seam A (the fence
    surface, 333 lines, property already stated in the tree at
    `daemon_nft.go:726`) is bounded pure motion but **not contiguous** — 9
    plucks — and clears nothing. Seam B is the god-function
    `nftRulesFromTerm` (423 lines). Team-lead chose B on the grounds that a
    file split leaving the god-function intact buys nothing, which was my own
    sentence for the *alternative* disposition.
  - **The contract is FOUR dispositions, not three.** The brief said three
    (skip / never-match / verbatim). Reading the function, "silently skip" is
    not a live disposition at all — it is the retired anti-pattern #6405/#6512
    removed — and there are four live ones:
    - **A. emit no rule** — a constrained address direction resolving to an
      empty positive set for this family (the term cannot match).
    - **B. emit the token verbatim** — unresolvable protocol / ICMP type /
      code, so `nft -f -` REJECTS the whole ruleset and the prior generation
      stays installed (#6806).
    - **C. fail the TERM closed with a scoped drop** — unrepresentable
      tcp-flags (#5512).
    - **D. make the term match nothing** — unrepresentable
      flexible-match-range (#6804), deliberately NOT C because a flex-match
      constraint is not necessarily protocol-scoped.
  - **The three disposition-bearing lowering steps are now named for their
    disposition** (`nftTermAddrPredicates` A, `nftTermProtocolPredicates` B,
    `nftTermICMPPredicates` B) and the set is stated once above them, with C
    and D pointed at their still-inline sites.
    `nftRulesFromTerm` 423 -> 309; `daemon_nft.go` 2661 -> 2236; new file 507.
  - **The file stays above 2000 and that is correct.** The tier is a trigger
    for asking whether a seam exists, not a number to get under; a split sized
    to clear a threshold is the churn #6870 was closed for.
  - **RUN count 1492 = 1492**, measured with `git stash -u` so the base
    actually builds — the trap that made #6880's first base count read 0. It
    proves nothing was decommissioned; it does NOT prove the contract survived,
    because this is not pure motion (the address block's two early `return nil`
    became an `emitNoRule` return).
  - **Mutation cells, one per disposition-bearing site**, all RED, 2145
    collected:
    - A drops the constraint -> `TestNftRuleFromTermWrongFamilyMatchesNothing`,
      `TestNftRuleFromTermAddressSemantics3433`
    - B (protocol) drops the token -> `TestLo0TextOracleKeepsUnresolvableTokens6806`,
      `TestLo0RenderersAgreeOnUnresolvableTokens6806`
    - B (ICMP) renders only resolved bytes -> the same two
  - **Stated precisely rather than implied**: the two B cells red the SAME two
    tests, because they are one contract at two sites — not two independent
    guards. And **C and D are not covered by these cells**: they are still
    inline and were not extracted or mutated. Three red cells pin A and B, not
    the whole four-way contract.
  - **The doc-stealing hazard caught itself.** My first extraction trimmed
    trailing blanks and asserted a closing `}`; it hit `joinNftFields`'s godoc
    instead. Corrected to stop at `nftRulesFromTerm`'s own closing brace, so
    that godoc stayed with its function.
  - **Seam A held, not dropped**, per team-lead: re-measure after this lands and
    decide separately. `daemon_ha.go` (2254) deliberately untouched — two god
    functions in a 65-function file with no property seam identified is not
    enough to act on, and inventing one is the failure mode.
  - **File(s)**: `pkg/daemon/daemon_nft.go`,
    `pkg/daemon/daemon_nft_term_lower.go`, `docs/log/7714.md`

- **Timestamp**: 2026-08-27
- **Action**: Seam A re-measured after the god-function landed — DECLINED, with
  the measurement recorded in place
  - **Re-measured at master `65bd392e3`** (`daemon_nft.go` now 2236 lines, 55
    functions): the fence cluster is **9 functions / 333 lines**, unchanged.
  - **My earlier "9 non-contiguous plucks" was wrong** — I counted functions
    where I should have counted RUNS. In file order the fence functions sit at
    positions 1, 3, 6-11 and 15 of 55: **4 contiguous runs**, one of which
    carries six functions (198 of the 333 lines). The extraction would have
    been 4 moves, not 9. Correcting it because the earlier number made the work
    look harder than it is, and the decision below should not rest on a wrong
    difficulty estimate.
  - **Declined anyway, for a better reason: the file boundary would not carry
    the property.** The seam's argument was "fences compute over a DIFFERENT
    address scope than the real ruleset" (`daemon_nft.go:726`). Measured, that
    is not what happens:
    - the scope is **chosen by the callers**. `BuildFenceAddrSets` is invoked
      at `:197` and `:607`, inside `applyLo0Filter` and `applyHostInboundFilter`
      — the REAL-ruleset functions — and handed to the fences as a
      `sets dpuserspace.FenceAddrSets` parameter. The fences RECEIVE the
      divergent scope; they do not derive it.
    - the two surfaces **share their address-set helpers**:
      `hostInboundDesiredDropAddrs` is called from both `applyHostInboundFilter`
      (`:575`) and `installHostInboundColdBootFence` (`:763`);
      `hostInboundDropAddrKey` and `hostInboundUncoveredDropAddrs` straddle them
      too.
  - So a split would move the RECEIVERS away from the CHOOSERS and away from the
    helpers they share with the real path — making the relationship less visible,
    which is the opposite of what the seam was for. **An argument that does not
    survive the file boundary is not a seam**, whatever its line count.
  - The measurement is recorded **at the property's existing statement**
    (`daemon_nft.go`, above the "Scope:" paragraph), not only in this log, so
    the next sweep reads why the cluster stays rather than re-deriving it —
    the same reasoning that kept the dominated flex-match belt in #7722.
  - **`daemon_ha.go` (2254) remains "not yet argued"** and untouched.
  - **File(s)**: `pkg/daemon/daemon_nft.go`, `docs/log/7714.md`
