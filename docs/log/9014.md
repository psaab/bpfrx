# 9014 — commit-confirmed ARM write raises degraded health

- **Timestamp**: 2026-09-06
- **Action**: added `confirmArmDegraded` + `confirmArmRec` + `confirmArmGen`;
  `noteConfirmArmFailureLocked` (ERROR + journal + retry debt); folded into
  `ConfigPersistDegraded()`; persist-retry loop re-drives `WriteConfirm`,
  generation-pinned so a resolved window is not resurrected. Routed
  `DB.WriteConfirm` through the `rbWriteFileDurable` seam so the failure is
  reachable in a test.
- **File(s)**: pkg/configstore/store.go, store_persist.go, store_commit.go,
  db.go, confirm_arm_degraded_9014_test.go, pkg/configstore/README.md
