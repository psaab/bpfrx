# #8879 batch 8

- **Timestamp**: 2026-09-05
- **Action**: admit 4 more SILENT rows
- **File(s)**: `pkg/config/compact_normalize_8662.go`,
  `pkg/config/elision_admissions_8879_test.go`,
  `pkg/config/compact_scope_blind_pairs_8852_test.go`

## Verdict table (gate column FIRST)

| pair | gate | warns b/e | strict b/e | braced | elided | LIVE | INERT |
|---|---|---|---|---|---|---|---|
| `forwarding-options family` | SILENT | 1/0 | ok/ok | `mode=packet-based` | `mode=` | yes | no |
| `protocols ospf3` | SILENT | 0/0 | ok/ok | `areas=1` | `<nil>` | yes | no |
| `security dynamic-address` | SILENT | 0/0 | ok/ok | `feeds=1` | `feeds=0` | yes | no |
| `system dataplane` | SILENT | 0/0 | ok/ok | `bin=/opt/xpf/dp-7717/w=5` | `<nil>` | yes | no |

**Ratio: 4 admitted, 0 declined.**

## The default-value trap, nearly stepped in

`forwarding-options family` compiles `mode` into `FamilyInet6Mode`, whose
documented default is **`flow-based`**. The obvious fixture — `mode flow-based`
— would have been a value equal to the compiled default: losing it and keeping
it produce the same answer, so the row's comparison could never fail and it
would have read CLEAN WHILE BROKEN.

The fixture uses `packet-based` for that reason, and the cell says so beside the
fixture rather than only in a log nobody reads at review time. This is the trap
lane-8015 hit on `sha256`, and it is the third time in this campaign a row was
one keystroke from being unfalsifiable.

## Mutation matrix

| mutant | executed | killed |
|---|---|---|
| drop `forwarding-options family` | 31 subtests | `:390` — `mode=packet-based` vs `mode=` |
| drop `protocols ospf3` | 31 subtests | `:390` — `areas=1` vs `<nil>` |
| drop `security dynamic-address` | 31 subtests | `:390` — `feeds=1` vs `feeds=0` |
| drop `system dataplane` | 31 subtests | `:390` — `bin=…/w=5` vs `<nil>` |

Each kills only its own row; integrity re-counted at 4 admissions after the
matrix, since a mutation harness that reverts the change under test has already
happened once in this campaign (batch 5).

## Corpus sweep

9 pass / 5 fail — identical to baseline.

## Nothing owed

No smoke: `system dataplane` names the userspace helper's binary/worker knobs,
but this change touches no cluster, VRRP, session-sync or failover code — the
braced spelling already compiled and is untouched, and the only behaviour change
is that the previously-dropped elided spelling now compiles its children. No doc
change: `docs/config-schema.md` documents the admission mechanism but does not
enumerate the pairs.
