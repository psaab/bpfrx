# 9422 — apply-groups-except

- **Timestamp**: 2026-09-07
- **Action**: Reproduced in an owned worktree off `origin/master` (`19b082fe8`).
  `apply-groups-except G` under `system`, with `apply-groups G` at the top
  level, compiled `System.HostName="FROM-GROUP"` — identical to the CONTROL
  with the `-except` line removed. Accepted by `SchemaValidate`,
  `CompileConfig` (strict), `CompileConfigLenient` and `configstore.CheckText`,
  `Warnings=[]`, in BOTH AST shapes. The token is absent from `setSchema`, so
  no channel could reject it.
- **File(s)**: (probe only, removed before commit)

- **Timestamp**: 2026-09-07
- **Action**: Chose IMPLEMENT over the #1960 reject posture. The reject would
  have reversed a landed decision: `TestApplyMetaInZoneInterfaceMemberSlotCompiles_7029`
  exists specifically to assert that a config carrying `apply-groups-except`
  COMPILES, on the ground that it is legal Junos, and a strict refusal would
  red that cell.
- **File(s)**: —

- **Timestamp**: 2026-09-07
- **Action**: Implemented the exclusion in `mergeNodes` (not as a pre-prune of
  the group body) so it is decided per DESTINATION — a `<*>` group key fans one
  source subtree into every matching container and those containers can
  disagree.
- **File(s)**: `pkg/config/apply_groups_except_9422.go` (new),
  `pkg/config/ast_groups.go`

- **Timestamp**: 2026-09-07
- **Action**: The `pkg/configstore` fixture hit a hole the `pkg/config` cells
  did not: a config with TWO top-level `system` blocks honoured the exclusion
  with one block and ignored it with two, because `mergeNodes` merges into the
  first matching container. Added `siblingsExcludeGroup`, which scans every
  `keysEqual` sibling, and a cell for it.
- **File(s)**: `pkg/config/apply_groups_except_9422.go`,
  `pkg/config/ast_groups.go`, `pkg/config/apply_groups_except_9422_test.go`

- **Timestamp**: 2026-09-07
- **Action**: Corrected the doc that asserted the capability. `docs/feature-gaps.md`
  claimed Configuration Groups Done and named `apply-groups-except` in the
  parenthetical while nothing consulted it; the row now states the implemented
  scope and records that the previous claim was false.
- **File(s)**: `docs/feature-gaps.md`, `docs/config-schema.md`

- **Timestamp**: 2026-09-07
- **Action**: Validation. `go test ./...` (whole repo). Mutation proof in the PR
  body.
