# #9136 — DHCP route `vrf` clause namespace

- **Timestamp**: 2026-09-06
- **Action**: Reproduced with one FullConfig carrying a virtual-router and a
  forwarding instance. `renderDHCPDefaults` emitted `vrf vr1` / `vrf fwd1`
  where the siblings emit `vrf vrf-vr1` / `table 4242`.
- **File(s)**: `pkg/frr/dhcp_route_vrf_namespace_9136_test.go` (new)

- **Timestamp**: 2026-09-06
- **Action**: Extracted `instanceRouteTarget` from `renderPreferredRoutes` and
  routed `renderDHCPDefaults` through it, so the two renderers share one
  resolver rather than one being fixed to agree with the other by hand.
- **File(s)**: `pkg/frr/config_render.go`

- **Timestamp**: 2026-09-06
- **Action**: The fix red two existing cells, as the issue predicted. Read both
  stated reasons first: each pins a CLAIM about presence/suppression, and the
  literal `vrf vrf1` was the author's assumption about the namespace — a
  positive control sharing the code's misconception. Claims kept verbatim,
  fixtures given the `VRFName` a real instance has, expected strings updated.
- **File(s)**: `pkg/frr/dhcp_route_vrf_8963_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Mutation table, 7 cells, all applied+built+bounded. First pass
  6 KILLED / 1 SURVIVED — the #5557 `sanitizeFRRValue` belt on the vrf clause
  was unguarded (deleting a control is a no-op unless something drives the
  input it rejects). Added a cell driving a control character through BOTH the
  lookup-hit and lookup-miss paths; re-scored 7/7 KILLED.
- **File(s)**: `pkg/frr/dhcp_route_vrf_namespace_9136_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Documented the namespace contract.
- **File(s)**: `pkg/frr/README.md`
