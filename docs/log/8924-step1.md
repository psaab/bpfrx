# #8924 step 1 — quote the examples the parser rejects

- **Timestamp**: 2026-09-05
- **Action**: make every `valueExamples` entry parse as written; guard the invariant
- **File(s)**: `pkg/config/schema_system.go`,
  `pkg/config/value_examples_parse_8924_test.go`,
  `pkg/config/testdata/compact_block_divergences_2419.txt`,
  `pkg/config/testdata/compact_block_permanent_exclusions_8690.txt`

This is **step 1 of 3** in the sequencing team-lead's blast-radius analysis
established. It is deliberately the half with no blast radius: the lexer is
untouched, so nothing that parses today stops parsing.

## Census first — the defect is exactly 3 examples repo-wide

Rather than fix the three sites the issue names, I walked the whole schema
asking whether each `valueExamples` entry parses in a `set` value position:

    3 valueExamples do NOT parse as written
      system services ssh ciphers  ->  aes256-gcm@openssh.com
      system services ssh ciphers  ->  chacha20-poly1305@openssh.com
      system services ssh macs     ->  hmac-sha2-256-etm@openssh.com

`key-exchange` is named in the issue but its two examples contain no `@` and
parse fine today — the affected set is smaller than the issue implies.

The census is now `TestValueExamplesParseAsWritten8924`. Guarding the invariant
rather than the three sites is what stops a fourth being written.

## Read the value back, don't just parse it

Quoting would be a fix in shape only if the quotes survived into the compiled
value — the operator would get a cipher literally named `"aes256-gcm@…"`, which
sshd cannot match. `TestQuotedExamplesCompileUnquoted8924` asserts the compiled
value carries no quotes **and** satisfies `ValidateSSHAlgorithm`. Verified by a
PRODUCTION mutant: making the lexer stop stripping the opening quote kills both
assertions.

## The fix unmasked two pre-existing silent drops — which another lane then fixed

`TestCompactBlockEquivalenceInventory2419` synthesises its fixtures from
`valueExamples`. Because the ssh examples did not parse, those sites were
**skipped** — "a spelling did not parse or compile" — and had never been
measured. With the examples quoted they became measurable and immediately
diverged:

    block   system { services { ssh { ciphers "aes256-gcm@openssh.com"; } } }  ->  [aes256-gcm@openssh.com]
    elided  system { services { ssh ciphers "aes256-gcm@openssh.com"; } }      ->  []

**Newly visible, not newly broken** — the same mechanism as this issue's own
provenance: an unparseable example made a row unrulable, and the unanswerable
verdict hid a real defect underneath. The inventory header had in fact already
noted these two as an unruled fixture ("an example containing '@' that the
parser needs quoted") without the divergence beneath ever being measured.

I registered both `open` in the #8690 exclusion register with the measurement.
**That registration is not in this change, because it stopped being true while
I was writing it.** Another lane landed #8930 (issue #8922) admitting `ssh
ciphers` and `ssh macs` to the fold, and post-merge the elided spelling keeps
its value. The register entries and inventory lines were removed; the #8690 and
#2419 guards caught the staleness themselves, which is the behaviour their
"a site fixed without removing its line reds it too" rule exists for.

Worth recording rather than quietly dropping: my measurement said *"admitting
`services ssh` does NOT fix it — the cause is the compiler reader, not the scope
predicate."* That was true and it was the wrong conclusion to draw from. The
fix WAS a scope admission, of a **different pair** — `ssh ciphers` at depth 3,
not `services ssh` at depth 2. Testing one admission and generalising to "the
scope predicate is not the answer" is the same one-instance-to-a-class error
this campaign keeps producing.

## Attribution of the inventory diff, because most of it is not mine

The regenerated header moves by more than this change causes. Isolated by
regenerating against a pristine schema and diffing the two generated files:

| counter | pristine regen | with this change | mine? |
|---|---|---|---|
| checked | 781 | 783 | **+2 yes** |
| skipped (did not parse) | 29 | 27 | **−2 yes** |
| skipped (under groups) | 813 | 813 | no |

The committed file said `checked: 776` / `groups: 808`, so **the inventory
header was already stale by 10 before this change** — its counters summed to
1787 against a real population of 1797 (site enumeration measured identical
either way). The gate asserts the divergent SET, not the header counters, so
that drift never reds anything: they are unasserted prose inside a generated
file.

## Not done here, deliberately

Steps 2 and 3 (tighten `ValidCommunityMember`, then the lexer change) are
separate. Step 2 turned out to be a live defect in its own right rather than a
prerequisite — filed as #8934 — so it does not block on step 3 ever happening.

## Corpus

9 pass / 5 fail — unchanged.
