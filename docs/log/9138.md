# #9138 — DHCP-learned DNS is not scoped by routing instance

- **Timestamp**: 2026-09-06
- **Action**: Confirmed `mergeDNSInput` walks every held lease with no
  interface/zone/VRF/trust filter while its sibling `collectDHCPRoutes` does
  filter. Wrote cells covering the subject (tenant-instance lease excluded) and
  three controls (default-context WAN lease, fxp0/mgmt, static name-server).
- **File(s)**: `pkg/daemon/dns_routing_instance_scope_9138_test.go` (new)

- **Timestamp**: 2026-09-06
- **Action**: Extracted `dhcpLeaseRoutingInstances` from `dhcpRouteVRFMap` as the
  shared authority and filtered the merge on it, so the two lease consumers
  cannot drift and the filter inherits the #9135 key-shape rule instead of
  re-deriving it.
- **File(s)**: `pkg/daemon/daemon_flow.go`, `pkg/daemon/daemon_dns.go`

- **Timestamp**: 2026-09-06
- **Action**: Ran the proposed blanket rule against every shipped config before
  landing it. The only DHCP client in `test/incus/xpf-test.conf` is `fxp0`,
  which is not in a routing instance; the routing-instance members in
  `docs/ha-cluster-{loss,userspace}.conf` are `gr-0/0/0.0` and in the test
  config `ge-0/0/2`, none of which run a DHCP client. The rule changes nothing
  in any shipped config.
- **File(s)**: —

- **Timestamp**: 2026-09-06
- **Action**: Measured two unguarded wirings and bound both. Severing the
  `d.dhcp.Leases()` read killed zero tests, so the lease/config join was
  extracted as `dnsInputLocked` and bound. Severing
  `reconcileDNSLocked`'s handoff to the reconciler also killed zero tests, so a
  `newDNSReconcilerFn` seam lets a cell read the file the reconcile actually
  writes.
- **File(s)**: `pkg/daemon/daemon_dns.go`,
  `pkg/daemon/dns_routing_instance_scope_9138_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Documented the scoping, including what is deliberately NOT
  excluded and why.
- **File(s)**: `docs/dns-ownership.md`
