# #9113 — the reachability rule is a CONJUNCTION, and I published half of it

- **Timestamp**: 2026-09-06
  - **Action**: corrected the rule stated in `flat_set_chain_walk_8939_test.go`
    and recorded the failure mode that produced it.

    I published, in #9101's comment and onward in messages, that *"a flat run is
    rejected iff the leaf it STARTS at declares a type/validator."* Measured
    through `compileTreeStrict`:

    ```
    container                    unknown-kw   run@untyped   run@typed
    system login class           ACCEPT       ACCEPT        REJECT
    security ike gateway         ACCEPT       ACCEPT        REJECT
    security ipsec proposal      REJECT       REJECT        REJECT
    security flow tcp-session    REJECT       --            REJECT
    ```

    **A flat run is ACCEPTED iff the container is OPEN-WORLD *and* the leaf it
    starts at is UNTYPED.** Either condition alone rejects it. `security ipsec
    proposal` is the counter-example: untyped starting leaf, rejected anyway,
    because #4313 made that container closed-world.

    **The column was never wrong.** It calls `SchemaValidateWithDefinitions` —
    the real gate — so every verdict is measured rather than derived from the
    rule. The third time this file has carried a correct measurement under a
    wrong account of why.

    **The failure mode is lane-8388's and it is genuinely new to this
    campaign's ledger.** lane-8526 had the container half; lane-8388 found the
    leaf half while correcting them, and named their own half as *the*
    discriminator rather than conjoining. In their words: *"I shipped a rule
    with the exact defect I had just diagnosed in theirs, in the same
    message."*

    That is not ordinary over-generalisation. Correcting someone puts you in a
    frame where their account is the thing being replaced, and a newly-measured
    half arrives feeling like the whole — you have just demonstrated their claim
    was incomplete, which is the worst state in which to test your own for the
    same property. Landed in `engineering-style.md` as an explicit question to
    ask: **replacement or conjunct?**

    Consequence for routing: lane-8526's 102 stands and their title is right —
    it measures the open-world half directly. It is a NECESSARY condition for
    flat-run reachability, not the reachable population; the reachable set is a
    strict subset gated per-leaf.
  - **File(s)**: pkg/config/flat_set_chain_walk_8939_test.go,
    docs/engineering-style.md, docs/log/9113.md
