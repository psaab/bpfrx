# #9135 — dhcpRouteVRFMap key shape

- **Timestamp**: 2026-09-06
- **Action**: Reproduced the producer/consumer key-shape mismatch with a cell
  that drives `collectDHCPRoutes` (the #8963 tests hand-build `frr.DHCPRoute`
  literals in `pkg/frr` and are upstream-blind). Both slash-spelled subjects
  red; both pre-existing dash-spelled controls green.
- **File(s)**: `pkg/daemon/dhcp_route_vrf_key_shape_9135_test.go` (new)

- **Timestamp**: 2026-09-06
- **Action**: Replaced the raw-member keying with `dhcpLeaseKeysForMember`,
  which derives keys from `config.DHCPLeaseIfName` — the consumer's own rule —
  and enumerates the units of a whole-device member. Corrected the doc comment,
  which claimed the map was "Keyed on the KERNEL/lease interface name" while the
  body inserted the raw Junos token.
- **File(s)**: `pkg/daemon/daemon_flow.go`

- **Timestamp**: 2026-09-06
- **Action**: Measured that `DHCPRoutes: d.collectDHCPRoutes()` in
  `assembleFRRConfig` was unguarded — replacing it with `nil` killed zero tests
  in `pkg/daemon`. Added a wiring-bind cell asserting the route AND its VRF.
- **File(s)**: `pkg/daemon/dhcp_route_vrf_key_shape_9135_test.go`

- **Timestamp**: 2026-09-06
- **Action**: Mutation table, 8 cells, all applied+built+bounded, 8 KILLED:
  delete-call-site (wiring), drop-absent-stanza-key, whole-device-units-lose-vlan,
  drop-vlan-suffix, naive-iface-lookup, drop-LinuxIfName, unconditional-vrf
  (control bind), sever-assembleFRRConfig-wiring. An earlier `keys := []string{base}`
  seed SURVIVED its mutation (redundant with the `DHCPLeaseIfName` arm) and was
  restructured away rather than excused.
- **File(s)**: —

- **Timestamp**: 2026-09-06
- **Action**: Documented the key-shape contract.
- **File(s)**: `pkg/daemon/README.md`
