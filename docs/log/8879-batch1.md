# 8879 batch 1 — four pairs admitted

**Action**: admitted `protocols bgp`, `security ike`, `security nat`,
`system syslog` to `compactNormalizeInScope`; three cells in
`elision_admissions_8879_test.go`; four registrations in the #8852 blind-pair
map.

## Admission rule, per pair

    elided REJECTED at strict  -> do NOT admit (the operator is already told)
    elided WARNS on lenient    -> do NOT admit (same, weaker)
    elided SILENT              -> admit

Measured before admitting, with hand-written fixtures rather than the sweep's
synthesized ones — all four SILENT, strict accepting and lenient not warning on
either arm:

    protocols bgp    braced as=65001 rid=10.0.0.1  ->  elided <nil>
    security ike     braced proposals=1            ->  elided 0
    security nat     braced pools=1                ->  elided 0
    system syslog    braced hosts=1                ->  elided <nil>

Ratio for this batch: 4 admitted, 0 declined.

## Shipped-config sweep

9 pass / 5 fail, identical to the pre-batch baseline and failing for the same
pre-existing reasons. Confirming a rule catches its target says nothing about
what else it catches, so the corpus is swept per batch.

## My own #8852 guard caught the widening

It RED because all four admissions are blind to arm 2 — "a NEW entry means a
widening was admitted that arm 2 does not measure at all", which is precisely
what happened. All four are `plain-container`, re-derived by the guard rather
than declared, and registered. Their fold correctness rests on the per-pair
cells here, not on the census.

## Mutation, including the assertion split

Removing the four admissions reds the four subtests. ADMITTING `system login`
reds the negative control — and that mutant showed the first assertion was
`Fatalf`, which stopped the second from ever running. Made non-fatal, both fire,
and the second one MEASURES the #8868 regression rather than asserting it: the
elided `system login` spelling is genuinely no longer refused once admitted.
