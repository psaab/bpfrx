# #8863 — a second `set … ssh-ed25519` revoked the first key

- **Timestamp**: 2026-09-05
  **Action**: Made the six `ssh-{rsa,dsa,ed25519}` leaves `multi: true` (root
  and per-user) and taught the compiler to read every value on the node rather
  than `Keys[1]`, so repeated `set` lines ACCUMULATE instead of replacing. Also
  dropped the duplicate-user-block key reset (#6992) so both spellings agree.
  **File(s)**: pkg/config/schema_system.go, pkg/config/compiler_system.go,
  pkg/config/ssh_key_accumulate_8863_test.go,
  pkg/config/duplicate_login_user_6992_test.go
