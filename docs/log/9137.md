# #9137 — IPsec link-local zone names a reth, not a kernel netdev

- **Timestamp**: 2026-09-06
- **Action**: Reproduced on a bondless-RETH cluster config (reth0 over ge-0/0/2
  + ge-7/0/2). `resolveConfiguredInterfaceAddress(reth0.0, 6)` returned
  `fe80::1%reth0`; the unit branch, the bare-ref fallback, and the end-to-end
  `PrepareConfig` -> swanctl `local_addrs` all carried it. Three controls green
  throughout: a non-reth base unchanged, a global address not zoned, and a
  degenerate binding that must not degrade to an EMPTY zone.
- **File(s)**: `pkg/ipsec/linklocal_reth_zone_9137_test.go` (new)

- **Timestamp**: 2026-09-06
- **Action**: Resolved reth in the zone derivation, guarded against an empty
  result. One assignment covers both the unit branch and the bare-ref fallback,
  which share the variable.
- **File(s)**: `pkg/ipsec/policy_addr.go`

- **Timestamp**: 2026-09-06
- **Action**: `pkg/ipsec/README.md` documented the OLD rule verbatim
  ("`config.LinuxIfName(base)` for the configured path"), so the fix would have
  left a false justification sitting next to corrected code. Rewrote that
  bullet and added the reth note with the finding's real bound (global-wins).
- **File(s)**: `pkg/ipsec/README.md`

- **Timestamp**: 2026-09-06
- **Action**: Mutation table, 6 cells, all applied+built+bounded, 6 KILLED:
  revert-the-fix, drop-empty-guard, drop-LinuxIfName, zone-everything (control
  bind), sever-PrepareConfig-wiring, drop-zoneQualify-unit-branch.
- **File(s)**: —
