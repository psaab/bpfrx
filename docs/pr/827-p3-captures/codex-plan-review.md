# Hostile Plan Review — PR #827 P3 Captures
## R2 Review
### Summary
R2 fixes most of the substantive R1 defects. The top-quartile gate is now tied to the spec, the bucket-edge math is corrected, K3 monotonicity is explicit, and the capture sequence is finally pinned to concrete commands. Two material problems remain: K0 can pass vacuously on an empty `per_binding` snapshot, and the plan still leaves two potential sources for `tx_kick_*` block deltas without declaring which one is authoritative. That is enough to keep the plan out of PLAN-READY.

### HIGH Findings
None.

### MED Findings
1. **K0 can pass vacuously on an empty binding set, so it does not actually guarantee the wire surfaced the new fields.** Quote: `“Every raw snapshot's every per_binding entry MUST contain all four keys ...”` ([plan §4.2, lines 111-117](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:111)). The spec requires the new fields to surface on every sample from `step1-capture.sh` ([#819 Issue B, lines 857-859](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:857)) and describes `flow_steer_samples.jsonl` as augmented with the new fields per sample ([#819 §4.3, lines 379-380](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:379)). But the sibling parser precedent accepts `per_binding=[]` as a legal shape ([step1, lines 69-70](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/test/incus/step1-histogram-classify.py:69)), which makes K0 vacuously true. Fix: require non-empty bindings on every warm sample, and explicitly decide whether `snap[0]` may be empty.

2. **The plan still leaves two candidate sources for P3 block deltas, so the `-1` sentinel design is not fully resolved.** Quote: step1 is extended to emit `tx_kick_*_delta` into `hist-blocks.jsonl` ([plan §3.2, lines 52-61](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:52)), `hist-blocks.jsonl` is listed as carrying those fields ([plan §4.1, lines 90-91](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:90)), and the classifier also recomputes its own deltas from raw snapshots ([plan §4.3, lines 144-147](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:144)). The spec separates these roles: derive P3 deltas from the snapshot stream ([#819 §4.3, lines 385-389](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:385)) and use `hist-blocks.jsonl` only for `T_D1` in the read-off ([#819 §5.3, lines 519-521](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:519)). Fix: pre-register raw snapshots as the sole authority for K0-K3 and all P3 delta math; treat step1's `tx_kick_*_delta` as diagnostic/cross-check only.

### LOW Findings
1. **The test-count bookkeeping is internally inconsistent.** Quote: `step3-tx-kick-classify_test.py — unit tests (14 tests)` ([plan §3.1, lines 36-37](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:36)); `## 5. Unit tests (14 total ...)` ([plan §5, line 272](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:272)); then the list runs to 18 tests ([plan §5, lines 276-316](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:276)) and the footer says `Target: 18 tests ... plan R1's 13` ([plan §5, line 322](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:322)), while R1 actually listed 11. Fix: make the counts agree everywhere.

### CLEAR Axes
- **Tie handling is no longer the R1 `>= 0.5` bug.** The `sorted(...)[2]` / `>= threshold` rule is consistent with the spec's `T_D1,b ≥ percentile(T_D1, 75)` plus the design's “top 3 of 12” gloss ([#819 §3.1, lines 94-103](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:94)). In the pathological all-12-tied case, `ElevatedBlocks = all 12` is inherited by the spec itself; it is not a new semantic rewrite.
- **The `-1` sentinel does not, by itself, create a K3 monotonicity false positive.** K3 is defined over raw cumulative snapshot counters, not over step1's emitted block deltas ([plan §4.2-§4.3, lines 126-147](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:126)). The remaining problem is source authority, not K3 arithmetic.
- **Bucket-edge translation is fixed.** `4096 ns` for IN and `< 2048 ns` for OUT now matches `bucket_index_for_ns()` ([umem.rs 198-202](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/userspace-dp/src/afxdp/umem.rs:198)).
- **Exact capture pinning is fixed.** §6.2 now pre-registers the concrete `step1-capture.sh`, `step1-histogram-classify.py`, staging-copy, and `step3-tx-kick-classify.py` sequence.
- **Integer-space T1 gating is fixed.** The verdict no longer depends on lossy float comparisons around the 2 µs / 4 µs thresholds.

### PLAN-READY verdict: NO
Conditions:
1. Tighten K0 so it cannot pass on `per_binding=[]` by accident; state the cold-snapshot policy explicitly.
2. Declare one authoritative source for `retry_count_delta` / `kick_*_delta` in the P3 classifier and demote the other to cross-check-only.
3. Clean up the test-count contradictions so the plan text matches the claimed scope.

## R1 Review (Preserved)
### Summary
The plan is not plan-ready as written. The biggest problems are not stylistic; they change the registered decision rule: `T_D1-elevated` is redefined from the spec's top-quartile rule to `>= 0.5`, and the bucket-3 boundary explanation is mathematically wrong against the actual bucket formula. The plan also declares `step1-histogram-classify.py` out of scope even though the authoritative #819 Issue B acceptance text still requires that parser work. The capture path is directionally right for P3 (`step1-capture.sh`, no sister perf harness), but the exact command and artifact-staging contract are not pinned tightly enough to reproduce the evidence tree.

### HIGH Findings
1. **`T_D1-elevated` is redefined, which changes T1 rather than implementing it.** Quote: `“Define T_D1-elevated block as a block where T_D1[b] ≥ 0.5.”` ([plan §4.4, lines 114-119](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:114)). The spec already defines elevated blocks as the top quartile: `T_D1,b ≥ percentile(T_D1, 75)` / top 3 of 12 ([#819 §3.1, lines 94-101](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:94), [§5.3 step 4, lines 525-527](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:525)). On current cells, your own note says every block is ~0.98, so `>= 0.5` collapses the spec's top-3 gate into “all 12 blocks.” Fix: use the top-quartile/top-3 rule exactly, with tie handling pinned up front.

2. **The plan explicitly drops a still-listed Issue B deliverable.** Quote: `“None required for the classifier. step1-histogram-classify.py already emits hist-blocks.jsonl ... Adding new fields to its own output is out of scope.”` ([plan §3.2, lines 53-55](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:53)). The authoritative design still lists, under Issue B acceptance, `“step1-histogram-classify.py parses the new histogram and counter into per-block deltas.”` ([#819 Issue B, lines 853-861](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:853)). Current `step1-histogram-classify.py` has no `tx_kick_*` parsing. Fix: either add the step1 parser/test work back into scope, or explicitly cite the prior PR/commit that satisfied Issue B and reframe it as a prerequisite already met.

### MED Findings
1. **The bucket-3 math is wrong, and `4000 ns` is not the actual lower edge implied by the code.** Quote: `“kick_latency_mean_ns ≥ 4000 is the bucket-3 lower edge ... Actually ... bucket 3 lower edge ... 2048 ns ... 4 µs threshold sits inside bucket 3.”` ([plan §4.4, lines 139-146](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:139)). The spec says `≥ 4 µs (lower edge of bucket 3)` ([#819 §3.2, lines 120-123](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:120), [§5.3, lines 526-529](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:526)), and `bucket_index_for_ns()` maps `4096 ns` to bucket 3, not `2048 ns` ([umem.rs 198-202](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/userspace-dp/src/afxdp/umem.rs:198)). The `1000` retry threshold itself is consistent; this defect is the latency translation. Fix: gate on `4096 ns`, or stop calling `4000 ns` the bucket boundary.

2. **Cumulative-counter monotonicity is not validated before delta math.** Quote: `“retry_count_delta[b] = retry_count[b+1] - retry_count[b]”` and analogous delta rules ([plan §4.3, lines 101-105](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:101)). The design's P3 recipe assumes bounded differences of cumulative snapshot counters ([#819 §4.3, lines 385-389](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:385)); step1's sibling rationale explicitly insists on pre-delta validation so corruption cannot cancel out across snapshots ([step1, lines 123-126](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/test/incus/step1-histogram-classify.py:123)). As written, a reset/backward jump in `retry_count`, `kick_count`, `kick_sum_ns`, or any hist bucket will flow into verdict math. Fix: add hard-stop monotonicity checks for all cumulative P3 counters before computing any block delta.

3. **The capture protocol is under-specified and path-inconsistent with the actual harnesses.** Quote: `“--evidence-dir <cell-dir> holds flow_steer_*”` ([plan §4.7, lines 212-216](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:212)) plus `“Output directory: docs/pr/819-.../evidence/<cell>/tx-kick/”` ([plan §6, lines 263-269](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:263)). The P3 spec is clear that there is **no** sister harness and P3 rides the existing `step1-capture.sh` flow ([#819 §4.3 Harness integration, lines 413-416](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:413)), but G2 also expects the exact command/output contract to be named ([lines 450-452](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:450)). `step1-capture.sh` writes into `docs/pr/line-rate-investigation/step1-evidence/...`, not the `tx-kick/` tree. Fix: pre-register the exact command sequence, the copy/staging step into `tx-kick/`, and state that `STEP1_SKIP_PERF_STAT` is **not** needed for bare P3.

4. **The test plan omits the R3 hard-stop and the second input contract.** Quote: R3 says missing keys default to zero and must hard-fail if the key is absent ([plan §9, lines 329-334](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:329)), but the 11 listed tests never cover that path and only reject the raw snapshot count mismatch ([plan §5, lines 222-250](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:222)). The spec requires the new fields to surface on every sample ([#819 Issue B, lines 857-860](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:857)), and P3 also depends on a 12-block `hist-blocks.jsonl` input ([#819 §5.3, lines 519-521](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:519)). Fix: add tests for absent `tx_kick_*` keys, `hist-blocks.jsonl` length != 12, and `shape` length != 16.

### LOW Findings
1. **`verdict.txt` is redundant relative to the spec'd and sibling outputs.** Quote: plan outputs `tx-kick-by-block.jsonl`, `correlation-report.md`, `.meta.json`, `.diag.json`, and `verdict.txt` ([plan §4.6, lines 170-201](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:170)). The design's P3 output format names `tx-kick-by-block.jsonl` and `correlation-report.md` ([#819 §4.3, lines 376-383](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:376)); the P1 sibling emits `.meta.json` and `.diag.json`, but not `verdict.txt` ([step2 P1, lines 324-355](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/test/incus/step2-sched-switch-classify.py:324)). Fix: either drop `verdict.txt`, or keep it as an explicitly non-authoritative convenience sidecar.

2. **The quantifier for T1-IN is written ambiguously.** Quote: `“across all T_D1-elevated blocks ... for at least one block”` ([plan §4.4, lines 121-125](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:121)). The spec's wording is already terse ([#819 §3.2, lines 120-129](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:120), [§5.3, lines 525-531](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:525)), so the plan should remove any leftover ambiguity, not add more. Fix: write the rule with explicit quantifiers: `IN iff ∃ b in ElevatedBlocks: retry_count_delta[b] >= ... && kick_latency_mean_ns[b] >= ...`; `OUT iff ∀ b in 0..11: ...`.

3. **Large-`u64` mean arithmetic is unpinned. (UNVERIFIABLE)** Quote: `kick_latency_mean_ns = kick_sum_ns_delta / kick_count_delta` ([plan §4.3, lines 105-107](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:105)) and the tests never cover large cumulative values ([plan §5, lines 222-250](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:222)). I cannot prove this will bite on 60 s captures, but the verdict thresholds are near 2 µs / 4 µs, so float conversion is an unnecessary edge. Fix: either add a large-value arithmetic test, or perform T1 gating in integer space via cross-multiplication (`sum_ns_delta >= threshold_ns * count_delta`).

### CLEAR Axes
- **Axis 4 — aggregation scope.** I found no grounded spec contradiction in summing across all `per_binding` entries. #819 §4.3 defines per-binding counters on the wire, but does not require per-worker-class filtering, and the step1 sibling's precedent is cell-level aggregation across `per_binding`.
- **Axis 8 — findings synthesis core branch.** The plan's split/INCONCLUSIVE branch does preserve per-cell continuation in the same spirit as #819 §6.3 / §7.2. I did not find a concrete spec contradiction there.

### PLAN-READY verdict: NO
Conditions to reach plan-ready:
1. Restore the spec's top-quartile definition of `T_D1-elevated`.
2. Fix the bucket-3 boundary language/threshold.
3. Reconcile Issue B's `step1-histogram-classify.py` deliverable instead of declaring it out of scope without evidence.
4. Pre-register the exact P3 capture/staging commands and add the missing hard-stop/input-contract tests.

## R3 Review
### Summary
R3 fixes the two R2 blockers in the plan body itself. §§3.2, 4.1, 4.2, 4.3, and 5 now consistently assign raw-snapshot parsing, K0-K3, and `tx_kick_*_delta` emission to `step1-histogram-classify.py`, with step3 consuming `hist-blocks.jsonl` only. K0 is also no longer vacuous: the plan now requires both `len(per_binding) >= 1` and all four kick keys on every entry. Two remaining blockers keep this at PLAN-READY NO: the step3 CLI still exposes a raw-snapshot-flavored `--evidence-dir` contract even though R3 says step3 never reads raw samples, and the new step1-as-sole-writer design still lacks a positive-path test that step1 actually emits correct `tx_kick_*_delta` fields.

### HIGH Findings
None.

### MED Findings
1. **The public step3 CLI still advertises raw-snapshot input, which muddies the new sole-authority contract.** Quote: `“Step3 does NOT re-open the raw snapshot files — it trusts step1's computed deltas.”` ([plan §4.1, lines 97-102](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:97)), but the CLI still requires `--evidence-dir` and documents it as holding `flow_steer_cold.json` + `flow_steer_samples.jsonl` ([plan §4.7, lines 284-295](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:284)), and the capture recipe still passes it ([plan §6.2, lines 421-426](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:421)). That is API/contract drift introduced by the R3 restructure: the implementation story is “hist-blocks only,” but the interface still suggests a second input class. Fix: remove `--evidence-dir` from step3 entirely, or explicitly mark it ignored/legacy and say `--hist-blocks` is the only read input.

2. **The plan still does not positively pin that step1 writes correct `tx_kick_*_delta` fields into `hist-blocks.jsonl`.** Quote: step1 is now the `“Sole source of truth for per-block kick deltas”` ([plan §3.2, lines 54-67](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:54)), and Issue B's acceptance criterion remains `“step1-histogram-classify.py parses the new histogram and counter into per-block deltas”` ([#819 Issue B, lines 857-861](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/819-step2-discriminator-design/design.md:857)). But the step1 test list covers aggregation/invariants and submit-path regression only ([plan §5.2, lines 337-369](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:337)); none of those tests explicitly assert that a valid 13-snapshot stream produces the expected `tx_kick_count_delta`, `tx_kick_sum_ns_delta`, `tx_kick_retry_delta`, and `tx_kick_hist_delta` fields in emitted block output. Fix: add one positive-path step1 test that drives a small 13-snapshot fixture through block computation and asserts the emitted per-block `tx_kick_*_delta` values exactly.

### LOW Findings
1. **There is still minor label drift after the R3 rewrite.** Quote: `step3-tx-kick-classify_test.py — unit tests (14 tests)` ([plan §3.1, lines 38-39](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:38)) vs the actual step3 list of 11 tests ([plan §5.1, lines 299-335](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:299)). Workflow/open-questions prose also still says `R2` in a few places ([plan §8, lines 493-495](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:493), [§10, lines 531-532](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:531)). Fix: update the stale labels and counts.

### CLEAR Axes
- **Source authority across the main design sections is now coherent.** §§3.2, 4.1, 4.2, 4.3, and 5 all describe step1 as the sole writer of `tx_kick_*_delta` into `hist-blocks.jsonl`, with step3 consuming only that file for per-block kick data.
- **K0 non-vacuity is fixed.** The plan now explicitly requires both `(i) len(per_binding) >= 1` and `(ii) every entry has all four kick keys` for every snapshot, so there is no remaining empty-snapshot pass-through path in the plan text.
- **No new raw-sample/delta double-counting path is described in the core design sections.** The remaining ambiguity is at the CLI contract boundary, not in §§3.2/4.1/4.2/4.3/5 themselves.

### PLAN-READY verdict: NO
Conditions:
1. Remove or explicitly deprecate `--evidence-dir` so the step3 interface matches the “hist-blocks only” design.
2. Add one positive-path step1 test that proves `hist-blocks.jsonl` emits correct `tx_kick_*_delta` fields on valid post-#826 input.

## R4 Review
### Summary
R4 closes the last substantive blockers. The plan is now unambiguous that `step1-histogram-classify.py` is the sole source of truth for `tx_kick_*_delta`, that K0 requires both non-empty `per_binding` and all four kick keys on every entry, and that step3 consumes `hist-blocks.jsonl` only. I did not find any remaining correctness, completeness, or edge-case gaps that would block implementation. A few editorial inconsistencies remain, but they do not affect the technical contract.

### HIGH Findings
None.

### MED Findings
None.

### LOW Findings
1. **There is still minor editorial drift in test counts and round labels.** Quote: `step3-tx-kick-classify_test.py — unit tests (14 tests)` ([plan §3.1, lines 38-39](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:38)) vs the actual step3 list of 11 tests ([plan §5.1, lines 302-338](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:302)); `extend it with the 8 tests below` ([plan §5.2, lines 342-344](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:342)) vs 11 enumerated step1 tests ([plan §5.2, lines 346-376](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:346)); `22 tests (9 step1 + 11 step3)` ([plan §5, lines 378-382](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:378)) though the listed split is 11 + 11; and §8/§10 still say `R2` in a few places ([plan §8, lines 506-507](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:506), [§10, lines 544-545](/home/ps/git/bpfrx/.claude/worktrees/pr-827-p3-captures/docs/pr/827-p3-captures/plan.md:544)). Fix: clean up the stale labels and counts.

### CLEAR Axes
- **Source authority is now explicit and consistent.** §§3.2, 4.1, 4.2, 4.3, 4.7, 5, and 6.2 all tell the same story: step1 computes and writes `tx_kick_*_delta`; step3 reads only `hist-blocks.jsonl`.
- **K0 non-vacuity is fully fixed.** The plan now requires both `len(per_binding) ≥ 1` and presence of all four kick keys on every entry for every snapshot.
- **The R3 restructure did not introduce a new technical contract gap.** The added positive-path step1 writer test closes the prior completeness hole, and the removed `--evidence-dir` requirement closes the prior API drift.

### PLAN-READY verdict: YES
