## HIGH

- HIGH — `docs/pr/829-slice-b/plan.md:97-148,151-159,203-210`; `docs/cross-worker-flow-fairness-research.md:77-96` — Confirmed bug: the plan defines `v_min` as a scalar atomic minimum of all historically observed `queue_vtime` values and updates it with `observe_v_local()` as an atomic-min CAS, but MQFQ's `V_min` is the current frontier across queues, so the proposed value can only fall from `u64::MAX` and then stick instead of rising as lagging workers catch up. Replace the single `AtomicU64` with per-worker current-frontier state plus a current-min reduction (array, tree, or sweep), and give inactive workers a sentinel so they stop pinning the minimum when they have no backlog. Rewrite tests 2-4 and 10 around "current minimum can rise" instead of "atomic-min descends."

- HIGH — `docs/pr/829-slice-b/plan.md:108-111,155-159,332-339`; `userspace-dp/src/afxdp/tx.rs:5256-5285,5414-5465`; `userspace-dp/src/afxdp/types.rs:1029-1048,1121-1135`; `userspace-dp/src/afxdp/coordinator.rs:1907-1938` — Design risk: the plan treats each binding-local `queue_vtime` as "this worker's `v_local`" and claims an `O(T_bytes)` per-flow fairness bound, but `queue_vtime` lives on each binding's `CoSQueueRuntime` while the lease is shared per `(ifindex, queue_id)`, so the gate is comparing binding-local service frontiers, not a worker-local or per-flow frontier. That leaves the same-worker multi-binding case undefined and means the paper's bound does not transfer directly to xpf's per-flow problem. Either enforce an explicit invariant that at most one binding per worker may service a lease, or move the frontier state to a worker-per-lease runtime shared by all bindings and define exactly which comparable frontier (`next-start`, `next-finish`, or another tag) is published.

- HIGH — `docs/pr/829-slice-b/plan.md:56-66,239-242,359-365`; `userspace-dp/src/afxdp/tx.rs:1717-1739,1932-1955,2068-2218,2368-2402,3786-3808,5670-5698` — Confirmed bug: the plan says the yield path returns before `shared_cos_lease_acquire` and therefore consumes no lease credits, but current code acquires queue-lease tokens in `select_exact_cos_guarantee_queue_with_fast_path()` via `maybe_top_up_cos_queue_lease()` before either flow-fair drain runs, and a yielded non-empty queue retains those tokens until it empties or all queue leases are released. Move the lag gate into queue selection before `maybe_top_up_cos_queue_lease()`, or immediately `release_unused()` any preacquired `queue.tokens` on yield; do not keep test 11 phrased around a call order the code does not have. Add one integration test for "yield once, lag drops, same binding drains the pending HOL packet" and another for "gate passes but lease top-up returns 0."

## MED

- MED — `docs/pr/829-slice-b/plan.md:340-346`; `userspace-dp/src/afxdp/tx.rs:5256-5285,5414-5465` — Design risk: R5's "pre-set `queue_vtime` on binding bring-up" mitigation is not actionable because it never names the hook, while the actual runtime builder still hardcodes `queue_vtime: 0` for every new `CoSQueueRuntime`. Say explicitly whether the seeding happens in `build_cos_interface_runtime()`, `apply_cos_queue_flow_fair_promotion()`, or `ensure_cos_interface_runtime()`, and plumb the shared lease reference there. Add a test that a fresh shared-exact runtime inherits the existing frontier instead of starting at zero.

- MED — `docs/pr/829-slice-b/plan.md:166-181,347-350` — Design risk: the plan's own rule is `T = 2 × per_flow_BDP(queue_rate)`, but the chosen fixed `256 KB` default is only a geometric midpoint between `50 KB` and `500 KB`, which is already about 5× looser than the low-rate cell's target and is not justified as a fairness bound. Either default from the per-queue formula the plan already states, or explicitly downgrade `256 KB` to a sweep midpoint with no theoretical guarantee and explain why a log-space midpoint is the right optimization objective. If one global constant remains, justify it against the stricter 1 Gbps cell rather than the midpoint arithmetic alone.

## LOW

- LOW — `docs/pr/829-slice-b/plan.md:186-197`; `userspace-dp/src/afxdp/types.rs:1418-1436` — Clarity/perf nit: the proposed CAS ordering matches the existing `shared_cos_lease_acquire()` pattern, but the rationale is stronger than necessary because the `v_min` gate only depends on the atomic scalar itself; no additional memory is being published or consumed through that load/store pair. Either keep `Acquire`/`AcqRel` as deliberate conservatism and say so, or switch the `v_min` path to `Relaxed` after a short proof. Do not claim the stronger ordering is required unless you can point to the protected data it synchronizes.

- LOW — `docs/pr/829-slice-b/plan.md:100-106,320-323`; `userspace-dp/src/afxdp/tx.rs:4513,12079-12180` — Clarity nit: the init-case arithmetic is correct (`v_local.saturating_sub(u64::MAX) == 0` for any `v_local <= u64::MAX`), but the plan describes the far-end behavior as "wraparound" even though current `queue_vtime` and finish-byte accounting use `saturating_add` and are pinned to saturate at `u64::MAX`, not wrap. Rewrite §4.1/R2 as "u64 saturation headroom is ~46 years at 100 Gbps" and say what the design does if saturation is ever reached. Also fix test 8's worked example so it uses a nonzero `v_local`, since the property being relied on is "any `v_local` under `u64::MAX`," not specifically zero.

- LOW — `docs/pr/829-slice-b/plan.md:195-197,324-331`; `docs/cross-worker-flow-fairness-research.md:93-96`; `userspace-dp/src/afxdp/types.rs:1353-1358` — Design risk: the plan does not state a worker-count ceiling or a fallback if one hot `v_min` cache line becomes contended, and R3's "confirm size doesn't cross a cacheline" mitigation is incomplete because keeping `SharedCoSLeaseState` under 64 B still places `v_min` on the same line as `credits`. State the intended `active_shards` envelope for slice-b and either isolate `v_min` on its own cache line now or name the tree/sharded reduction fallback from the research doc as the next step. If a scalar stays, make the false-sharing story explicit rather than inferring it from struct size.

PLAN-READY: NO — the core algorithm currently uses the wrong frontier representation and the wrong call order, so the plan does not yet support its fairness or lease-safety claims.

## R2

Verdict: PLAN-READY NO

1. Q1 — HIGH — `docs/pr/829-slice-b/plan.md:121-160,166-184,288-292`; `userspace-dp/src/afxdp/tx.rs:1793-1796,3102-3192,3894-3900` — The 3-worker gate math shows liveness but not a strict `O(T)` spread bound under the actual batch semantics. Example with `T=64 KB`, published frontiers `[W0,W1,W2]=[128,96,64] KB`: `current_min_frontier()=64`; gate results are `lag(W0)=64` (passes because the plan uses `>`), `lag(W1)=32` (passes), `lag(W2)=0` (passes). If `W0` then drains one batch of `B=32 KB` and publishes `160`, the new state is `[160,96,64]`, so `current_min_frontier()` stays `64`, `lag(W0)=96` (now gated), `lag(W1)=32` (still passes), `lag(W2)=0`; once `W2` advances to `96`, the min rises and `W0` re-enters. That is live, but the instantaneous spread reached `160-64 = 96 KB = T+B`, and the code's batch advance is explicitly bounded by `secondary_budget <= cos_guarantee_quantum_bytes(queue) <= 512 KB`, not by `T`. The plan must either weaken the claim/tests to `O(T + batch_advance)` / `<= T + quantum_ceiling`, or move the publish/gate granularity inside the batch.

2. Q2 — PASS — `docs/pr/829-slice-b/plan.md:102-117,380-385`; `userspace-dp/src/afxdp/worker.rs:433-465,705-723,827-867,1493-1496,1761-1811`; `userspace-dp/src/afxdp/coordinator.rs:220-263` — The current architecture really is one thread per worker, and that thread walks bindings sequentially inside `worker_loop`; resets and shutdown teardown of CoS runtime also run on that same worker thread. The coordinator only signals stop / joins threads and does not mutate `BindingWorker` or `CoSQueueRuntime` directly. I do not see an existing cross-thread callback that would write the same frontier slot, so the single-writer-per-slot claim is sound as long as `publish_worker_frontier()` and `mark_worker_idle()` are kept strictly on worker-owned code paths and not called from coordinator-side lease management.

3. Q3 — PASS — `docs/pr/829-slice-b/plan.md:131-142,154-160,248-279`; `userspace-dp/src/afxdp/types.rs:1029-1135` — The sentinel arithmetic itself is correct. If all slots are `u64::MAX`, `current_min_frontier()` returns `u64::MAX`, the `if v_min < u64::MAX` guard skips the lag test, and the worker is not gated; that is the right no-op behavior when all workers are idle or this is the first wakeup. If one worker is active and another slot is still `u64::MAX`, the reduction correctly ignores the idle slot and uses the active worker's frontier. The only problematic "one worker active, another appears MAX" path is not arithmetic; it is bookkeeping: if a binding-level empty check marks the whole worker idle while another binding on that same worker still has lease backlog, the sentinel will hide a real active worker. That blocker is tracked under Q5.

4. Q4 — MED — `docs/pr/829-slice-b/plan.md:80-86,372-379`; `userspace-dp/src/afxdp/tx.rs:4960-4968,5089-5097,5256-5289,5414-5478`; `userspace-dp/src/afxdp/worker.rs:667-676,697-723,1761-1811`; `userspace-dp/src/afxdp/coordinator.rs:346-368,552-587` — In production, `build_cos_interface_runtime()` is reached only through `ensure_cos_interface_runtime()`, which is called from `enqueue_local_into_cos()` / `enqueue_prepared_into_cos()` when a binding first needs an egress runtime or after `reset_worker_cos_runtimes()` has cleared `binding.cos_interfaces`. So it is not "once at daemon start"; it also re-runs after CoS config changes and after a real hot-add that comes in through coordinator `reconcile()` (workers are stopped and respawned with new `BindingPlan`s, then the runtime is rebuilt lazily on first use). The plan's hook location is still off, though: `build_cos_interface_runtime(config, now_ns)` does not have access to `iface_fast.queue_fast_path` or the shared lease, so seeding cannot happen there without a signature change. The workable hook is `ensure_cos_interface_runtime()` after the `iface_fast` lookup, or a new post-build seeding helper that receives the lease reference explicitly.

5. Q5 — HIGH — `docs/pr/829-slice-b/plan.md:102-117,148-160,250-257,274-279,380-385,399-406`; `userspace-dp/src/afxdp/types.rs:1029-1135`; `userspace-dp/src/afxdp/tx.rs:5256-5289,5414-5465`; `userspace-dp/src/afxdp/coordinator.rs:552-572`; `userspace-dp/src/afxdp/mpsc_inbox.rs:33-40` — R2 still introduces three blocking issues. First, HIGH-2 is not actually resolved: each binding has its own `queue_vtime`, but the slot is defined as "whichever binding most recently dispatched on this lease"; that lets the per-worker frontier move backward when a later dispatch comes from a lower-vtime binding, and `mark_worker_idle()` on one empty binding can wrongly drop the whole worker out of the min while another binding on the same worker still has lease backlog. This needs a real worker-per-lease runtime or other worker-owned aggregator that tracks all bindings on that worker for that lease, including an active-binding count before emitting `u64::MAX`. Second, raw `worker_id` is not proven dense `0..N-1`; the coordinator groups workers by arbitrary `binding.worker_id`, so `worker_frontiers[wid]` plus "length = num_workers" is unsafe without a dense slot remap carried in fast-path state. Third, the plan cites the existing `CachePadded` pattern, but `mpsc_inbox::CachePadded` is private; either export a shared padding utility or define a new local wrapper. Test coverage also still misses the two-binding-same-worker cases that would expose both the backward-frontier overwrite and the false-idle bug.

Blocking items before PLAN-READY YES:

- Replace the binding-local "latest writer wins" slot semantics with a true worker-per-lease frontier model that cannot move backward and cannot mark the worker idle while any binding on that worker still has pending work on the lease.
- Fix the fairness claim and tests to match the actual batch semantics: either prove / enforce `batch_advance <= T`, or restate the bound as `T + batch_advance` and assert against the concrete quantum ceiling.
- Introduce a dense worker-slot mapping instead of indexing `worker_frontiers` by raw `worker_id`.
- Move the bring-up seed and teardown-idle hooks to concrete production sites that actually have lease access (`ensure_cos_interface_runtime()`, `reset_binding_cos_runtime()`, worker-loop shutdown), and add tests for those exact paths.

## R3 Review (Round 3)

### R3-1
`PLAN-ONLY.` The uniqueness proof exists only in the plan: `register_binding()` is specified as `next_slot` atomic fetch-add with one-time slot ownership (`docs/pr/829-slice-b/plan.md:57-71,126-149`), but no `register_binding`, `next_slot`, or `frontier_slot` code exists in `userspace-dp/src/afxdp` yet. Concurrent registration is plausible because workers are separate threads (`userspace-dp/src/afxdp/coordinator.rs:651-729`) and each lazily runs `ensure_cos_interface_runtime()` (`userspace-dp/src/afxdp/tx.rs:4960-4968,5089-5097,5256-5289`). If implemented exactly as written, two concurrent fetch-add callers should get distinct slot numbers; the unproven part is one-time registration discipline across later rebuilds.

### R3-2
`PLAN-ONLY.` The plan explicitly leaks slots on teardown (`docs/pr/829-slice-b/plan.md:472-478`), and there is no reclaim path in the repo because none of the slot APIs exist yet. Full `reconcile()` stops workers and clears the shared lease maps (`userspace-dp/src/afxdp/coordinator.rs:253-263,346-368`), so that path would reset leaks. But in-process CoS config refreshes reset per-binding runtimes (`userspace-dp/src/afxdp/worker.rs:667-676,1761-1811`) while `build_shared_cos_queue_leases_reusing_existing()` keeps the same lease Arc when config still matches (`userspace-dp/src/afxdp/coordinator.rs:1116-1137,1907-1939`). Source does not quantify churn rate, so “64 is sufficient over daemon lifetime” is not justified from code alone.

### R3-3
`CONFIRMED.` The bring-up hook has lease access at the proposed site. `ensure_cos_interface_runtime()` builds the runtime and then calls `apply_cos_queue_flow_fair_promotion(&mut runtime, &iface_fast.queue_fast_path)` (`userspace-dp/src/afxdp/tx.rs:5281-5284`). That helper iterates `runtime.queues.zip(queue_fast_path)` (`userspace-dp/src/afxdp/tx.rs:5312-5318`), and each `WorkerCoSQueueFastPath` already carries `shared_queue_lease` (`userspace-dp/src/afxdp/worker.rs:1645-1660`). The current implementation does not use the lease yet, but the plan’s scope assumption is correct.

### R3-4
`PLAN-ONLY.` A single `drain_pending_tx()` can execute many back-to-back `drain_shaped_tx()` calls (`userspace-dp/src/afxdp/tx.rs:408-438,456-498`), so one poll pass is not one batch. But `drain_shaped_tx()` itself services at most one exact batch before returning (`userspace-dp/src/afxdp/tx.rs:1437-1488`), and exact selection is re-entered on each call (`userspace-dp/src/afxdp/tx.rs:1566-1574,1717-1805`). If the R3 gate lands in selection as planned (`docs/pr/829-slice-b/plan.md:152-178`), lag is rechecked once per batch, so the natural overshoot is `T + one batch`, not `T + N×512 KB`. That consequence is still unimplemented today.

### R3-5
`PLAN-ONLY.` Concurrent registration during reload is a real path: workers are distinct threads (`userspace-dp/src/afxdp/coordinator.rs:651-729`), forwarding/lease updates are published live (`userspace-dp/src/afxdp/coordinator.rs:984-1063`), and runtimes are rebuilt lazily per worker (`userspace-dp/src/afxdp/tx.rs:5256-5289`). If `next_slot` is implemented as atomic fetch-add as planned (`docs/pr/829-slice-b/plan.md:57-71,130-132`), two concurrent callers should not observe the same old value, so no collision/ABA on slot numbers. The remaining risk is double-registration of the same logical binding after `reset_worker_cos_runtimes()` rebuilds (`userspace-dp/src/afxdp/worker.rs:667-676,1761-1811`), because the plan neither persists nor reclaims slot ownership.

### R3-6
`REFUTED.` The R3 test plan is still incomplete. It lacks an integration test that overflowed `register_binding()` disables gating on a real queue path (`docs/pr/829-slice-b/plan.md:290-292,311-362`), lacks a concurrent registration race test (only concurrent publish is listed at `docs/pr/829-slice-b/plan.md:305-307`), and lacks a same-worker multi-binding fairness-under-load test (tests 18-19 are structural, while test 20 uses different workers at `docs/pr/829-slice-b/plan.md:334-348`). It also never pins the multi-batch-per-loop bound case, and the plan contradicts itself on test count: `23` at `docs/pr/829-slice-b/plan.md:364-365`, then `18` at `docs/pr/829-slice-b/plan.md:367,412-413`.

### Verdict
PLAN-READY NO — the bring-up hook location is valid, but slot uniqueness and exhaustion handling are still only plan-level arguments, and the R3 test matrix still misses overflow and concurrent-registration coverage.

## R4 Review (Round 4)

### R4-1
`PASS.` `§8a` now addresses the R3-1 / R3-5 procedural objection in the only form available before code exists: it converts slot ownership, idle independence, and fetch-add race freedom into implementation-blocking tests `#18`, `#19`, and `#25` (`docs/pr/829-slice-b/plan.md:348-356,385-389,496-509`; `docs/pr/829-slice-b/codex-plan-review.md:48-61`). That still leaves them `PLAN-ONLY` today, but that is acceptable at plan-review time. `R3-2` is different: slot-leak acceptability is still policy rather than proof, only bounded operationally by overflow-disable-gate plus `register_overflow`, with full `reconcile()` resetting shared lease maps on a real worker restart (`docs/pr/829-slice-b/plan.md:227-238,380-384`; `userspace-dp/src/afxdp/coordinator.rs:253-263,346-368`).

### R4-2
`MED.` Tests `#24`-`#26` close three concrete R3 gaps: overflow integration on the real selection path, concurrent registration under contention, and the one-drain-per-lease invariant that keeps the bound at `T + one batch` instead of `T + N × batch` (`docs/pr/829-slice-b/plan.md:380-395`; `docs/pr/829-slice-b/codex-plan-review.md:63-64`; `userspace-dp/src/afxdp/tx.rs:1437-1488,1566-1574,1717-1805`). The only uncovered case R3 named is a direct same-worker multi-binding fairness-under-load test: `#18` and `#19` are structural, while `#20` still uses different workers (`docs/pr/829-slice-b/plan.md:348-362`). Given the R4 design is per-binding-slot and worker dispatch is single-threaded (`userspace-dp/src/afxdp/worker.rs:658-720`), that missing load test is useful but not a blocker to implementation.

### R4-3
`LOW.` I do not see a new algorithmic defect introduced by the R3→R4 edits. The remaining new problems are plan hygiene issues that can mislead implementation if left in place: stale status/workflow text (`docs/pr/829-slice-b/plan.md:3,453-454`), stale `worker_frontiers` wording after the rename to `binding_frontiers` (`docs/pr/829-slice-b/plan.md:126-132,282-287`), conflicting test totals (`docs/pr/829-slice-b/plan.md:398-401,446`), and an env-var name mismatch between the algorithm section and validation recipe (`docs/pr/829-slice-b/plan.md:257-260,433-437`). Those should be cleaned before coding starts, but they do not reopen the design.

### Verdict
PLAN-READY YES — with `test_concurrent_registration_returns_unique_slots` explicitly validating the fetch-add allocator during implementation, the plan is now specific enough to build; the remaining issues are cleanup and one non-blocking coverage enhancement, not unresolved design ambiguity.
