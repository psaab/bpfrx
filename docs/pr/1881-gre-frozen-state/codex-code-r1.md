**Verdict: NEEDS-REVISION (1)**

1. **Deferred apply misses same-id GRE attachment drift.**  
   `prune_local_tunnel_sources_for_snapshot()` only builds a desired set by `id` for rows that are still `gre/ip6gre` ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:849)) and only prunes entries whose id is absent from that set ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:859)). It ignores `row.ifindex` / `linux_name` attachment changes, unlike the armed reconcile stale pass, which explicitly prunes `logical_ifindex` or name drift ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:573)).  
   Counterexample: non-same-plan `apply_snapshot` with `defer_workers=true` plus GRE endpoint `id=1` still `gre` but moved from old ifindex/name to new ifindex/name. The handler calls this prune helper and then stores the snapshot without `refresh_runtime_snapshot()` ([snapshot.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/server/handlers/snapshot.rs:114), [snapshot.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/server/handlers/snapshot.rs:127)). Since forwarding is not rotated, the thread-side park gate never sees the new attachment; the old TUN reader can keep running on stale state. The existing defer test only covers same snapshot and removed endpoint ([tests.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/tests.rs:2674)), so this hole is unpinned.

**Required Trace**

Mid-packet refresh is coherent but not absolute zero-stale: the loop loads one forwarding Arc per outer iteration ([tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/tunnel.rs:171)) and uses that same `&forwarding` for build/encap ([tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/tunnel.rs:216)). If refresh lands after that load, one already-loaded packet can finish on old state. On the next iteration, `load_arc_if_changed` rotates the Arc and recomputes attachment validity ([tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/tunnel.rs:172)); if stale, local-origin reads are dropped before build ([tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/tunnel.rs:207)). The prune/join path publishes the map excluding stale ids before join ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:591)), then stops/joins ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:598)).

Other checked items: publish-live-only excludes swept tombstones via `handle.is_none()` ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/coordinator/mod.rs:693)); liveness/reconcile are serialized under the server mutex ([handlers/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/server/handlers/mod.rs:82)); write-side `EINVAL` is isolated from read fatality ([tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1881-engineer-impl/userspace-dp/src/afxdp/tunnel.rs:30)). I did not run cargo tests because this session is read-only; `git diff --check 7cf46dc68..HEAD -- userspace-dp/` passed.

Codex session ID: 019eba76-93e3-7b50-bc42-cede1a7b0b5e
Resume in Codex: codex resume 019eba76-93e3-7b50-bc42-cede1a7b0b5e
