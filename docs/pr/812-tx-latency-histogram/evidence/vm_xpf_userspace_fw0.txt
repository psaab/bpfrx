Target VM: loss:xpf-userspace-fw0
Date: 2026-04-21 (UTC)

# uname
Linux xpf-userspace-fw0 7.0.0-rc7+ #55 SMP PREEMPT_DYNAMIC Sun Apr 12 16:14:06 PDT 2026 x86_64 GNU/Linux

# glibc
ldd (Debian GLIBC 2.42-14) 2.42

# VDSO mapping (from /proc/self/maps)
7f13408a6000-7f13408a8000 r-xp 00000000 00:00 0                          [vdso]

# strace -c -e trace=clock_gettime ./vdso_probe
ok: 10000 clock_gettime calls (xor=9b6f7)
EXIT=0
strace summary file /tmp/st.out: 0 lines, 0 bytes (zero clock_gettime syscalls recorded).

# Interpretation
- /proc/self/maps shows a [vdso] segment mapped into user space (2 pages at 0x7f13408a6000).
- A 10,000-iteration `clock_gettime(CLOCK_MONOTONIC, ...)` loop under `strace -e trace=clock_gettime -c` produces
  ZERO syscall entries in the strace summary file, confirming the calls go through the VDSO and never enter
  the kernel syscall table on this VM.
- No seccomp profile is in place on this VM that would block clock_gettime (systemd unit for xpfd is the only
  process of interest and uses no SeccompFilter).

# Conclusion
On the loss-userspace cluster VM (`xpf-userspace-fw0`, kernel 6/7 series, glibc 2.42-14), `libc::clock_gettime`
resolves through the VDSO — no syscall cost. The plan's per-commit-stamp path using `monotonic_nanos()` is safe
on this deployment target. Userspace cluster is the sole measurement target per project discipline; the
bpfrx/eBPF cluster is explicitly not a supported measurement surface for this investigation.
