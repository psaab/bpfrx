**Verdict: PLAN-KILL for #967**

The plan’s core claim holds. The measured `__memmove_evex_unaligned_erms` cost is tied to full-payload cross-UMEM frame construction, not the tiny Ethernet header rewrite. The source backs that up: `build_forwarded_frame_into_from_frame` copies the L3 payload into the target frame at [frame/mod.rs:262](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/frame/mod.rs:262), and TX dispatch uses that path for direct cross-allocation sends at [tx/dispatch.rs:606](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/tx/dispatch.rs:606). In-place rewrite is narrowly gated to same-UMEM/self-target paths at [tx/dispatch.rs:415](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/tx/dispatch.rs:415) and [poll_descriptor.rs:258](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/poll_descriptor.rs:258).

There is a real callsite, but not a useful SIMD one. `rewrite_forwarded_frame_in_place` and `apply_rewrite_descriptor` do use `copy_within` when VLAN header length changes at [frame/mod.rs:545](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/frame/mod.rs:545) and [frame/mod.rs:845](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/frame/mod.rs:845), and the Ethernet header writer is just tiny fixed-size stores at [frame/mod.rs:1484](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/userspace-dp/src/afxdp/frame/mod.rs:1484). PSHUFB does not remove the full-payload move, and for 14/18-byte header stores it adds unsafe SIMD, CPU feature dispatch, fallback paths, and tests around work the compiler/libc already handles well.

Perf threshold fails hard: the real table shows `__memmove_evex_unaligned_erms` at 13.43%, `poll_binding_process_descriptor` at 9.45%, and `enqueue_pending_forwards` at 4.20%, with no rewrite/header symbol ≥2% ([diagnostic.md](/home/ps/git/bpfrx/.claude/worktrees/simd-triage/docs/pr/778-skb-alloc-zerocopy/diagnostic.md:46)). Also, EVEX means the measured copy is already AVX-512-class libc code; replacing that story with AVX2 `pshufb` is directionally wrong.

One minor plan cleanup: “header-only rewrite-in-place” is imprecise for VLAN push/pop, because `copy_within` can shift the whole L3 payload. That does not rescue #967; it makes PSHUFB less relevant. Close #967 and point to #776/#777/#779 unless a new call-graph perf capture shows `copy_within` or `write_eth_header_slice` itself at ≥2% under the smoke workload.

Codex session ID: 019df694-2999-7f71-b3a1-913bdf85d481
Resume in Codex: codex resume 019df694-2999-7f71-b3a1-913bdf85d481
