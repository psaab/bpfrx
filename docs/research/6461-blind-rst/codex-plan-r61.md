PLAN NO

All plan references are pinned to `0f98f0c29`.

1. **BLOCKER — Capability intersection cannot constrain an intermediate endpoint that does not implement BIT 5.** The fold says old B ignores BIT 5, sees A’s `repair=1`, yet both endpoints compute `decision=0` and use legacy ([plan.md:4176](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4176)). B cannot condition repair activation on an unknown dependency: under its older matching-CONFIRM rule, A and B both advertise `repair=1`, so B activates repair while A sees B’s missing BIT 5 and selects legacy ([plan.md:4013](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4013)). The lifetime latch prevents repairing this through a later reduced record ([plan.md:4040](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4040)). Use a repair version old B cannot recognize, or a genuine ADVERTISEMENT→conditional-CONFIRM exchange in which A does not assert old repair support until it knows B supports the decision protocol. “Trailing tolerance” also cannot justify compatibility: BIT 5 is inside the existing fixed-width capability word, not an additive trailing field.

2. **BLOCKER — A one-shot token cannot be CAS-consumed before every activation side effect.** The tuple and readiness-generation binding are now present ([plan.md:4363](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4363)), but ownership activation is multi-stage. `commitRequestedPeerFailover` first applies the override and runs election ([failover.go:313](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:313), [failover.go:335](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:335)); the daemon later performs `SetCluster`, `SetRGActive`, route changes, and `ForceRGMaster` ([daemon_ha.go:287](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/daemon/daemon_ha.go:287), [daemon_ha.go:314](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/daemon/daemon_ha.go:314)). Consuming a one-shot token before election leaves nothing for later steps; delaying consumption leaves the first election unauthorized. Revalidating independently can instead leave a partially activated transition if readiness advances mid-sequence. Replace this with a durable `Authorized → Claimed → Applied` transaction: one initial CAS claims the exact tuple before any side effect, later idempotent steps validate that immutable claim and record progress, and failure rolls back or retains the old-owner lease.

3. **BLOCKER — G2 fencing is ordered after G1’s positive completion effects.** The executor fold now exists normatively, but a new activation supersedes G1 only after G1 completes or is “safely abandoned” ([plan.md:4513](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4513)); only afterward does the activation bump its generation and re-arm the election hold ([plan.md:4536](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4536)). A G1 worker can validate, clear the hold, restore one instance, and block during the remaining multi-instance release ([manager.go:389](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/vrrp/manager.go:389)). If overflow creates G2 missing state then, the restored instance can process `preemptNowCh` before G2’s fence exists ([instance.go:880](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/vrrp/instance.go:880)). G2 must first revoke T1, publish `G2/not-ready`, and establish the ownership fence; only then may it drain or abandon G1. Target validation must be atomic with each externally visible mutation, not merely performed once on API entry.

4. **HIGH — The ownership-commit fence does not identify a shared lock or its complete span.** The five-class predicate now correctly includes config publication and parked-FIFO drain ([plan.md:4289](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4289)). The commit rule merely says “the SAME lock domain” ([plan.md:4300](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4300)). Today `SetSyncHold` uses `vrrp.Manager.mu` and per-instance state ([manager.go:354](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/vrrp/manager.go:354)), while `becomeMaster` uses separate instance/VIP state. It rechecks `ownerGen` at [instance.go:1305](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/vrrp/instance.go:1305), releases `vipMu`, then publishes advert/event at [instance.go:1330](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/vrrp/instance.go:1330). Readiness can advance after the check but before publication. Name one promotion gate and hold its permit from generation validation through the final ownership publication, with explicit ordering across normal VRRP and private/direct RG commits.

5. **HIGH — ISSU `ForceSecondary` bypasses the forced-repair transfer protocol.** The corrected ManualFailover ordering is sound: its pre-hook completes before `SecondaryHold` is committed ([failover.go:62](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:62), [failover.go:120](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:120)). `ForceSecondary`, however, immediately marks every RG secondary without invoking that hook ([failover.go:140](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:140), [failover.go:159](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/failover.go:159)). If B lacks E1, an ISSU drain can demote A without the forced repair/JOURNAL_END/token sequence, after which B assumes ownership incomplete. Every planned demotion entry point, including `ForceSecondary`, must use the same terminally validated repair protocol.

6. **MEDIUM — The safe transfer gate contradicts “operator is never blocked.”** The operative rule correctly withholds a token until pending rejections, repair, and epoch/park state clear ([plan.md:4333](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4333)), but later promises that an operator is never blocked ([plan.md:4393](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4393)). A persistent local-authority tuple conflict can keep a cohort pending until the local holder exits ([plan.md:1911](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:1911)); repeated repair cannot make it satisfiable. Define an operator-visible not-ready failure or a separately explicit disruptive mode. The safe path cannot promise unconditional progress.

7. **MEDIUM — The normative BIT-5 vector is internally inconsistent.** The named `dialer_cap` literal still ends with `1f000000`, while its label says `0x3F` ([plan.md:4662](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4662)). The complete inputs use `3f` for both sides and the published digests correctly match those complete inputs ([plan.md:4671](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/docs/research/6461-blind-rst/plan.md:4671)). Implementations constructing the test from the named records therefore disagree with implementations consuming the complete input. Change the standalone literal to `3f000000`.

**r60-1 disposition: NOT RESOLVED.** The tuple inventory is fixed, but the bilateral intersection claim cannot govern an older endpoint.

**r60-2 disposition: PARTIALLY RESOLVED.** The five-class predicate is complete; the promotion linearization lock and span remain undefined.

**r60-3 disposition: PARTIALLY RESOLVED.** ManualFailover now repairs before retry-stop/demotion, but the planned ISSU `ForceSecondary` path bypasses it.

**r60-4 disposition: PARTIALLY RESOLVED.** The token carries the required tuple and replay identity, but one-shot re-consumption cannot authorize a multi-stage activation transaction.

**r60-5 disposition: PARTIALLY RESOLVED.** The executor is now normative and nonblocking, but supersession fences G2 too late and “safe abandonment” is insufficient for partially executed target effects.

**r60-6 disposition: RESOLVED.** §9(d12) adds the original requested cases; the new traces above require corrected or additional cases.

Bottom line: v9.9.54.14 repairs the five-class readiness predicate, ManualFailover repair ordering, capability inventory, and the missing executor mechanism, and I found no new allocator or translated-tuple lifecycle trace. It does not converge because mixed-version capability activation still splits protocol classes, the one-shot token cannot span the real activation transaction, G2 can remain unfenced while G1 releases readiness, and a planned ISSU drain bypasses repair entirely. These leave concrete availability and HA state-desynchronization paths, so final sign-off remains PLAN NO.
