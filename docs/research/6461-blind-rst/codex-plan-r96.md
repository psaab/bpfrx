# Codex hostile plan-review — round 96 (v10.12.0)

Reviewer: Codex (fresh thread, job task-ms92domw-83bege, 2026-07-31). Verdict: PLAN NO (6B/1H/1M). The headline: master's own cold-path merge keeps the released P1 over the fresh P2 (NatDecision::merge prefers existing fields, nat/mod.rs:123-133), falsifying v10.12.0's 'correct because master' premise — the plan now asserts parity, not correctness, and documents the purge-aftermath released-tuple reuse family.

## Verbatim review

PLAN NO 1. **BLOCKER — the cold master-split still installs and buffers released P1, not newl...

[2026-07-31T15:09:40.391Z] Assistant message
PLAN NO

1. **BLOCKER — the cold master-split still installs and buffers released P1, not newly owned P2.** Master does reach the seed transaction after purge: P1 is released, but the retained lookup still carries P1 (`userspace-dp/src/afxdp/session_glue/promote.rs:181-207`; `userspace-dp/src/afxdp/session_glue/mod.rs:1194-1200`). The cold arm copies that full NAT decision into `pending_decision`, allocates P2, then merges P2 into it (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4662,4745-4758`). `NatDecision::merge` gives existing fields precedence (`userspace-dp/src/nat/mod.rs:123-133`), so P1’s source address/port survive. Install, shared/BPF publication, buffering, and replay consequently use P1 while the allocator owns P2 (`poll_descriptor/mod.rs:4780-4795,4811-4888,5057-5067`; `userspace-dp/src/afxdp/neighbor_dispatch.rs:272-348`). On install refusal, rollback also passes P1 and the allocator refuses the P1/P2 mismatch, leaking P2 (`poll_descriptor/mod.rs:4890-4909`; `userspace-dp/src/nat/allocator.rs:1398-1404`). Trailing reinjection independently consumes the outer retained P1 (`poll_descriptor/mod.rs:5126-5138`; `userspace-dp/src/afxdp/tx/dispatch/slow_path.rs:193-200`). This falsifies `plan.md:695-717,1620-1635,2189-2202`. The narrower claim that a successfully installed `MissingNeighborSeed` emits no Open is correct (`userspace-dp/src/session/entry.rs:272-274`; `userspace-dp/src/session/install.rs:225-260`).

2. **BLOCKER — close→ACK is not end-state-equivalent to master, on either warm or cold next hops.** FIN/RST cannot populate flow cache (`userspace-dp/src/afxdp/flow_cache.rs:354-394`; `userspace-dp/src/tcp_flags.rs:83-90`). With a warm hop, master purges on the close, so the following ACK clean-misses, installs `ForwardFlow`, and emits Open (`poll_descriptor/mod.rs:2449-2458`; `session/install.rs:225-260`). The plan retains on the close; the ACK then purges, forwards retained P1, and can cache that sessionless decision (`poll_descriptor/mod.rs:3900-3959`). Cache lookup precedes session resolution, and missing-session touch/account are no-ops (`poll_descriptor/mod.rs:298-327`; `userspace-dp/src/session/mod.rs:1118-1133,1177-1196`), so subsequent ACKs can continue using released P1 without install/Open until an independent invalidation.

   The cold case diverges further: master’s close seeds `closing/reset`, selecting the 2-second RST or 30-second FIN timeout. Under the plan, the retained close followed by a cold ACK seeds a non-closing established entry, normally selecting 300 seconds (`session/install.rs:163-180`; `session/mod.rs:60-84,2161-2180`). Thus retention materially extends the bad P1/P2 seed state. Pointing to master’s different ACK-first packet order does not establish equivalence for this same close→ACK history, and the claimed cache-corner documentation is absent from the exact document.

3. **BLOCKER — #6600 state can cross HA activation and become locally owned instead of reaching a purge fence.** Import publishes before the worker attempts the silently refused reservation (`userspace-dp/src/afxdp/ha/session_import.rs:115-121,215-223`; `userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:64-96`; `userspace-dp/src/nat/allocator.rs:1682-1689`). The proposal preserves that conflicted row on a close that master would purge. If its RG activates, HA publishes the active state and refreshes worker rows (`userspace-dp/src/afxdp/ha/state.rs:39-72,121-170`; `commands/refresh_owner_rgs.rs:43-62`; `session/mod.rs:1642-1666`). Active ownership makes `should_keep_synced_hit_transient` false, so the next non-close materializes/promotes rather than purging (`session_glue/promote.rs:48-59`; `session_glue/mod.rs:1178-1198,1235-1252`). Promotion republishes and emits Open for the conflicted P1 without retrying reservation (`session_glue/promote.rs:99-139`; `session/mod.rs:1480-1530`). Master had already removed this family on the close. The proposed FIN/RST rule therefore newly worsens #6600 and invalidates the “non-close purge or peer delete” end-state description.

4. **HIGH — the corrected retention bound is neither consistently stated nor a bound on all forwarding state.** The header correctly recognizes that `SyncedSessionEntry` has no deadline (`userspace-dp/src/afxdp/worker/mod.rs:375-401`) and local GC does not remove shared backing (`userspace-dp/src/afxdp/worker/loop_body/mod.rs:1481-1522`). Nevertheless §7 still says “first NON-close packet or expiry” and calls the extension bounded (`plan.md:1901-1912`), contradicting `plan.md:7-10,2517-2519`.

   A synchronized delete removes shared/local rows but does not invalidate flow cache (`userspace-dp/src/afxdp/ha/session_import.rs:243-320`; `userspace-dp/src/afxdp/session_glue/commands/delete_synced.rs:9-54`). Cache validation checks config/FIB/RG epoch/lease, has no idle eviction, and keeps active entries resident (`userspace-dp/src/afxdp/flow_cache.rs:767-780,991-1039`). Therefore peer delete bounds shared-row retention only; an already cached P1 decision can outlive it.

5. **BLOCKER — overdue-K atomic reindex still corrupts authority and session-limit accounting.** The plan explicitly permits `metadata.is_reverse` to change (`plan.md:1310-1317`) but does not transition the presence-based counted class. Synced upsert increments only forward, non-seed entries, while removal decrements according to the entry’s new orientation (`userspace-dp/src/session/install.rs:434-447`; `userspace-dp/src/session/mod.rs:1782-1821`). Forward→reverse therefore leaks a count; reverse→forward adds no count and can later decrement another flow’s shared per-IP total (`session/mod.rs:901-941`).

   Preserving K’s origin is also not authority-safe. Reactive materialization may derive either `SharedMaterialize` or `WorkerLocalImport` from the current shared origin (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1115`; `userspace-dp/src/session/entry.rs:242-269`). Both suppress Close, but only `SharedMaterialize` is promotable, and that distinction gates promotion/Open/replication (`session_glue/promote.rs:86-138`). Since shared publication can replace the same-key origin (`userspace-dp/src/afxdp/shared_ops.rs:897-916`), retaining K’s stale origin can wrongly grant or suppress ownership promotion. The helper needs counted-class adjustment and the current S2-derived materialization origin.

   The wheel portion is sound if key, `wheel_tick`, `last_seen_ns`, and `expires_after_ns` remain unchanged and no push occurs (`userspace-dp/src/session/wheel.rs:53-57`; `userspace-dp/src/session/expire.rs:50-72,155-168`).

6. **BLOCKER — mutually exclusive requirements remain live throughout v10.12.0.** The master-split rule correctly requires same-dispatch cold seeding (`plan.md:1620-1635,2185-2195`), matching `poll_descriptor/mod.rs:4662,4745-4795,4811-4889`. But §§5.6/5.8, §9, and §11 still require no same-dispatch seed and a later clean miss (`plan.md:1490-1495,1594-1599,2241-2256,2491-2499`). Section 9 still says retention both requires a successful reservation and is unconditional (`plan.md:2122-2128`). It also retains the broad one-decision oracle despite the acknowledged outer/pending split (`plan.md:2241-2256`; `poll_descriptor/mod.rs:4662,5126-5138`). Section 11 still says counters are unchanged despite the accounting carve-out (`plan.md:2503-2505`; `session/mod.rs:1177-1210`). These are implementation-defining contradictions, not merely history.

7. **MEDIUM — `SeedInstalled` is not a valid arm-head outcome.** A shared-only purge may free no local slot; installation can refuse at capacity, after which the arm rolls back and drops (`userspace-dp/src/session/install.rs:123-125`; `userspace-dp/src/afxdp/poll_descriptor/mod.rs:4787-4910`). NAT64 MissingNeighbor also drops before the seed block (`poll_descriptor/mod.rs:4634-4656`). The pre-action provenance should be `SeedEligible`/`PurgedRetained`; only the later result can become `SeedInstalled` or `SeedRefused`. The triplicated test text at `plan.md:2206` is additional editorial residue.

Round-95 dispositions: r95-1 UNRESOLVED; r95-2 UNRESOLVED; r95-3 UNRESOLVED; r95-4 UNRESOLVED; r95-5 UNRESOLVED; r95-6 RESOLVED substantively with a §11 straggler; r95-7 UNRESOLVED.