PLAN NO

1. **BLOCKER — Repair activation is not bound to the connection that negotiated repair-vN.** The plan makes protocol class connection-local and arms a fresh repair on activation ([plan.md:4084](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:4084)), while repair correctness depends on a pinned, authorized stream token ([plan.md:1601](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1601)). However, the current bulk entry point accepts no connection token: `BulkSync` calls `getActiveConn()` ([pkg/cluster/sync_bulk.go:50](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:50)), which always prefers fabric 0 when present ([pkg/cluster/sync_conn.go:27](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:27)). Concrete trace: fabric 0 remains legacy-latched; fabric 1 reconnects and commits repair-vN; activation arms not-ready and a repair ID, but the bulk selects legacy fabric 0. It either cannot emit repair frames or loses the terminal exchange, leaving the obligation permanently armed despite a usable repair-capable connection. Bind activation, repair ID, redrive, and terminal exchange to the exact negotiated slot token, or retire all legacy sibling connections before exposing repair-vN.

2. **HIGH — A rejected stale readiness callback can still release the operational hold.** `BulkEnd` queues `OnBulkSyncReceived` asynchronously ([pkg/cluster/sync_conn_read.go:241](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:241)). The callback currently marks bulk primed, stops the timer, and calls `ReleaseSyncHold()` before setting the Manager readiness bit ([pkg/daemon/daemon_ha_sync.go:90](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/daemon/daemon_ha_sync.go:90)); releasing that hold can immediately restore preemption ([pkg/vrrp/manager.go:380](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/vrrp/manager.go:380)). Concrete trace: G1 queues its callback; G2 activates repair, advances the generation, arms an obligation, and sets not-ready; delayed G1 then releases the VRRP hold before its final ready CAS is rejected. The generation check must authorize the entire completion action—not merely `SetSyncReady`—including timer cancellation, `syncBulkPrimed`, hold release, and the Manager mirror. These effects need one serialized, generation-conditional completion transaction.

3. **HIGH — The capability-decision owner rule is not total under supported endpoint syntax.** The new fold assigns ownership using address order ([plan.md:4067](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:4067)), but the same plan already requires stable node-ID ordering because address parsing is not total ([plan.md:1355](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1355)). Current code returns “dial” on parse failure ([pkg/cluster/sync_conn.go:12](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:12)), and the regression test preserves that behavior ([pkg/cluster/sync_test.go:1337](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_test.go:1337)). Thus hostname or otherwise unparsable endpoints can make both nodes act as owner on crossed setup connections; competing installs then replace the same fabric slot ([pkg/cluster/sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244)), permitting repeated setup/cold-prime churn. Use the already-specified authenticated stable node-ID ordering and bind the decision owner to the setup token and dialer/acceptor role.

4. **MEDIUM — “Discard partial frame and re-establish framing” is not an implementable retained-stream rule as written.** `readSyncFrameRaw` consumes the header and payload with `io.ReadFull` ([pkg/cluster/sync_auth.go:289](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:289)). If a deadline fires after consuming only part of a header or authenticated frame, TCP provides no record boundary to which the receiver can jump; starting the ordinary receive loop at its next header read ([pkg/cluster/sync_conn_read.go:27](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:27)) interprets the suffix as a new frame. The plan also says a decision timeout closes and retries, then retains the older statement that a late confirmation never aborts the connection ([plan.md:4075](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:4075)). Require every incomplete or timed-out decision frame to close the transport and retry on a fresh stream, or specify a buffered authenticated-frame reader that retains the exact partial byte count until the complete frame is consumed.

**r55-1 disposition: PARTIALLY RESOLVED.** The declaration/decision/ACK exchange closes the original split-class schedule: neither endpoint dispatches before the common class is committed. It remains incomplete because repair activation is not tied to the capable fabric, the owner ordering is contradictory, and partial-frame recovery is underspecified.

**r55-2 disposition: PARTIALLY RESOLVED.** The generation CAS prevents an old callback from directly restoring the readiness boolean, but it does not yet guard the callback’s earlier timer and VRRP-hold side effects.

Bottom line: v9.9.54.4 closes the two narrow races targeted by the folds, but it does not yet converge as a composed session-sync protocol. A repair-capable secondary fabric can remain unusable because bulk selection prefers a legacy sibling, and a stale completion can still release the operational hold before its generation check fails. Those are concrete availability and HA-state risks; the capability transaction must be connection-token-bound and readiness completion must become a single generation-conditional operation before final sign-off.
