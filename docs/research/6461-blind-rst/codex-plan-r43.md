PLAN NO

Reviewed the pinned plan object `ede76685f` and code at `ba1a1e444`. The branch advanced during review, but later changes were excluded. Plan line numbers below refer to `git show ede76685f:docs/research/6461-blind-rst/plan.md`.

1. **BLOCKER — RESET_GEN/RESET_ACK has no loss-safe control channel or simultaneous-reset state machine.** The plan requires A to quiesce dialing, reject inbound connections, record both slots empty, and then send `RESET_ACK(g)` (`plan.md@ede76685f:1320-1325`). Under the existing transport, both-empty means `getActiveConn()` returns nil ([sync_conn.go:27](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:27)), and the sender waits for another connection ([sync_conn_write.go:268](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_write.go:268)). Simultaneous resets can therefore leave both nodes suppressing dial/accept with no ACK path.

   Sending RESET_GEN before closing is insufficient: loss of that connection cannot distinguish “GEN never arrived” from “ACK was lost.” A may continue its independent retry loops ([sync_conn.go:435](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:435)); B’s timeout backstop can reopen without proof that A drained. The next A install can observe an occupied sibling slot, making `wasDisconnected == false` and skipping cold-prime ([sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244)). Required change: carry reset exchange on a defined setup/control connection, never treat timeout alone as proof, retransmit exact incarnation-and-direction-scoped generations, and define two independent transitions for simultaneous resets. Capability-scoping correctly preserves the legacy quiet-peer behavior at [sync_test.go:4721](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_test.go:4721).

2. **BLOCKER — The single-counter and row-version contract remains internally contradictory.** The new rule selects one hold-cell counter and a canonical row version (`plan.md@ede76685f:1934-1969`), but operative text still:

   - Releases through the last `GroupHold` Arc finalizer (`:1795-1812`, `:2576-2580`).
   - Prescribes re-increment-direct plus clone-drop undo (`:1975-1978`) instead of the new count-preserving swap-back.
   - Immediately declares the version to be worker-local `install_epoch` (`:1980-1994`).

   In code, `install_epoch` is allocated independently per `SessionTable` ([session/mod.rs:737](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/mod.rs:737), [session/mod.rs:1384](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/mod.rs:1384)); canonical `SyncedSessionEntry` has no row version ([worker/mod.rs:375](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/worker/mod.rs:375)); promotion still updates the worker before canonical publication ([promote.rs:99](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/promote.rs:99), [promote.rs:131](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/promote.rs:131)). Literal implementations can therefore perform the wrong undo, release through both Arc and cell paths, or miss an intervening promotion. Sweep every Arc-finalizer/variant-specific undo and `install_epoch` alternative; define a per-entry CAS field, with values allocated from a never-reused coordinator sequence and refreshed across deletion/reinsertion.

3. **BLOCKER — Capacity compatibility and local-authority merge still have unsatisfiable valid states.** Equal peer capacities do not bound the merged table. B’s local flows publish into the same shared map ([poll_descriptor/mod.rs:2560](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2560), [poll_descriptor/mod.rs:2591](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2591)); import admission compares that map’s total length against B’s worker-derived cap ([session_import.rs:24](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:24), [session_import.rs:91](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:91)). Symmetric peers can advertise capacity C while B holds L local entries and A supplies a valid K-entry repair with `K + L > C`. The quiesced merge must then exceed capacity, reject the repair, or discard local state. Worker count can also change after negotiation because it is a binding-plan input ([planning.rs:93](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/server/helpers/planning.rs:93)).

   There is also no safe result when A’s missing E1 uses public tuple P and B has allocated P to different-key local E2. E1’s exact reserve fails against E2’s occupancy ([allocator.rs:1617](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1617), [allocator.rs:1682](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1682)); publishing both displaces one reverse alias ([shared_ops.rs:918](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:918)). Peer-wins disrupts E2; local-wins plus terminal ACK falsely reports E1 protected. Required change: generation-tagged capacity plus commit-time preflight of the complete peer/local union against every target map; local authority wins tuple conflicts, conflicting peer cohorts remain unpublished, and the repair remains degraded/not-ready without `JOURNAL-END` acknowledgement.

4. **BLOCKER — The normative wire schema still omits the repair protocol, and terminal retry is incomplete.** The lifecycle requires repair cutoff, repair-ID-bearing markers, declared count, and `JOURNAL-END` (`plan.md@ede76685f:1505-1526`, `:1622-1629`), but normative §5.8 still defines only INSTALL/Open and DELETE tails (`:3414-3474`). The adjacent inventories still say Part B adds identity tails only (`:3503-3508`, `:3542-3549`). Following the declared normative section preserves today’s `BulkEnd` behavior—reconcile, ACK, and readiness callback immediately ([sync_conn_read.go:241](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:241))—before the post-cut journal completes.

   Terminal ACK loss also lacks an idempotent recovery contract. Current code records and reports pending ACK age ([sync_bulk.go:169](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:169), [sync_bulk.go:200](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:200)); redrive is disconnect-triggered ([sync_conn.go:572](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:572)). If B applies J1 and its ACK is lost, A remains obligated, while B no longer has a current obligation against which to recognize J1. Define exact layouts/capability rules and retain a completed-repair receipt so duplicate J1 is non-mutating but re-ACKed, or require a timed fresh-generation repair.

5. **HIGH — Slot-token revocation misses same-fabric supersession and does not forbid reuse.** The per-fabric publication token is the right fence (`plan.md@ede76685f:1413-1429`), but “revoked at disconnect” is insufficient. Installing C2 closes and overwrites C1 under `s.mu` ([sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244)); C1’s later disconnect sees a stale connection and returns ([sync_conn.go:480](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:480)). A C1 handler paused before canonical publication can therefore retain T1 and publish after repair unless replacement revokes T1 before the slot swap. “Opaque” also does not establish never-reused identity. Required change: mint `(node/process incarnation, monotone counter)` tokens, revoke the displaced token atomically on replacement, and mirror revocation before repair commit. The legacy pending frame must move after token installation; today it is handled first ([sync_conn.go:119](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:119)).

6. **HIGH — Cleanup quarantine has no identity-scoped cancellation lifecycle.** Arm-before-read and the lock-free drain close the original lost wakeup (`plan.md@ede76685f:2235-2252`), but the queue is deduplicated only by family key and does not specify success disarm or same-/different-identity reimports. Equal-generation resends are accepted ([session_import.rs:53](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:53)); forward and reverse canonical entries publish before fan-out ([session_import.rs:115](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:115), [session_import.rs:187](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:187), [session_import.rs:215](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:215)). A same-E1 resend can republish, then the old zero-holder stage-2 cleanup removes that newly published family before its worker clones are fanned out. Workers subsequently receive entries with the canonical/BPF family absent. Make quarantine state `(family key, SessionIdentity, quarantine generation, row version)`; same-identity import atomically cancels it, different identity starts a new epoch, and successful stage 2 removes the flag and queue entry atomically.

7. **HIGH — Imported identity minting contradicts the wire-authoritative identity rule.** The plan says identities are inherited end to end, but then directs the HA coordinator to “mint once before fanout” (`plan.md@ede76685f:3007-3030`). Normative §5.8 says the sender’s pair is wire-carried and explicitly states that a receiver-minted identity cannot equal it (`:3416-3469`). If A sends E1 with IA and B mints IB, A’s conditional delete for IA is refused on B. B retains stale E1 and its NAT hold; a later E2 reusing that tuple can be rejected or omitted during repair. The import choke point is canonical publication followed by fan-out ([session_import.rs:115](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:115), [session_import.rs:215](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:215)). Replace “mint” with byte-for-byte adoption of the wire identity; only locally born forwards mint.

8. **MEDIUM — The quiesced rebuild has no actual bounded-stop contract.** Control mutations can serialize under the existing global server-state mutex ([handlers/mod.rs:122](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/server/handlers/mod.rs:122)), so the migration gate and repair quiesce are not inherently deadlocked. However, the plan calls the outage bounded (`plan.md@ede76685f:1576-1585`) while the current teardown performs an unbounded worker `join()` and removes XSK slots only afterward ([worker_manager.rs:146](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/worker_manager.rs:146)). A stuck worker can therefore leave the repair paused indefinitely. Pin serialization, prohibit holding migration WRITE across quiesce/join, set an explicit deadline, and leave XSK disabled plus readiness degraded on rebuild/rollback failure.

9. **LOW — The previous test list was added, but the new transition boundaries are untested and §9 retains stale discharge wording.** Section 9(d6) covers the round-42 list (`plan.md@ede76685f:3939-3956`), but earlier bullets still describe bulk/ACK discharge rather than exact `JOURNAL-END` (`:3838-3855`, `:3874-3877`). Add tests for reset-message loss and simultaneous reset, same-fabric token supersession, merged-capacity and NAT-conflict outcomes, terminal-marker/ACK retry, identity adoption, cleanup reimport/disarm, and quiesce deadline failure.

**Round-42 dispositions**

- **r42-1 — Partially resolved.** Legacy capability scoping is correct; negotiated reset transport, loss, and simultaneous-reset semantics remain open.
- **r42-2 — Partially resolved.** Exact publication tokens are correct in principle; supersession revocation and never-reused minting are missing.
- **r42-3 — Partially resolved.** The desired single counter and canonical row version are stated, but contradictory Arc, undo, carriage, and `install_epoch` rules remain operative.
- **r42-4 — Partially resolved.** Static asymmetry and the impossible root swap are addressed, but merged capacity, dynamic capacity, NAT conflict, and bounded-quiesce outcomes remain undefined.
- **r42-5 — Partially resolved.** `BulkEnd` is commit-only in the main lifecycle, but normative schema and idempotent terminal retry remain incomplete.
- **r42-6 — Partially resolved.** Arm-before-read closes the old wakeup race; identity-scoped cancellation and successful disarm remain missing.
- **r42-7 — Resolved for its original test list.** Section 9(d6) adds those cases; the findings above require additional tests.

Bottom line: Part A remains converged, but Part B is not ready for final sign-off. Concrete paths remain for repair never to establish a usable reset epoch, stale handlers to publish after repair, valid local-plus-peer state to exceed capacity or collide on a NAT tuple, ownership conversion to release or pin the wrong credit, completed repair to wedge on terminal ACK loss, and cleanup or identity divergence to retain/remove the wrong family. Those paths preserve availability impact, HA state desynchronization, and tuple-continuity failures. No files were edited.
