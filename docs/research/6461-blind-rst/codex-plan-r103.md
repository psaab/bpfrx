# Codex hostile plan-review — round 103 (v10.19.0)

Reviewer: Codex (fresh thread, job task-ms99jr2y-s66884, 2026-07-31). Verdict: PLAN NO (4B/2H/1M). r102-1/r102-2/r102-9 RESOLVED (the contract block landed; the named inertness copies fixed); the rest UNRESOLVED at v10.19.0 — the displaced-set carrier needed the inline-vs-accumulator split, the refusal outcomes were undefined and the capacity parenthetical code-wrong, OverdueSkipped could not carry an empty set when a placeholder was shadowed, §9 contradicted the SSOT, and parity/inertness stragglers remained.

## Verbatim review

PLAN NO 1. **BLOCKER — The displaced-identity set still has no realizable carrier.** The SSOT...

[2026-07-31T18:28:31.522Z] Assistant message
PLAN NO

1. **BLOCKER — The displaced-identity set still has no realizable carrier.** The SSOT fixes the materializer’s return as `(SessionLookup, MaterializeOutcome)` and names only `ResolvedFlowSessionDecision.materialization` (`docs/research/6461-blind-rst/plan.md:1677-1690`), then requires every transition/result to carry `displaced: BoundedIdentitySet` without defining an enum payload, tuple member, or resolved-result field (`plan.md:1718-1732`). Both resolved constructors therefore initialize only the outcome, not the set (`userspace-dp/src/afxdp/session_glue/mod.rs:1254-1261`, `:1330-1344`). The materializer has no `WorkerScratch` handle (`session_glue/mod.rs:1092-1143`), while the three identities are currently discarded by `ResolvedSessionLookup`, placeholder substitution, `_previous`, and promotion’s metadata/bool returns (`userspace-dp/src/afxdp/shared_ops.rs:522-560`, `:602-628`; `userspace-dp/src/session/install.rs:295-322`; `userspace-dp/src/afxdp/session_glue/promote.rs:71-140`). The contract needs an explicit result field/API through all producers.

2. **BLOCKER — Refusal outcomes are undefined, code-misclassified, and lack safe consumer semantics.** `MaterializeOutcome` has only four variants, but the same SSOT later invokes undefined `ValidatorRefused` and `UpsertRefused` outcomes (`plan.md:1677-1679`, `:1732-1743`). “`UpsertRefused` (capacity)” is false: synced upsert has no capacity check and refuses only a non-peer local predecessor (`userspace-dp/src/session/install.rs:295-315`); capacity refusal belongs to fresh install (`:123-125`). Today the materializer ignores that false result and returns S2 anyway (`userspace-dp/src/afxdp/session_glue/mod.rs:1098-1119`). Promotion can then no-op for a non-ForwardCandidate (`session_glue/promote.rs:86-90`), and the plan additionally skips it for every closing packet. K consequently remains installed while the dispatch carries S2. Only `OverdueSkipped` gates authority consumers (`plan.md:1696-1705`), so an upsert-refused LocalDelivery path can still tear down/release/publish using S2’s identity (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:698-714`, `:768-784`, `:824-840`; `userspace-dp/src/afxdp/session_glue/mod.rs:477-581`). Define the refusal type, returned decision/backing state, and teardown/cache/commit/MissingNeighbor behavior.

3. **BLOCKER — `OverdueSkipped` cannot carry an empty displaced set.** A differently keyed fabric placeholder P can coexist with canonical probation K; lookup deliberately shadows P with shared S2 (`userspace-dp/src/afxdp/shared_ops.rs:602-610`, `:614-626`; positive test `userspace-dp/src/afxdp/session_glue/tests.rs:704-759`). FIN/RST bypasses the cache (`userspace-dp/src/afxdp/flow_cache.rs:352-358`), so an overdue close reaches this lookup. If upsert is skipped and the set is empty as specified (`plan.md:1742-1743`), P’s existing cache aliases survive. A later pure ACK cache-hits before resolution (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:298-327`) and refreshes P (`poll_descriptor/flow_cache_hit.rs:295-317`), potentially indefinitely. K’s eventual reap carries only K’s final identity (`userspace-dp/src/session/entry.rs:337-343`) and cannot derive P. `OverdueSkipped` must retain at least every shadowed-placeholder identity; cache-insert suppression alone is insufficient. The otherwise enumerated five authority consumers and S2 buffer/replay path are complete.

4. **HIGH — Per-result and per-batch storage are conflated.** `BoundedIdentitySet` is described both as the returned per-transition set and as one `WorkerScratch` accumulator sized for 3×64 identities (`plan.md:1718-1725`, `:1744-1755`). Current resolution receives no scratch (`userspace-dp/src/afxdp/session_glue/mod.rs:1124-1143`), and `WorkerScratch` has no such field (`userspace-dp/src/afxdp/worker/scratch.rs:33-62`). More importantly, current-binding invalidation needs only the current descriptor’s delta while the full batch remains available for sibling fan-out; otherwise descriptor two re-invalidates descriptor one’s newly inserted S2 alias. The ownership/order is feasible—descriptor loop owns current (`poll_descriptor/mod.rs:110-131`), and `poll_binding` owns left/current/right after it returns (`userspace-dp/src/afxdp/worker/lifecycle.rs:53-55`, `:209-225`)—but the contract needs separate inline result plus preallocated batch accumulator, or explicit ranges/cursors and clear/drain semantics.

5. **HIGH — Section 9 still contradicts the SSOT and misses required refusal tests.** It categorically asserts `EMPTY for a refusal or an OverdueSkipped` (`plan.md:2305-2309`), contradicting both the mandatory new-S2/placeholder families and the SSOT’s own validator-refused predecessor case (`plan.md:1718-1735`). It has no validator-refused-with-predecessor, upsert-refused→promotion/no-promotion, or overdue-plus-shadowed-placeholder test. Separately, its transient-purge test says a SYN-bearing second close FreshPrimary-installs unconditionally (`plan.md:2339-2347`), while the later test correctly requires a warm next hop or lapsed seed (`:2440-2445`). Cold close #1 installs `MissingNeighborSeed` (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4787-4795`), and close #2 hits that local seed first (`userspace-dp/src/afxdp/shared_ops.rs:594-613`).

6. **HIGH — “FULL MASTER PARITY” remains overclaimed.** Sections 5.6 and 5.8 still say the whole transient-purge path is fully identical and that no packet-level rule survives (`plan.md:1533-1540`, `:1643-1667`). Elsewhere the plan correctly documents the deliberate pre-purge difference and longer-lived reverse companion (`plan.md:761-781`, `:2083-2090`, `:2332-2338`). Lookup marks and propagates before the purge decision (`userspace-dp/src/session/lookup.rs:105-128`, `:198-218`; `userspace-dp/src/afxdp/session_glue/mod.rs:1157-1196`), while purge removes only the matched key (`session_glue/promote.rs:181-207`). Only the purge decision and subsequent dispatch have master parity.

7. **MEDIUM — Equivalent inertness stragglers remain.** Option A still says every refused demote idles normally, §5.5 says it remains on its pre-attack trajectory, and §10.6.1 says refused closes never reap early (`plan.md:478-485`, `:1212-1214`, `:2631-2634`). A refused translated peer-synced close can still satisfy the independent purge predicate and be deleted immediately (`userspace-dp/src/afxdp/session_glue/promote.rs:48-59`, `:181-207`).

Round-102 dispositions: r102-1 **RESOLVED**; r102-2 **RESOLVED**; r102-3 **UNRESOLVED**; r102-4 **UNRESOLVED**; r102-5 **UNRESOLVED**; r102-6 **UNRESOLVED**; r102-7 **UNRESOLVED**; r102-8 **UNRESOLVED**; r102-9 **RESOLVED at the named copies, with equivalent stragglers remaining**.