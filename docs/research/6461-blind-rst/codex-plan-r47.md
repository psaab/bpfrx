PLAN NO

1. **BLOCKER — Repair discharge remains internally contradictory.** The correct rule appears at `plan.md@e6bec91c1:1447-1452`, `1929-1936`, and `3867-3872`: applying `JOURNAL_END` clears only the receiver’s inbound obligation/readiness; matching full-triple `JOURNAL_ACK` clears only the sender’s outbound obligation/cold-prime latch. However, conflicting clauses remain at `1441-1446`, `1682-1688`, `1941-1942`, `3854-3860`, `4303-4306`, `4325-4329`, and `4403-4405`, variously allowing `JOURNAL_END` or a “clean replacement bulk” to discharge an unspecified or sender-side obligation.

   Concrete trace: A takes its bulk cutoff before E2, then records E2 in the post-cut journal. B processes `BulkEnd`; today that immediately reconciles, sends a bare ACK, and invokes readiness at [sync_conn_read.go:205](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:205), releasing the VRRP hold at [daemon_ha_sync.go:90](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/daemon/daemon_ha_sync.go:90). A likewise treats writing `BulkEnd` as success at [sync_bulk.go:183](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:183) and clears cold-prime at [sync_conn.go:194](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:194). If the stream fails before E2 and the exact terminal exchange, B can become ready without E2, producing HA state desynchronization and possible translation re-resolution.

   Required change: remove every generic discharge statement. Call `JOURNAL_END` the terminal marker, not the terminal ACK; reserve `JOURNAL_ACK` for sender-side discharge. Negotiated `BulkEnd` and bare `BulkAck(u64)` must never clear either obligation or readiness.

2. **HIGH — Reset-lane process-incarnation transition is not linearized.** The fold requires revalidating the “current” incarnation at `plan.md@e6bec91c1:1366-1383`, but never defines how current changes during overlapping authenticated setups. Accepts run in independent goroutines at [sync_conn.go:388](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:388), while setup tracking records only connection membership at [sync_admission.go:66](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_admission.go:66). Authentication replay ordering is per connection at [sync_auth.go:161](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:161), and the process nonce is random rather than ordered ([heartbeat.go:624](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/heartbeat.go:624)).

   Concrete interleaving: an authenticated n1 setup stalls; the peer restarts and n2 completes; then n1 resumes. Rejecting every mismatch can leave the genuine restart permanently non-current, while last-completer-wins can regress to n1 and apply its stale reset, closing repaired connections or invalidating n2 publications. Both can leave readiness degraded indefinitely.

   Required change: maintain `{current, pending, retired}` peer incarnations and every slot/pre-slot lane under one lock. A different incarnation must first drain/revoke the current incarnation’s handlers, then atomically retire current and promote pending. Recheck `(node_id, incarnation, lane_token)` immediately before mutation, never readopt retired incarnations, and keep a reset-generation high-water per incarnation.

3. **MEDIUM — The normative reset handshake does not fully specify authenticated identity binding.** Section 5.8’s consolidated capability tuple at `plan.md@e6bec91c1:3836-3852` omits the stable node ID and current process incarnation that §5.2 says the hello binds. Current `AUTH_PROOF` authenticates only a nonce at [sync_auth.go:217](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:217), and it is a distinct required frame at [sync_auth.go:387](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:387). The stated RESET-only allowlist—HELLO/capability plus RESET frames, rejecting everything else—therefore either excludes the required proof or leaves its admission implicit.

   Required change: list the identity fields and complete handshake sequence in §5.8. Authenticate the canonical hello transcript, including node ID, process incarnation, capabilities, and nonces, or send it only after the authenticated frame wrapper is established. Add overlapping old/new-incarnation and transcript-binding tests.

Disposition r46-1: **partially resolved** — the correct direction-specific rule was added, but contradictory discharge clauses remain.

Disposition r46-2: **resolved** — `plan.md@e6bec91c1:1569-1589` now holds one family-transaction permit across forward/reverse/DNAT writes, drains or CAS-invalidates it before replacement, and makes rollback token-conditional. This closes the T1-write/T2-replace/T1-rollback interleaving governed today by [session_store.go:274](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/session_store.go:274) and [manager_ha.go:1771](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/userspace/manager_ha.go:1771).

Disposition r46-3: **partially resolved** — arbitrary pre-slot data frames and unkeyed reset access are prohibited, but incarnation transition and authenticated transcript binding remain incomplete.

Disposition r46-4: **resolved** — §9(d8), `plan.md@e6bec91c1:4429-4442`, adds the requested v9.9.37–39 boundary tests.

Bottom line: Part A remains converged, and the family-transaction fold closes the prior partial-family overwrite trace. Part B is not ready for sign-off because contradictory terminal-discharge clauses still permit readiness before post-cut state is applied, leaving a concrete HA desynchronization and translated-tuple continuity path. The reset lane also needs a defined process-incarnation handoff to avoid a normal-restart readiness deadlock.
