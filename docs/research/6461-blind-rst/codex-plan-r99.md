# Codex hostile plan-review — round 99 (v10.15.1)

Reviewer: Codex (fresh thread, job task-ms96f72v-019zd8, 2026-07-31). Verdict: PLAN NO (5B/1H/1L). Headline: the FULL MASTER PARITY claim was behaviorally false — the local lookup runs before the purge decision, so master's close marks the matched entry and propagates the shortened lifetime before the purge, and the demote gate's refusal on anchorless peer-synced entries is a deliberate Part-A delta with a replica-lifetime cost (now stated as the absorbing-state residual applied to this path). Also: OverdueSkipped needed its ResolvedFlowSessionDecision transport + ExistingResolved composition + the accounting-explicit consumer rule; the alias set was still incomplete; the conceded chain's rationale needed the constructor-side correction. The ReplacedSyncedLocal close skip confirmed sound again (provided provenance is inspected before take_synced_local, lookup.rs:407-418).

## Verbatim review

PLAN NO 1. **BLOCKER — the purge-path retraction is not complete in normative text.** Section...

[2026-07-31T17:05:17.702Z] Assistant message
PLAN NO

1. **BLOCKER — the purge-path retraction is not complete in normative text.** Section 3 still says a peer-synced close never purges and becomes `ExistingResolved` (`plan.md:430`), while §§5.8/9/11 still require the retracted warm-path cache suppression (`plan.md:1570-1576,2157-2186,2223-2243,2484-2491`). Master’s predicate is flag-agnostic and the resolver purges before continuing with the retained lookup (`userspace-dp/src/afxdp/session_glue/promote.rs:48-59`; `userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196`). A warm purged pure ACK remains cache-eligible and is inserted normally (`userspace-dp/src/afxdp/flow_cache.rs:352-394`; `userspace-dp/src/afxdp/poll_descriptor/mod.rs:3900-3959`), so the stale requirements would reintroduce a plan delta and make “the next packet clean-misses and installs” false.

2. **BLOCKER — Part A still changes pre-purge companion state, so “FULL MASTER PARITY” is behaviorally false.** Local lookup runs before the purge decision (`userspace-dp/src/afxdp/shared_ops.rs:594-635`; `userspace-dp/src/afxdp/session_glue/mod.rs:1157-1196`). Master marks the matched close and propagates the shortened lifetime to its companion before returning (`userspace-dp/src/session/lookup.rs:105-128,198-218`; `userspace-dp/src/session/mod.rs:1232-1277`). The proposed no-anchor refusal suppresses both mutations. Purge subsequently deletes only the matched local key and forward shared aliases, not the local reverse companion (`userspace-dp/src/afxdp/session_glue/promote.rs:181-207`; `userspace-dp/src/afxdp/shared_ops.rs:960-1013`), while HA import synthesizes and fans that reverse entry to every worker (`userspace-dp/src/afxdp/ha/session_import.rs:104-115,187-223`). Thus master leaves the current-worker reverse replica closing for 2/30 seconds; the plan leaves it on its ordinary peer-synced trajectory. This lengthens stale/conflicted-P1 companion exposure for the #6599/#6600 class and must either be designed away or documented and tested as a real Part-A delta.

3. **BLOCKER — `OverdueSkipped` is not mechanically transported through its complete consumer set.** The current materializer returns only `SessionLookup` and discards the upsert result; `ResolvedFlowSessionDecision` has no materialization outcome (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1121`; `userspace-dp/src/afxdp/shared_ops.rs:563-578`). Promotion runs before that resolved result is returned and can rewrite, refresh, publish, and replicate K (`userspace-dp/src/afxdp/session_glue/mod.rs:1235-1261`; `userspace-dp/src/afxdp/session_glue/promote.rs:71-139`). The plan names only teardown, anchor, and cache consumers (`plan.md:1582-1587`), omitting explicit promotion and clear/refresh plumbing.

   More critically, an S2 `MissingNeighbor` decision can otherwise enter the common seed block, derive/allocate, install, and publish state (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4662-4829`; install replacement at `userspace-dp/src/session/install.rs:139`). `OverdueSkipped` must explicitly compose to the live-backed `ExistingResolved`/buffer-only arm.

   Accounting should remain an explicit allowed consumer: `account_packet` preserves #2501 semantics and keys on the packet query tuple, normally the surviving placeholder rather than canonical K (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:3494-3503`; `userspace-dp/src/session/mod.rs:1177-1211`; placeholder override at `userspace-dp/src/afxdp/shared_ops.rs:602-628`). Delivery, buffering, replay, reinjection, and ordinary telemetry may likewise consume S2; authority mutations may not. Section 9 currently tests none of this outcome propagation or the three teardown sites (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:698,768,824`).

4. **BLOCKER — probation-reap invalidation is not alias-complete.** The cache stores the packet’s query tuple and invalidation is exact-key only (`userspace-dp/src/afxdp/flow_cache.rs:578-580,1105-1120`). Yet lookup accepts reverse-translated aliases for reverse entries and forward-wire aliases for forward entries (`userspace-dp/src/session/lookup.rs:62-100,253-315`); reply matching also accepts reverse-wire or reverse-canonical tuples (`userspace-dp/src/session/key.rs:19-26`; `userspace-dp/src/session/lookup.rs:222-250`). The plan specifies only canonical plus forward-wire keys (`plan.md:1369-1377,2258-2265`).

   It also permits S1→S2 adoption with changed NAT/orientation (`plan.md:1280-1305`). Upsert reindexes only the session table, while `ExpiredSession` carries only the final S2 identity (`userspace-dp/src/session/install.rs:295-433`; `userspace-dp/src/session/entry.rs:337-343`). Consequently, a previously cached S1 alias cannot be derived at S2’s reap and survives indefinitely. Old aliases must be invalidated during every adoption, retained cumulatively, or removed via a safe cache scan/clear.

5. **HIGH — the conceded close-driven #6599 chain is real only with a warm/post-seed-expiry qualification, and §7 still denies that closes participate.** Warm close #1 purges and forwards retained; FIN/RST cannot enter the ACK cache (`userspace-dp/src/afxdp/flow_cache.rs:352-394`). SYN-bearing close #2 then clean-misses, passes #4400, seeds closing/reset, and emits Open (`userspace-dp/src/afxdp/poll_descriptor/session_admission.rs:83-87`; `userspace-dp/src/session/install.rs:179-180,225-260`). That is master-identical.

   With a cold next hop, however, close #1 installs and publishes `MissingNeighborSeed`; close #2 hits that local seed rather than clean-missing (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4787-4829`; lookup order at `userspace-dp/src/afxdp/shared_ops.rs:594-613`). The documented chain therefore requires warm resolution or prior seed removal. Sections 7/10.6.2/11 still assert that #6599 has no closing flags and cannot be seen by the demote gate (`plan.md:1849-1876,2390-2402,2528-2531`). The scope conclusion remains defensible because the ultimate defect is identity-less synchronization, but “no FIN/RST involved” is no longer a valid rationale.

6. **LOW — deadline selection still uses overflowing additions.** The plan computes D using raw `K.last_seen_ns + K.expires_after_ns` and `now_ns + candidate` before applying the saturating subtraction (`plan.md:1300-1310`), while the wheel derives its target with `saturating_add` (`userspace-dp/src/session/expire.rs:50-57`). Both candidate deadlines must use `saturating_add`. Once D is computed that way, the non-overdue encoding `last_seen_ns=now_ns`, `expires_after_ns=D.saturating_sub(now_ns)` re-derives D exactly; the `D == now_ns` skip boundary is otherwise consistent.

The `ReplacedSyncedLocal` close skip remains self-contained and sound provided provenance is inspected before the destructive `take_synced_local` call (`userspace-dp/src/session/lookup.rs:407-418`).

Round-98 dispositions: r98-1 RESOLVED; r98-2 RESOLVED; r98-3 UNRESOLVED textually; r98-4 UNRESOLVED; r98-5 UNRESOLVED; r98-6 UNRESOLVED textually; r98-7 UNRESOLVED textually/behaviorally; r98-8 UNRESOLVED; r98-9 UNRESOLVED.