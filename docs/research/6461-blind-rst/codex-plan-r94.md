# Codex hostile plan-review — round 94 (v10.10.0)

Reviewer: Codex (fresh thread, job task-ms8zw4fz-ykbkkc, 2026-07-31). Verdict: PLAN NO (5B/2H/1M) — the round that dismantled the install-free shape and produced the RWoLB retraction to master-verbatim. Dispositions: r93-1/r93-2/r93-3/r93-4 UNRESOLVED (all folded by the retraction); r93-5/r93-6 RESOLVED.

## Verbatim review

PLAN NO 1. **BLOCKER — reservation-failed closes can now be dropped.** Master purges the back...

[2026-07-31T14:04:12.907Z] Assistant message
PLAN NO

1. **BLOCKER — reservation-failed closes can now be dropped.** Master purges the backing but retains `hit.lookup` for the current dispatch and returns a HIT (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196,1254-1261`). V10.10 instead sends purged backing through cold/miss re-entry. Because reservation failure explicitly allows FIN/RST to purge, a bare close reaches #4400 and drops (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:1642-1650`; `userspace-dp/src/afxdp/poll_descriptor/session_admission.rs:82-87`). This violates “the FIN/RST packet itself is always delivered.” The supposedly defense-in-depth bare-close oracle is reachable; this case needs a distinct purge-but-forward/buffer-stored-decision outcome.

2. **BLOCKER — INSTALL-FREE creates unowned NAT state.** Fresh SNAT/NAT64 derivation allocates P2 before forwarding (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:2142-2156,2256-2271`; `userspace-dp/src/nat/source.rs:1548-1571`; `userspace-dp/src/nat/allocator.rs:1034-1066`). Normally, successful installation anchors that allocation (`poll_descriptor/mod.rs:2449-2458`); no-install/failure paths roll it back and drop or suppress buffering (`:2372-2393,2459-2491,2626-2652`). If INSTALL-FREE retains P2, a one-packet flow has no session reap owner and leaks its `live_by_flow` slot/port indefinitely; rollback recreates the retracted freed-tuple-transmit race. MissingNeighbor is conclusive: buffering stores only the decision/key (`userspace-dp/src/afxdp/types/mod.rs:77-90`), while timeout/replay has no NAT release (`userspace-dp/src/afxdp/neighbor_dispatch.rs:212-232,270-405`). “The next packet installs” cannot repair the no-next-packet case.

3. **BLOCKER — flow-cache suppression makes #6599 newly more reachable.** Master’s second packet is not universally a clean miss. Pure ACK/PSH-ACK traffic is cache-eligible (`userspace-dp/src/tcp_flags.rs:83-90`; `userspace-dp/src/afxdp/flow_cache.rs:354-394`). After purge, master returns `install_failed=false` (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196,1254-1261`) and can cache the sessionless decision (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:3856-3960`). The next eligible packet hits that cache before session resolution (`:298-327`); absent backing merely makes touch/accounting no-op (`userspace-dp/src/session/mod.rs:1118-1133,1177-1196`). V10.10’s mandatory no-cache rule instead makes packet two deterministically miss, install, and emit Open, accelerating the #6599 overwrite for this subclass. “No FIN/RST, so the lifetime validator cannot engage” is narrowly true, but the proposed non-close RWoLB dispatch rule does engage and is not behaviorally neutral.

4. **BLOCKER — historical reservation success is not a live ownership fence.** HA import fans the same entry to every worker (`userspace-dp/src/afxdp/ha/session_import.rs:215-223`); cloned allocators share one token (`userspace-dp/src/nat/allocator.rs:707-744`). Same-flow reservations return success idempotently without refcounting (`:1664-1674`). Any sibling replica can then reap silently (`userspace-dp/src/session/expire.rs:342-344`) yet unconditionally release that sole token (`userspace-dp/src/afxdp/worker/loop_body/mod.rs:1490-1505`; `userspace-dp/src/nat/allocator.rs:1318-1332,1366-1376`). Surviving entries retain `success=true`; after P1 is reassigned, their FIN/RST packets are newly retained and repeatedly forwarded using conflicted P1. Thus the proposal worsens the documented #6522/#6600 interaction. A refcount/holder fence or equivalent is required; a cached bit or racy packet-time check is insufficient.

5. **BLOCKER — overdue-K resurrects the retracted skip-upsert split-brain.** `materialize_shared_session_hit` returns S2 independently of whether its upsert changed the table (`userspace-dp/src/afxdp/session_glue/mod.rs:1098-1119`). Skipping the overdue upsert leaves K/S1 directly lookupable (`userspace-dp/src/session/lookup.rs:48-68`; `userspace-dp/src/afxdp/shared_ops.rs:602-635`) while the current packet forwards on S2—the exact failure v10.10 itself explains when retracting skip-the-upsert. Worse, a committed non-close subsequently clears and refreshes the retained probation K, potentially preserving S1 for an ordinary timeout rather than merely until GC. The overdue test checks only wheel reap, not decision agreement or committed admission. Adopt S2 in place without changing K’s old wheel hint, or remove K locally; retaining untouched K is unsafe.

6. **HIGH — reservation-outcome recording is not a defined implementation shape.** Both reservation wrappers return `()` and swallow success/failure (`userspace-dp/src/nat/source.rs:868-954`; `userspace-dp/src/nat64.rs:1252-1298`). Worker upsert installs first, calls both wrappers, and has no result setter (`userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:64-120`). Shared-only hits carry only decision/metadata/origin (`userspace-dp/src/afxdp/shared_ops.rs:523-559,594-635`), so the publish-before-command window has no outcome to consult. A boolean is also insufficient: DNAT-only/static rows may need no reservation, while shared-only rows are unknown. At minimum, `Unknown/Failed`, `NotRequired`, and currently `Held` semantics and their propagation through lookup/materialization/demotion must be normative.

7. **HIGH — MissingNeighbor still has two decision consumers.** The arm copies outer `decision` to `pending_decision`, derives SNAT only into the latter (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4662-4668,4735-4761`), and buffers/replays that value (`:5057-5067`; `userspace-dp/src/afxdp/neighbor_dispatch.rs:270-348`). The trailing reinjection independently consumes outer `decision` (`poll_descriptor/mod.rs:5126-5138`; `userspace-dp/src/afxdp/tx/dispatch/slow_path.rs:186-211`). Without an explicit overwrite/threaded outcome, buffering can use P2 while reinjection uses the pre-SNAT decision. The claimed sole-decision rule is therefore not mechanically closed.

8. **MEDIUM — v10.10.0 remains internally contradictory.** At requested commit `4b4e3f4`, active installful language remains in site 9, §5.2, §5.8, §7, §9, §10.6.2, and §11 (`plan.md@4b4e3f4:428,689-725,1600-1601,1817-1840,2175-2187,2328-2337,2419-2424`), contradicting INSTALL-FREE at `:1550-1554,1580-1590,2122-2130`. “State-mutation-free” is also false because allocation, pending-neighbor storage, telemetry, queues, and the initial purge mutate state. Rollback-then-forward and RWoLB alive-probation appear only as retractions, but skip-the-upsert has reappeared normatively in the overdue branch.

The deadline encoding itself is correct for `D > now_ns`: `now_ns + (D - now_ns) = D`, and the wheel uses the saturating sum (`userspace-dp/src/session/expire.rs:50-57`; `userspace-dp/src/session/wheel.rs:39-50`). The cited lifetime-shortening, Close-emission, admission, and install/upsert sites otherwise match the document (`session/lookup.rs:105-128,150-156`; `session/expire.rs:342-344`; `session/install.rs:113-180,295-400`).

r93-1 **UNRESOLVED** — INSTALL-FREE leaks P2 and changes cache/#6599 behavior.  
r93-2 **UNRESOLVED** — reservation success is stale and the failed fallback drops closes.  
r93-3 **UNRESOLVED** — overdue skip preserves K/S1 and can refresh it.  
r93-4 **UNRESOLVED** — reservation failure makes bare-close re-entry reachable.  
r93-5 **RESOLVED** — prompt-producer scope and SharedPromote caveat are consistent.  
r93-6 **RESOLVED** — #4400 citations and branch-base fabric provenance are corrected.