PLAN NO

1. **BLOCKER — overflow repair does not force a full resync when the other fabric survives.** The plan drops the parked window, disconnects, and relies on reconnect bulk repair ([plan.md:1120](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1120)). In the actual dual-fabric transport, however, `handleDisconnect` removes only the failed connection ([sync_conn.go:480](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:480)). A surviving fabric avoids the full-disconnect path at line 498, and survivor re-bulk runs only while `outboundBulkAcked` is false ([sync_conn.go:572](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:572)); that flag is deliberately sticky forever after any successful historical bulk ([sync.go:479](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync.go:479)). Reconnection of the failed fabric also does not cold-prime because `wasDisconnected` requires both slots empty ([sync_conn.go:248](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:248)), while routine fabric changes resume incrementally ([sync_conn.go:208](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:208)). Concrete trace: B consumes A’s E1 INSTALL or DELETE into its parked fab0 buffer, overflows and discards it, then closes only fab0 while fab1 remains usable. A immediately continues on fab1, so its outbound queue need not overflow and arm `syncBackfillNeeded` ([sync_conn_write.go:36](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_write.go:36)); no authoritative bulk follows. If the repair latch remains set, B is permanently non-transfer-ready; if it is cleared on reconnect, failover occurs with E1 or its reservation missing, permitting a mid-flow re-resolution/kill. The one sentence about an authenticated “resync request” at plan line 1128 defines neither a wire message nor its acknowledgement/fabric semantics. **Required change:** make overflow create a logical-peer full-resync obligation that forces `BulkSync` over the surviving active connection—or close both fabrics—and clear that obligation only after the replacement full-bulk ACK. It must override/reset the sticky cold-prime gates.

2. **BLOCKER — the single coordinator reservation conflicts with the plan’s per-entry holder/refcount contract.** The new fold reserves once at the coordinator, then says worker fan-out and later materialization “NEVER create or mutate” ownership ([plan.md:1226](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1226)). Elsewhere the same plan requires every installed replica to retain and increment the allocation refcount ([plan.md:915](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:915)), every per-worker token’s `Drop` to decrement it ([plan.md:1650](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1650)), and replicas/materializations/synthesized entries to retain at commit ([plan.md:1710](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1710)). The architecture really does fan the entry to every worker ([session_import.rs:215](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:215)), can materialize another worker-local entry later ([session_glue/mod.rs:1092](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/mod.rs:1092)), and reaps each worker entry independently with an allocation release ([loop_body/mod.rs:1481](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/worker/loop_body/mod.rs:1481)). Under the scalar refcount contract, coordinator reserve establishes one holder; W0 and W1 add none; W0’s first reap releases the sole holder while W1 still forwards E1, after which E2 can claim P through the normal bitmap path ([allocator.rs:1007](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1007)). Making worker releases no-ops instead leaves ownership without a defined final-release and escrow lifecycle. **Required change:** define a coordinator-owned group-hold object whose `Arc` clones are distributed to every entry and release the allocator exactly when the last clone and canonical owner disappear, or explicitly pre-retain every fan-out recipient and dynamically retain each later materialization. Reconcile the contradictory “NoChange only” and per-entry-retain clauses and test early replica reap.

3. **MEDIUM — the §9 test contract still contains obsolete and incomplete expectations.** The v9.9.18 test still requires overflow to drop oldest, set backfill, and keep the connection up ([plan.md:2879](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:2879)), directly contradicting v9.9.19’s disconnect/full-resync rule at lines 2894–2899. The rename test also says only that a renamed pool’s retired allocator persists ([plan.md:2913](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:2913)), although `SourceNatPoolAllocatorKey` includes `pool_name` ([source.rs:327](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/source.rs:327)) and current reuse is exact-key-only ([source.rs:726](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/source.rs:726)). Persistence of A alone does not stop independently keyed B from issuing E1’s tuple; the test must assert compatible cross-key migration, old-slot retargeting into B, and release-accounting continuity. Remove the superseded overflow test and strengthen the rename assertion.

**Round-34 dispositions**

- **r34-1 — partially resolved.** Lossy-bulk reconciliation/ACK suppression is correct, but the repair is not guaranteed across the dual-fabric connection lifecycle.
- **r34-2 — partially resolved.** Coordinator serialization and reserve-before-publication close the W0-pending/W1-published rollback trace, but the resulting holder distribution and release contract remains unsafe.
- **r34-3 — resolved.** `slot.with_current()` closes the load-before-gate race: read-permit acquisition plus generation revalidation places release on exactly one side of the cut. Revalidation must remain lock-free/atomic to preserve the stated lock order.
- **r34-4 — resolved.** Requiring both the observed-INSTALL high-water and empty parked/repair state closes the apply-success-before-drain takeover window. Partition-era over-advance remains conservatively fail-closed rather than admitting an uncovered session.
- **r34-5 — partially resolved.** The bare-`rollback_flow` test was correctly replaced by receipt-specific undo at plan lines 2850–2855, but the remaining overflow and rename test clauses are inconsistent with the normative mechanisms.

Bottom line: Part A remains converged, and the receipt, slot-linearization, and takeover-after-drain folds close their specific round-34 traces. Final sign-off is still blocked because overflow can silently lose an HA delta without triggering the promised full resync on a dual-fabric peer, and the coordinator-only reservation has no coherent relationship to the per-replica release/refcount invariant. Those defects respectively leave the standby permanently unready or incomplete and can free/reissue an SNAT tuple while a replica still forwards the original flow.
