# Codex hostile plan-review — round 100 (v10.16.0)

Reviewer: Codex (fresh thread, job task-ms97csej-11i9cu, 2026-07-31). Verdict: PLAN NO (4B/1H/1M). r99-6 RESOLVED (saturating arithmetic correct); the rest UNRESOLVED at v10.16.0. The deadline arithmetic was explicitly verified correct.

## Verbatim review

PLAN NO 1. BLOCKER — The purge-cache retraction remains normatively contradictory. Section 9 ...

[2026-07-31T17:29:32.250Z] Assistant message
PLAN NO

1. BLOCKER — The purge-cache retraction remains normatively contradictory. Section 9 still requires the warm purge dispatch to suppress cache insertion and calls this a “hardening delta” (`docs/research/6461-blind-rst/plan.md:2209-2216`), despite §5.8 requiring normal caching (`plan.md:1604-1610`). Master makes pure ACKs cache-eligible and inserts them normally (`userspace-dp/src/afxdp/flow_cache.rs:352-394`, `userspace-dp/src/afxdp/poll_descriptor/mod.rs:3900-3959`); the next ACK then cache-hits before session resolution (`poll_descriptor/mod.rs:298-327`) rather than clean-missing and installing. Consequently the packet-two/install and close→ACK timeout-delta assertions at `plan.md:2224-2237` are also stale. Similar unconditional “next packet clean-misses” claims remain at `plan.md:1513-1529`, `:1611-1613`, and `:2279-2284`.

2. BLOCKER — `OverdueSkipped` is asserted but still lacks a normative producer/transport design. The header promises a `ResolvedFlowSessionDecision` field (`plan.md:8-10`), while §5.8 merely names the outcome (`plan.md:1619-1626`) without defining the field, initialization, or materializer result. Current `materialize_shared_session_hit` returns only `SessionLookup` and discards the upsert result (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1121`); the resolved type has no outcome field (`userspace-dp/src/afxdp/shared_ops.rs:563-578`). Promotion also runs before that result is constructed (`session_glue/mod.rs:1235-1261`) and can update, publish, and replicate (`session_glue/promote.rs:99-139`). Section 5.8’s reason that promotion cannot engage because no local entry exists is false: overdue K explicitly remains installed (`plan.md:1340-1352`). The ordinary probation promotion gate may ultimately suppress it, but that—not nonexistence—must be the specified guard.

3. BLOCKER — The claimed five-consumer composition is not carried through the poller. Only `install_failed` is hoisted from `resolved` (`poll_descriptor/mod.rs:509`), and the result is reduced to `resolved.decision` at `poll_descriptor/mod.rs:883`. A new field therefore cannot reach flow-cache insertion (`:3900-3959`), the MissingNeighbor arm (`:4034`), or later anchor/probation commit hooks without another outer carrier. Section 5.8’s generic outcome list (`plan.md:1635-1675`) never specifies `OverdueSkipped + MissingNeighbor => ExistingResolved`; absent that branch, S2 proceeds into allocation, seed install, and publication (`poll_descriptor/mod.rs:4662-4829`), potentially replacing K via `userspace-dp/src/session/install.rs:139`.

   All three destructive LocalDelivery teardown sites also need explicit guards (`poll_descriptor/mod.rs:698-714`, `:768-784`, `:824-840`); teardown otherwise deletes using S2 identity (`session_glue/mod.rs:477-581`). Section 9 tests none of this—it checks only no-upsert/no-refresh/GC (`plan.md:2125-2137`). Its “K fields bit-identical” assertion also conflicts with the newly allowed accounting: `account_packet` mutates K’s counters, ToS, and observed flags (`poll_descriptor/mod.rs:3494-3503`, `userspace-dp/src/session/mod.rs:1177-1210`).

4. BLOCKER — The alias inventory is complete, but its lifecycle and execution mechanics are not. The named shapes do cover every accepted query: canonical/reverse-translated (`userspace-dp/src/session/lookup.rs:62-100`), forward-wire (`:253-293`), and both reply tuples (`lookup.rs:222-250`, `userspace-dp/src/session/key.rs:19-26`). However, adoption occurs where only `SessionTable` is available (`session_glue/mod.rs:1092-1121`), while caches are per binding and exact-key/physical-ingress invalidation requires every binding (`flow_cache.rs:203-218`, `:1105-1120`). The existing reap path deliberately iterates all bindings (`worker/loop_body/mod.rs:1467-1520`); descriptor processing owns only the current binding and processes a whole RX batch (`poll_descriptor/mod.rs:110-131`). Deferring invalidation permits descriptor 2 to consume S1 after descriptor 1 adopts S2. The plan needs explicit bounded alias transport, immediate current-binding invalidation before cache insertion/the next descriptor, and sibling-binding fan-out.

   Initial probation construction is also uncovered. Lookup can substitute a shared entry for a differently keyed live fabric-wire placeholder (`shared_ops.rs:594-628`; fixture `session_glue/tests.rs:704-759`), while materialization installs only the shared canonical key (`session_glue/mod.rs:1092-1115`). A following ACK can therefore hit the pre-existing placeholder cache before probation resolution (`poll_descriptor/mod.rs:298-327`) until the eventual probation reap. Similarly, a changed-identity shared hit can replace a non-probation peer-synced predecessor because the upsert guard protects only locally owned rows (`session/install.rs:310-322`), yet `plan.md:1378` leaves that replacement unchanged. Prior-set invalidation must cover initial probation construction and every successful site-2c replacement, not only probation-to-probation adoption. Section 5.8 also retains the incomplete alias summary at `plan.md:1627-1629`.

5. HIGH — The pre-purge companion delta is accurately traced once but contradicted elsewhere and untested. The §5.2 account is code-correct (`plan.md:745-765`): lookup precedes purge (`shared_ops.rs:594-635`, `session_glue/mod.rs:1157-1196`), master marks and propagates before purge (`session/lookup.rs:105-128`, `:198-218`; `session/mod.rs:1232-1277`), and purge removes the matched forward but not its local reverse companion (`session_glue/promote.rs:181-207`). Thus the proposal does not make #6599’s Open overwrite newly reachable or accelerate purge dispatch, but it does lengthen reverse-replica exposure in the #6599/#6600 broken state.

   Nevertheless §5.2 also says “NO delta/byte-parity” (`plan.md:699-706`), §7 says the plan changes “NOTHING” for #6600 (`:1939-1944`), and §10.6.2/§11 say the gate cannot engage (`:2456-2458`, `:2585-2590`). It does engage on the first close’s pre-purge lookup; it merely cannot prevent the later constructor-side Open. The §9 parity test (`plan.md:2154-2164`) also never asserts the documented surviving companion’s closing/timeout state.

6. MEDIUM — Refused-close inertness is over-scoped. “The entry ages exactly as if the refused packet had never arrived” (`plan.md:1543-1551`) is false for an anchorless transient-purge hit: validation may refuse its mark, but the independent flag-agnostic purge still deletes it (`session_glue/mod.rs:1178-1196`, `session_glue/promote.rs:181-207`). Scope this invariant to mark/refresh/requeue effects, excluding master-parity purge and dispatch.

The deadline arithmetic is correct: both candidates use `saturating_add`, encoding uses `D.saturating_sub(now_ns)`, and the wheel reconstructs the deadline with `saturating_add` (`plan.md:1320-1333`, `userspace-dp/src/session/expire.rs:50-57`).

Round-99 dispositions: r99-1 UNRESOLVED; r99-2 UNRESOLVED; r99-3 UNRESOLVED; r99-4 UNRESOLVED; r99-5 UNRESOLVED; r99-6 RESOLVED.