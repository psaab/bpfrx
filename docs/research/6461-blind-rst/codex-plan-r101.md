# Codex hostile plan-review — round 101 (v10.17.0)

Reviewer: Codex (fresh thread, job task-ms9835jf-wg0xep, 2026-07-31). Verdict: PLAN NO (5B/1H/1M). All r100 findings marked UNRESOLVED at v10.17.0 — the round's theme: the v10.17.0 folds stated outcomes as prose; r101 demands and receives the full mechanical specification (producer contract, normative transport splice, displaced-identity set, realizable invalidation timing).

## Verbatim review

PLAN NO 1. **BLOCKER — `OverdueSkipped` still lacks a normative producer and transport contra...

[2026-07-31T17:45:30.888Z] Assistant message
PLAN NO

1. **BLOCKER — `OverdueSkipped` still lacks a normative producer and transport contract.** The header promises an OUT result and a `ResolvedFlowSessionDecision.materialization` field, but §5.8 never defines either type, updates the materializer signature, or specifies how the result survives construction of the resolved decision (`plan.md:3-8`, `plan.md:1575-1606`). Today `materialize_shared_session_hit` returns only `SessionLookup` and discards the upsert result (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1121`), while `ResolvedFlowSessionDecision` has no materialization field (`userspace-dp/src/afxdp/shared_ops.rs:563-578`). This matters before final resolution: promotion is attempted immediately after materialization and can mutate ownership, publish, and replicate (`userspace-dp/src/afxdp/session_glue/mod.rs:1235-1261`, `userspace-dp/src/afxdp/session_glue/promote.rs:99-139`). The still-live rationale that promotion cannot engage because no local entry exists is also false: overdue K deliberately remains installed (`plan.md:1337-1348`, `plan.md:1637-1647`). The design must define a typed materializer result available before promotion, independently of `install_failed`, and then carry it into the resolved result.

2. **BLOCKER — The downstream `OverdueSkipped` guards remain test prose rather than an executable design.** The poller currently hoists only `install_failed` (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:509`) and reduces resolution to `resolved.decision` at `:883`. No normative splice defines a carrier reaching the cache insertion (`:3900-3959`), MissingNeighbor/seed selection (`:4034`, `:4662-4829`), or commit hooks. Likewise, §5.8’s outcome list never maps `OverdueSkipped + MissingNeighbor` to live-backed `ExistingResolved`; that rule exists only in §9 (`plan.md:1653-1693`, `plan.md:2180-2190`). The three terminal teardown paths require explicit guards because they can derive companions, remove state, release NAT, and publish deletes under the materialized S2 identity (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:698-714`, `:768-784`, `:824-840`; `userspace-dp/src/afxdp/session_glue/mod.rs:477-581`). Once correctly carried, buffering/replay itself is sound: the buffer stores S2’s decision and retry consumes that stored decision without another SessionTable lookup (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:5057-5068`, `userspace-dp/src/afxdp/neighbor_dispatch.rs:272-405`). Accounting may intentionally advance K (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:3494-3503`, `userspace-dp/src/session/mod.rs:1177-1210`), but the five authority-mutating consumers still need normative gates.

3. **BLOCKER — Alias invalidation cannot identify all displaced predecessors.** The enumerated alias shapes are now substantially complete: direct and reverse-translated lookups (`userspace-dp/src/session/lookup.rs:62-100`), reply-match tuples (`userspace-dp/src/session/lookup.rs:222-250`, `userspace-dp/src/session/key.rs:19-26`), and forward-wire aliases (`userspace-dp/src/session/lookup.rs:253-315`). The problem is obtaining the prior identities. `ResolvedSessionLookup` carries no shadowed-local identity (`userspace-dp/src/afxdp/shared_ops.rs:522-560`); fabric-placeholder substitution discards the placeholder’s key and decision (`userspace-dp/src/afxdp/shared_ops.rs:602-628`); and canonical upsert discards `_previous`, returning only a boolean (`userspace-dp/src/session/install.rs:295-322`). One transition can simultaneously shadow a differently keyed query-side placeholder and replace a canonical non-probation predecessor. Invalidating singular “PRIOR identity” is therefore insufficient. The transition result must return the bounded set of displaced identities/keys—at least the new S2 alias family plus any removed canonical predecessor’s old family—and return no invalidation transition for refusal or `OverdueSkipped`.

4. **BLOCKER — The proposed current/sibling invalidation timing is not mechanically realizable as written.** Materialization has no cache handle (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1143`); the current binding and descriptor batch are owned by the poller (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:110-131`); sibling bindings are accessible only from the outer lifecycle (`userspace-dp/src/afxdp/worker/lifecycle.rs:53-56`, `:209-225`). The cited reap mechanism is an all-binding loop, including the current binding (`userspace-dp/src/afxdp/worker/loop_body/mod.rs:1481-1521`). Reusing it after the batch could both miss a same-batch descriptor and evict a newly inserted correct S2 cache entry when old and new aliases overlap (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:3900-3959`). The design needs: immediate current-binding invalidation after resolution and before every early exit/cache insertion/next descriptor; accumulation of every transition in the batch; then fan-out to sibling bindings only. §9 also lacks initial-placeholder, simultaneous-two-predecessor, same-batch, sibling, multi-transition, and “new current S2 survives” cases (`plan.md:2357-2371`).

5. **BLOCKER — A retracted cold-path timeout delta remains and is code-inconsistent.** Warm-path cache parity is now accurately described: a purged cache-eligible ACK can be inserted, and a subsequent packet cache-hits before session resolution (`userspace-dp/src/afxdp/flow_cache.rs:352-394`, `userspace-dp/src/afxdp/poll_descriptor/mod.rs:298-327`, `:3900-3959`). However, §9 still claims a cold close→ACK path yields the plan’s non-closing roughly-300-second seed versus master’s 2/30-second closing seed (`plan.md:2281-2284`). Both versions run the same seed transaction using the current packet’s raw TCP flags (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4787-4795`), and installation initializes the seed’s closing/reset state from those flags (`userspace-dp/src/session/install.rs:179-180`). The following ACK hits that local seed first (`userspace-dp/src/afxdp/shared_ops.rs:594-613`); it cannot transform it into a non-closing seed. This is a stale close-aware-purge artifact and contradicts the claimed master-split parity.

6. **HIGH — Purge-dispatch parity is valid, but the plan still contradicts its acknowledged companion-lifetime delta.** Lookup and close marking occur before the purge decision (`userspace-dp/src/afxdp/shared_ops.rs:594-635`, `userspace-dp/src/afxdp/session_glue/mod.rs:1157-1196`). Master marks the matched entry and propagates closing state to its companion (`userspace-dp/src/session/lookup.rs:105-128`, `:198-218`; `userspace-dp/src/session/mod.rs:1232-1277`), then purge removes the matched forward side without removing that reverse companion (`userspace-dp/src/afxdp/session_glue/promote.rs:181-207`). The proposal therefore leaves the companion on its ordinary peer-synced trajectory—correctly acknowledged at `plan.md:742-762`—but §10.6.2 still says the gate does not worsen the #6599/#6600 class (`plan.md:2504-2508`), and §11 says the demote gate cannot engage (`plan.md:2633-2638`). It does engage and refuse the first close’s pre-purge mark; it simply cannot prevent the later constructor-side Open. Thus Open overwrite is not newly reachable, but conflicted-P1 companion exposure is lengthened. Also retain the warm-next-hop/lapsed-seed qualification wherever the close #1→close #2 clean-miss chain is stated: with a live cold seed, close #2 hits it instead (`userspace-dp/src/afxdp/shared_ops.rs:594-613`).

7. **MEDIUM — Refused-close inertness remains overbroad.** The header scopes the claim correctly, but normative §5.7 still says the entry ages exactly as though the refused packet never arrived, and §9 demands a bit-identical pre-refusal expiry trajectory (`plan.md:1561-1569`, `plan.md:2231-2233`). An anchorless close can be refused by the demote gate and then independently encounter master’s flag-agnostic transient purge (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196`, `userspace-dp/src/afxdp/session_glue/promote.rs:48-59`, `:181-207`). The invariant must be limited to no gate-caused mark, refresh, or wheel requeue, excluding independent purge/teardown effects.

Round-100 dispositions: r100-1 **UNRESOLVED**; r100-2 **UNRESOLVED**; r100-3 **UNRESOLVED**; r100-4 **UNRESOLVED**; r100-5 **UNRESOLVED**; r100-6 **UNRESOLVED**.