PLAN NO

1. **BLOCKER — Ordinary peer-INSTALL reservation rejection has no convergence/readiness fence.** The plan rejects E1 when B’s locally authoritative E2 already owns tuple P (`plan.md@8194bbaa9:1042-1049,1667-1674`). The allocator enforces that collision at [allocator.rs:1664](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1664) and [allocator.rs:1682](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1682). Outside an active repair, however, no obligation is armed. A advances its sweep cutoff after local enqueue—not receiver acceptance—at [sync_conn_sweep.go:137](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_sweep.go:137) and [sync_conn_sweep.go:185](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_sweep.go:185). Consequently, E1 is not retried after E2 releases P; A can fail while B remains ready but lacks E1, causing HA state loss and possible tuple reissue. Every reservation rejection must arm a durable inbound repair/not-ready condition or send a sequenced rejection request, with retry after the conflicting holder drops.

2. **BLOCKER — Capacity preflight races local admission.** The plan explicitly computes `K + L ≤ C` before quiescence (`plan.md@8194bbaa9:1657-1666`). Packet workers remain able to create and publish E2 at [poll_descriptor/mod.rs:2560](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2560) and [poll_descriptor/mod.rs:2591](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2591). Thus preflight can observe `K+L=C`, followed by E2 admission and a quiesced union of `C+1`. The rebuild must then exceed capacity, omit state, or fail after disabling the dataplane. Freeze admissions first, await quiescence, then repeat the complete capacity and NAT-conflict preflight inside that frozen interval through commit or rollback.

3. **BLOCKER — Normative §5.8 still cannot encode causal repair completion.** The schema at `plan.md@8194bbaa9:3582-3595` omits `RESYNC_REQUEST`, the terminal ACK carrying exact `(repair_id, journal_epoch, terminal_seqno)`, the capacity generation, and the repair/identity-enforcement/lease-input capability bits. Current `BulkEnd` immediately reconciles and ACKs at [sync_conn_read.go:205](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:205), while `BulkAck` contains only a `u64` and accepts any epoch not below pending at [sync_conn_read.go:249](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:249). A literal implementation therefore cannot prevent O1’s delayed ACK from discharging O2’s sender obligation. The stale §9 assertions at `plan.md:3997-3999,4028-4031` still prescribe bulk-ACK discharge, and the Part-B inventories at `:3657-3661,3696-3703` still mention only identity tails. Consolidate every repair frame, namespace, capability, terminal ACK, receipt key/retention rule, and discharge predicate in §5.8, then remove the older clauses.

4. **HIGH — Simultaneous reset recovery lacks deterministic physical-connection ownership.** The plan says B dials the next setup and models simultaneous resets as independent directional barriers (`plan.md@8194bbaa9:1327-1351`). Today only one address-ordered endpoint owns each fabric dialer at [sync_conn.go:12](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:12) and [sync_conn.go:319](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:319); crossed reset setups also compete for one slot, with each installation closing its predecessor at [sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244). The two endpoints can retain opposite halves of different connections, repeatedly failing cold-prime while both obligations remain armed. Specify one tie-broken setup carrying both directional reset triples—or a separate control channel—and idempotent equal-generation re-ACK behavior.

5. **HIGH — Slot-token revocation lacks a Go↔helper linearization point.** v9.9.33 correctly requires nonreuse, pre-swap T1 revocation, and pending-frame handling after T2 installation (`plan.md@8194bbaa9:1453-1479`), but says revocation is “mirrored into the repair commit.” Ordinary supersession may have no repair commit. A T1 handler can pass the Go check, pause, then publish after T2’s slot swap while Rust still accepts T1. Today helper publication is a separate call at [sync_conn_gen.go:435](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_gen.go:435), and the [SessionStore interface](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/session_store.go:50) carries no token or registry epoch. Require an acknowledged helper-side `replace(slot,T1,T2,token_epoch)` transaction before T2 dispatch; every canonical mutation and repair commit must validate that epoch.

6. **HIGH — The rebuild deadline does not cover quiesce/handoff or contain a timed-out worker.** The plan adds only a worker-join deadline (`plan.md@8194bbaa9:1675-1680`), while its escrow protocol requires quiesce acknowledgement and complete token handoff before joining (`:905-918`). Either preceding phase can still block indefinitely. Current shutdown joins before removing XSK slots at [worker_manager.rs:141](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/worker_manager.rs:141), [worker_manager.rs:149](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/worker_manager.rs:149), and [worker_manager.rs:154](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/worker_manager.rs:154), before forwarding is discarded at [coordinator/mod.rs:680](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/mod.rs:680). Define one deadline over quiesce, handoff, and join. On expiry, disable XSK, abort the rebuild, retain the old worker/allocator/registry/escrow generation until actual exit, and reject every late publication.

7. **MEDIUM — Quarantine supersession and lock placement remain incomplete.** Same-identity cancellation is now specified, but different-identity E2 installs while Q(E1) continues; only successful stage-2 removal retires the flag, and retry entries remain deduplicated solely by family key (`plan.md@8194bbaa9:2359-2391`). An identity mismatch can therefore leave obsolete durable retry work. Moreover, current publication spans separate forward, reverse, and fan-out operations at [session_import.rs:115](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:115), [session_import.rs:187](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:187), and [session_import.rs:215](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:215); [publish_shared_session](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:897) releases `synced` before locking companion maps. Put quarantine state in the canonical `synced` lock domain and define different-identity publication or identity mismatch as terminal supersession of the old quarantine generation.

**Round-43 dispositions**

- **r43-1 — partially resolved:** retransmission, timeout-not-proof, durable readiness degradation, and directional generations landed; setup ownership and simultaneous-reset convergence did not.
- **r43-2 — resolved:** Arc last-reference detection, cell-zero release, swap-back undo, and canonical row version are now consistent.
- **r43-3 — partially resolved:** generation-tagged capacity and local-authority precedence landed; preflight timing and ordinary-conflict convergence remain open.
- **r43-4 — partially resolved:** several frames and the completed receipt were added, but the normative terminal ACK, namespaces, capabilities, and old-clause sweep remain incomplete.
- **r43-5 — partially resolved:** token nonreuse, supersession revocation, and pending-frame order are specified; cross-runtime replacement is not linearized.
- **r43-6 — partially resolved:** the same-E1 race is addressed; lock placement and different-identity quarantine retirement remain open.
- **r43-7 — resolved:** imports now adopt the wire `SessionIdentity` byte-for-byte; only locally born forwards mint.
- **r43-8 — partially resolved:** a worker-join deadline and WRITE ordering were added, but the complete shutdown phase and timeout containment are unbounded.
- **r43-9 — partially resolved:** §9(d7) was added, but older ACK-discharge assertions remain contradictory and several transition tests are missing.

Bottom line: Part A remains converged, and v9.9.33 resolves the internal hold-counter and imported-identity defects. Part B is not yet signable: an ordinary NAT reservation rejection can leave the standby ready without a live cohort, capacity preflight races new local admissions, and the normative repair schema cannot enforce terminal causality. Those traces still permit HA state loss, tuple reissue or mid-flow translation change, and prolonged dataplane unavailability.
