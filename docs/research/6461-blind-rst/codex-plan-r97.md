# Codex hostile plan-review — round 97 (v10.13.0)

Reviewer: Codex (fresh thread, job task-ms93o3kt-x5xp48, 2026-07-31). Verdict: PLAN NO (3B/3H/1M/1L). Headline: the unconditional cache suppression was history-blind (the purge predicate has no prior-close history) and reintroduced the #6599 acceleration for ACK-first flows — the suppression is now conditioned on the new close-retained marker on the shared row. Verified positively: direct remove_entry cleans key/indexes/slab/count; stale wheel hints drop safely; the master cold path really runs the seed transaction; MissingNeighborSeed emits no Open. Dispositions: r96-3/r96-7 RESOLVED; the rest UNRESOLVED at v10.13.0.

## Verbatim review

PLAN NO 1. BLOCKER — The purge-dispatch cache suppression is history-blind and reintroduces t...

[2026-07-31T15:54:29.823Z] Assistant message
PLAN NO

1. BLOCKER — The purge-dispatch cache suppression is history-blind and reintroduces the exact #6599 acceleration previously retracted. For an ACK-first/PSH-ACK or UDP flow, master purges, retains P1 with `install_failed=false`, caches it, and subsequent packets bypass resolution (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196,1254-1261`; `userspace-dp/src/afxdp/flow_cache.rs:354-394`; `userspace-dp/src/afxdp/poll_descriptor/mod.rs:298-327,3900-3959`). v10.13 suppresses that cache, so the next packet clean-misses, installs/publishes/replicates, and emits Open (`userspace-dp/src/afxdp/poll_descriptor/session_admission.rs:53-60,83-87`; `poll_descriptor/mod.rs:2449-2458,2560-2615`; `userspace-dp/src/session/install.rs:225-260`), enabling the latest-generation overwrite (`pkg/cluster/sync_conn_write.go:53-59`; `pkg/cluster/sync_conn_gen.go:435-453`; `pkg/dataplane/session_store.go:257-297`).

   A purge bit cannot distinguish ACK-first from close→ACK: the purge predicate has no prior-close history (`userspace-dp/src/afxdp/session_glue/promote.rs:48-59`), and `SyncedSessionEntry` carries only install-time flags (`userspace-dp/src/afxdp/worker/mod.rs:375-401`). Thus the proposal newly affects flows containing no FIN/RST, contradicting `plan.md:735-738,1909-1914,2205-2213,2486-2498`.

   The plumbing itself is feasible: add a separate field to `ResolvedFlowSessionDecision`, copy it into a per-descriptor boolean beside `flow_cache_install_failed`, and gate the sole insert (`userspace-dp/src/afxdp/shared_ops.rs:563-578`; `poll_descriptor/mod.rs:364-370,509,3900`). It must not overload `install_failed`. The missing historical state, not signal transport, is the blocker.

2. BLOCKER — Overdue-K removal destroys probation before admission and has no valid committed-non-close transition. The rule is not close-only. Resolution/materialization runs before the input-filter gate (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1119`; `userspace-dp/src/afxdp/poll_descriptor/mod.rs:411-431,592-638`), so a filtered ACK can remove K even though §9 says an uncommitted non-close leaves probation untouched (`plan.md:2217-2238`). If the packet commits, K is already absent, leaving nothing for the promised clear/refresh/promote/anchor commit hook (`plan.md:1348-1362`).

   The next non-close finds the shared row (`userspace-dp/src/afxdp/shared_ops.rs:594-635`), whose wire-free state contains no probation/deadline, and ordinary synced upsert creates an established/full-timeout entry before admission (`userspace-dp/src/session/install.rs:345-400`). Therefore “next packet re-materializes with a fresh probation clock” is false for non-closes. The design needs a durable probation tombstone or an explicit same-dispatch/post-commit recovery transaction.

3. BLOCKER — An overdue-remove ACK can create an indefinitely sessionless flow-cache entry. The current resolved-hit result is `created=false, install_failed=false`; the poller copies that value and admits the decision to cache (`userspace-dp/src/afxdp/session_glue/mod.rs:1254-1261`; `userspace-dp/src/afxdp/poll_descriptor/mod.rs:487-510,3900-3959`). The next packet hits cache before session resolution, while session touch/accounting silently no-op because K is gone (`poll_descriptor/mod.rs:298-327`; `userspace-dp/src/afxdp/poll_descriptor/flow_cache_hit.rs:295-317`; `userspace-dp/src/session/mod.rs:1118-1133,1177-1196`). Cache validation checks configuration/FIB/RG epochs but not local-session existence and has no idle eviction (`userspace-dp/src/afxdp/flow_cache.rs:962-1039`). This falsifies `plan.md:1329-1331,2155-2157`. The overdue result needs explicit cache-forbidden provenance reaching the common insert point.

4. HIGH — Close→ACK has neither packet-count parity nor a livelock-free installation guarantee. With a warm hop, master purges on the close and installs on the following ACK. The plan retains the close, purges and suppresses cache on that ACK, and requires a third same-direction permitted packet. A reverse packet can instead lack the forward NAT match and take normal reverse-policy denial—the code explicitly relies on creating state before reverse traffic arrives (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:2449-2458,4658-4661`). Re-import between packets can repeatedly republish the peer row, producing purge→suppress→forward on every dispatch (`userspace-dp/src/afxdp/ha/session_import.rs:39-60,115-121,215-223`); a later genuine miss can also fail capacity admission and drop (`poll_descriptor/mod.rs:2355-2393`). The “next packet installs” and packet-count assertions at `plan.md:1513-1515,1623-1625,2205-2213,2285-2287` are false.

5. HIGH — Conflicted-row retention is reachable through #6522, not only #6600. Imports fan the same flow to all workers; allocator clones share one unrefcounted token, and any sibling expiry releases it (`userspace-dp/src/afxdp/ha/session_import.rs:215-223`; `userspace-dp/src/nat/allocator.rs:742-745,1318-1332,1664-1674`; `userspace-dp/src/afxdp/worker/loop_body/mod.rs:1490-1505`). A local flow can then claim P1 while another worker/shared row survives. Reactive materialization does not retry reservation (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1119`); the plan retains a close where master purges, and RG activation can later promote, republish, replicate, and emit Open for conflicted P1 (`session_glue/promote.rs:48-59,99-139`; `userspace-dp/src/session/mod.rs:1480-1530`). This contradicts “only after #6600” and “neither worsens #6522” (`plan.md:1955,2463-2478`).

6. MEDIUM — The cold master-split mechanics are correctly identified, but its oracle and impact are not. The first cold purged packet installs and publishes `MissingNeighborSeed`; therefore the next packet hits that local seed rather than clean-missing (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4780-4795,4811-4888`; `userspace-dp/src/afxdp/shared_ops.rs:594-635`). This contradicts `plan.md:2325-2345`. Also, master’s close seeds 2-second RST/30-second FIN state, whereas close→ACK under the plan seeds established state around 300 seconds (`userspace-dp/src/session/install.rs:157-180`; `userspace-dp/src/session/mod.rs:2151-2181`). The seed consumes global table/NAT and publishes shared/BPF/DNAT state despite emitting no Open, so `plan.md:1515-1519,2287-2289` understates a 10×/150× resource-retention delta.

7. HIGH — Live normative contradictions remain. The document calls the purged dispatch “byte-identical” and the close-aware gate its “ONLY departure,” while also adding cache suppression (`plan.md:703,741-742,1503-1515`). It explains that this exact no-cache shape accelerates #6599 at `plan.md:734-738`, then reinstates it. The overdue branch removes K, but immediately retains “decision agreement after the in-place adopt,” says installation cannot be skipped, and elsewhere promises every re-materialization preserves the deadline (`plan.md:1313-1335,1360-1362,1821-1845`). These are incompatible implementation and test contracts.

8. LOW — Deadline encoding is sound for `D > now_ns`: storing `last_seen_ns=now_ns` and `expires_after_ns=D-now_ns` makes the wheel’s saturating sum re-derive D (`userspace-dp/src/session/expire.rs:50-57`). At `D == now_ns`, however, strict expiry has not occurred; GC expires only when `now_ns-last_seen_ns > expires_after_ns` (`expire.rs:166-168`). Call this “due or overdue,” or specify equality removal as deliberate shortening.

Verified positively: direct `remove_entry` cleans the primary key, NAT/owner indexes, slab slot, and presence count; stale wheel hints safely drop as gone/stale (`userspace-dp/src/session/mod.rs:1746-1822`; `userspace-dp/src/session/expire.rs:145-168`). S2 is owned, so no dangling Rust borrow remains. The master cold path really does run the seed transaction, and `MissingNeighborSeed` emits no Open (`userspace-dp/src/session/entry.rs:272-274`; `userspace-dp/src/session/install.rs:225-260`).

Round-96 dispositions — r96-1 UNRESOLVED; r96-2 UNRESOLVED; r96-3 RESOLVED for the documented RG crossover but incomplete for #6522; r96-4 UNRESOLVED; r96-5 UNRESOLVED; r96-6 UNRESOLVED; r96-7 RESOLVED.