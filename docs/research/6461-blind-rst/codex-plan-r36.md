PLAN NO

1. **BLOCKER — the full-resync obligation is driven on the wrong node and is not causally sequenced.** Overflow is detected by receiver B, but the plan says B raises a local obligation with “no new wire message” and drives existing `BulkSync` ([plan.md:1126](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1126)). `BulkSync` exports the invoking node’s locally owned table ([sync_bulk.go:50](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:50), [sync_bulk.go:95](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:95)), so B sends B→A; it cannot reconstruct A’s INSTALL/DELETE that B discarded. Existing sender-side drives are reconnect cold-prime ([sync_conn.go:130](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:130)), `forceResync` sweep ([sync_conn_sweep.go:118](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_sweep.go:118)), and survivor redrive ([sync_conn.go:572](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:572)); no receiver-to-sender resync request exists among the message types ([sync.go:38](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync.go:38)). Thus B can remain missing A’s E1 state while either staying permanently not-ready or becoming ready with incomplete state.

   Discharge is also insufficiently sequenced. Bulk epochs are allocated at [sync_bulk.go:65](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:65), but the pending epoch is installed only near `BulkEnd` ([sync_bulk.go:169](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:169)); the ACK handler accepts any epoch not lower than the pending value ([sync_conn_read.go:257](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:257)). A delayed pre-obligation ACK or an ACK for O1 after a second overflow O2 can therefore discharge a boolean obligation unless it carries a generation-specific target. Failed redrive also needs an explicit re-kick: while `bulkRedriveInFlight` is set, another disconnect loses the CAS trigger, and completion currently only clears the flag ([sync_conn.go:594](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:594)).

   **Required repair:** the proposed close-both-fabrics primary path is sound only as a logical all-fabric reset: atomically close both local slots and suppress reconnection long enough that the sender observes a true both-empty epoch. Otherwise one fabric can reconnect before the other EOF is processed, leaving `wasDisconnected == false` ([sync_conn.go:248](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:248)). After that reset, normal cold-prime correctly makes A send A→B. Keep separate inbound-repair and outbound-bulk obligations: B clears inbound repair only after receiving a clean post-reset `BulkEnd`; A clears outbound repair only on the exact ACK for a bulk started after the applicable obligation generation. An optional fast path is also coherent, but it requires a rolling-gated `RESYNC_REQUEST`: B requests, A arms and repeatedly drives its outbound full bulk over the survivor, and request generation N is discharged only by the exact replacement bulk associated with N. Unnegotiated peers must use close-both.

2. **HIGH — the group-hold model still conflicts with remaining direct allocator-retain instructions.** The new normative clause correctly gives an imported flow one Arc group-hold and makes worker reap drop only its clone ([plan.md:1273](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1273)). Earlier operative text still requires materialization/upsert consumers to register a holder directly in the allocator ([plan.md:858](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:858)) and describes per-worker token drops as allocator-refcount decrements ([plan.md:1725](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1725)). Current code reflects that direct model: worker imports reserve at [upsert_synced.rs:80](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:80), while every expired forward directly releases at [loop_body/mod.rs:1491](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/worker/loop_body/mod.rs:1491).

   If coordinator-only reservation is implemented while the current direct reap remains, the first imported replica to reap can release the sole allocator reservation despite surviving Arc clones. If direct per-worker retains remain but reap is changed to clone-only, those direct counts never drain, causing pool-capacity leakage. Locally born flows still need their existing direct-holder model, so one undifferentiated drop path cannot satisfy both. **Required change:** define the token representation explicitly—such as `DirectHold` for locally born allocations and `GroupHold(Arc<…>)` for imported flows—or move every flow to the group model. Enumerate fan-out, reverse synthesis, materialization, replacement, escrow, reap, and panic-drop behavior for each variant, and remove or scope the stale direct-retain clauses.

3. **MEDIUM — the repaired §9 test text does not test resync causality.** The updated overflow test now repeats the normative “drive over survivor and clear on ACK” contract ([plan.md:2960](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:2960)), but it would pass a wrong-direction B→A bulk. It also omits delayed pre-obligation ACK, O2-during-O1, failed-redrive re-kick, and the reconnect-between-two-EOF interleaving. Add deterministic tests asserting that receiver B’s loss causes sender A’s full-table iteration and that only a clean bulk started after the latest obligation generation discharges readiness.

**Round-35 dispositions**

- **r35-1 — not resolved.** The fold recognizes the dual-fabric problem, but its no-message receiver-local driver cannot initiate the required sender-to-receiver bulk.
- **r35-2 — partially resolved.** The Arc group-hold closes the abstract W0-reap/W1-live trace, but the direct-holder and group-holder lifecycle models remain simultaneously operative.
- **r35-3 — resolved.** The specific obsolete drop-oldest test and incomplete compatible-rename assertion were corrected. The new resync-causality omissions are separate consequences of finding 1.
- **SMR lock-order fold — resolved.** Reserve→unlock→canonical publish is implementable against the separate shared-map mutexes at [session_manager.rs:12](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/session_manager.rs:12); removing per-worker allocator reservation for group-held imports also eliminates that re-entry point. Group-hold finalizers must follow the declared canonical→allocator order and must not reacquire canonical locks from inside allocator cleanup.

Bottom line: the retarget ordering, lock-order, rename, and original early-replica-reap concepts have materially converged, but final confirmation is premature. The receiver still cannot make the peer send the replacement snapshot over a surviving fabric, and the obligation is not protected against stale ACKs, repeated overflow, or redrive failure. Close-both is a valid primary repair when implemented as a true all-fabric reset with a reconnect barrier; a negotiated request is a valid optimization when direction and generation are explicit. The group-hold also needs one unambiguous typed lifecycle before it can guarantee both imported-flow continuity and locally born allocation release.
