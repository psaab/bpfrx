# Codex hostile plan-review — round 105 (v10.21.0)

Reviewer: Codex (fresh thread, job task-ms9ax3r9-b35u75, 2026-07-31). Verdict: PLAN NO (2B/4M/1L). The two-field split had not propagated through its own SSOT (producer/fields/carriage still said MaterializeOutcome); the second MissingNeighbor outcome list omitted UpsertRefused; the capacity-4 claim contradicted the exclusive-producer claim (Codex supplied the 3-family uniqueness proof this round). r104-3/r104-5/r104-7 RESOLVED (with retired-terminology stragglers).

## Verbatim review

PLAN NO 1. **BLOCKER — The two-field split was not propagated through its own SSOT.** Section...

[2026-07-31T19:06:23.887Z] Assistant message
PLAN NO

1. **BLOCKER — The two-field split was not propagated through its own SSOT.** Section 5.8 defines `validation` and `transition`, but the producer still returns the retired `(SessionLookup, MaterializeOutcome)`, the resolved result still carries `materialization: MaterializeOutcome`, and the poller still reads `resolved.materialization` (`docs/research/6461-blind-rst/plan.md:1693-1705`, `:1728-1743`). `MaterializeOutcome` is now undefined. Current code has a single-return materializer, promotion immediately afterward, and two result constructors, so the concrete return/carriage path remains unspecified (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1121`, `:1235-1261`, `:1330-1344`; `userspace-dp/src/afxdp/shared_ops.rs:563-578`).

   The legal Phase-1 product is also unstated: `(None,None)` for non-materializing paths; `(None,T)` for a non-close site-2c materialization; and `(Refused,T)` for a closing site-2c materialization, where `T ∈ {Installed, AdoptedPreservingDeadline, UpsertRefused, OverdueSkipped}`. Every site-2c close refuses (`plan.md:1302-1310`), while site 2b reports acceptance/refusal through `reverse_installed`/`install_failed`, so `Accepted` has no producer (`session_glue/mod.rs:1264-1284`, `:1330-1344`). The unconstrained product nevertheless represents `Accepted,*` and `Refused,None`, with no defined consumer behavior. Section 9 confirms the fold miss by retaining singular-outcome language and `ValidatorRefused` (`plan.md:2400-2404`, `:2418`, `:2427-2436`).

2. **BLOCKER — The actual MissingNeighbor outcome list still omits `UpsertRefused`.** The SSOT says both `OverdueSkipped` and `UpsertRefused` compose to live-backed, buffer-only `ExistingResolved` (`plan.md:1764-1773`), but the later normative outcome list names only `OverdueSkipped` (`:1891-1903`), and the propagation test likewise covers only overdue (`:2427-2437`). On `UpsertRefused + MissingNeighbor`, promotion no-ops because the disposition is not `ForwardCandidate` (`userspace-dp/src/afxdp/session_glue/promote.rs:86-90`). Following the later list therefore permits the common seed transaction (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4662-4829`), whose fresh installer removes the surviving predecessor (`userspace-dp/src/session/install.rs:139`). This is the original r104-2 corruption trace.

3. **HIGH — The displaced-set capacity and producer proof contradict themselves.** The SSOT gives the inline set capacity four and says P, K, S2, and the promotion preimage can all occur, then says their producers are mutually exclusive; the batch accumulator remains reserved for only `3 × 64` families (`plan.md:1777-1785`, `:1823-1826`; `userspace-dp/src/afxdp/mod.rs:278-281`). Lookup substitution, materialization/upsert, and promotion are sequential in one dispatch (`userspace-dp/src/afxdp/shared_ops.rs:602-628`; `userspace-dp/src/afxdp/session_glue/mod.rs:1157-1253`; `userspace-dp/src/session/install.rs:295-322`).

   Code suggests a valid three-unique-family proof: after successful upsert, the promotion preimage deduplicates with installed S2; after refused upsert, the preimage is K. Promotion changes resolution/owner/fabric state but not `(key, NAT, orientation)` (`session_glue/mod.rs:1200-1234`; `session_glue/promote.rs:92-107`; `session/mod.rs:1344-1378`). The plan must state that proof or reserve `4 × 64`. It also needs to say how the promotion OUT obtains the preimage: today the old identity is held inside `update_session` immediately before overwrite, while the outer promotion receives only a boolean (`session/mod.rs:1344-1348`, `:1393-1396`, `:1673-1675`; `session_glue/promote.rs:99-107`).

4. **MEDIUM — `UpsertRefused` still has contradictory post-failure semantics.** The SSOT scopes “unmodified” to the failed-upsert instant and acknowledges same-resolve promotion/accounting, but then says S2 is “FOR FORWARDING ONLY” (`plan.md:1713-1721`). Section 9 again requires the predecessor to survive unmodified for the dispatch without constraining promotion or accounting (`:2419-2423`). Successful promotion overwrites and publishes S2 (`userspace-dp/src/afxdp/session_glue/promote.rs:99-139`); allowed accounting mutates the predecessor’s counters, ToS, and flags (`userspace-dp/src/session/mod.rs:1177-1210`). The test and wording need explicit no-promotion/no-accounting fixtures or assertions for the resulting promoted state.

5. **MEDIUM — The claimed consumer set misses pre-hoist policy-hit accounting.** The contract says telemetry consumes S2 and says carriage occurs at the `:509` hoist (`plan.md:1739-1763`). Before that hoist, however, the poller resolves the bound policy counter: when S2 lacks a bound counter, it falls back to the surviving table entry under the packet tuple and increments that entry’s rule counter (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:487-509`; `userspace-dp/src/session/lookup.rs:345-354`). On `UpsertRefused` or `OverdueSkipped`, this can attribute S2’s packet to K/P’s policy rule. This is telemetry-only, but contradicts “telemetry consume S2.” Either explicitly accept predecessor attribution or suppress the fallback/use S2’s positional counter for divergent transitions.

6. **MEDIUM — The inertness fold still missed §10.6.1.** It says Part A reduces reap rate because “refused closes never reap early” (`plan.md:2743-2745`). A refused close can independently satisfy the flag-agnostic translated-peer purge predicate and be deleted immediately (`userspace-dp/src/afxdp/session_glue/promote.rs:48-59`, `:167-207`).

7. **LOW — Superseded summaries remain internally contradictory.** The earlier §5.6 text still permits EMPTY for a no-predecessor refusal and routes sibling invalidation through the reap path (`plan.md:1443-1461`), contrary to the later SSOT. Its explicit supersession at `:1693-1695` limits the implementation ambiguity. Section 7 likewise still calls the whole purge path full master parity (`:2062-2065`) while acknowledging the pre-purge demote delta at `:2142-2145`; lookup/mark precedes purge (`userspace-dp/src/session/lookup.rs:105-128`, `:198-218`; `userspace-dp/src/afxdp/session_glue/mod.rs:1157-1197`).

Round-104 dispositions: r104-1 **UNRESOLVED**; r104-2 **UNRESOLVED**; r104-3 **RESOLVED**; r104-4 **UNRESOLVED**; r104-5 **RESOLVED normatively, with retired terminology remaining**; r104-6 **UNRESOLVED**; r104-7 **RESOLVED in §5.8, with a §7 straggler**; r104-8 **UNRESOLVED**.