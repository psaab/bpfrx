# Codex hostile plan-review — round 89 (v10.5.1)

Reviewer: Codex (resumed thread 019fa3e0-cef9-7ef2-876f-3686dfc22ddf; job task-ms8s878t-chbkk4, 2026-07-31). Verdict: PLAN NO (3B/2L). All three BLOCKERs sit in the Part-B probation/constructor rules; none in the gate.

## Verbatim review

PLAN NO 1. **BLOCKER — `ResolvedWithoutLocalBacking` still permits a SYN-bearing close to bec...

[2026-07-31T10:36:07.348Z] Assistant message
PLAN NO

1. **BLOCKER — `ResolvedWithoutLocalBacking` still permits a SYN-bearing close to become an authoritative HA kill.** The plan treats SYN|RST/SYN|FIN as harmless only for an invented miss tuple ([plan.md:347](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:347), :1696-1700). RWoLB instead begins with a real peer-synced translated victim: the code classifies it at [promote.rs:48](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/promote.rs:48) and removes its shared/local backing at :167-207. Full miss re-entry preserves the provenance mechanically but does not use it at the constructor. The #4400 predicate rejects only closing packets without SYN ([session_admission.rs:83](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/session_admission.rs:83)); therefore a SYN-bearing close can reach the raw `ForwardFlow` install at [poll_descriptor/mod.rs:2449](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2449), which seeds `closing/reset` from the packet at [install.rs:179](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/install.rs:179) and emits an Open at :234-260. The fresh-generation Open is accepted and written under the victim key (`sync_conn_write.go:53-59`, `sync_conn_gen.go:435-458`, `session_store.go:257-297); its two-second reap emits Close at `session/expire.rs:342-377`, and the receiver’s helper delete unconditionally executes `sessions.delete(&key)` at `session_glue/commands/delete_synced.rs:16-17`. That kills E1 and companions on the peer. `ReplacedSyncedLocal` has the same gap: LocalDelivery requires only SYN at [local_delivery.rs:20](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/forwarding/local_delivery.rs:20), then removes the synced victim and raw-installs flags at :90-113; making only its anchor untrusted does not suppress constructor-born closing. Both contexts must refuse/suppress every closing seed, including SYN-bearing combinations, and must not emit Open.

2. **BLOCKER — round-88’s direct probation fold is written correctly, but reactive re-materialization bypasses it before admission.** The new rule explicitly skips lookup’s stamp, timeout recomputation, and wheel push; extends that to `touch_if_stale`; and permits clear+refresh only at the matched entry’s successful final-admission hook across all named drop classes ([plan.md:1202](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:1202), :1637-1658). However, the supported fabric-placeholder/shared-canonical coexistence is tested at [session_glue/tests.rs:704](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/tests.rs:704). The shared entry deliberately wins over the placeholder at [shared_ops.rs:594](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:594), then `materialize_shared_session_hit` runs before filtering and unconditionally upserts at [session_glue/mod.rs:1092](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/mod.rs:1092). That upsert removes an existing peer-synced canonical row and restamps `last_seen_ns`, the ordinary timeout, and wheel at [session/install.rs:295](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/install.rs:295), :345-400, :427-433. Consequently, after one refused close creates probation K, another packet through the surviving placeholder can replace K before input/TTL/output admission—restarting probation or replacing it with a full-timeout row. Repetition pins the uncapped synced-upsert slot. Materialization must detect an identity-agreeing existing probation entry and preserve its absolute deadline/state until that entry’s commit hook. The unqualified statements at plan :1250-1252 and :1272-1273 also still contradict the probation exception.

3. **BLOCKER — probation’s promised ≤20-second deadline is not fenced from HA/GC retention.** Expiry evaluates HA retention before removal at [session/expire.rs:168](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/expire.rs:168). `SharedMaterialize` is peer-synced ([session/entry.rs:245](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/entry.rs:245)), so it can SelfHeal and restamp `last_seen_ns` at `expire.rs:213-237`, Hold and rebucket at :239-273, or inherit companion freshness at :296-320 and :490-523. The Hold ceiling is three times the entry timeout (`session/mod.rs:103-121`), and an active companion can retain it repeatedly. Separately, `refresh_for_ha_transition` restamps and requeues at [session/mod.rs:1594](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/mod.rs:1594), called by both owner-RG refresh and demotion. The plan’s `ExpiredSession` probation flag at plan :1179-1193 is downstream of these `continue` paths, so it cannot enforce local-only reap. Probation reaching its immutable deadline must bypass SelfHeal, Hold, and companion retention, while HA transitions must preserve that deadline.

4. **LOW — §3.1’s main drift conclusions are sound, but the table is not factually complete or fully verified.** Confirmed: production `cluster_peer_return_fast_path` is absent at `origin/master`; #6432’s arm head is exactly `origin/master:poll_descriptor/mod.rs:4015`, with `StageOutcome` at :4028; and the selected rows input-filter `592→612`, TTL `846→866`, slow `account_packet 3494→3556`, and cache accounting `312→318` are correct. Errors remain in [plan.md:357](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:357): the range contains 19 commits, not 25; #6458’s named function is at master `fabric.rs:289-307`, not ~331-342—the worktree’s [fabric.rs:331](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/forwarding/fabric.rs:331) is a different function; the tunnel pushes are worktree [tunnel.rs:739](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/tunnel.rs:739) to master :765-767, not `563-615→760-767`; and `live_by_flow` already resides at [allocator.rs:481](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:481) in both revisions—`source.rs:1548` is an allocation call, not the old field. Relevant #6473 static-NAT-before-DNAT reordering is omitted, although full current-pipeline re-entry makes it non-fatal.

5. **LOW — two editorial folds remain internally inconsistent.** The summary says “seed-lifecycle completion” ships at [plan.md:69](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/docs/research/6461-blind-rst/plan.md:69), while :1306-1311 explicitly retreats to master behavior. Site-9’s accepted-close test still promises exactly one Close at :1676-1678 without the `MissingNeighborSeed` exception, despite that exception appearing at :1704-1711/:1764-1768 and being enforced by [session/expire.rs:342](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/expire.rs:342).

The gate arithmetic and RFC 1982 bounds are correct, and the imported-entry zero-trust state is genuinely absorbing. #6432’s typed `StageOutcome` wrapping also composes structurally; the fatal defect is that RWoLB provenance is discarded semantically when constructor authority is assigned.

Round-88 dispositions: B1 probation pre-commit pin — **UNRESOLVED**; L1 fresh-P2 flow-cache/rollback consumers — **RESOLVED**; races (e)-(g) relocation — **RESOLVED**; distinct commit-hook wording — **RESOLVED**; stale clean-pre-SNAT wording — **RESOLVED**; exactly-one-Close qualification — **UNRESOLVED**.
