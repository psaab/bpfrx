PLAN NO

Plan references below are pinned to commit `9502e7fe6`; later documentation commits that appeared in the worktree were not reviewed as v9.9.23.

1. **BLOCKER — the heartbeat cascade relies on a nonexistent coupling.** The plan says closing both session-sync TCP fabrics stops heartbeat acknowledgements and causes a 5×200 ms detector to clear both TCP slots (`plan.md@9502e7fe6:1173-1203`). The repository has two different mechanisms:

   - The 5-count detector belongs to the separate UDP cluster heartbeat, whose default interval is actually 100 ms ([heartbeat.go:52](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/heartbeat.go:52)). Its sender owns an independent UDP socket ([heartbeat.go:643](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/heartbeat.go:643)); closing SessionSync TCP sockets does not stop it. Its timeout updates election state but never touches `SessionSync.conn0/conn1` ([heartbeat_manager.go:358](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/heartbeat_manager.go:358)).

   - Session-sync heartbeat detection is per TCP receive loop, uses a 10-second read deadline, and returns after two misses ([sync.go:90](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync.go:90), [sync_conn_read.go:14](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:14)). Its defer clears only that exact connection ([sync_conn.go:480](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:480)).

   Therefore the original schedule remains: B closes both; A processes fab0 EOF while fab1 remains installed; fab0 reconnects before fab1 clears; `wasDisconnected` is false because one slot remained occupied ([sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244)); later fab1 clears while fab0 is live, so no cold-prime occurs. The barrier also mentions only redial, while inbound accepts remain independent ([sync_conn.go:388](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:388)). Use a SessionSync-specific reset/ack, or gate both dialing and accept-side installation until both per-slot TCP teardowns are acknowledged using the actual liveness bounds.

2. **BLOCKER — repair content still lacks a sender cutoff and transactional commit.** Buffering incrementals observed before the current repair’s `BulkEnd` closes the exact fab0-DELETE/fab1-INSTALL inversion, but it does not cover frames delayed until after `BulkEnd`. Bulk uses one pinned connection while `sendLoop` independently drains opaque frames over whichever connection is active ([sync_bulk.go:53](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:53), [sync_conn_write.go:268](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_write.go:268)). A stale E1 INSTALL queued before the repair can arrive after an E1-absent repair has reconciled and cleared the freeze. `BulkStart` has reset the generation map ([sync_conn_read.go:183](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:183), [sync_conn_gen.go:324](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_gen.go:324)); absence reconciliation records no generation tombstone ([sync.go:1080](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync.go:1080)). The late INSTALL is accepted after readiness clears.

   The “wrong-ID bulk is wholly non-mutating” rule (`plan.md@9502e7fe6:1228-1252`) also fails when O2 supersedes O1 after O1 began: O1 was current when its initial members and generation reset applied immediately ([sync_conn_read.go:98](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:98)); making it non-current later cannot undo those mutations. The freeze buffer also has no explicit capacity/overflow transaction. Finally, primary discharge still compares against a process-local bulk counter that restarts with the sender ([sync.go:474](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync.go:474), [sync_bulk.go:65](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:65)); after sender restart, epoch 1 cannot exceed B’s old raising point.

   Required change: establish a sender cutoff, stage the bulk plus generation-map changes until matching `BulkEnd`, serialize or sequence all post-cut operations, and make buffer overflow invalidate/retry the repair without committing partial state. Repair epochs must be namespaced by authenticated sender incarnation.

3. **BLOCKER — allocation generation does not close the pending-release ABA.** The fold gives `LiveAllocation` and deferred receipts generation `g`, then says a same-K/P `NoChange` transfers the incumbent GroupHold without changing `g` (`plan.md@9502e7fe6:1514-1534`). Concrete schedule:

   1. E1/G1’s last clone drops and queues `D(K,P,g)`; actual allocator release is deferred.
   2. Before D drains, E2 with the same K/P arrives. Exact reserve is `NoChange` ([allocator.rs:1664](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1664)).
   3. G1 no longer exists—it reached last-drop—so it cannot be cloned. If E2 creates G2, it still references generation `g`.
   4. D drains, finds the live record still has `g`, and removes it through the flow/tuple release path ([allocator.rs:1318](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1318)). P can then be claimed while E2 remains live ([allocator.rs:1007](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1007)).

   Last-drop must atomically mark `PendingRelease(g,ticket)`. A same-K/P `NoChange` must atomically cancel/claim that ticket or create a real retained credit before publication; drain must compare both allocation generation and release ticket/state. The generation namespace should also live on the stable slot—or include an allocator-instance nonce with its counter floor preserved across compatible retargets—rather than restart independently in each rebuilt allocator.

4. **HIGH — DirectHold coverage is fixed, but cross-variant transfer and registry lifetime remain undefined.** The plan now correctly enumerates deterministic PAT/NAT64 and both address-only paths (`plan.md@9502e7fe6:1497-1503`). However, a locally born DirectHold entry can be demoted in place ([install.rs:542](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/session/install.rs:542)) and later replaced by peer state when local replacement is permitted ([upsert_synced.rs:29](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:29)). Same K/P returns `NoChange`, but the fold describes cloning an incumbent GroupHold; this incumbent is DirectHold. Creating an uncredited GroupHold lets the eventual Direct release free P beneath the imported replacement.

   The proposed durable DirectHold registry (`plan.md@9502e7fe6:1504-1513`) also lacks keys, strong-reference ownership, retirement, and stop/rebind persistence. Current detached snapshots and commands are freely cloned ([runtime.rs:408](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/types/runtime.rs:408), [coordinator/mod.rs:753](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/mod.rs:753)), and `SessionManager` has no analogous registry today ([session_manager.rs:12](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/session_manager.rs:12)). Define a clonable `AllocationRef { slot, allocation_id, SessionIdentity }`, explicit registry keeper lifetime, and a receipt×token-variant transition matrix. The replay clause saying commands carry no token (`plan.md@9502e7fe6:1778`) must be scoped to DirectHold references; GroupHold commands must own and account for a clone.

5. **MEDIUM — `SessionIdentity` is correct normatively, but scalar executable contracts remain.** The fold establishes the right umbrella invariant at `plan.md@9502e7fe6:826-835` and `:2425-2429`. Nevertheless, detailed clauses still require detached consumers, Close processing, helper aliases, purge paths, and tests to compare named scalar `flow_incarnation_id` fields (`plan.md@9502e7fe6:859-865`, `:2058-2068`, `:2128-2133`, `:3592-3595`, `:3614-3628`). Current deletion remains key-driven ([session_import.rs:243](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:243), [runtime.rs:408](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/types/runtime.rs:408)). Following those specific field contracts permits A1 `(n1,id7)` cleanup to match restarted A2 `(n2,id7)`. Replace every scalar comparison, inheritance rule, command field, and detailed test with the single `SessionIdentity` type.

6. **LOW — §9 and scope inventories still contradict the new folds.** Section 9 retains the superseded sender-side first-EOF cascade test at `plan.md@9502e7fe6:3302-3305`, immediately before the mutually exclusive heartbeat-detector test at `:3322-3325`. The overview still says the NAT-release work is filed separately (`:16-20`), and the final residual repeats “filed” (`:3821-3829`), while §11 says #6522 ships and is closed (`:3796-3808`). The new d4 tests also omit post-`BulkEnd` delayed incrementals, O2 after partial O1 apply, freeze-buffer overflow, pending-release reacquisition, and DirectHold→GroupHold `NoChange`.

**Round-38 dispositions**

- **r38-1 — not resolved.** The proposed detector is either the unrelated UDP election heartbeat or the per-connection TCP timeout; neither provides the asserted peer-wide slot clear.

- **r38-2 — partially resolved.** Observed incrementals are ordered after the nominal repair, but sender cutoff, transactional staging, overflow, and sender-restart sequencing remain open.

- **r38-3 — partially resolved.** Generation comparison handles reuse after completed removal, but not reacquisition while the same-generation release is already queued.

- **r38-4 — partially resolved.** The global full-pair invariant is now correct; explicit scalar field contracts and tests still contradict it.

- **r38-5 — partially resolved.** Local allocation-path coverage is resolved; registry lifetime, queued GroupHold ownership, and DirectHold→GroupHold replacement are not.

- **r38-6 — partially resolved.** The §11 heading and d4 additions are improvements, but superseded tests and stale scope/residual text remain.

Bottom line: Part A remains converged, with no new blind-demotion availability trace. Part B still has concrete schedules that prevent deterministic full resync, commit stale post-repair state, or release/reissue a translated tuple beneath a live replacement. Those session-sync and NAT-lifetime correctness gaps preclude final sign-off on v9.9.23.
