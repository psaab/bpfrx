# Codex hostile plan-review — round 93 (v10.9.0)

Reviewer: Codex (FRESH thread — the 92-round resumed thread hit the content filter at final assembly twice in a row (jobs task-ms8yfjb8-2egd0h, task-ms8ykbrl-erek5p); the fresh thread (job task-ms8ymxmb-r4jxbv) ran with a self-contained neutral-framed prompt and completed). Verdict: PLAN NO (2B/1H/2M/1L). The fresh eyes caught what 92 rounds of shared context missed: master's post-purge packet stays in the HIT branch on the retained lookup, so v10.4.1's same-dispatch re-entry collapsed the pre-existing #6599 class from two packets to one.

## Verbatim review

PLAN NO 1. **BLOCKER — #6599 is worsened by the proposed same-dispatch re-entry.** Upstream p...

[2026-07-31T13:26:34.420Z] Assistant message
PLAN NO

1. **BLOCKER — #6599 is worsened by the proposed same-dispatch re-entry.** Upstream purges the synced backing, then retains `hit.lookup` and returns `Some` (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1197,1254-1261`). The caller therefore remains in the hit branch (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:412-431,883-885`); the fresh `ForwardFlow` install/Open at `:2449-2458` and `userspace-dp/src/session/install.rs:225-260` requires a later packet. The proposed RWoLB outcome instead re-enters the miss pipeline for the same packet (`docs/research/6461-blind-rst/plan.md:665-676,1514-1529`), reducing purge→overwrite from two packets to one before latest-generation-wins application (`pkg/cluster/sync_conn_gen.go:205-217,435-453`; `pkg/dataplane/session_store.go:257-297`). No FIN/RST only proves the demote validator does not run; it does not make this proposed non-close behavior upstream-equivalent.

2. **HIGH — the close-aware purge amplifies #6600 for FIN/RST traffic.** Import publishes before enqueueing worker installation (`userspace-dp/src/afxdp/ha/session_import.rs:115-121,215-223`); the worker installs before attempting reservation (`userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:64-96`), and a conflicting owner causes an ignored reservation failure (`userspace-dp/src/nat/allocator.rs:1682-1689`; `userspace-dp/src/nat/source.rs:945-953`). Upstream’s purge predicate is flag-agnostic (`userspace-dp/src/afxdp/session_glue/promote.rs:48-59`): a close forwards once using the saved lookup but removes the conflicted local/shared backing (`session_glue/mod.rs:1178-1195`; `promote.rs:181-207`). FIN/RST is not flow-cache eligible (`userspace-dp/src/afxdp/flow_cache.rs:352-369`), so a subsequent bare close misses and #4400 drops it (`poll_descriptor/mod.rs:1642-1650`). The proposal retains the `SyncImport` row for every close, making conflicted-P1 use repeatable until expiry or a non-close purge. Thus “packet-class-agnostic” does not mean “not worsened,” and §11’s assertion that neither issue has a closing driving packet is false.

3. **BLOCKER — adopt-S2 can indefinitely pin an overdue probation entry.** Algebraically, the specified encoding re-derives `max(D, now)`, not always `D`:

   `now.saturating_add(D.saturating_sub(now))`.

   `D < now` is reachable because expiration is strict and wheel-driven (`userspace-dp/src/session/expire.rs:130-168`; `userspace-dp/src/session/wheel.rs:39-50`). Re-materialization removes/recreates the row, resets `wheel_tick`, and queues it again (`userspace-dp/src/session/install.rs:312-322,345-401,427-433`). With timeout zero it targets the current tick. A packet just after each absolute tick boundary but before phase-shifted GC can advance `wheel_tick`; GC drains the prior hint and discards it as stale (`userspace-dp/src/session/expire.rs:137-165`). Repeating once per tick pins K indefinitely, defeating the ≤20-second invariant on an upsert path without the fresh-install capacity check. The design needs an overdue-K branch or preserved due generation, plus a phase-shifted regression test. Its “immutable/never restamped” wording also conflicts with writing `last_seen_ns = now` and allowing S2 to shorten D (`plan.md:1251-1271,1668-1692,1934-1939`).

4. **MEDIUM — §9 retains an unreachable pre-v10.8 test oracle.** It requires an RWoLB bare close to reach #4400 (`plan.md:2026-2029`), while the final rule says every closing packet remains shared-backed `ExistingResolved` and can never become RWoLB (`plan.md:1386-1400,1521-1526,2047-2053`). The actual #4400 predicate is TCP close without SYN (`userspace-dp/src/afxdp/poll_descriptor/session_admission.rs:83-87`). Remove this oracle or explicitly label it a defense-in-depth test of an impossible typed outcome.

5. **MEDIUM — the producer-count invariant contradicts the document’s own cross-worker caveat.** Section 5.6 admits a same-node `SharedPromote` copy is a second legitimate emitter (`plan.md:1195-1200`), but §§7/9 require “exactly one emitting entry/producer” (`plan.md:1631-1639,1916-1918`). `SharedPromote` is not peer-synced (`userspace-dp/src/session/entry.rs:245-249`), and the Close gate does not require a close mark (`userspace-dp/src/session/expire.rs:342-377`), so that copy remains emission-eligible until delete fan-out removes it. Scope the invariant to one prompt marked producer, or specify the fan-out ordering being tested.

6. **LOW — master-drift bookkeeping remains stale.** Section 3 cites #4400 at `poll_descriptor/mod.rs:1634-1644/:1640` and its predicate at `session_admission.rs:53`; the bodies are actually `poll_descriptor/mod.rs:1642-1650` and `session_admission.rs:83-87`, as §3.1 partly acknowledges. Section 9 also mandates a fabric-seed provenance test (`plan.md:1891-1897`) although §3.1 says upstream #6478 deleted that constructor (`plan.md:415-423`).

The requested lookup, Close-emission, admission, and install/upsert spot-checks otherwise match the cited code. The owned-RWoLB alive-probation, skip-the-upsert, and rollback-then-forward shapes appear only as retractions.