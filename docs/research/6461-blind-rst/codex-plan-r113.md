# Codex hostile plan-review — round 113 (v10.28.0)

Reviewer: Codex (fresh thread, job task-ms9h5qve-hzyz3j, 2026-07-31). Verdict: PLAN NO (3B/1H/2M). r112-3 RESOLVED (the total invariant). The v10.28.0 admission-point promote had no implementable producer/carrier/apply contract and the enqueue is not a commit-to-deliver point — the v10.29.0 answer is the lookup-phase promote (master's own timing, no token). Also: the commit hook needed an alias-safe canonical matched token (lookup resolves the alias and discards actual_key); the establishment promote is a sixth authority consumer the contract must gate.

## Verbatim review

PLAN NO 1. BLOCKER — The direct-overdue predicate is arithmetically correct, but the commit h...

[2026-07-31T22:02:01.040Z] Assistant message
PLAN NO

1. BLOCKER — The direct-overdue predicate is arithmetically correct, but the commit hook lacks an alias-safe matched-entry identity. `lookup_with_origin` resolves a reverse-translated alias and captures its canonical `actual_key`, then discards it on return (`userspace-dp/src/session/lookup.rs:62-68`, `:85-102`, `:194-219`). Downstream wraps the hit as `QueryKey` (`userspace-dp/src/afxdp/shared_ops.rs:602-612`) and carries the packet tuple (`userspace-dp/src/afxdp/session_glue/mod.rs:1199`, `:1254-1261`), while table mutation requires the canonical key (`userspace-dp/src/session/mod.rs:1022-1051`). Cache entries likewise retain the query key (`userspace-dp/src/afxdp/flow_cache.rs:201-224`, `:578-581`). Consequently, a committed reverse-translated probation hit cannot reliably clear/refresh K or evaluate K’s overdue state. Thread an identity-checked canonical matched token through slow resolution and flow-cache entries, and test canonical plus translated/cache aliases. The `saturating_add(...) <= now_ns` boundary itself correctly implements the deliberate one-instant shortening relative to strict GC expiry (`userspace-dp/src/session/expire.rs:166-168`).

2. BLOCKER — Enqueue-time establishment promotion has no implementable producer/carrier/apply contract. Current lookup computes `promote_from_reverse`, mutates the matched entry, and propagates using canonical key/NAT (`userspace-dp/src/session/lookup.rs:129-149`, `:173-218`). `SessionLookup` carries only decision/metadata (`userspace-dp/src/session/entry.rs:191-206`), `ResolvedFlowSessionDecision` has no promotion token (`userspace-dp/src/afxdp/shared_ops.rs:563-578`), and the poller reduces `resolved` to its decision at `userspace-dp/src/afxdp/poll_descriptor/mod.rs:883`, long before `PendingNeighAdmission::Buffer` at `:5017-5069`. Specify a proof result bound to canonical family identity, its hoist, matched-entry timeout/wheel and companion semantics, and an identity-checked application only after successful insertion—not on duplicate/capacity refusal.

3. BLOCKER — Establishment promotion is a new sixth authority consumer, but the supposedly exhaustive `MaterializeReport` contract does not gate it. A placeholder can be shadowed by shared S2 (`userspace-dp/src/afxdp/shared_ops.rs:602-628`); the synced upsert can refuse against non-peer K (`userspace-dp/src/session/install.rs:310-315`), while materialization still returns S2 (`userspace-dp/src/afxdp/session_glue/mod.rs:1098-1119`). With `MissingNeighbor`, ownership promotion no-ops (`userspace-dp/src/afxdp/session_glue/promote.rs:86-90`) and S2 is buffered (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:5018-5068`). A later establishment apply could therefore mutate surviving K/its companion despite K/S2 identity divergence. It must consume `effective_transition` and suppress at least `UpsertRefused` and `OverdueSkipped`, alongside the refusal/closing and identity gates.

4. HIGH — Pending-queue insertion is not a “commit-to-deliver” point. An inserted packet can time out without transmission (`userspace-dp/src/afxdp/neighbor_dispatch.rs:208-232`), then after resolution can still fail frame access, output-filter/CoS, rewrite, or target dispatch (`:294-325`, `:344-404`). Thus enqueue can establish state for a SYN-ACK the firewall later denies or drops. Master promotes even earlier, so this class is not newly reachable, but v10.28’s claimed boundary is false. Either document and test this master-parity resource/liveness residual, or carry the identity-bound proof in the pending packet and apply it after successful retry admission.

5. MEDIUM — Active text remains mutually contradictory. The plan says pending machinery is untouched (`plan.md:636-641`), requires buffered SYN-ACK promotion at enqueue (`:649-655`), then says that SYN-ACK remains OPENING and promotes on the next unbuffered packet (`:664-667`). Section 9 repeats “NEVER drives the establishment promote” immediately before requiring enqueue promotion (`:2653-2669`). Its “next unbuffered packet does all three” claim also omits `UpsertRefused`, closing/proof predicates, and final-admission failure. Moreover, companion promotion deliberately sets only `established` and preserves the short opening deadline until the forward ACK (`userspace-dp/src/session/mod.rs:1243-1252`).

6. LOW — The effective-transition mapping is now total, but the concrete refusal gates still say only `validation == Some(Refused)` rather than explicitly requiring `site == Some(Site2c)` as the normative invariant promises (`plan.md:1761-1769`, `:1781-1786`, `:1873-1877`).

Round-112 dispositions: r112-1 UNRESOLVED; r112-2 UNRESOLVED; r112-3 RESOLVED; r112-4 UNRESOLVED.