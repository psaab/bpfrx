PLAN NO

1. **BLOCKER — Repair discharge remains contradictory.** The correct direction split exists at `plan.md@459412e33:1460-1466`, `1480-1485`, and `1978-1992`. However, the sender’s outbound obligation is still said to discharge at sender→receiver `JOURNAL_END` at `1474-1479`; generic `JOURNAL_END` discharge remains at `1733-1734`; normative §5.8 still calls it the “terminal ACK” and “ONLY discharge” at `3905-3911`; and §9 retains generic assertions at `4355-4358`, `4379-4381`, and `4455-4457`.

   Concrete trace: A writes the post-cut journal and `JOURNAL_END`, but the connection closes before B applies the marker. Following the stale clauses, A clears its outbound obligation/cold-prime without receiving `JOURNAL_ACK`; B correctly remains inbound-not-ready and emits no ACK, stranding repair until a slower reset/escalation cycle. Current code demonstrates why the return frame is necessary: bulk success currently occurs immediately after writing `BulkEnd` at [sync_bulk.go:183](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:183), clearing cold-prime at [sync_conn.go:194](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:194), while the receiver separately reconciles and signals readiness at [sync_conn_read.go:205](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:205) and [daemon_ha_sync.go:90](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/daemon/daemon_ha_sync.go:90).

   Required change: every clause must state one of exactly two rules: applied `JOURNAL_END` clears receiver inbound/readiness; matching full-triple `JOURNAL_ACK` clears sender outbound/cold-prime. `BulkEnd` and bare `BulkAck(u64)` clear neither for negotiated repairs.

2. **HIGH — The advertised authenticated-transcript fold is absent at the pinned commit.** Normative §5.8 still lists only `(capacity, capacity_config_generation, …, repair-vN, reset-vN)` at `plan.md@459412e33:3887-3903`; it contains neither `node_id`, `process_incarnation`, `AUTH_PROOF`, nor a canonical transcript. Section 5.2’s allowlist still permits only HELLO/capability and RESET frames at `1368-1376`, while current keyed setup requires the distinct `syncMsgAuthProof` frame at [sync_auth.go:60](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:60) and [sync_auth.go:387](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:387).

   A direct proof-algorithm change would also break rolling upgrades: current peers compute `HMAC(tag || nonce)` at [sync_auth.go:217](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:217) and verify it at [sync_auth.go:401](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:401). A new peer computing a whole-transcript proof and an old peer computing the nonce-only proof reject each other and reconnect indefinitely.

   Required change: specify an auth-transcript protocol version and exact canonical bytes. If either peer is v1, retain the v1 proof and mask `reset-vN` plus transcript-dependent capabilities. If both are v2, exchange bounded raw HELLO records, authenticate a domain-separated, length-prefixed ordered pair of those exact records, then derive/install the authenticated wrapper and permit RESET frames. The wrapper is currently installed only after proof verification at [sync_auth.go:406](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:406), so `AUTH_PROOF` cannot itself run “inside” that wrapper.

3. **MEDIUM — The incarnation transition needs a nonblocking drain contract.** The new `{current,pending,retired}` registry at `plan.md@459412e33:1391-1410` closes the original n1→n2→late-n1 regression under atomic token revocation. But “DRAINS/REVOKES under `s.mu`” still permits a blocking interpretation.

   Concrete deadlock: transition code holds `s.mu`, closes n1, and waits for its handler; `receiveLoop` returns and defers `handleDisconnect` at [sync_conn_read.go:14](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:14), which blocks acquiring the same mutex at [sync_conn.go:480](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:480). Pending n2 never promotes and readiness remains degraded.

   Required change: while holding `s.mu`, revoke/remove exact n1 lane tokens, retire n1, promote n2, then release the lock before closing or joining anything. If an asynchronous pending state is retained, key it by transition epoch and live-lane set; natural lane death cancels it, and completion CASes the same epoch before promotion. Also key the high-water by `(direction, node_id, process_incarnation)`—not merely incarnation—and define lower/equal/higher `RESET_GEN` behavior, including equal-generation re-ACK.

Disposition r47-1: **partially resolved** — several correct direction-specific clauses landed, but operative, normative, and test contradictions remain.

Disposition r47-2: **partially resolved** — the core retired-incarnation fence landed, but drain/pending/high-water lifecycle semantics remain incomplete.

Disposition r47-3: **not resolved** — the claimed transcript/schema/allowlist changes are not present in `459412e33`.

Bottom line: Part A remains converged, and this round found no new blind-demotion or translated-tuple-release trace in the existing NAT-lifetime machinery. Part B is still not signable: its terminal exchange remains internally inconsistent, the pinned plan lacks the claimed authenticated transcript, and the new incarnation transition admits a concrete readiness deadlock unless revocation and handler cleanup are explicitly separated.
