# Codex hostile plan-review — round 102 (v10.18.0)

Reviewer: Codex (fresh thread, job task-ms98oy28-q47ajr, 2026-07-31). Verdict: PLAN NO (5B/2H/2M). The normative MaterializeOutcome contract was still absent (the v10.17/18 edits landed in summary paragraphs while the §5.8 outcome list never gained the field/enum/composition; two constructor sites and the pre-promotion ordering unspecified); 'refusal carries the EMPTY set' unsound in both refusal meanings; invalidation timing had no realizable API contract; zero-alloc capacity contract missing; close→close-chain qualifications incomplete; companion-exposure contradiction survived; equivalent unscoped inertness claims remained. r101-5 and r101-7 RESOLVED. Folded as the v10.19.0 single normative contract block.

## Verbatim review

PLAN NO 1. **BLOCKER — The promised normative `MaterializeOutcome` contract is still absent.*...

[2026-07-31T18:03:31.199Z] Assistant message
PLAN NO

1. **BLOCKER — The promised normative `MaterializeOutcome` contract is still absent.** `MaterializeOutcome` appears only in the header (`plan.md:4-8`). Section 5.8 never defines the enum, changes `materialize_shared_session_hit` to return `(SessionLookup, MaterializeOutcome)`, adds `ResolvedFlowSessionDecision.materialization`, or initializes it at the two constructors (`plan.md:1593-1715`). Current code returns only `SessionLookup` and ignores the upsert result (`userspace-dp/src/afxdp/session_glue/mod.rs:1092-1121`); the resolved struct lacks the field (`userspace-dp/src/afxdp/shared_ops.rs:563-578`); its constructors are `session_glue/mod.rs:1254-1261` and `:1330-1344`. Promotion runs before the first constructor (`session_glue/mod.rs:1235-1253`), so the required pre-promotion outcome is not mechanically specified.

2. **BLOCKER — `OverdueSkipped` carriage and composition remain test-only.** Section 5.8 merely summarizes five consumers (`plan.md:1655-1664`); its normative MissingNeighbor outcome list never mentions `OverdueSkipped` or maps it to `ExistingResolved` (`plan.md:1675-1715`). The hoist, teardown guards, and composition exist only in §9 (`plan.md:2218-2228`). Current poller code hoists only `install_failed` (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:509`) and reduces the wrapper to `resolved.decision` at `:883`, leaving no outcome at teardown (`:698-714`, `:768-784`, `:824-840`), cache insertion (`:3900-3959`), or MissingNeighbor seed work (`:4034`, `:4662-4829`). Once a real carrier exists, the named consumer set is otherwise complete: accounting is intentionally allowed (`poll_descriptor/mod.rs:3494-3503`, `session/mod.rs:1177-1210`), deferred RST teardown cannot engage (`session_glue/mod.rs:863-875`), and neighbor replay uses the buffered decision without another SessionTable lookup (`poll_descriptor/mod.rs:5057-5068`, `neighbor_dispatch.rs:272-405`).

3. **BLOCKER — The displaced-identity set has no typed producer or carrier.** The plan only asserts that every site-2c transition returns a bounded set (`plan.md:1417-1428`); §5.8 defines no identity/set type, outcome payload, or field carrying it to the poller. Current lookup discards the shadowed placeholder identity (`userspace-dp/src/afxdp/shared_ops.rs:522-560`, `:602-628`), upsert discards `_previous` and returns only `bool` (`userspace-dp/src/session/install.rs:295-322`), and `ResolvedFlowSessionDecision` has no alias-set field (`userspace-dp/src/afxdp/shared_ops.rs:563-578`). The lookup → upsert → materializer → resolved-result transport remains unspecified. The alias inventory itself is complete: direct/reverse-translated (`session/lookup.rs:62-100`), reply-wire/reply-canonical (`session/lookup.rs:222-250`, `session/key.rs:19-26`), and forward-wire (`session/lookup.rs:258-293`) keys.

4. **BLOCKER — “Refusal carries the EMPTY set” is unsound in both meanings of refusal.** A close-validator refusal still installs an alive probation entry (`plan.md:1283-1292`), so it can replace a peer-synced canonical predecessor and cannot universally carry an empty displaced set (`plan.md:1425-1428`). If “refusal” instead means the upsert refusing a non-peer predecessor, that predecessor can still be overwritten later in the same resolve: upsert returns false (`userspace-dp/src/session/install.rs:310-315`), materialization nevertheless returns S2 (`userspace-dp/src/afxdp/session_glue/mod.rs:1098-1119`), and promotion still runs (`:1235-1253`). `SharedPromote` is non-peer (`userspace-dp/src/session/entry.rs:245-253`), so `update_session` accepts the local→local update and overwrites/reindexes the predecessor (`userspace-dp/src/session/mod.rs:1350-1370`, `:1373-1475`), followed by publication and replication (`userspace-dp/src/afxdp/session_glue/promote.rs:99-139`). Its old cache aliases therefore escape the purported empty set. Define distinct `ValidatorRefused`/`UpsertRefused` outcomes and either suppress promotion or capture the identity it displaces.

5. **BLOCKER — The invalidation timing has no realizable API contract.** The ordering described at `plan.md:1429-1439` is conceptually correct, but descriptor processing owns only the current binding and returns `()` (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:110-131`); only `poll_binding` owns `left/current/right` after descriptor processing returns (`userspace-dp/src/afxdp/worker/lifecycle.rs:53-55`, `:209-225`). `WorkerScratch` has no transition accumulator (`userspace-dp/src/afxdp/worker/scratch.rs:33-62`). The cited reap routine cannot literally provide the fan-out: it accepts `ExpiredSession`, performs NAT/BPF teardown, and iterates including the current binding (`userspace-dp/src/afxdp/worker/loop_body/mod.rs:1481-1521`). Specify the preallocated batch output, immediate current-binding drain after resolution, and lifecycle fan-out over `left + right` before the next RX batch.

6. **MEDIUM — The new batch set does not close the zero-allocation invariant.** The plan promises zero hot-path allocations (`plan.md:1881-1887`) but gives no fixed capacity or preallocation rule for up to three identity families per transition across a 64-descriptor batch (`userspace-dp/src/afxdp/mod.rs:278-281`). Existing scratch vectors deliberately require construction-time `with_capacity` because zero-capacity/default vectors violate this contract (`userspace-dp/src/afxdp/worker/scratch.rs:19-32`). Tests also omit upsert-refused→promotion and refused/overdue plus shadowed-placeholder-cache cases (`plan.md:2202-2217`).

7. **HIGH — The cold-seed correction is present, but the close→close chain remains unqualified elsewhere.** Section 5.6 and §9 still say close #1 purges and a SYN-bearing close #2 clean-misses and FreshPrimary-installs (`plan.md:1540-1547`, `plan.md:2236-2244`). The required warm-or-lapsed-seed qualification appears only later (`plan.md:1957-1959`, `plan.md:2540-2543`). When close #1 is cold, it installs `MissingNeighborSeed` from its raw flags (`userspace-dp/src/afxdp/poll_descriptor/mod.rs:4787-4795`), and close #2 finds that local seed before a clean miss (`userspace-dp/src/afxdp/shared_ops.rs:602-613`). Section 3.1 likewise incorrectly calls the clean-miss dispatch the purged class’s “only install point” despite the cold seed install (`plan.md:453`, `poll_descriptor/mod.rs:4787-4829`).

8. **HIGH — The companion-exposure contradiction survives.** The plan correctly traces that lookup precedes purge and the gate leaves the reverse companion on its ordinary trajectory, lengthening stale/conflicted-P1 exposure (`plan.md:746-766`, `plan.md:1979-1987`). Code confirms close propagation occurs during lookup (`userspace-dp/src/session/lookup.rs:105-128`, `:198-218`; `userspace-dp/src/session/mod.rs:1232-1277`), while purge removes the matched forward side but not its local companion (`userspace-dp/src/afxdp/session_glue/promote.rs:181-207`). Yet §10.6.2 still says the gate “neither worsens the class” (`plan.md:2544-2553`), and §7 retains an obsolete statement about the retracted close-aware gate (`plan.md:1944-1949`). Constructor-side Open reachability remains master-identical; companion exposure does not.

9. **MEDIUM — Equivalent unscoped inertness claims remain outside the two repaired copies.** The overview says a refused close never reaps early (`plan.md:137-142`), §2 says every imported entry lingers until inactivity expiry (`plan.md:308-324`), §7 says refusal mutates nothing at all (`plan.md:1787-1789`), and §9 still requires ordinary-timeout emission parity (`plan.md:2417-2427`). An anchorless translated peer-synced hit can be refused and then independently purged immediately (`userspace-dp/src/afxdp/session_glue/mod.rs:1178-1196`, `userspace-dp/src/afxdp/session_glue/promote.rs:48-59`, `:181-207`). These claims need the same gate-effects-only scope now used in §5.7.

Round-101 dispositions: r101-1 **UNRESOLVED**; r101-2 **UNRESOLVED**; r101-3 **UNRESOLVED**; r101-4 **UNRESOLVED**; r101-5 **RESOLVED**; r101-6 **UNRESOLVED**; r101-7 **RESOLVED** at its cited copies, with fresh equivalent stragglers in finding 9.