PLAN NO

1. **BLOCKER — Pending rejections can become permanent and miss the actual tuple-release event.** The fold at `plan.md@22b7b5724:1709-1726` closes the original live-E1 trace only while E1 remains retryable. If B rejects E1 because local E2 owns P ([allocator.rs:1664](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1664), [allocator.rs:1682](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1682)), and E1 then closes before import, B’s DELETE path only deletes an installed key ([sync_conn_gen.go:493](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_gen.go:493)); nothing retires the pending rejection. B therefore remains not-ready for a cohort that can never import, and repeated identities grow the set without a stated bound. Persistent ownership has a second gap: E2’s final flow drop merely schedules lease expiry while retaining P ([allocator.rs:1349](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:1349)); P is actually freed later by GC ([allocator.rs:2138](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:2138), [allocator.rs:2292](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/nat/allocator.rs:2292)), with no hold-drop notification. The existing sweep is not a periodic full resend: it selects `Created >= lastSweepTime` and advances that cutoff after enqueue success ([sync_conn_sweep.go:137](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_sweep.go:137), [sync_conn_sweep.go:185](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_sweep.go:185)). Required change: bound/deduplicate the set; cancel entries on matching DELETE, clean authoritative absence, newer same-key identity, and peer-incarnation retirement; retry from a bounded pending-set timer or every transition that can make the claim satisfiable, including lease GC.

2. **BLOCKER — Negotiated repair completion still has no coherent return acknowledgement or bounded-retry contract.** The operative text says the sender emits `JOURNAL-END` and the receiver sends an ACK (`plan.md@22b7b5724:1589-1612`), while normative §5.8 calls `JOURNAL-END` itself the “terminal ACK” and defines no receiver→sender frame (`:3665-3687`). Today the only return frame is bare `BulkAck(u64)` ([sync_bulk.go:282](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/cluster/sync_bulk.go:282)), handled by the epoch-only logic at [sync_conn_read.go:249](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/cluster/sync_conn_read.go:249). Stale clauses still authorize bulk-ACK/clean-bulk completion at `plan.md:1791-1799,4091-4093,4278-4282`; the current cold-prime latch similarly clears as soon as `doBulkSync` merely writes `BulkEnd` ([sync_conn.go:194](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:194)). Separately, the bounded receipt omits `journal_epoch`, while ACK loss re-drives the same repair ID indefinitely. After receipt eviction B can neither re-ACK without reapplying nor distinguish a changed same-ID attempt; either outcome permits permanent degraded readiness or false completion. Define distinct `JOURNAL_END` and `JOURNAL_ACK` frames carrying the full immutable `(repair_id, journal_epoch, terminal_seqno)`, retain receipts through the terminal retransmission lifetime or mint a fresh repair ID after expiry, and remove every negotiated BulkEnd/BulkAck discharge clause.

3. **BLOCKER — Different-identity quarantine supersession strands the old family’s companions.** The plan first says E2 installs while E1 stage-2 cleanup proceeds identity-conditionally (`plan.md@22b7b5724:2428-2433`), then says E2 terminally cancels E1’s retry entry (`:2454-2459`), while deduplication remains family-key-only. Concrete trace: E1(K,P1) prepublishes K and reverse R1 before fan-out ([session_import.rs:104](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:104), [session_import.rs:187](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:187)); all replicas reject, then E2(K,P2) publishes. Publication replaces K and inserts only P2-derived aliases ([shared_ops.rs:907](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:907), [shared_ops.rs:918](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:918)); it does not remove R1/P1 aliases. Canceling Q1 leaves R1, old NAT/wire state, and G1’s hold. Later deletion derives companions from current K/P2 ([session_import.rs:257](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:257)), so it cannot recover R1, which remains materializable at [session_glue/mod.rs:1157](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/mod.rs:1157). Preserve immutable old-family cleanup records keyed by `(family, SessionIdentity, quarantine_generation)` and remove only identity-matching old companions before dropping G1.

4. **BLOCKER — Whole-sequence timeout lacks a cross-attempt teardown fence.** v9.9.35 retains an unexited old generation after deadline expiry (`plan.md@22b7b5724:1735-1746`), but the escrow contract still permits the next reconcile/restore to bring up another dataplane (`:2339-2348`). Existing control flow allows another apply to re-enter reconciliation ([snapshot.rs:195](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/server/handlers/snapshot.rs:195), [reconcile/mod.rs:337](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/reconcile/mod.rs:337), [reconcile/mod.rs:391](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/coordinator/reconcile/mod.rs:391)). If timeout occurs before quiesce acknowledgement, an old worker may still be inside the loop—it checks stop only at [loop_body/mod.rs:332](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/worker/loop_body/mod.rs:332)—and can later publish BPF/shared/DNAT state at [poll_descriptor/mod.rs:2578](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2578). A new generation cannot safely replay or allocate while that worker’s holds were never handed off. Add a durable teardown-failed latch prohibiting rebind/reconcile until every unquiesced old worker exits or the process restarts; expose the retained generation and recovery action operationally.

5. **HIGH — Helper token replacement covers success only, not unknown outcomes or the complete family write.** `replace(slot,T1,T2,token_epoch)` at `plan.md@22b7b5724:1493-1509` closes the successful supersession race, but defines no idempotence/query rule if the helper commits T2 and its ACK is lost. Helper IPC explicitly has unreachable/hung outcomes ([manager_ha.go:1771](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/dataplane/userspace/manager_ha.go:1771), [manager_ha.go:1806](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/dataplane/userspace/manager_ha.go:1806)). Moreover, a cluster install writes forward, reverse, and DNAT state sequentially ([session_store.go:274](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/session_store.go:274), [session_store.go:284](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/session_store.go:284), [session_store.go:289](/home/ps/git/kimi-xpf/.claude/worktrees/6461-blind-rst/pkg/dataplane/session_store.go:289)); reverse writes bypass helper validation ([manager_ha.go:1125](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/userspace/manager_ha.go:1125)). T1 can therefore commit its forward, be superseded, then resume stale reverse/DNAT writes. Require an idempotent queryable CAS, a staged non-dispatchable slot during uncertain outcomes, bounded retry with repair/not-ready fallback, and epoch validation or atomic rollback across every family side effect.

6. **HIGH — The sole reset setup conflicts with the active barrier and lacks a total owner.** The plan rejects accept-side setup while `barrierActive` (`plan.md@22b7b5724:1275-1281,1374-1380`) but assigns one address-ordered endpoint to dial the setup carrying both reset triples (`:1341-1354`). If B owns the barrier while A is the selected dialer, B rejects A’s only setup before B can exchange its reset generation. Simultaneous barriers reject both directions until the timeout fallback. Current address ordering is also not total: parse failure returns “dial” on either endpoint ([sync_conn.go:12](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:12)), behavior preserved by [sync_test.go:1337](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_test.go:1337), and each fabric currently selects its owner independently ([sync_conn.go:319](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:319)). Define an authenticated barrier-exempt pre-slot reset lane, a stable node-pair ordering key, and deterministic live-fabric fallback; install the connection only after both reset triples have drained and acknowledged.

7. **HIGH — Capability negotiation cannot distinguish repair/reset implementations.** Normative §5.8 lists capacity generation, heartbeat-ACK, identity-enforcement, and lease-input capabilities at `plan.md@22b7b5724:3669-3671`, but no repair-protocol version or reset-generation capability. An intermediate peer supporting identity/heartbeat features but only legacy `BulkEnd/BulkAck` is therefore indistinguishable from a `JOURNAL-END`/RESET-capable peer; the new node can wait indefinitely for a terminal frame the peer cannot send. Today’s hello is auth-only and keyed-only ([sync_auth.go:314](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:314)), so the planned connection-independent hello must explicitly negotiate `repair-vN` and `reset-vN` before any corresponding frame. INSTALL-tail tolerance itself is sound ([sync_protocol.go:470](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_protocol.go:470)).

**Round-44 dispositions**

- **r44-1 — partially resolved:** degraded readiness and a retry trigger landed; cancellation, bounds, persistent-GC wakeup, and reliable resend did not.
- **r44-2 — resolved:** freeze → quiesce acknowledgement → repeated preflight closes the in-flight-admission race.
- **r44-3 — partially resolved:** most schema inventory was added, but terminal direction, receipt lifetime/keying, repair/reset capabilities, and stale discharge clauses remain.
- **r44-4 — partially resolved:** single-setup intent and idempotent equal-generation ACK landed; barrier admission, total ownership, and fabric fallback remain open.
- **r44-5 — partially resolved:** the successful helper replacement is linearized; failure/unknown outcomes and composite side effects are not.
- **r44-6 — partially resolved:** one deadline now covers all teardown phases; safe containment across later reconcile/rebind attempts is missing.
- **r44-7 — partially resolved:** canonical lock placement landed, but different-identity supersession cancels cleanup still required for the old companions.

Bottom line: Part A remains converged, and v9.9.35 correctly fixes the frozen preflight ordering. Part B is not yet signable: ordinary rejection/expiry can leave the standby indefinitely unavailable, the negotiated repair protocol cannot causally complete, quarantine supersession can preserve stale reverse state and tuple ownership, and teardown retry can rebuild before old ownership is safely handed off. These are concrete state-desynchronization, tuple-lifecycle, and availability traces in the issue’s scope—not Phase-2 preferences or already-documented residuals.
