PLAN NO

Review pinned to the `plan.md` blob at `4064fa3f7`; later worktree changes were excluded.

1. **BLOCKER — The separate capability frame causes a v2↔v1 reconnect loop in the specified handshake order.**  
   `plan.md@4064fa3f7:3962-3965,4047-4049` sends a new capability frame after HELLO and before AUTH_PROOF, claiming an old peer skips it through `handleMessage`. During authentication, however, the v1 peer bypasses `handleMessage`: after HELLO it reads exactly one raw frame at [sync_auth.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:392) and requires that frame to be `syncMsgAuthProof` at lines 401-404. General unknown-frame skipping starts only at [sync_conn_read.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:96), after authentication and wrapper installation at [sync_conn.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:116). Thus v2 sends CAPABILITY, v1 reads it as the expected proof, rejects authentication, closes at `sync_conn.go:106-110`, and retries indefinitely through `sync_conn.go:435-477`.  
   **Required change:** after reading peer HELLO, a v2 endpoint must send v1 AUTH_PROOF immediately when either version is v1; capability exchange must then occur inside the authenticated wrapper. Pre-proof capability records may be used only after both HELLO versions establish v2.

2. **BLOCKER — AUTH_PROOF_v2 neither unambiguously orders nor necessarily covers the separate capability record.**  
   The plan preserves the legacy HELLO and places v2 fields in a separate frame (`plan.md@4064fa3f7:3958-3966`). The literal formula at lines 4017-4023 hashes only two “raw HELLO frames,” while lines 4034-4045 require node ID, process incarnation, and capabilities to be covered. The actual HELLO contains only `{version,keyed,nonce}` at [sync_auth.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:345). Moreover, `prover_record || verifier_record` conflicts with “dialer-first”: for an acceptor proof, those dictate opposite orders. Two conforming implementations can therefore hash different bytes and fail every proof at `sync_auth.go:401-404`; alternatively, implementations following the literal formula leave capability negotiation outside the proof, permitting asymmetric repair or identity-enforcement state.  
   **Required change:** specify one byte formula containing both sides’ exact legacy-HELLO payloads and exact capability-record payloads in fixed dialer/acceptor order, with the prover role separately encoded. Include actual hexadecimal golden inputs and outputs for both proof directions.

3. **MEDIUM — The §9 v1 test contract still contradicts the new total capability rule.**  
   `plan.md@4064fa3f7:4713-4723` still requires only `reset-vN` masking and says `repair-vN` is negotiated independently; it does not require the mask-all rule or mutually authenticated `CAPABILITY_CONFIRM` from lines 3978-3992. Because the v1 proof covers only the nonce at [sync_auth.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:217), following §9 permits one endpoint to enter repair-era completion while the other follows legacy BulkEnd processing at [sync_conn_read.go](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:205), leaving readiness and completion state inconsistent.  
   **Required change:** make §9 assert that every capability is disabled on a v1-proof connection until matching authenticated CONFIRMs have been received on that same connection.

**Round-50 dispositions**

- **r50-1 — RESOLVED.** `plan.md@4064fa3f7:4094-4105` keys obligations by creation protocol and both incarnations, defines explicit persist-or-supersede downgrade handling, and prevents legacy BulkEnd from clearing a negotiated obligation.
- **r50-2 — RESOLVED.** `plan.md@4064fa3f7:1405-1425` adds the setup-admission generation to the transition CAS, couples promotion to slot authorization, and makes the delayed n2 completion lose after n3 promotion. The existing admission cap at `sync_admission.go:39-83` and handshake deadline at `sync_auth.go:336-339` make the provisional setup state implementable without an unbounded setup population.
- **r50-3 — PARTIALLY RESOLVED.** The mask-all-or-mutual-CONFIRM rule closes the original unauthenticated-capability schedule when followed, but findings 1 and 3 leave the v1 transition mechanically inconsistent.
- **r50-4 — PARTIALLY RESOLVED.** The tag, role byte, lengths, and endianness are now stated, but finding 2 leaves record membership and acceptor-proof ordering contradictory.

Bottom line: the obligation-downgrade and setup-generation folds close their round-50 traces, and I found no new demote-gate or NAT-lifetime trace. Sign-off still fails because the newly consolidated authentication protocol can reconnect-loop against v1 and does not define one capability-covering v2 proof transcript. Those are concrete session-sync availability and negotiation-state failures governed by the cited handshake lines.
