PLAN NO

Reviewed the pinned plan at `1088547db` against code at `b2429fcaa`; no files were edited.

1. **BLOCKER — the reset barrier still has an unbounded pre-ACK connection case.** `plan.md@1088547db:1257-1273` incorrectly equates every installed connection with one that has already acknowledged a heartbeat. A keyed handshake clears its deadline before installation ([sync_auth.go:336](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:336)); an unkeyed connection has no handshake deadline at all ([sync_auth.go:329](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_auth.go:329)). After installation, missed-heartbeat accounting remains disabled until `peerHeartbeatAckEver` becomes true ([sync_conn_read.go:27](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:27), [sync_conn_read.go:296](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:296)). If the close/read error is lost before that first ACK, the slot persists beyond every finite or exponentially extended barrier. A post-barrier install then observes a nonempty registry, sets `wasDisconnected == false`, and need not cold-prime ([sync_conn.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:244)). The barrier generation also needs an explicit linearization rule: admission currently uses `preAuthMu` ([sync_admission.go:66](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_admission.go:66)), while installation uses `s.mu`; generation capture, `barrierActive`, slot closure, and installation rejection must be checked under one ordering. Required change: timeout every installed connection independently of prior ACK history, and atomically reject both stale-generation and barrier-active setup attempts inside `installConn`.

2. **BLOCKER — closing/resetting streams does not fence a frame already decoded by an old receive handler.** The plan’s “every old stream is dead” and repair-cutoff rule (`plan.md@1088547db:1303-1320`) covers unread TCP bytes, but `receiveLoop` verifies a frame and calls `handleMessage` synchronously before returning to its socket read ([sync_conn_read.go:70](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:70)). An old handler can pass its generation check, pause before `PutClusterSyncedV4`, then resume after an E1-absent repair commits ([sync_conn_gen.go:435](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_gen.go:435)). `handleDisconnect` only clears the socket slot; it neither joins that handler nor revokes its already-started mutation ([sync_conn.go:480](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn.go:480)). E1 can consequently republish after the validating BulkEnd on both the primary and optional repair paths. Required change: close-and-join old receive loops before repair, or carry the connection/repair generation into the coordinator and revalidate it atomically inside canonical publication. The currently unnamed “repair cutoff” must also be explicitly carried in the repair wire schema.

3. **BLOCKER — `Converted(old_state)` still has a DirectHold publication-gap release race.** The plan converts direct ownership to GroupHold inside the allocator critical section (`plan.md@1088547db:1717-1720`) but mandates releasing that lock before canonical publication (`plan.md@1088547db:1807-1814`). During that gap, the old DirectHold remains reachable from the incumbent worker/canonical entry. Worker reap releases it ([loop_body/mod.rs:1481](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/worker/loop_body/mod.rs:1481)), and canonical replacement displaces the old entry separately ([shared_ops.rs:897](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/shared_ops.rs:897)). Thus conversion can decrement the direct credit, unlock, and then an old-token drop can decrement it again before E2 publishes, leaving E2’s GroupHold over an undercounted or released tuple. The proposed version fence lacks a shared linearization point as well: promotion mutates the worker table first ([promote.rs:99](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/promote.rs:99)) and publishes canonical state later at `:131-138`. Required change: use a stable shared hold-cell whose variant swap governs every outstanding Drop, or dual-hold through publication with an atomic per-entry handoff; promotion, reap/replacement, and conversion must participate in one canonical version/CAS before worker mutation.

4. **BLOCKER — the staging capacity/progress contract can loop permanently and still contradicts all-or-nothing publication.** `plan.md@1088547db:1398-1412` derives the staged budget as a multiple of the receiver’s current shared-table size. An empty or lightly populated restarted receiver cannot size a legitimate full snapshot from A: BulkStart carries only an epoch and no member count ([sync_bulk.go:65](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:65)), while the only current bound is per-frame 16 MiB ([sync_conn_read.go:60](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:60)). Every retry therefore recomputes the same inadequate budget, invalidates, and re-arms indefinitely. Separately, `plan.md:1367-1374` requires no partial visibility, whereas `:1408-1412` says chunks commit before the generation map. Current row application becomes live immediately ([sync_conn_gen.go:452](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_gen.go:452), [session_store.go:257](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/dataplane/session_store.go:257)); without an explicitly invisible shadow and atomic visibility switch, interruption exposes partial members with old generation state. Required change: budget from configured capacity or an authenticated declared count bounded by capacity, add a repair deadline, and define an invisible shadow plus atomic commit epoch/root swap or complete rollback.

5. **HIGH — the post-cut journal has no terminal ordering marker.** The plan says receiver buffers flush after BulkEnd (`plan.md@1088547db:1333-1338`) but also says the repair ACK follows journal flush (`:1390-1396`). It does not require the journal to use the bulk’s pinned stream or define a marker by which the receiver knows the journal is complete. Bulk uses one captured connection ([sync_bulk.go:53](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_bulk.go:53)); ordinary sends can select another fabric ([sync_conn_write.go:268](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_write.go:268)); and today BulkEnd immediately reconciles and ACKs ([sync_conn_read.go:205](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/pkg/cluster/sync_conn_read.go:205)). A post-cut E2 delta delayed on fab1 can therefore arrive after fab0’s BulkEnd has cleared readiness. Required change: seal the journal at a second cutoff, transmit it in order on the same pinned repair stream, terminate it with an explicit marker, and ACK only that marker after bulk commit and journal application.

6. **HIGH — a late GroupHold clone drop does not retrigger quarantined-family cleanup.** The coordinator pre-publishes forward and reverse canonical entries before asynchronous worker fan-out ([session_import.rs:115](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:115), [session_import.rs:187](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/ha/session_import.rs:187)). The revised stage 1 correctly counts pending/executing clones (`plan.md@1088547db:1963-1981`), but `Claimed → Abandoned` can become the final outcome before the executor performs its late recheck and drops its clone (`:1982-1996`). Cleanup at the terminal transition sees that clone and stops; after the later drop, no further outcome exists to wake cleanup. The canonical family and its internal clones remain and can later be materialized ([session_glue/mod.rs:1092](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/mod.rs:1092), [session_glue/mod.rs:1157](/home/ps/git/kimi-xpf/.claude/worktrees/6461-research-blind-rst/userspace-dp/src/afxdp/session_glue/mod.rs:1157)). Required change: retain failed stage-1 families on a durable retry queue and wake it on every external-clone drop, or atomically run stage 2 on the external-holder count’s transition to zero.

7. **LOW — §9 does not yet encode the v9.9.27 contracts faithfully.** The runtime scalar-identity sweep is corrected, but `plan.md@1088547db:3811` tests “same `session_id`” even though `:2728-2740` explicitly distinguishes RT_FLOW `session_id` from `flow_incarnation_id`. Section 9 also lacks direct tests for pre-ACK installed-slot teardown, decoded-handler cutoff revalidation, post-cut journal terminal ordering, empty-receiver staging, interrupted shadow commit, Converted publication-gap rollback, and Abandoned→late-drop cleanup. Its `:3637-3639` “never occupy a slot” wording also conflicts with the normative `:1224-1229` statement that refused retries may install briefly before dying.

**Round-40 dispositions**

- **r40-1 — not resolved.** Barrier generation addresses stale setup in intent, but the unbounded installed/pre-ACK slot remains.
- **r40-2 — partially resolved.** Reset plus generation fencing covers unread transport bytes; decoded/in-progress handlers remain unfenced.
- **r40-3 — not resolved.** The receipt taxonomy and inverse are repaired, but token conversion and version publication are not linearized.
- **r40-4 — partially resolved.** Bounded journaling and overflow re-arm are specified; terminal delivery/ACK ordering is not.
- **r40-5 — not resolved.** The capacity basis cannot admit a larger authoritative table, and chunk publication does not satisfy the stated transaction.
- **r40-6 — partially resolved.** DirectHold/GroupHold carriage and the registry claim are consistent, but the new external-clone accounting lacks a late-drop cleanup wakeup.
- **r40-7 — partially resolved.** Full-pair runtime comparisons and detector constants are corrected; the operative test contract remains incomplete.

Part A’s demotion gate remains converged, and I found no new direct demotion path. Part B cannot receive final sign-off: the remaining schedules permit indefinite repair unavailability, stale state to publish after a validated repair, a translated tuple to be released beneath a live replacement, or repair to loop forever on a valid full table. These are concrete availability, tuple-lifecycle, and HA state-desynchronization failures in the issue’s stated harm classes.
