PLAN NO

1. **BLOCKER — Repair activation does not re-acquire the VRRP election hold.** The plan permits a later connection to transition from legacy to repair-vN and arms only the repair obligation plus packed not-ready state (`plan.md@9fa07c8ec:4122-4137`). In VRRP mode, however, the operational hold is acquired only during daemon startup (`pkg/daemon/daemon_run_bringup.go:226-233`). A completed legacy bulk releases it (`pkg/daemon/daemon_ha_sync.go:90-100`), restoring preemption and triggering it immediately (`pkg/vrrp/manager.go:389-404`). The packed `syncReady` state gates private-RG election, not normal VRRP (`pkg/cluster/manager.go:289-293`). Concrete trace: legacy bulk completes → VRRP hold releases → a later connection activates repair-vN and becomes not-ready → before `JOURNAL_END`, a priority event preempts because VRRP remains enabled. The activation transaction must acquire a generation-scoped election hold before exposing repair-vN; only matching-generation completion may release it. Its timeout must not silently release while a negotiated repair obligation remains armed.

2. **HIGH — The packed CAS does not define serialization of the subsequent external-effect bundle.** The plan says the writer CASes `{g, not-ready} → {g, ready}` and that this authorizes timer cancellation, `syncBulkPrimed`, `ReleaseSyncHold`, and the Manager mirror (`plan.md@9fa07c8ec:4165-4200`). Those remain separate operations in current code (`pkg/daemon/daemon_ha_sync.go:90-100`), with the callback queued asynchronously at `pkg/cluster/sync_conn_read.go:241-247`. Concrete interleaving:

   - G1 successfully CASes `{g, not-ready} → {g, ready}`, then pauses.
   - G2 activation publishes `{g+1, not-ready}` and arms its repair.
   - G1 resumes its already-authorized bundle and releases the hold or writes the Manager mirror after G2.

   The activation’s described CAS also expects `{g, not-ready}`; if G1 wins first, no retry rule is specified. The correct contract is: the readiness writer is one-shot; activation CAS-loops from `{g, *}` to `{g+1, not-ready}`. If activation wins, the writer drops every effect. If the writer wins, activation must wait for the entire completion bundle under a named shared serialization mechanism, then bump the generation and re-arm the election hold before exposing repair-vN. CAS must precede irreversible effects; restart must reconstruct not-ready/hold state after a process crash mid-bundle.

3. **HIGH — The post-wrapper decision phase lacks an authenticated pre-install frame reader.** The plan orders wrapper installation before CONFIRM declarations and `CAPABILITY_DECISION`, with slot installation and dispatch afterward (`plan.md@9fa07c8ec:4087-4093`). Authenticated writes append a sequence/HMAC trailer (`pkg/cluster/sync_protocol.go:49-62`), but the cited setup reader explicitly reads only header and payload and does not strip that trailer (`pkg/cluster/sync_auth.go:289-310`). Trailer consumption and verification exist only in the ordinary receive loop (`pkg/cluster/sync_conn_read.go:71-94`), which cannot start early because it dispatches messages before the slot token exists. Literal reuse leaves the trailer to be parsed as the next header, causing a bad-magic reconnect loop. Specify a pre-install, allowlisted authenticated reader that consumes and verifies header, payload, and trailer while advancing `authConn.recvSeq`. A CONFIRM timeout may retain and legacy-latch the stream only if zero bytes were consumed; partial CONFIRM frames must close, and complete late CONFIRMs must be consumed and ignored before declarations.

4. **HIGH — Setup-owner totality remains internally contradictory and equal node IDs are undefined.** The new primary rule correctly selects by authenticated stable node ID (`plan.md@9fa07c8ec:4067-4079`), but the same operative paragraph still says an “address-ordered owner” dies and retry uses the “same deterministic address-ordered rule” (`plan.md@9fa07c8ec:4097-4101`). Current address parsing returns “dial” at either endpoint on failure (`pkg/cluster/sync_conn.go:12-20`), enabling crossed setups to repeatedly supersede the same slot (`pkg/cluster/sync_conn.go:244-266`). Additionally, equal authenticated node IDs have no strict ordering. Existing HA code treats duplicate IDs as invalid and fails closed (`pkg/cluster/heartbeat.go:811-820`, `pkg/cluster/election.go:195-202`). Sweep the address-order stragglers and explicitly reject equal node IDs with an operator-visible not-ready state; setup token and connection role must not silently turn duplicate IDs into competing owners.

5. **LOW — §9 does not cover the new convergence boundaries.** The protocol tests end at the older `(d9)` contracts (`plan.md@9fa07c8ec:5040-5072`). Add cases for pinned-token death and successor repair, both packed-CAS winner orders, VRRP-hold re-arming, equal-node-ID rejection, authenticated decision trailers, and absent-versus-partial CONFIRM timeout.

**r56-1 disposition: RESOLVED.** Exact slot-token binding closes the fabric-0 preference trace. Token revocation, durable logical-peer obligations, failed-drive rearming, and fresh per-connection activation provide a coherent successor path after mid-repair connection death. The missing explicit regression test is LOW.

**r56-2 disposition: PARTIALLY RESOLVED.** G2-bump-before-G1 now rejects G1 before any side effect, and the packed word closes the original dual-lock readiness-bit race. The opposite CAS order, external-effect serialization, and VRRP-hold re-acquisition remain unresolved.

**r56-3 disposition: PARTIALLY RESOLVED.** Stable authenticated node-ID ownership closes the hostname/address-parser trace in the principal rule, but contradictory address-order retry text and duplicate-ID handling remain.

**r56-4 disposition: PARTIALLY RESOLVED.** Closing incomplete decision frames fixes the original retained-stream boundary error. Partial CONFIRM handling and authenticated trailer consumption in the new pre-install phase are still unspecified.

Bottom line: the exact-connection repair binding converges, and no new translated-tuple ownership trace was found. Final sign-off is still blocked because a repair activation can leave normal VRRP preemption enabled, a successful old-generation CAS can race its external effects past a newer activation, and the new authenticated decision phase cannot be implemented with the cited readers without leaving frame trailers behind. Those gaps permit premature takeover or persistent session-sync unavailability, so v9.9.54.7 is not yet converged.
