PLAN NO

1. **BLOCKER — The completion matrix lacks a rule for an existing negotiated obligation crossing a capability downgrade.** The steady-state matrix is correct at `plan.md:4004-4028`, but an outbound repair obligation can already exist under `repair-vN` (`plan.md:1491-1519`) when the peer reconnects as legacy; capabilities are explicitly per-connection (`plan.md:3367-3382`). The resulting INSTALL-only prime cannot produce `JOURNAL_ACK`. Clearing the old obligation on emission treats an unacknowledged prime as repair; retaining it leaves cold-prime/readiness indefinitely degraded. Symmetrically, legacy `BulkEnd` currently releases readiness through `pkg/cluster/sync_conn_read.go:205-247` and `pkg/daemon/daemon_ha_sync.go:90-100`, but must not satisfy an older, repair-ID-bound inbound obligation. The sender-restart rule at `plan.md:1784-1792` covers only a new sender incarnation.  
   **Required change:** key obligations by creation protocol and both peer incarnations. On downgrade, either retain an explicit degraded state until negotiated repair returns or atomically supersede the obligation into a documented legacy baseline after an appropriate post-reset prime. Legacy completion must never clear a negotiated obligation implicitly.

2. **BLOCKER — A delayed pre-auth setup can regress the peer incarnation after a newer process is current.** Handshakes run concurrently at `pkg/cluster/sync_conn.go:388-430`; setup tracking records only connection membership at `pkg/cluster/sync_admission.go:66-83`; authentication finishes before slot installation at `pkg/cluster/sync_conn.go:100-130`; and process incarnations are random, not ordered, at `pkg/cluster/heartbeat.go:624-632`. Concrete schedule: `n2` is admitted and pauses pre-auth; `n3` authenticates and becomes current; then `n2` completes. Because `n2` never entered pending/retired, `plan.md:1403-1415` treats it as the newest authenticated incarnation and retires `n3`. The setup-generation fence acts only later at installation (`plan.md:1267-1281`), so `n2` can perform the transition and then fail installation; `n3` is barred from readoption by `plan.md:1430-1432`.  
   **Required change:** include the setup admission generation in the incarnation-transition CAS and atomically couple promotion with slot authorization. Promotion must revoke older unbound pre-slot tokens. Add the `n2-preauth → n3-promote → n2-resume` schedule.

3. **HIGH — Capabilities remain unauthenticated on a v1-proof connection.** The legacy prefix fold is valid: `pkg/cluster/sync_auth.go:345-376` accepts trailing HELLO bytes, and proof precedes wrapper installation at `:387-406` and `pkg/cluster/sync_conn.go:100-118`. However, `plan.md:3943-3946` requires capabilities to be masked or post-authenticated, while `:3951-3960` masks only reset and requires `repair-vN` to remain available; `:3986-3997` still places capability negotiation in the raw pre-proof HELLO, with no authenticated confirmation frame. The v1 proof covers only the nonce (`sync_auth.go:217-224`). An asymmetric repair bit leaves one side waiting for `JOURNAL_ACK`; an asymmetric identity-enforcement bit can permit a delete toward a decoder that performs generation/key deletion at `pkg/cluster/sync_conn_gen.go:263-290,493-506`.  
   **Required change:** either mask every capability under v1 proof, or define a post-wrapper authenticated `CAPABILITY_CONFIRM` carrying the full tuple and require mutual agreement before enabling any feature.

4. **HIGH — The v2 transcript is still not byte-exact.** `plan.md:3961-3963` says to authenticate the exact raw HELLO records, while `:3980-3984` instead describes reconstructing a field list whose order conflicts with the preserved legacy prefix at `:3936-3942`. The referenced “record-order rule” and proof direction at `:3947-3950` have no literal formula or vector. Independent implementations can therefore calculate different proofs, fail at the equivalent of `pkg/cluster/sync_auth.go:401-404`, and reconnect-loop through `pkg/cluster/sync_conn.go:106-110,435-477`.  
   **Required change:** publish one exact HMAC byte formula—including prover role/order, raw record lengths, tag bytes, integer widths—and golden vectors for both directions.

**Disposition r49-1: partially resolved.** The three steady-state completion modes are coherent; outstanding-obligation downgrade transitions are not.

**Disposition r49-2: partially resolved.** Legacy-prefix tolerance, either-v1 proof selection, and proof-before-wrapper ordering are resolved; v1 capability authentication and byte-exact v2 construction remain incomplete.

**Disposition r49-3: resolved.** Section 9(d9), `plan.md:4625-4642`, includes the boundaries requested in r49. The newly identified transition schedules require additional tests.

Bottom line: Part A remains converged, and this round found no new demotion-gate or translated-tuple lifetime trace. Sign-off is still blocked because repair obligations have no safe negotiated-to-legacy transition, a delayed pre-auth connection can replace a newer peer incarnation, and the authentication contract cannot yet bind capabilities or produce interoperable v2 proofs unambiguously.
