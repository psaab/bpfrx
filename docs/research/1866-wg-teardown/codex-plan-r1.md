===== RESULT =====
PLAN-NEEDS-CHANGES

1. **Backoff state is not durable enough.** The plan stores `last_spawn_attempt_ns` inside `WgControlThread` ([plan.md:130](/home/ps/git/bpfrx/.claude/worktrees/1866-research/docs/research/1866-wg-teardown/plan.md:130)), then removes that same map entry on the finished sweep: `self.wg_control_threads.remove(&id)` ([plan.md:140](/home/ps/git/bpfrx/.claude/worktrees/1866-research/docs/research/1866-wg-teardown/plan.md:140)). That contradicts the claim that EADDRINUSE yields “at most one bind attempt ... per 3s” ([plan.md:158](/home/ps/git/bpfrx/.claude/worktrees/1866-research/docs/research/1866-wg-teardown/plan.md:158)). Fix: preserve per-id backoff outside the live-thread entry, or keep a tombstone.

2. **Tick-placement latency proof is incomplete.** `refresh_bindings` is not only the 1/s Go status poll. The Rust server calls `refresh_status` for every non-suppressed control response ([handlers/mod.rs:164](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/server/handlers/mod.rs:164)), and `refresh_status` calls `state.afxdp.refresh_bindings(...)` ([helpers.rs:16](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/server/helpers.rs:16)). The plan also describes “socket bind + TUN open” on the control-socket thread ([plan.md:282](/home/ps/git/bpfrx/.claude/worktrees/1866-research/docs/research/1866-wg-teardown/plan.md:282)); current code correctly keeps bind/TUN work inside the spawned aux thread ([mod.rs:552](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:552), [wg_control.rs:93](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/wg_control.rs:93), [slowpath.rs:349](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/slowpath.rs:349)). Make that an invariant.

3. **The residual-unknown stance is acceptable only with stronger diagnosis.** The source supports deterministic prune: removed endpoint means `desired` no longer contains the id, stale removal stops and joins ([mod.rs:508](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:508)), and `refresh_runtime_snapshot` calls it ([mod.rs:727](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:727)). So the live “original thread stayed bound” likely means a stale snapshot/publisher path, not the ordinary prune path. Add one log on WG endpoint-set transitions at Go publish and Rust apply; current apply log only emits generation/fib ([snapshot.rs:59](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/server/handlers/snapshot.rs:59)). Also include route-overlay publishers in the suspect list; they copy `*m.lastSnapshot` ([manager.go:840](/home/ps/git/bpfrx/.claude/worktrees/1866-research/pkg/dataplane/userspace/manager.go:840)) just like policy schedule state does ([manager.go:725](/home/ps/git/bpfrx/.claude/worktrees/1866-research/pkg/dataplane/userspace/manager.go:725)).

4. **D1 is real, but “forever” is overstated.** Exact dead-entry path is verified: child can `return` on bind/open/nonblocking failure ([wg_control.rs:93](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/wg_control.rs:93), [wg_control.rs:115](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/wg_control.rs:115)); parent already inserted the map entry ([mod.rs:568](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:568)); unchanged identity reuses the same engine Arc ([wg.rs:61](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/forwarding_build/wg.rs:61)); stale test passes and `contains_key` skips spawn ([mod.rs:512](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:512), [mod.rs:530](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:530)). But full reconcile also heals it because teardown calls `stop_inner(false)` ([teardown.rs:28](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/reconcile/teardown.rs:28)) and `stop_inner` clears the WG map ([mod.rs:262](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/afxdp/coordinator/mod.rs:262)).

5. **#1868 sequencing is mandatory, not cosmetic.** Current branch still drops interface-level WG tunnels with empty source in `collectAppliedTunnels` ([daemon_run.go:93](/home/ps/git/bpfrx/.claude/worktrees/1866-research/pkg/daemon/daemon_run.go:93)); #1868 changes that to allow `Mode == "wireguard"` ([#1868 daemon_run.go:104](/home/ps/git/bpfrx/.claude/worktrees/1736-engineer/pkg/daemon/daemon_run.go:104)). #1868 also changes `bind_wg_socket` to return `(UdpSocket, bool)` ([#1868 wg_control.rs:298](/home/ps/git/bpfrx/.claude/worktrees/1736-engineer/userspace-dp/src/afxdp/coordinator/wg_control.rs:298)) and adds `wg_send_to` v4-mapping ([#1868 wg_control.rs:383](/home/ps/git/bpfrx/.claude/worktrees/1736-engineer/userspace-dp/src/afxdp/coordinator/wg_control.rs:383)). Implement #1866 only after rebasing over #1868.

**Open Questions**
1. Tick placement: acceptable if it is async spawn-only, backoff is fixed, and it is explicitly not just “1/s”.
2. Residual unknown: ship D1 hardening, but not as root cause. Add endpoint-set transition logs and run the #1868 teardown canary.
3. Retry forever: yes, but with durable backoff and rate-limited logging. No give-up cap.
4. `is_finished()`: sufficient for early-exit/dead-thread detection; not a protocol liveness detector for a live stale session.
5. Two live threads: no, if removal only happens after `is_finished()`/join and lifecycle stays under the server mutex ([handlers/mod.rs:82](/home/ps/git/bpfrx/.claude/worktrees/1866-research/userspace-dp/src/server/handlers/mod.rs:82)). Use tombstone/separate backoff, not immediate probe-join.
6. #1868/#1865: land after #1868 and preserve its WG send/bind changes. No counters is fine for #1865, but call this lifecycle logging, not telemetry.

V4’s live journal claim and the GitHub issue body could not be independently verified here because `gh issue view 1866` is blocked by sandbox network policy.

Codex session ID: 019eb637-67c2-7612-a8a7-3fa569417906
Resume in Codex: codex resume 019eb637-67c2-7612-a8a7-3fa569417906
