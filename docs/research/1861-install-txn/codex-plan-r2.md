**Verdict: PLAN-NEEDS-MINOR**

One self-containedness defect remains in `plan.md`: the SMR r2 instruction that the reverse residual arm must set `install_failed` is only in [claude-smr-plan-r2.md](/home/ps/git/bpfrx/.claude/worktrees/1861-research/docs/research/1861-install-txn/claude-smr-plan-r2.md:33), not in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1861-research/docs/research/1861-install-txn/plan.md:285). `plan.md` says the impossible reverse residual “forward stays committed in release” but does not say to set the cache-suppression flag. If an engineer follows only `plan.md`, that release residual can still continue into the common cache insert at [mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:1995). This is minor because the residual is impossible under today’s cap-only install failure model, but the plan should copy the SMR r2 line into §5.2/§5.4 and add the pin.

C1 is otherwise fixed. The current source really does return a repaired reverse decision even when the install fails: `install_reverse_session_from_forward_match` conditionally installs at [shared_ops.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/shared_ops.rs:729), then returns `reverse` unconditionally at line 762. `resolve_flow_session_decision` currently reports `created: true` at [session_glue/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/session_glue/mod.rs:1081), and the only production flow-cache insert is the ungated block at [poll_descriptor/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:1995). The proposed `install_failed` gate is the right shape.

C2 is verified. NAT64 source selection happens before install admission at [poll_descriptor/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:574), and `allocate_v4_source` is just a round-robin `fetch_add` with no reservation at [nat64.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/nat64.rs:97). Flow cache persistence is excluded by `!decision.nat.nat64` at [flow_cache.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/flow_cache.rs:227).

C3 is a valid scoped exclusion, with one caveat: I verified the source issue, not the external follow-up issue filing. Tunnel code publishes shared state and enqueues `UpsertLocal` at [tunnel.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/tunnel.rs:306), while the worker apply side discards the install result at [session_glue/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/session_glue/mod.rs:556). The explicit scope/signoff text is present in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1861-research/docs/research/1861-install-txn/plan.md:436).

C4 is verified. `icmp_embed` is session/shared-map based: the wrapper passes session/shared maps only at [icmp_embed/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/icmp_embed/mod.rs:95), and the v4/v6 matchers call `lookup_forward_nat_across_scopes` / `lookup_session_across_scopes` at [nat_match_v4.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/icmp_embed/nat_match_v4.rs:41) and [nat_match_v6.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/icmp_embed/nat_match_v6.rs:49). No flow-cache consultation.

Gate soundness checks pass, assuming the residual note is folded into `plan.md`: DNS fast-path is “no install required,” not “install failed” ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:1252)); LocalDelivery is non-cacheable ([forwarding.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/types/forwarding.rs:274)); fabric-return bypasses the common cache block with `continue` ([mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1861-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:479)); non-flow packets fail the `if let Some(flow)` cache gate.

I did not find a missing I1-I14 row. The map covers the production install/upsert sites I found: `mod.rs:479/1275/1436/2448`, `shared_ops.rs:729`, `forwarding/mod.rs:1099`, `session_glue/mod.rs:560`, `session/mod.rs:766`, and tunnel enqueue/publish. No KILL-class counterexample.

Codex session ID: 019eb64c-397a-7e93-8b64-2f2dc2fc0570
Resume in Codex: codex resume 019eb64c-397a-7e93-8b64-2f2dc2fc0570
