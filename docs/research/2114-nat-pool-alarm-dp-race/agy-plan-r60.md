# AGY adversarial plan-review — round 60 (plan v60 @ e8e8beabd)

Direct agy print-mode invocation from the trusted workspace with --add-dir at a 12m timeout (see reviewer-ids.md infra notes). Verdict: NEEDS-REVISION (3 MAJOR — the unobservable peer actuated state IS Codex M1, the mint-boundary in-flight registration race IS Codex M5, the mint-vs-QUEUED identity contradiction IS Codex M3; 1 MINOR — the seqlock/attempt-generation counter divergence IS part of Codex M3; folds 5/5 FOLDED; 3 fresh attacks SUCCEEDED as the majors/minor). Structure confirmed. Raw output follows verbatim (file:/// link markup as emitted).

---

(A) Fold verification:
1. Item 1 (r59 M1, election green vs live-forwarding false green, actuated state check): PARTIAL — plan.md:5387-5403 requires verifying RG0 ACTIVE AND VRRP MASTER (the actuated state) on "the cluster status surface", but `pkg/cluster/status.go:12-25` and `pkg/cluster/heartbeat_manager.go:306-355` collect and display only election `NodeState` (`StatePrimary`/`StateSecondary`) via `PeerGroupState` (`pkg/cluster/heartbeat.go:636-641`); status surfaces and heartbeat packets do not expose peer `rg_active` or VRRP mastership today, nor does the plan specify updating status schemas/wire formats to carry them.
2. Item 2 (r59 M2, callback fence & ensureFabricIPVLAN outcome reporting): FOLDED — plan.md:6781-6786 specifies full-form fence checks `runCtx.Err() || stopping.Load()` after `applySem` acquisition AND before each mutation (`pkg/daemon/daemon_run_shutdown.go:50-64,214-230`), while plan.md:6788-6800 specifies aggregating all `ensureFabricIPVLAN` step errors via `errors.Join`, enforcing the existing link KIND check, and retiring the arm FAILED on any error (`pkg/daemon/daemon_ha_fabric.go:29-53,72-93,102-148`).
3. Item 3 (r59 M4, QUEUED ordering/retirement model & placement): PARTIAL — QUEUED ordering, per-attempt SET, canceled-acquisition retirement, and process-lifetime snapshot rules are now placed in §5.1 (plan.md:6492-6515), acceptance copy (plan.md:5371, 5417), and §9, but an internal timing contradiction exists: plan.md:6487 states generation tokens are minted AFTER `applySem` admission, while plan.md:6492 states QUEUED is published at ENQUEUE, BEFORE semaphore wait (where no generation token has been minted yet).
4. Item 4 (r59 M5, residual (vi) withdrawn & total re-registration): PARTIAL — Residual (vi) is withdrawn and total re-registration under `m.mu` is specified (plan.md:6569-6577), but totality is not achievable for arms whose launch/registration is IN FLIGHT when the mint re-registration query runs under `m.mu` (`pkg/dataplane/userspace/manager.go:424-433`, `pkg/daemon/daemon_apply_interfaces.go:100`).
5. Item 5 (r59 m1 + m2, file inventory & post-reactivation no-pending term): FOLDED — Changed-file inventory in §5.1 includes `daemon_apply_interfaces.go` and `daemon_ha_fabric.go` (plan.md:6739-6746), and the post-reactivation predicate explicitly includes the no-pending-outstanding term (plan.md:5371, 5417).

(B) Fresh attacks:
- Generation-tagged publication vs seqlock: SUCCEEDED (becomes MINOR finding 1 below).
- Total re-registration at mint boundary (in-flight arm at mint): SUCCEEDED (becomes MAJOR finding 2 below).
- Token mint timing vs QUEUED publication at ENQUEUE: SUCCEEDED (becomes MAJOR finding 3 below).

(C) New findings:
- MAJOR 1: Unobservable peer actuated state on status surfaces (plan.md:5387-5403 vs `pkg/cluster/status.go:12-25` + `pkg/cluster/heartbeat_manager.go:306-355` + `pkg/cluster/heartbeat.go:636-641`). The plan mandates verifying RG0 ACTIVE AND VRRP MASTER (the actuated state) on "the cluster status surface" before declaring repair complete, but `m.FormatStatus()` and heartbeat RPCs collect only `PeerGroupState`, which carries election `NodeState` (`StatePrimary`/`StateSecondary`) without `rg_active` or VRRP mastership. Without adding peer `rg_active` and VRRP mastership to heartbeat packets and cluster status structs, the peer's actuated state cannot be verified from status surfaces.
- MAJOR 2: Mint boundary in-flight arm registration race (plan.md:6569-6577 vs `pkg/daemon/daemon_apply_interfaces.go:100` + `pkg/dataplane/userspace/manager.go:430-433`). The plan relies on querying manager outstanding debts under `m.mu` at the mint boundary to perform total re-registration. If an async arm launched by Apply A1 is spawned (`go worker()`) before Apply A2 reaches the mint boundary, A2's query under `m.mu` misses the in-flight arm if the goroutine has not yet acquired `m.mu` to register. The arm then registers under old token A1; its completion is discarded by A2 as stale, while A2 never tracked the arm as pending, allowing A2 to report CONVERGED while the arm executes or fails. Synchronous arm registration inside the apply thread prior to goroutine spawn is required.
- MAJOR 3: Token mint location vs QUEUED publication timing contradiction (plan.md:6487 vs plan.md:6492). Line 6487 states generation tokens are minted AFTER `applySem` admission (`daemon_apply_commit.go:172-175`), while line 6492 states an apply attempt publishes QUEUED at ENQUEUE, BEFORE the semaphore wait. An enqueued attempt waiting on `applySem` has not admitted and therefore lacks a minted generation token, leaving its QUEUED publication untagged or requiring minting at ENQUEUE (which contradicts line 6487).
- MINOR 1: Counter divergence between seqlock version and attempt generation tag (plan.md:6363-6375 vs plan.md:6480-6508). The seqlock `version` and attempt `generation` tag are two distinct counters. The seqlock `version` increments (+2) on every store update pass (ENQUEUE, admission, arm state change, outcome), whereas `generation` is fixed per attempt. Multiple snapshot writes within one attempt or suppressed lower-generation publications cause seqlock `version` and attempt `generation` to diverge continuously.

(D) Structure confirmation:
The 2-of-3 majority §4.7 delivery structure stands (PR-1 = `d.dp` core; follow-up = G+H+H2), while Antigravity's r28 (A) CONVERGE dissent remains recorded in plan.md:8789.

(E) Verdict line:
NEEDS-REVISION
