# Codex hostile plan-review — round 54 (plan v54 @ 8cc30c5f1)

Task: task-msa5w52v-18utff (session 019fbca0-b184-7312-b020-edc3daaad19b).
Verdict: NEEDS-REVISION (6 MAJOR, 2 MINOR; fold verification 1 FOLDED / 3 PARTIAL / 1 NOT-FOLDED). Structure confirmed. Raw companion output follows verbatim.

---

Fold verification:
1. NOT-FOLDED — The ordering is de-circularized at docs/research/2114-nat-pool-alarm-dp-race/plan.md:4846-4849, but the proposed recheck remains non-atomic: ShowActive releases Store.mu before returning (pkg/configstore/store_format.go:31-36), the marker is already claimed (pkg/daemon/daemon_ha_sync.go:474-489), and QueueConfig allocates/writes its wire generation later (pkg/cluster/sync_conn_config.go:234-243).
2. PARTIAL — The lifecycle nouns now exist at docs/research/2114-nat-pool-alarm-dp-race/plan.md:5975-5988, but the cited #6034 seed updates only the manager-neighbor REPLACE generation (pkg/dataplane/userspace/process_status.go:165-172; pkg/dataplane/userspace/protocol_status.go:73-84), while the helper initializes that unrelated field to zero (userspace-dp/src/server/lifecycle.rs:184-216). It cannot seed an apply-attempt namespace or prevent pre-restart/current-token aliasing.
3. PARTIAL — Section 5.1 claims token/pending-count publication and rendering at docs/research/2114-nat-pool-alarm-dp-race/plan.md:5997-6003, but the package inventory still renders only last-outcome/failure-count at :6136-6145, and the runbook/formal acceptance still require only count==0 plus last-success at :4981-4987 and :7443-7460. The no-pending predicate is therefore not operationally checkable.
4. PARTIAL — h2d is corrected at docs/research/2114-nat-pool-alarm-dp-race/plan.md:7564-7569, but live §5.1 still records deferred-MAC debt as immediate count++ on an asserted bounded retry at :5942-5950 and says every non-converged return increments at :6009-6010, contradicting terminal-only exhaustion at :5991-5995. The actual retries are unbounded (pkg/dataplane/userspace/manager_worker_arm_5134.go:18-21,28-38; pkg/dataplane/userspace/process_status.go:183-198). The other count++ hits at plan.md:2172 and :2233 are superseded revision history.
5. FOLDED — The NoteApplyOutcome-shaped boundary and odd-before/even-after writer discipline are finally normative at docs/research/2114-nat-pool-alarm-dp-race/plan.md:5910-5920. No current lock inversion was found: the established order is applySem→s.mu (pkg/daemon/daemon_apply_commit.go:624-628), and the Store releases s.mu before invoking its daemon callback (pkg/configstore/store_commit.go:803-823).

New findings:
MAJOR 1 — “Immediately before QueueConfig” is not an exclusion boundary. After rechecking A, the claimant can pause after Store.mu is released; a commit can promote and push B under Store.mu (pkg/configstore/store_commit.go:220-224); then A resumes, draws a newer wire generation, and is admitted by the receiver (pkg/cluster/sync_conn_config.go:234-243,267-272,325-395). This falsifies the “can NEVER be pushed” claim at docs/research/2114-nat-pool-alarm-dp-race/plan.md:4838-4842 and :7365-7371; validation must be coupled to send by actual serialization or an equivalent reservation protocol.

MAJOR 2 — A stale drop poisons the marker because CLAIMED and PUSHED remain the same state. The reconciler records (epoch,gen) before validation/send and later passes no-op on it (pkg/daemon/daemon_ha_sync.go:474-489): under A→B→A in one epoch, the first A claimant drops after seeing B, but the later needed A pass is suppressed. This is reachable because autonomous event-engine commits intentionally use syncPeer=false (pkg/daemon/daemon_apply_commit.go:596-599). Section 9 still tests the obsolete stale-push-and-re-drive behavior instead of drop plus conditional claim release (docs/research/2114-nat-pool-alarm-dp-race/plan.md:7570-7577).

MAJOR 3 — Generation-only revalidation does not revalidate the other desired-state gates. ActiveConfig and ShowActive are separate transactions (pkg/daemon/daemon_ha_sync.go:462-471; pkg/configstore/store_format.go:31-36,55-60), so enabled A→disabled B can capture and send B while the generation check succeeds, contradicting docs/research/2114-nat-pool-alarm-dp-race/plan.md:4853-4855. Authority can likewise change after pkg/daemon/daemon_ha_sync.go:451-454; the new primary rejects the late send at :544-548 while the marker remains claimed. The final barrier must coherently revalidate authority, connection epoch/liveness, ConfigSync, and active identity.

MAJOR 4 — The new publication boundary has no coherent entry/token semantics for its claimed pre-promotion coverage. The plan says state is written centrally and tokens are minted only at applyConfigLocked (docs/research/2114-nat-pool-alarm-dp-race/plan.md:5880-5889,5975-5980), but commit preflight/compile failures return before that entry (pkg/daemon/daemon_apply_commit.go:98-126,194-222,551-575). Moreover, commitWithGenBinding still invokes commitFn after the initial compile error, falsifying plan.md:5916-5920’s claim that such failures never touch Store commit paths. An outer mint/publication site and outcome classification are required.

MAJOR 5 — A token plus scalar pending count is not a join. Two arms of token T with count=2 can receive a duplicate completion for arm A, decrement twice, and falsely report zero while arm B remains live; plan.md:6000-6001 specifies neither arm identity, idempotence, nor exactly-once registration. Registration ordering is also exposed: ApplyConfig can process status and launch a callback before returning (pkg/dataplane/userspace/manager_compile.go:357-402; pkg/dataplane/userspace/maps_sync.go:451-457), so a daemon-side post-return increment can lose an early completion. Use token+arm-ID state/bitset or explicit exactly-once completion objects registered before launch.

MAJOR 6 — The claimed exhaustive asynchronous-arm inventory is false. OnXSKBound launches a goroutine (pkg/dataplane/userspace/maps_sync.go:451-457) whose critical fabric-IPVLAN failure is merely logged (pkg/daemon/daemon_apply_interfaces.go:98-109), while PrepareLinkCycle suppresses/logs its command failures through a void interface (pkg/daemon/daemon_apply_dataplane.go:289-296; pkg/dataplane/userspace/process_linkcycle.go:145-162). Neither is tokened, registered, or exercised by the four-arm h2g leg at docs/research/2114-nat-pool-alarm-dp-race/plan.md:7584-7588, so lastOK can still become green with apply-derived work failed.

MINOR 1 — The recheck identity is undefined. The captured value is a uint64 FNV hash (pkg/daemon/daemon_ha_sync.go:381-388,467-472), whereas Store exposes ActiveDigest as a SHA-256 string (pkg/configstore/store.go:772-779,812-829); Store’s only uint64 generation accessor is candidate-only (pkg/configstore/store_gen.go:43-49). Section 5.1 inventories no active-generation API at docs/research/2114-nat-pool-alarm-dp-race/plan.md:5902-5920.

MINOR 2 — The runbook still requires the authority’s reconcile pass to be “OBSERVED complete” at docs/research/2114-nat-pool-alarm-dp-race/plan.md:4829-4831 while withdrawing ConfigsSent and the private marker as witnesses at :4843-4845. The replacement read→commit→two-reads sequence does not observe that pass, so this stale prerequisite must be removed or supplied with a real witness.

Structure confirmation: §4.7 stands unchanged: PR-1 owns A1/core at docs/research/2114-nat-pool-alarm-dp-race/plan.md:5835-5845, while G+H+H2 remain together in the follow-up at :5846-5860.

NEEDS-REVISION

Codex session ID: 019fbca0-b184-7312-b020-edc3daaad19b
Resume in Codex: codex resume 019fbca0-b184-7312-b020-edc3daaad19b
