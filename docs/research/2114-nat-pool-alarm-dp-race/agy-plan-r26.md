# AGY adversarial plan-review — round 26 (plan v26 @ 1fda3b3c2)

Direct agy print-mode invocation from the trusted workspace with --add-dir (see reviewer-ids.md infra notes). Verdict: PLAN-READY (all 7 folds FOLDED; 3 fresh attacks FAILED). Raw output follows verbatim.

---

(A) Verification of Folds 1–7:

1. FOLDED. Key-class permanent failures (`gcm.Open` authentication failure at [crypto.go:355-356](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L355-L356), master-key IO at [crypto.go:318,448-449](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L318), invalid master-key length at [crypto.go:451-453,460-462](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L451-L453)) do not take the repair-write exemption; the debt retains with a `MASTER.KEY RESTORATION` operator message ([plan.md:765-773, 1948-1969](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L765-L773)). Any latch/debt clear following a key-class failure validates `active.json` readability under the current key ([store_persist.go:26-35](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store_persist.go#L26-L35)), closing the key-swap laundering scenario (`readOrCreateMasterKey` accepting K′ at [crypto.go:457-465](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L457-L465)). For plaintext configs, `maybeEncryptTreeJSON` checks `masterPasswordPRF(tree)` at [crypto.go:263](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L263) (returning plaintext) and `maybeDecryptTreeJSON` at [crypto.go:308-314](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L308-L314) detects no envelope (`!ok`), so active-side validation is a clean no-op.

2. FOLDED. D is fully suppressed while any W debt pends ([plan.md:774-785, 1778-1810](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L774-L785)). W holds exclusive slot access (`(w-u)` restore-over or `(w-a)` make durable). This closes the kill-shot scenario where a live C visible after a post-rename W failure is tombstoned by D's plaintext synthesized tombstone when a later W restore fails pre-rename due to key length ([db.go:207-217](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/db.go#L207-L217), [crypto.go:262-270](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L262-L270)). Confirmed-commit failure (`writeConfirmState` at [store_commit.go:531-553](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store_commit.go#L531-L553)) creates a W debt by construction, so no confirmed-commit failure routes to D.

3. FOLDED. R terminalization rationale is corrected ([plan.md:786-790, 1925-1940](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L786-L790)): payload synthesis is possible, but blind action is unsafe because a permanent read error leaves the slot's occupant unprovable (a newer live record could be overwritten), not because "the owner is known".

4. FOLDED. Key-class remediation is operator-correct ([plan.md:790-795, 2235-2244](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L790-L795)): journal carries exact crypto cause (`invalid master key length in /etc/xpf/master.key`, `gcm.Open` auth error), health detail and runbook direct operators to restore original `master.key`, with explicit warning that removing a record that might be a live window's sacrifices crash recovery.

5. FOLDED. Exposure window is stated honestly ([plan.md:796-799, 1845-1850, 3962-3964](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L796-L799)): seconds-wide under transient failure, unbounded up to confirm window end under deterministic write failure (invalid key, read-only FS) retrying until operator repair; no post-crash heal either way.

6. FOLDED. Store ownership is stated as OPERATIONAL ([plan.md:800-804, 2245-2250](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L800-L804)): systemd unit singleton runs one `xpfd` per `.configdb/` ([daemon.go:1042-1053](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/daemon/daemon.go#L1042-L1053)); no `flock` exists in [store.go:296-319](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store.go#L296-L319) or [db.go:37-70](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/db.go#L37-L70). Documented as an assumption with enforcement as a follow-up.

7. FOLDED. Partial copies repaired ([plan.md:803-809, 2045-2088, 3967-3970](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/docs/research/2114-nat-pool-alarm-dp-race/plan.md#L803-L809)): both x15 legs split non-key-class vs key-class; `FirstCommit` rationale copies corrected (NEW reader: `Resolved` precedes H; OLD reader: `FirstCommit=true` -> `compiled=nil`/`everCommitted=false`/`committed=0` -> bootstrap handling per [store_persist.go:176-184](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store_persist.go#L176-L184)); generic ConfirmDebt message names `SLOT_DELETE`; §5.1 seeding shorthand names the failure class split.

---

(B) Fresh Attacks:

1. **Attack: Active-side validation over-blocks sanctioned `confirm.json` removal for plaintext active configs.**
   - *Hypothesis*: When `confirm.json` encounters a key-class failure and the operator removes `confirm.json`, the probe's active-side read requirement might fail if `master.key` is missing or invalid, over-blocking remediation even when `active.json` is plaintext.
   - *Outcome*: FAILED. When `active.json` is plaintext (no `master-password`), `maybeDecryptTreeJSON` ([crypto.go:308-314](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/crypto.go#L308-L314)) finds no envelope (`!ok`) and returns `(data, false, nil)` without calling `readMasterKey()`. `ReadActive()` succeeds cleanly regardless of `master.key` presence/validity. The active-side validation for plaintext configs is a complete no-op and does not over-block.

2. **Attack: Non-exhaustive W-free determination allows D debt execution while a W debt is active.**
   - *Hypothesis*: A race or state transition during plain commit or `SyncApply` could instantiate or execute a D debt while a W debt pends.
   - *Outcome*: FAILED. Plain commit and `SyncApply` call `clearPendingConfirmLocked()`, which cancels the window in memory (`s.armedArmID = ""`), rendering any existing W debt stale (which is then cleared). If a W debt is active (`s.armedArmID != ""`), the retry loop (`persistRetryLoop`) explicitly checks for pending W debts and suppresses D execution entirely until W resolves.

3. **Attack: Synthetic tombstone `FirstCommit = false` pin breaks OLD readers on restart.**
   - *Hypothesis*: An old reader encountering an expired synthetic tombstone with `FirstCommit = false` might misclassify the configuration state.
   - *Outcome*: FAILED. On pre-H2 readers, `FirstCommit = true` forces `s.compiled = nil`, `s.persistMarkerCommitted = false`, `s.everCommitted = false`, writing `committed = 0` ([store_persist.go:176-184](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store_persist.go#L176-L184)), which triggers an erroneous day-0 bootstrap import. `FirstCommit = false` enters the else branch ([store_persist.go:185-194](file:///home/ps/git/kimi-xpf/.claude/worktrees/2114-research-nat-pool-alarm-dp-race/pkg/configstore/store_persist.go#L185-L194)), compiling `prevTree` (which equals active C) with `committed = 1`, maintaining normal operational posture.

---

PLAN-READY
